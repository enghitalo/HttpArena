window.LB_DOCS = {"":{"t":"Knowledge Base","html":"<div class=\"doc-body\"><p>Learn how HttpArena benchmarks work, what each test profile measures, and how the load generators drive traffic.</p>\n<div class=\"doc-cards\"><a class=\"doc-card\" href=\"#doc=load-generators\" data-doc=\"load-generators\"><span class=\"dc-t\">Load Generators</span><span class=\"dc-s\">How gcannon (HTTP/1.1) and h2load (HTTP/2) drive traffic for the benchmark suite.</span></a><a class=\"doc-card\" href=\"#doc=hardware\" data-doc=\"hardware\"><span class=\"dc-t\">Hardware &amp; Topology</span><span class=\"dc-s\">Reference machine: Threadripper PRO 3995WX. NUMA, cache, SMT, and how the harness pins resources.</span></a><a class=\"doc-card\" href=\"#doc=scoring\" data-doc=\"scoring\"><span class=\"dc-t\">Scoring</span><span class=\"dc-s\">How the composite score is computed: normalization, arithmetic mean, and participation penalties.</span></a><a class=\"doc-card\" href=\"#doc=running-locally\" data-doc=\"running-locally\"><span class=\"dc-t\">Running Locally</span><span class=\"dc-s\">Set up and run the full benchmark suite on your own machine with Docker and gcannon.</span></a><a class=\"doc-card\" href=\"#doc=add-framework\" data-doc=\"add-framework\"><span class=\"dc-t\">Add a Framework</span><span class=\"dc-s\">Step-by-step guide to adding your framework: endpoints, Dockerfile, metadata, and PR.</span></a></div></div>"},"scoring/composite-score":{"t":"Composite Score","html":"<div class=\"doc-body\"><p>The composite score combines results from multiple test profiles into a single number that reflects overall framework performance. Each profile is normalized to a 0\u2013100 scale, then <strong>summed</strong> across all scored profiles. An optional memory toggle adds a 0\u201350 bonus per profile for memory-efficient frameworks on top of their raw-throughput score.</p>\n<h2 id=\"how-it-works\">How it works</h2>\n<h3 id=\"step-1-average-rps-per-profile\">Step 1: Average RPS per profile</h3>\n<p>For each framework and profile, compute the average RPS across all connection counts. This rewards frameworks that scale well across concurrency levels rather than just their peak.</p>\n<h3 id=\"step-2-normalize-per-profile\">Step 2: Normalize per profile</h3>\n<p>For each profile, normalize against the best-performing framework:</p>\n<pre><code>rpsScore = (framework_avg_rps / best_avg_rps) \u00d7 100</code></pre>\n<p>This produces a 0\u2013100 value where the top framework scores 100.</p>\n<p><strong>Exception: JSON Compressed.</strong> The <code>json-comp</code> profile applies a compression-ratio gain before normalization so frameworks that ship smaller response bodies are rewarded directly. Instead of averaging raw rps, <code>avgRps</code> is first scaled by <code>(minBpr / myBpr)\u00b2</code> where <code>myBpr = avgBw / avgRps</code> and <code>minBpr</code> is the smallest bytes-per-response across the field. Doubling the response size quarters the score. See the <a href=\"#doc=test-profiles/h1/isolated/json-compressed/implementation\" data-doc=\"test-profiles/h1/isolated/json-compressed/implementation\" data-anchor=\"scoring\">JSON Compressed implementation</a> for the full formula and rationale.</p>\n<h3 id=\"step-3-sum-across-scored-profiles\">Step 3: Sum across scored profiles</h3>\n<p>The final composite score is the <strong>sum</strong> of per-profile scores across all <strong>scored</strong> profiles:</p>\n<pre><code>composite = sum(scored_profile_scores)</code></pre>\n<p>Summing instead of averaging means the composite scales with the number of scored profiles: a framework that places well in many profiles separates cleanly from one that only wins a single profile. A perfect-across-the-board framework earns 100 points per profile, so with the current 26 scored profiles for framework (flagship and emerging) entries the raw-throughput ceiling is ~2,600, rising to ~3,900 when the memory-efficiency toggle is on (each profile adds up to 50 more points). Engine and infrastructure entries are scored on smaller subsets and have correspondingly lower ceilings.</p>\n<p>Frameworks that don&#x27;t participate in a scored profile receive 0 for that profile, which lowers their composite by the full 100-point ceiling of that profile.</p>\n<h2 id=\"scored-vs-reference-only-profiles\">Scored vs reference-only profiles</h2>\n<p>Not all profiles count toward the composite score. Profiles marked as <strong>scored</strong> contribute to the composite. Reference-only profiles (marked with <strong>\\</strong>*) are displayed for comparison but do not affect the ranking.</p>\n<h3 id=\"h11-isolated\">H/1.1 Isolated</h3>\n<table><thead><tr><th>Profile</th><th>Scored</th><th>Workload</th></tr></thead><tbody><tr><td>Baseline</td><td>Yes</td><td>Mixed GET/POST with query parsing</td></tr><tr><td>Pipelined</td><td>Yes</td><td>16 requests batched per connection</td></tr><tr><td>Short-lived</td><td>Yes</td><td>Connections closed after 10 requests</td></tr><tr><td>JSON</td><td>Yes</td><td>Dataset processing and serialization</td></tr><tr><td>JSON Compressed</td><td>Yes</td><td>JSON with <code>Accept-Encoding: gzip, br</code> and multiplier <code>?m=N</code></td></tr><tr><td>JSON TLS</td><td>Yes</td><td>JSON workload over HTTP/1.1 + TLS on port 8081</td></tr><tr><td>Upload</td><td>Yes</td><td>20 MB body ingestion, return byte count</td></tr><tr><td>Static</td><td>Yes</td><td>20 static files served over HTTP/1.1</td></tr><tr><td>Async DB</td><td>Yes</td><td>Async Postgres query with connection pooling</td></tr><tr><td>CRUD</td><td>Yes</td><td>Realistic REST API against Postgres: cached reads (75%), updates (15%), list (5%), upsert create (5%). Cache-aside with 200ms TTL (in-process or Redis sidecar)</td></tr><tr><td>Fortunes</td><td>No (*)</td><td>DB query + HTML template render. Reference-only - engine-comparison test, not part of the composite ranking</td></tr><tr><td>TCP Frag</td><td>No (*)</td><td>Baseline with MTU 69 - TCP fragmentation stress</td></tr></tbody></table>\n<h3 id=\"h11-workload\">H/1.1 Workload</h3>\n<table><thead><tr><th>Profile</th><th>Scored</th><th>Workload</th></tr></thead><tbody><tr><td>API-4</td><td>Yes</td><td>Baseline + JSON + async-db on 4 CPUs</td></tr><tr><td>API-16</td><td>Yes</td><td>Baseline + JSON + async-db on 16 CPUs</td></tr></tbody></table>\n<h3 id=\"h2\">H/2</h3>\n<table><thead><tr><th>Profile</th><th>Scored</th><th>Workload</th></tr></thead><tbody><tr><td>Baseline</td><td>Yes</td><td>Query parsing over TLS with multiplexed streams</td></tr><tr><td>Static</td><td>Yes</td><td>20 static files served over TLS with multiplexed streams</td></tr><tr><td>Baseline h2c</td><td>Yes</td><td>Query parsing over cleartext h2 on port 8082 (prior-knowledge, anti-cheat rejects dual-serving HTTP/1.1)</td></tr><tr><td>JSON h2c</td><td>Yes</td><td>JSON serialization workload over cleartext h2 on port 8082</td></tr></tbody></table>\n<h3 id=\"h3\">H/3</h3>\n<table><thead><tr><th>Profile</th><th>Scored</th><th>Workload</th></tr></thead><tbody><tr><td>Baseline</td><td>Yes</td><td>Query parsing over QUIC (UDP) with TLS 1.3</td></tr><tr><td>Static</td><td>Yes</td><td>20 static files served over QUIC (UDP) with TLS 1.3</td></tr></tbody></table>\n<h3 id=\"gateway\">Gateway</h3>\n<table><thead><tr><th>Profile</th><th>Scored</th><th>Workload</th></tr></thead><tbody><tr><td>Gateway H2</td><td>Yes</td><td>Two-service proxy + server stack over HTTP/2 + TLS, mixed workload (static 30%, JSON 35%, baseline 20%, async-db 15%), 64-CPU budget</td></tr><tr><td>Gateway H3</td><td>Yes</td><td>Same two-service stack over HTTP/3 + QUIC at the edge</td></tr><tr><td>Production Stack H2</td><td>Yes</td><td>Four-service CRUD API (edge + Redis + JWT auth + server) with 10K-item cache-aside, JWT verified every request, concurrent reads + writes</td></tr></tbody></table>\n<h3 id=\"grpc\">gRPC</h3>\n<table><thead><tr><th>Profile</th><th>Scored</th><th>Workload</th></tr></thead><tbody><tr><td>Unary</td><td>Yes</td><td>gRPC unary call over cleartext HTTP/2</td></tr><tr><td>Unary TLS</td><td>Yes</td><td>gRPC unary call over TLS</td></tr></tbody></table>\n<h3 id=\"websocket\">WebSocket</h3>\n<table><thead><tr><th>Profile</th><th>Scored</th><th>Workload</th></tr></thead><tbody><tr><td>Echo</td><td>Yes</td><td>WebSocket echo throughput</td></tr></tbody></table>\n<p>TCP Frag and Noisy are reference-only - shown for comparison but not counted in the composite score.</p>\n<h2 id=\"memory-efficiency-bonus\">Memory efficiency bonus</h2>\n<p>An optional toggle rewards memory efficiency with an <strong>additive</strong> bonus per profile. It never scales down the raw-throughput score - it only adds on top of it, up to +50 points for the most memory-efficient framework in that profile.</p>\n<p>This uses an <strong>efficiency ratio</strong> (<code>rps / memoryMB</code>), not absolute memory usage. A framework that is fast <em>and</em> lean gets the largest bonus; a framework that uses little memory only because it is slow earns less, because its rps is in the numerator of the ratio.</p>\n<p>CPU efficiency was intentionally dropped: a framework that leaves CPU on the table already scores worse on RPS, so penalizing it a second time for &quot;low efficiency&quot; double-counted the same signal and flattened the throughput differentiation that the benchmark exists to measure.</p>\n<h3 id=\"how-the-memory-bonus-is-computed\">How the memory bonus is computed</h3>\n<p>For each profile, compute the efficiency ratio for every framework:</p>\n<pre><code>memEfficiency = sqrt(rps) / memoryMB</code></pre>\n<p>Why <code>sqrt(rps)</code> instead of <code>rps</code>? A plain <code>rps / MB</code> ratio double-counts throughput: high-rps frameworks would win both <code>rpsScore</code> <em>and</em> <code>memScore</code> because <code>rps</code> dominates the ratio. Taking the square root dampens rps to log-scale - it still matters (a dead framework shouldn&#x27;t win &quot;efficiency&quot;), but memory can now actually move the needle.</p>\n<p>Normalize against the best efficiency in that profile:</p>\n<pre><code>memScore = (framework_memEff / best_memEff) \u00d7 100</code></pre>\n<p>Add half of it on top of the RPS score:</p>\n<pre><code>profileScore = rpsScore + 0.5 \u00d7 memScore</code></pre>\n<p>With the toggle on, per-profile scores range 0\u2013150 (up to 100 from throughput, up to 50 from memory efficiency). Frameworks with no memory data keep their plain <code>rpsScore</code>.</p>\n<h3 id=\"example\">Example</h3>\n<table><thead><tr><th>Framework</th><th>RPS</th><th>Mem (MB)</th><th>sqrt(rps)/MB</th></tr></thead><tbody><tr><td>A</td><td>500,000</td><td>50</td><td>14.14</td></tr><tr><td>B</td><td>100,000</td><td>20</td><td>15.81</td></tr></tbody></table>\n<ul><li>RPS scores: A = 100, B = 20</li><li>Memory efficiency scores: A = 89.4, B = 100 (best)</li></ul>\n<p>With the memory toggle on:</p>\n<ul><li>A: <code>100 + 0.5 \u00d7 89.4 = 144.7</code></li><li>B: <code>20 + 0.5 \u00d7 100 = 70.0</code></li></ul>\n<p>B actually wins the memory term despite A&#x27;s 5\u00d7 throughput advantage, because <code>sqrt(rps)</code> only gives A a \u221a5 \u2248 2.24\u00d7 boost in the numerator - not enough to beat B&#x27;s 2.5\u00d7 memory savings. A still wins overall thanks to its raw throughput lead, but B&#x27;s lean memory footprint is now rewarded meaningfully instead of being drowned out.</p>\n<h2 id=\"type-specific-scoring\">Type-specific scoring</h2>\n<p>Types are scored <strong>separately</strong> - each has its own composite ranking and normalization pool. The scored profiles differ by type:</p>\n<ul><li><strong>Frameworks</strong> (Flagship, Emerging and Experimental, in either Standard or Tuned mode) are scored on all scored profiles across H/1.1, H/2, H/3, gRPC, and WebSocket.</li><li><strong>Infrastructure</strong> (nginx, h2o, and similar proxies/servers) are scored only on Baseline, Pipelined, Short-lived, and Static - the profiles that don&#x27;t require executing application logic. Other profiles (JSON, async-db, etc.) may be displayed as reference data but do not count toward the infrastructure composite.</li><li><strong>Engines</strong> are scored on a reduced set: Baseline, Pipelined, Short-lived, API-4, H/2 (both), H/3 (both), gRPC (both), and WebSocket, since most engines don&#x27;t implement the heavier endpoints (JSON, upload).</li></ul>\n<p>The Type filter on the composite leaderboard switches between these rankings. Flagship, Emerging and Experimental can be combined (they share the framework normalization pool); Infrastructure and Engine are each exclusive. Experimental is hidden by default and shown only when selected. Tuned entries (a <code>mode</code>, not a type) are shown within whichever framework types are selected, marked with a ring.</p>\n<h2 id=\"why-this-approach\">Why this approach</h2>\n<ul><li><strong>Sum across profiles</strong> - larger numbers separate strong all-rounders from frameworks that only win a single profile; a framework that covers 15 profiles at 80% crushes one that wins one profile at 100%</li><li><strong>Normalization</strong> - each profile contributes equally regardless of absolute RPS scale (baseline at 1M vs JSON at 200K), and is capped at 100 points per profile</li><li><strong>Additive memory bonus</strong> - memory-efficient frameworks earn up to +50 per profile on top of their RPS score; slow frameworks can&#x27;t game the bonus because <code>rps</code> is in the efficiency numerator</li><li><strong>Average across connections</strong> - each framework is scored on its average RPS across all connection counts, rewarding consistent scaling</li></ul></div>"},"scoring":{"t":"Scoring","html":"<div class=\"doc-body\"><p>How HttpArena computes the composite score that ranks frameworks across all test profiles.</p>\n<div class=\"doc-cards\"><a class=\"doc-card\" href=\"#doc=scoring/composite-score\" data-doc=\"scoring/composite-score\"><span class=\"dc-t\">Composite Score</span><span class=\"dc-s\">Sum of per-profile normalized scores (0\u2013100 each) across all scored profiles, with an optional memory-efficiency bonus.</span></a></div></div>"},"test-profiles":{"t":"Test Profiles","html":"<div class=\"doc-body\"><p>HttpArena runs every framework through multiple benchmark profiles. Each profile isolates a different performance dimension, ensuring frameworks are compared fairly across varied workloads.</p>\n<p>Your framework must implement endpoints depending on which test profiles it participates in. All HTTP/1.1 endpoints are served on <strong>port 8080</strong>. HTTPS/H2/H3 endpoints are served on <strong>port 8443</strong>.</p>\n<p>Each profile is run at multiple connection counts to show how frameworks scale under increasing concurrency.</p>\n<h2 id=\"api-specification\">API Specification</h2>\n<p>All benchmark endpoints are described in machine-readable OpenAPI 3.0 specifications. Each spec covers request/response schemas, status codes, standard and tuned implementation rules, and links back to the relevant documentation pages.</p>\n<p>The endpoints are split across two files:</p>\n<table><thead><tr><th>Spec</th><th>Profiles covered</th><th></th></tr></thead><tbody><tr><td><a href=\"/openapi.json\" target=\"_blank\" rel=\"noopener\">openapi.json</a></td><td>All HTTP/1.1, HTTP/2, HTTP/3, gRPC, and WebSocket profiles</td><td><a href=\"https://redocly.github.io/redoc/?url=https://www.http-arena.com/openapi.json\" target=\"_blank\" rel=\"noopener\">Browse</a></td></tr><tr><td><a href=\"/openapi-gateway.json\" target=\"_blank\" rel=\"noopener\">openapi-gateway.json</a></td><td>Gateway profiles (production-stack)</td><td><a href=\"https://redocly.github.io/redoc/?url=https://www.http-arena.com/openapi-gateway.json\" target=\"_blank\" rel=\"noopener\">Browse</a></td></tr></tbody></table>\n<h2 id=\"benchmark-parameters\">Benchmark parameters</h2>\n<p>Five load generators are dispatched per profile - each one is built for a specific protocol + workload shape. See <a href=\"#doc=load-generators\" data-doc=\"load-generators\">Load Generators</a> for per-tool details.</p>\n<table><thead><tr><th>Parameter</th><th>Value</th></tr></thead><tbody><tr><td>Load generators</td><td><code>gcannon</code> (HTTP/1.1, upload, WebSocket), <code>wrk</code> (static + json-tls rotation), <code>h2load</code> (HTTP/2, h2c, gateway), <code>h2load-h3</code> (HTTP/3 / QUIC), <code>ghz</code> (gRPC)</td></tr><tr><td>Threads</td><td>64 for <code>gcannon</code> / <code>wrk</code> / <code>h2load</code> / <code>h2load-h3</code> (<code>$THREADS</code> / <code>$H2THREADS</code> / <code>$H3THREADS</code>); <code>ghz</code> scales workers dynamically as <code>connections \u00d7 4</code></td></tr><tr><td>Duration</td><td>5s default; <code>async-db</code> 10s; <code>api-4</code>, <code>api-16</code>, <code>crud</code> 15s (hardcoded in the profile dispatcher)</td></tr><tr><td>Runs</td><td>3 per (profile, connection count) - best RPS wins</td></tr><tr><td>Networking</td><td>Docker <code>--network host</code> for all containers (server + load generator + Postgres + Redis sidecars)</td></tr></tbody></table>\n<h2 id=\"data-mounts\">Data mounts</h2>\n<p>Data files are <strong>mounted automatically</strong> by the benchmark runner - your Dockerfile does not need to include them. The following paths are available inside the container at runtime:</p>\n<table><thead><tr><th>Path</th><th>Description</th></tr></thead><tbody><tr><td><code>/data/dataset.json</code></td><td>50-item dataset for <code>/json</code>, <code>/db</code>, and <code>/async-db</code></td></tr><tr><td><code>/data/static/</code></td><td>20 static assets for <code>/static/*</code> (HTML, JS, CSS, SVG, WebP, woff2, JSON). 15 assets ship with pre-built <code>.gz</code> and <code>.br</code> sibling files (e.g. <code>app.js</code>, <code>app.js.gz</code>, <code>app.js.br</code>) so frameworks that support precompressed serving can skip on-the-fly compression. The 5 already-binary formats (<code>hero.webp</code>, <code>thumb1.webp</code>, <code>thumb2.webp</code>, <code>bold.woff2</code>, <code>regular.woff2</code>) have no precompressed variants. See the <a href=\"#doc=test-profiles/h1/isolated/static\" data-doc=\"test-profiles/h1/isolated/static\">Static</a> profile for how to wire Accept-Encoding lookup.</td></tr><tr><td><code>/certs/server.crt</code>, <code>/certs/server.key</code></td><td>TLS certificate and key for HTTPS / H2 / H2 h2c (port 8082 is cleartext) / H3</td></tr></tbody></table>\n<h2 id=\"environment-variables\">Environment variables</h2>\n<p>Set by the benchmark runner when the relevant profile runs - your process will see them via <code>os.environ</code> / <code>std::env::var</code> / equivalent.</p>\n<table><thead><tr><th>Variable</th><th>Profiles</th><th>Value</th></tr></thead><tbody><tr><td><code>DATABASE_URL</code></td><td><code>async-db</code>, <code>crud</code>, <code>api-4</code>, <code>api-16</code></td><td>Postgres connection string (<code>postgres://bench:bench@127.0.0.1:5432/benchmark</code>)</td></tr><tr><td><code>DATABASE_MAX_CONN</code></td><td>same as above</td><td><code>256</code> - the Postgres sidecar&#x27;s <code>max_connections</code>; size your pool \u2264 this</td></tr><tr><td><code>REDIS_URL</code></td><td><code>crud</code></td><td><code>redis://127.0.0.1:6379</code> - multi-process frameworks can use Redis as a cross-process cache; single-heap frameworks (Go, ASP.NET, etc.) typically ignore it and keep their in-process cache</td></tr></tbody></table>\n<p>Gateway and <code>production-stack</code> profiles are compose-orchestrated, so their services receive additional env (e.g. <code>JWT_SECRET</code> for the production-stack auth sidecar) via their <code>compose.*.yml</code> files rather than through the runner. See the per-profile pages under <a href=\"#doc=test-profiles/gateway\" data-doc=\"test-profiles/gateway\">Gateway</a> for details.</p>\n<div class=\"doc-cards\"><a class=\"doc-card\" href=\"#doc=test-profiles/h1\" data-doc=\"test-profiles/h1\"><span class=\"dc-t\">H/1.1</span><span class=\"dc-s\">Isolated single-endpoint benchmarks and multi-endpoint workload mixes over plain TCP.</span></a><a class=\"doc-card\" href=\"#doc=test-profiles/h2\" data-doc=\"test-profiles/h2\"><span class=\"dc-t\">H/2</span><span class=\"dc-s\">Baseline and static file benchmarks over encrypted TLS connections with stream multiplexing.</span></a><a class=\"doc-card\" href=\"#doc=test-profiles/gateway\" data-doc=\"test-profiles/gateway\"><span class=\"dc-t\">Gateway</span><span class=\"dc-s\">Multi-service deployments: proxy + server (H2/H3) and full CRUD production stack with JWT auth + cache-aside.</span></a><a class=\"doc-card\" href=\"#doc=test-profiles/h3\" data-doc=\"test-profiles/h3\"><span class=\"dc-t\">H/3</span><span class=\"dc-s\">Baseline and static file benchmarks over QUIC for frameworks with native H/3 support.</span></a><a class=\"doc-card\" href=\"#doc=test-profiles/grpc\" data-doc=\"test-profiles/grpc\"><span class=\"dc-t\">gRPC</span><span class=\"dc-s\">Unary RPC throughput over cleartext HTTP/2 using Protocol Buffers serialization.</span></a><a class=\"doc-card\" href=\"#doc=test-profiles/ws\" data-doc=\"test-profiles/ws\"><span class=\"dc-t\">WebSocket</span><span class=\"dc-s\">WebSocket echo throughput measuring frame processing performance.</span></a></div></div>"},"load-generators":{"t":"Load Generators","html":"<div class=\"doc-body\"><p>HttpArena uses a different load generator for each transport / workload.</p>\n<div class=\"doc-cards\"><a class=\"doc-card\" href=\"#doc=load-generators/h1\" data-doc=\"load-generators/h1\"><span class=\"dc-t\">HTTP/1.1</span><span class=\"dc-s\">gcannon (io_uring) for most tests, wrk for static file serving.</span></a><a class=\"doc-card\" href=\"#doc=load-generators/h2\" data-doc=\"load-generators/h2\"><span class=\"dc-t\">HTTP/2</span><span class=\"dc-s\">h2load - nghttp2&#x27;s load generator with TLS and stream multiplexing.</span></a><a class=\"doc-card\" href=\"#doc=load-generators/h3\" data-doc=\"load-generators/h3\"><span class=\"dc-t\">HTTP/3</span><span class=\"dc-s\">h2load-h3 - nghttp2 + ngtcp2 for QUIC-based HTTP/3 benchmarks.</span></a><a class=\"doc-card\" href=\"#doc=load-generators/grpc\" data-doc=\"load-generators/grpc\"><span class=\"dc-t\">gRPC</span><span class=\"dc-s\">ghz - proto-aware gRPC load tester for streaming and unary RPCs.</span></a><a class=\"doc-card\" href=\"#doc=load-generators/ws\" data-doc=\"load-generators/ws\"><span class=\"dc-t\">WebSocket</span><span class=\"dc-s\">gcannon --ws - io_uring WebSocket echo driver reusing the HTTP/1.1 engine with a frame-aware send/recv loop.</span></a></div></div>"},"hardware":{"t":"Hardware & Topology","html":"<div class=\"doc-body\"><p>The reference hardware for HttpArena results is a single-socket AMD Threadripper PRO 3995WX workstation. Every benchmark run uses the same machine, so numbers are directly comparable framework-to-framework. This page explains the CPU topology - NUMA, cache hierarchy, SMT - and how the benchmark harness pins resources against it.</p>\n<h2 id=\"processor\">Processor</h2>\n<table><thead><tr><th>Spec</th><th>Value</th></tr></thead><tbody><tr><td>Model</td><td>AMD Ryzen Threadripper PRO 3995WX</td></tr><tr><td>Architecture</td><td>Zen 2</td></tr><tr><td>Physical cores</td><td>64</td></tr><tr><td>Logical threads</td><td>128 (SMT2)</td></tr><tr><td>Sockets</td><td>1</td></tr><tr><td>Base / boost clock</td><td>2.7 GHz / 4.2 GHz</td></tr><tr><td>TDP</td><td>280 W</td></tr><tr><td>Memory channels</td><td>8 \u00d7 DDR4-3200</td></tr><tr><td>Aggregate DRAM bandwidth</td><td>~205 GB/s</td></tr><tr><td>Total L3</td><td>256 MB (16 \u00d7 16 MB)</td></tr><tr><td>Total L2</td><td>32 MB (64 \u00d7 512 KB)</td></tr><tr><td>Total L1d / L1i</td><td>2 MB each (64 \u00d7 32 KB)</td></tr></tbody></table>\n<h2 id=\"numa-layout\">NUMA layout</h2>\n<p>Confirmed with <code>numactl --hardware</code>:</p>\n<pre><code>available: 1 nodes (0)\nnode 0 cpus: 0-127\nnode 0 size: 257 GB\nnode distances:\nnode   0\n  0:  10</code></pre>\n<p><strong>One NUMA node</strong>, all 128 logical threads, all DRAM channels. This is <strong>NPS1</strong> mode (Nodes Per Socket = 1), configured in BIOS. All memory is symmetrically accessible to every core with identical reported latency, so the kernel scheduler has full placement freedom and software doesn&#x27;t need NUMA policies.</p>\n<h3 id=\"could-nps4-help\">Could NPS4 help?</h3>\n<p>No, and it could hurt. NPS4 would split the chip into 4 NUMA nodes (16 cores + 2 DRAM channels each), surfacing physical proximity asymmetries to the kernel. That&#x27;s useful for <em>shared-nothing</em> workloads - multiple independent DBs, per-node JVMs, etc. HttpArena&#x27;s server workloads are the opposite: heavily <em>shared mutable state</em> (thread-pool queues, <code>IMemoryCache</code>, Npgsql multiplexer state, Postgres shared buffers). Under NPS4 that shared state gets a &quot;home node&quot; and cross-node access becomes measurably slower. Also, at our throughput (~1 KB memory traffic per request \u00d7 350K rps \u2248 350 MB/s) we use <strong>0.2%</strong> of aggregate memory bandwidth - the resource NPS partitions isn&#x27;t one we&#x27;re constrained on.</p>\n<h2 id=\"cache-hierarchy\">Cache hierarchy</h2>\n<table><thead><tr><th>Level</th><th>Size</th><th>Shared by</th></tr></thead><tbody><tr><td>L1i + L1d</td><td>32 KB each</td><td>One physical core (plus its SMT sibling)</td></tr><tr><td>L2</td><td>512 KB</td><td>One physical core (plus its SMT sibling)</td></tr><tr><td><strong>L3</strong></td><td><strong>16 MB</strong></td><td><strong>One CCX - 4 physical cores / 8 threads</strong></td></tr></tbody></table>\n<p>Zen 2 arranges cores into <strong>CCXs</strong> (core complexes) of 4 physical cores. Each CCX has its own exclusive L3 slice. Physical chiplets on the 3995WX contain 2 CCXs each, and there are 8 chiplets \u2192 <strong>16 CCXs total</strong>, 16 MB L3 per CCX.</p>\n<p>Within a CCX, L3 access is ~40 cycles. Crossing CCXs (Infinity Fabric) is ~110 cycles. So <strong>CCX boundaries are the real locality boundaries</strong> on this chip, more so than NUMA.</p>\n<h3 id=\"ccx-to-cpu-mapping\">CCX-to-CPU mapping</h3>\n<p>Verified from <code>/sys/devices/system/cpu/cpuN/cache/index3/shared_cpu_list</code>:</p>\n<table><thead><tr><th>CCX</th><th>Physical cores (and SMT siblings)</th></tr></thead><tbody><tr><td>0</td><td>0\u20133, 64\u201367</td></tr><tr><td>1</td><td>4\u20137, 68\u201371</td></tr><tr><td>2</td><td>8\u201311, 72\u201375</td></tr><tr><td>3</td><td>12\u201315, 76\u201379</td></tr><tr><td>4</td><td>16\u201319, 80\u201383</td></tr><tr><td>5</td><td>20\u201323, 84\u201387</td></tr><tr><td>6</td><td>24\u201327, 88\u201391</td></tr><tr><td>7</td><td>28\u201331, 92\u201395</td></tr><tr><td>8</td><td>32\u201335, 96\u201399</td></tr><tr><td>9</td><td>36\u201339, 100\u2013103</td></tr><tr><td>10</td><td>40\u201343, 104\u2013107</td></tr><tr><td>11</td><td>44\u201347, 108\u2013111</td></tr><tr><td>12</td><td>48\u201351, 112\u2013115</td></tr><tr><td>13</td><td>52\u201355, 116\u2013119</td></tr><tr><td>14</td><td>56\u201359, 120\u2013123</td></tr><tr><td>15</td><td>60\u201363, 124\u2013127</td></tr></tbody></table>\n<h2 id=\"smt-simultaneous-multithreading\">SMT (Simultaneous Multithreading)</h2>\n<p>Each physical core has two hardware threads. The sibling of CPU <code>N</code> is CPU <code>N+64</code>:</p>\n<pre><code>cpu0  \u2192 0,64\ncpu1  \u2192 1,65\n...\ncpu63 \u2192 63,127</code></pre>\n<p>Verified from <code>/sys/devices/system/cpu/cpuN/topology/thread_siblings_list</code>.</p>\n<p><strong>Why this matters for pinning:</strong> SMT siblings share L1, L2, execution units, and decode bandwidth. When you cpuset-pin a consumer, always include both threads of a pair - splitting an SMT pair across two different consumers creates pathological cache thrashing. When the harness assigns, say, <code>0,64</code> to Redis and <code>1-31,65-95</code> to the server, both sides get coherent pairs.</p>\n<p>SMT2 roughly yields +30% throughput on our workload over single-thread-per-core - useful for latency-bound async handlers where one logical thread is usually idle waiting on I/O while its sibling can execute.</p>\n<h2 id=\"how-httparena-pins-against-this-topology\">How HttpArena pins against this topology</h2>\n<p>Most profiles use a 64-thread server cpuset: <code>0-31,64-95</code>. That&#x27;s <strong>32 physical cores</strong> spanning <strong>CCX 0\u20137</strong> (8 CCXs = 128 MB L3 budget). The load generator (gcannon) gets <code>32-63,96-127</code> - symmetric split, CCX 8\u201315. One half of the chip drives the test, the other half serves it. Same NUMA node either way.</p>\n<p>For profiles that need a sidecar (Postgres, Redis), the harness reshuffles:</p>\n<p><strong>crud profile</strong> (uses Redis):</p>\n<table><thead><tr><th>consumer</th><th>phys</th><th>threads</th><th>cpuset</th><th>L3 reach</th></tr></thead><tbody><tr><td>Redis</td><td>1</td><td>2</td><td><code>0,64</code></td><td>CCX 0 (shared with 3 server cores)</td></tr><tr><td>Server</td><td>31</td><td>62</td><td><code>1-31,65-95</code></td><td>CCX 0\u20137 (112 MB L3 exclusive + 16 MB shared with Redis)</td></tr><tr><td>Gcannon</td><td>32</td><td>64</td><td><code>32-63,96-127</code></td><td>CCX 8\u201315 (128 MB L3)</td></tr><tr><td>Postgres</td><td>unpinned</td><td>-</td><td>-</td><td>kernel-scheduled, typically lands on server CCXs for request-path L3 locality</td></tr></tbody></table>\n<p>Redis sharing a CCX with a few server cores is <em>beneficial</em> - data the server just wrote to Redis (on cache miss) stays in the shared L3 when it reads back (on hit). Moving Redis to a non-server CCX would introduce a ~70-cycle inter-CCX coherence hop per read.</p>\n<p><strong>production-stack profile</strong> (explicit multi-service pinning):</p>\n<table><thead><tr><th>Service</th><th>cpuset</th></tr></thead><tbody><tr><td>edge (nginx)</td><td><code>4-15,68-79</code> (12 phys)</td></tr><tr><td>authsvc (JWT verifier)</td><td><code>16-19,80-83</code> (4 phys)</td></tr><tr><td>Redis (cache)</td><td><code>15,79</code> (1 phys)</td></tr><tr><td>Postgres (unpinned)</td><td>-</td></tr><tr><td>server (framework)</td><td><code>0-3,20-31,64-67,84-95</code> (16 phys)</td></tr><tr><td>gcannon</td><td><code>32-63,96-127</code> (32 phys)</td></tr></tbody></table>\n<p>This split is empirically tuned - see the CHANGELOG entry for 2026-04-16 for the calibration history and why an edge-heavy allocation works at the given rps.</p>\n<h2 id=\"kernel-tuning-applied-per-run\">Kernel tuning applied per run</h2>\n<p><code>scripts/lib/system.sh</code> runs before each benchmark:</p>\n<ul><li>CPU governor \u2192 <code>performance</code> (no DVFS ramp delays)</li><li><code>net.core.somaxconn</code> \u2192 65535 (accept queue)</li><li><code>net.ipv4.tcp_max_syn_backlog</code> \u2192 65535</li><li><code>net.core.netdev_max_backlog</code> \u2192 65535</li><li><code>net.ipv4.ip_local_port_range</code> \u2192 <code>1024 65535</code> (avoid ephemeral port exhaustion under <code>-r</code> reconnect storms)</li><li><code>net.ipv4.tcp_tw_reuse</code> \u2192 1</li><li><code>net.ipv4.tcp_max_tw_buckets</code> \u2192 131072</li><li><code>net.core.rmem_max</code> / <code>wmem_max</code> \u2192 7.5 MB (UDP buffer for QUIC)</li><li>Loopback MTU \u2192 1500 (realistic Ethernet; the default 65536 hides kernel segmentation cost)</li><li>Docker daemon restart to pick up the new limits</li></ul>\n<p>Post-run <code>system_restore</code> reverts governor and loopback MTU to defaults.</p>\n<h2 id=\"practical-takeaways\">Practical takeaways</h2>\n<ol><li><strong>CCXs are the locality unit, not NUMA nodes.</strong> Pin consumers to contiguous 4-core groups when you can; avoid splitting a CCX across two consumers unless you explicitly want them to share L3 (like Redis + server).</li><li><strong>Keep SMT pairs together.</strong> Every cpuset in the harness respects <code>(N, N+64)</code> pairing - preserved automatically if you specify cpusets like <code>a-b,(a+64)-(b+64)</code>.</li><li><strong>NUMA is a non-issue on this chip.</strong> Don&#x27;t waste time with <code>numactl --membind</code> or NPS subdivision.</li><li><strong>Memory bandwidth is 99.8% idle</strong> at our rps. Memory-side optimizations only help workloads with much higher per-request data movement (analytics, streaming) than REST-API-style request handling.</li></ol></div>"},"add-framework/directory-structure":{"t":"Directory Structure","html":"<div class=\"doc-body\"><p>Create a directory under <code>frameworks/</code> with your framework&#x27;s name:</p>\n<pre><code>frameworks/\n  your-framework/\n    Dockerfile\n    meta.json\n    ... (source files)</code></pre>\n<h2 id=\"dockerfile\">Dockerfile</h2>\n<p>The Dockerfile should build and run your server. Containers are started with <code>--network host</code>, so bind to:</p>\n<ul><li><strong>Port 8080</strong> - HTTP/1.1</li><li><strong>Port 8443</strong> - HTTPS with HTTP/2 and HTTP/3 (if supported)</li></ul>\n<p>Example (Go):</p>\n<pre><code class=\"language-dockerfile\">FROM golang:1.22-alpine AS build\nWORKDIR /app\nCOPY go.mod go.sum ./\nRUN go mod download\nCOPY . .\nRUN CGO_ENABLED=0 go build -o server .\n\nFROM alpine:3.19\nCOPY --from=build /app/server /server\nCMD [&quot;/server&quot;]</code></pre>\n<h2 id=\"mounted-volumes\">Mounted volumes</h2>\n<p>The benchmark runner mounts these paths into your container (read-only):</p>\n<table><thead><tr><th>Path</th><th>Purpose</th></tr></thead><tbody><tr><td><code>/data/dataset.json</code></td><td>50-item dataset for <code>/json</code> endpoint</td></tr><tr><td><code>/data/static/</code></td><td>20 static assets (CSS, JS, HTML, fonts, images) - 15 ship with <code>.gz</code> and <code>.br</code> sibling files for precompression-aware frameworks</td></tr><tr><td><code>/certs/server.crt</code></td><td>TLS certificate for HTTPS/H2/H3</td></tr><tr><td><code>/certs/server.key</code></td><td>TLS private key for HTTPS/H2/H3</td></tr></tbody></table>\n<p>Postgres (profiles <code>async-db</code>, <code>crud</code>, <code>api-4</code>, <code>api-16</code>, and the compose-orchestrated gateway + production-stack) is provided by a separate sidecar container, reachable via the <code>DATABASE_URL</code> environment variable - not a mount. Redis (profile <code>crud</code>) is similarly reachable via <code>REDIS_URL</code>. See <a href=\"#doc=running-locally/configuration\" data-doc=\"running-locally/configuration\">Configuration</a> for the full env var list.</p></div>"},"add-framework/meta-json":{"t":"meta.json","html":"<div class=\"doc-body\"><p>Create a <code>meta.json</code> file in your framework directory:</p>\n<pre><code class=\"language-json\">{\n  &quot;display_name&quot;: &quot;your-framework&quot;,\n  &quot;language&quot;: &quot;Go&quot;,\n  &quot;engine&quot;: &quot;net/http&quot;,\n  &quot;type&quot;: &quot;flagship&quot;,\n  &quot;mode&quot;: &quot;standard&quot;,\n  &quot;description&quot;: &quot;Short description of the framework and its key features.&quot;,\n  &quot;repo&quot;: &quot;https://github.com/org/repo&quot;,\n  &quot;enabled&quot;: true,\n  &quot;tests&quot;: [&quot;baseline&quot;, &quot;pipelined&quot;, &quot;limited-conn&quot;, &quot;json&quot;, &quot;json-comp&quot;, &quot;upload&quot;, &quot;api-4&quot;, &quot;api-16&quot;, &quot;baseline-h2&quot;, &quot;static-h2&quot;],\n  &quot;maintainers&quot;: [&quot;your-github-username&quot;]\n}</code></pre>\n<h2 id=\"fields\">Fields</h2>\n<table><thead><tr><th>Field</th><th>Description</th></tr></thead><tbody><tr><td><code>display_name</code></td><td>Name shown on the leaderboard</td></tr><tr><td><code>language</code></td><td>Programming language (e.g., <code>Go</code>, <code>Rust</code>, <code>C#</code>, <code>Java</code>)</td></tr><tr><td><code>engine</code></td><td>HTTP server engine (e.g., <code>Kestrel</code>, <code>Tomcat</code>, <code>hyper</code>)</td></tr><tr><td><code>type</code></td><td><code>flagship</code>, <code>emerging</code> or <code>experimental</code> for frameworks, <code>engine</code> for bare-metal implementations, <code>infrastructure</code> for reverse proxies / static-file servers</td></tr><tr><td><code>mode</code></td><td>Frameworks only: <code>standard</code> (default - idiomatic, production-style usage) or <code>tuned</code> (non-default config / optimizations)</td></tr><tr><td><code>description</code></td><td>Shown in the framework detail popup on the leaderboard</td></tr><tr><td><code>repo</code></td><td>Link to the framework&#x27;s source repository</td></tr><tr><td><code>enabled</code></td><td>Set to <code>false</code> to skip this framework during benchmark runs</td></tr><tr><td><code>tests</code></td><td>Array of test profiles this framework participates in</td></tr><tr><td><code>maintainers</code></td><td>Array of GitHub usernames to notify when a PR modifies this framework</td></tr></tbody></table>\n<h2 id=\"available-test-profiles\">Available test profiles</h2>\n<table><thead><tr><th>Profile</th><th>Protocol</th><th>Required endpoints</th></tr></thead><tbody><tr><td><code>baseline</code></td><td>HTTP/1.1</td><td><code>/baseline11</code></td></tr><tr><td><code>pipelined</code></td><td>HTTP/1.1</td><td><code>/pipeline</code></td></tr><tr><td><code>limited-conn</code></td><td>HTTP/1.1</td><td><code>/baseline11</code></td></tr><tr><td><code>json</code></td><td>HTTP/1.1</td><td><code>/json/{count}?m=N</code></td></tr><tr><td><code>json-comp</code></td><td>HTTP/1.1</td><td><code>/json/{count}?m=N</code> (must honor <code>Accept-Encoding: gzip, br</code>)</td></tr><tr><td><code>json-tls</code></td><td>HTTP/1.1 + TLS</td><td><code>/json/{count}?m=N</code> (port 8081, ALPN <code>http/1.1</code>)</td></tr><tr><td><code>upload</code></td><td>HTTP/1.1</td><td><code>/upload</code></td></tr><tr><td><code>api-4</code></td><td>HTTP/1.1</td><td><code>/baseline11</code>, <code>/json/{count}</code>, <code>/async-db</code> (4 CPU, 16 GB)</td></tr><tr><td><code>api-16</code></td><td>HTTP/1.1</td><td><code>/baseline11</code>, <code>/json/{count}</code>, <code>/async-db</code> (16 CPU, 32 GB)</td></tr><tr><td><code>static</code></td><td>HTTP/1.1</td><td><code>/static/*</code> (port 8080)</td></tr><tr><td><code>async-db</code></td><td>HTTP/1.1</td><td><code>/async-db?min=X&amp;max=Y&amp;limit=N</code> (requires <code>DATABASE_URL</code>)</td></tr><tr><td><code>crud</code></td><td>HTTP/1.1</td><td><code>/api/items</code>, <code>/api/items/{id}</code> (GET/POST/PUT; requires <code>DATABASE_URL</code>, optional <code>REDIS_URL</code>)</td></tr><tr><td><code>baseline-h2</code></td><td>HTTP/2</td><td><code>/baseline2</code> (TLS, port 8443)</td></tr><tr><td><code>static-h2</code></td><td>HTTP/2</td><td><code>/static/*</code> (TLS, port 8443)</td></tr><tr><td><code>baseline-h2c</code></td><td>HTTP/2 cleartext</td><td><code>/baseline2</code> (port 8082, prior-knowledge)</td></tr><tr><td><code>json-h2c</code></td><td>HTTP/2 cleartext</td><td><code>/json/{count}?m=N</code> (port 8082, prior-knowledge)</td></tr><tr><td><code>baseline-h3</code></td><td>HTTP/3</td><td><code>/baseline2</code> (QUIC, port 8443)</td></tr><tr><td><code>static-h3</code></td><td>HTTP/3</td><td><code>/static/*</code> (QUIC, port 8443)</td></tr><tr><td><code>gateway-64</code></td><td>HTTP/2</td><td>Compose stack serving <code>/static/*</code>, <code>/json</code>, <code>/async-db</code>, <code>/baseline2</code> (TLS, port 8443)</td></tr><tr><td><code>gateway-h3</code></td><td>HTTP/3</td><td>Compose stack serving <code>/static/*</code>, <code>/json</code>, <code>/async-db</code>, <code>/baseline2</code> (QUIC, port 8443)</td></tr><tr><td><code>production-stack</code></td><td>HTTP/2</td><td>Compose stack: edge + JWT auth sidecar + Redis + server (TLS, port 8443)</td></tr><tr><td><code>unary-grpc</code></td><td>gRPC</td><td><code>BenchmarkService/GetSum</code> (h2c, port 8080)</td></tr><tr><td><code>unary-grpc-tls</code></td><td>gRPC</td><td><code>BenchmarkService/GetSum</code> (TLS, port 8443)</td></tr><tr><td><code>stream-grpc</code></td><td>gRPC</td><td><code>BenchmarkService/StreamSum</code> (h2c, port 8080)</td></tr><tr><td><code>stream-grpc-tls</code></td><td>gRPC</td><td><code>BenchmarkService/StreamSum</code> (TLS, port 8443)</td></tr><tr><td><code>echo-ws</code></td><td>WebSocket</td><td><code>/ws</code> echo (port 8080)</td></tr></tbody></table>\n<p>Only include profiles your framework supports. Frameworks missing a profile simply don&#x27;t appear in that profile&#x27;s leaderboard.</p>\n<p>Per-profile endpoint contracts, request/response shapes, and validation rules live under the <a href=\"#doc=test-profiles\" data-doc=\"test-profiles\">Test Profiles</a> section - link to the specific profile&#x27;s Implementation page from your PR description when adding a new framework.</p></div>"},"add-framework":{"t":"Add a Framework","html":"<div class=\"doc-body\"><p>Adding a framework to HttpArena takes a few steps: create a Dockerfile, add metadata, implement the required endpoints, and open a PR.</p>\n<div class=\"doc-cards\"><a class=\"doc-card\" href=\"#doc=add-framework/directory-structure\" data-doc=\"add-framework/directory-structure\"><span class=\"dc-t\">Directory Structure</span><span class=\"dc-s\">How to organize your framework&#x27;s files - Dockerfile, meta.json, and source code.</span></a><a class=\"doc-card\" href=\"#doc=test-profiles\" data-doc=\"test-profiles\"><span class=\"dc-t\">Test Profiles</span><span class=\"dc-s\">All HTTP endpoints your framework must implement, organized by test profile.</span></a><a class=\"doc-card\" href=\"#doc=add-framework/meta-json\" data-doc=\"add-framework/meta-json\"><span class=\"dc-t\">meta.json</span><span class=\"dc-s\">Framework metadata - display name, language, type, and which tests to participate in.</span></a><a class=\"doc-card\" href=\"#doc=add-framework/implementation-rules\" data-doc=\"add-framework/implementation-rules\"><span class=\"dc-t\">Implementation Rules</span><span class=\"dc-s\">Rules for keeping benchmarks realistic - use framework APIs, production settings only, standard libraries.</span></a></div></div>"},"running-locally/archiving-rounds":{"t":"Archiving Rounds","html":"<div class=\"doc-body\"><p>Snapshot current benchmark results as named rounds that users can browse on the leaderboard.</p>\n<h2 id=\"overview\">Overview</h2>\n<p>You can archive the current benchmark results as a named snapshot. On the leaderboard, users can switch between archived rounds and the current ongoing results.</p>\n<h2 id=\"creating-a-snapshot\">Creating a snapshot</h2>\n<pre><code class=\"language-bash\">./scripts/archive.sh create &quot;Round 1 - March 2026&quot;</code></pre>\n<p>When you create a snapshot, it bundles all current result data from <code>site/data/*.json</code> into a single <code>site/data/rounds/&lt;id&gt;.json</code> file. After rebuilding Hugo, the round selector will appear on the leaderboard page letting users switch between &quot;Current&quot; and any archived rounds.</p>\n<h2 id=\"listing-archived-rounds\">Listing archived rounds</h2>\n<pre><code class=\"language-bash\">./scripts/archive.sh list</code></pre>\n<h2 id=\"deleting-an-archived-round\">Deleting an archived round</h2>\n<pre><code class=\"language-bash\">./scripts/archive.sh delete 1</code></pre></div>"},"running-locally/manual-load-testing":{"t":"Manual load testing","html":"<div class=\"doc-body\"><p>Sometimes you want to start a framework yourself, poke at individual endpoints, try different flags, or run a load generator outside the benchmark harness altogether. This page is the &quot;bring your own server, fire a docker load-gen at it&quot; workflow.</p>\n<h2 id=\"when-to-use-this\">When to use this</h2>\n<ul><li>Iterating on a framework implementation and you want fast feedback without the full tuning / build pipeline.</li><li>Trying a different connection count, duration, or request template than any stock profile uses.</li><li>Debugging a single endpoint in isolation.</li><li>Running a load generator against a server that isn&#x27;t in <code>frameworks/</code> at all - anything on <code>localhost</code> works.</li></ul>\n<p>If you want reproducible numbers that match the leaderboard, use <code>benchmark.sh</code>. This page is for the in-between.</p>\n<h2 id=\"step-1-start-the-server\">Step 1 - start the server</h2>\n<h3 id=\"option-a-use-scriptsrunsh\">Option A: use <code>scripts/run.sh</code></h3>\n<p>Starts a framework container interactively with the same mounts the benchmark uses. <code>Ctrl+C</code> to stop.</p>\n<pre><code class=\"language-bash\">./scripts/run.sh actix</code></pre>\n<p>This exposes <code>8080</code> (HTTP/1.1, h2c gRPC), <code>8443</code> (HTTPS / HTTP/2 / HTTP/3 / gRPC-TLS), and <code>8081</code> (h1 TLS for <code>json-tls</code>).</p>\n<p>If you need postgres for <code>async-db</code> / <code>api-*</code>:</p>\n<pre><code class=\"language-bash\">docker run -d --name httparena-postgres --network host \\\n    -e POSTGRES_USER=bench -e POSTGRES_PASSWORD=bench -e POSTGRES_DB=benchmark \\\n    -v &quot;$(pwd)/data/pgdb-seed.sql:/docker-entrypoint-initdb.d/seed.sql:ro&quot; \\\n    postgres:18 -c max_connections=256</code></pre>\n<h3 id=\"option-b-your-own-binary\">Option B: your own binary</h3>\n<p>Any server on <code>localhost:8080</code> (or wherever) works. The load generators don&#x27;t care what&#x27;s on the other end.</p>\n<h2 id=\"step-2-build-the-load-generator-images-once\">Step 2 - build the load-generator images once</h2>\n<p>If you&#x27;ve run <code>benchmark-lite.sh</code> or <code>LOADGEN_DOCKER=true ./scripts/benchmark.sh ...</code> at least once, these already exist. Otherwise build them up front - it&#x27;s a one-off cost:</p>\n<pre><code class=\"language-bash\">docker build -t gcannon:latest    -f docker/gcannon.Dockerfile    docker\ndocker build -t h2load:latest     -f docker/h2load.Dockerfile     docker\ndocker build -t h2load-h3:local   -f docker/h2load-h3.Dockerfile  docker\ndocker build -t wrk:local         -f docker/wrk.Dockerfile        docker\ndocker build -t ghz:local         -f docker/ghz.Dockerfile        docker</code></pre>\n<p>The slow one is <code>h2load-h3:local</code> (compiles quictls + nghttp3 + ngtcp2 + nghttp2 --enable-http3 from source - 5\u201310 minutes). Everything else is under a minute.</p>\n<h2 id=\"step-3-run-a-load-generator\">Step 3 - run a load generator</h2>\n<p>Every image ships with the tool as its <code>ENTRYPOINT</code>, so <code>docker run &lt;image&gt; &lt;tool-args&gt;</code> works directly. Always use <code>--network host</code> so the container can reach <code>localhost</code>.</p>\n<p>A handy alias for brevity. The <code>memlock</code> and <code>seccomp</code> flags are <strong>required</strong> for gcannon - without them <code>io_uring_queue_init</code> returns <code>Operation not permitted</code> and gcannon exits silently with zero requests.</p>\n<pre><code class=\"language-bash\">DFLAGS=&#x27;--rm --network host --ulimit nofile=1048576:1048576 --ulimit memlock=-1:-1 --security-opt seccomp=unconfined&#x27;</code></pre>\n<h3 id=\"gcannon-http11-websocket-raw-templates\">gcannon - HTTP/1.1, WebSocket, <code>--raw</code> templates</h3>\n<pre><code class=\"language-bash\"># Baseline: 512 connections, 64 threads, 5s\ndocker run $DFLAGS gcannon:latest \\\n    http://localhost:8080/baseline11?a=1\\&amp;b=1 \\\n    -c 512 -t 64 -d 5s -p 1\n\n# Pipelined (depth 16)\ndocker run $DFLAGS gcannon:latest \\\n    http://localhost:8080/pipeline \\\n    -c 512 -t 64 -d 5s -p 16\n\n# Multi-template raw request rotation (mount repo&#x27;s requests/ dir read-only)\ndocker run $DFLAGS \\\n    -v &quot;$(pwd)/requests:/requests:ro&quot; \\\n    gcannon:latest http://localhost:8080 \\\n    --raw /requests/get.raw,/requests/json-get.raw,/requests/async-db-get.raw \\\n    -c 1024 -t 64 -d 10s\n\n# WebSocket echo\ndocker run $DFLAGS gcannon:latest \\\n    http://localhost:8080/ws --ws \\\n    -c 512 -t 64 -d 5s</code></pre>\n<p>Useful flags: <code>-r &lt;N&gt;</code> force-reconnect every N requests (<code>0</code> = keep-alive forever), <code>-s &lt;code&gt;</code> expected status, <code>--recv-buf &lt;bytes&gt;</code> receive buffer size, <code>--json</code> machine-readable output.</p>\n<h4 id=\"dynamic-placeholders-in-raw-templates\">Dynamic placeholders in raw templates</h4>\n<p>Raw request files support <code>{RAND:min:max}</code> and <code>{SEQ:start}</code> placeholders that are substituted per-request at send time. Useful for CRUD benchmarks where each request should target a different database row:</p>\n<pre><code class=\"language-bash\"># Template with {RAND} - each request reads a random item\nprintf &#x27;GET /crud/{RAND:1:100000} HTTP/1.1\\r\\nHost: localhost:8080\\r\\n\\r\\n&#x27; &gt; /tmp/crud-read.raw\n\n# Template with {SEQ} - each request creates a unique item\nprintf &#x27;POST /crud HTTP/1.1\\r\\nHost: localhost:8080\\r\\nContent-Type: application/json\\r\\nContent-Length: 72\\r\\n\\r\\n{&quot;id&quot;:{SEQ:100001},&quot;name&quot;:&quot;Bench&quot;,&quot;category&quot;:&quot;test&quot;,&quot;price&quot;:100,&quot;qty&quot;:50}&#x27; &gt; /tmp/crud-create.raw\n\n# Mix them - gcannon round-robins across templates\ndocker run $DFLAGS \\\n    -v /tmp:/requests:ro \\\n    gcannon:latest http://localhost:8080 \\\n    --raw /requests/crud-read.raw,/requests/crud-create.raw \\\n    -c 512 -t 64 -d 5s</code></pre>\n<p>Values are zero-padded to a fixed width so <code>Content-Length</code> stays correct. <code>{RAND}</code> uses per-connection RNG (no contention), <code>{SEQ}</code> uses a global atomic counter (unique across all threads).</p>\n<h3 id=\"h2load-http2-and-h2h2c-grpc-unary\">h2load - HTTP/2 and h2/h2c gRPC unary</h3>\n<pre><code class=\"language-bash\"># HTTP/2 over TLS, 256 connections, 100 streams/conn\ndocker run $DFLAGS h2load:latest \\\n    https://localhost:8443/baseline2?a=1\\&amp;b=1 \\\n    -c 256 -m 100 -t 128 -D 5\n\n# gRPC unary over h2c (need a prebuilt request body)\ndocker run $DFLAGS \\\n    -v &quot;$(pwd)/requests:/requests:ro&quot; \\\n    h2load:latest \\\n    http://localhost:8080/benchmark.BenchmarkService/GetSum \\\n    -d /requests/grpc-sum.bin \\\n    -H &#x27;content-type: application/grpc&#x27; -H &#x27;te: trailers&#x27; \\\n    -c 256 -m 100 -t 128 -D 5</code></pre>\n<p><code>-D</code> takes seconds (not <code>5s</code>), <code>-m</code> is the max concurrent streams per connection, <code>-t</code> is worker threads.</p>\n<h3 id=\"h2load-h3-http3-over-quic\">h2load-h3 - HTTP/3 over QUIC</h3>\n<p>Same binary family as h2load, same flags, plus <code>--alpn-list=h3</code>:</p>\n<pre><code class=\"language-bash\">docker run $DFLAGS h2load-h3:local \\\n    --alpn-list=h3 \\\n    https://localhost:8443/baseline2?a=1\\&amp;b=1 \\\n    -c 64 -m 64 -t 64 -D 5</code></pre>\n<p>If h3 errors out with <code>connect error</code>, bump the host&#x27;s UDP buffer sizes - the benchmark driver sets these to 7.5 MB but a plain shell doesn&#x27;t inherit them:</p>\n<pre><code class=\"language-bash\">sudo sysctl -w net.core.rmem_max=7500000 net.core.wmem_max=7500000</code></pre>\n<h3 id=\"wrk-static-file-rotation-json-tls\">wrk - static file rotation, json-tls</h3>\n<p><code>wrk</code> needs the lua scripts from <code>requests/</code> for multi-URL rotation:</p>\n<pre><code class=\"language-bash\">docker run $DFLAGS \\\n    -v &quot;$(pwd)/requests:/requests:ro&quot; \\\n    wrk:local \\\n    -t 64 -c 1024 -d 5s \\\n    -s /requests/static-rotate.lua \\\n    http://localhost:8080</code></pre>\n<p>A simple single-URL test (no script, no rotation):</p>\n<pre><code class=\"language-bash\">docker run $DFLAGS wrk:local \\\n    -t 8 -c 256 -d 5s \\\n    http://localhost:8080/baseline11?a=1\\&amp;b=1</code></pre>\n<h3 id=\"ghz-grpc-streaming\">ghz - gRPC streaming</h3>\n<p>ghz needs the <code>.proto</code> file mounted in. It emits a big text summary by default; add <code>--format=json</code> for scriptable output.</p>\n<pre><code class=\"language-bash\"># Unary, h2c\ndocker run $DFLAGS \\\n    -v &quot;$(pwd)/requests:/requests:ro&quot; \\\n    ghz:local \\\n    --insecure --proto /requests/benchmark.proto \\\n    --call benchmark.BenchmarkService/GetSum \\\n    -d &#x27;{&quot;a&quot;:1,&quot;b&quot;:2}&#x27; \\\n    --connections 64 -c 256 -z 5s \\\n    localhost:8080\n\n# Bidi-ish streaming: one call fans out 5000 messages, 64 concurrent connections\ndocker run $DFLAGS \\\n    -v &quot;$(pwd)/requests:/requests:ro&quot; \\\n    ghz:local \\\n    --insecure --proto /requests/benchmark.proto \\\n    --call benchmark.BenchmarkService/StreamSum \\\n    -d &#x27;{&quot;a&quot;:1,&quot;b&quot;:2,&quot;count&quot;:5000}&#x27; \\\n    --connections 64 -c 256 -n 50000 \\\n    localhost:8080</code></pre>\n<p>For TLS (<code>grpc-tls</code> / <code>stream-grpc-tls</code>) use <code>--skipTLS</code> instead of <code>--insecure</code> and point at port <code>8443</code>.</p>\n<h2 id=\"cpu-pinning-optional\">CPU pinning (optional)</h2>\n<p>The benchmark script pins the load generator to one half of the CPU topology via <code>--cpuset-cpus</code> to avoid stealing cycles from the framework. For a manual run you can do the same:</p>\n<pre><code class=\"language-bash\">docker run $DFLAGS --cpuset-cpus=32-63,96-127 gcannon:latest ...</code></pre>\n<p>On a laptop there&#x27;s no point - the container will get more cores than the server has anyway. Just leave it off.</p>\n<h2 id=\"reusing-the-benchmarks-request-templates\">Reusing the benchmark&#x27;s request templates</h2>\n<p>Everything under <code>requests/</code> is fair game:</p>\n<table><thead><tr><th>File</th><th>What it is</th></tr></thead><tbody><tr><td><code>requests/get.raw</code>, <code>post_cl.raw</code>, <code>post_chunked.raw</code></td><td>Raw HTTP/1.1 requests for <code>--raw</code> mode.</td></tr><tr><td><code>requests/json-{1,5,10,...}.raw</code></td><td>Different JSON body sizes for the <code>json</code> profile.</td></tr><tr><td><code>requests/async-db-{5,10,20,35,50}.raw</code></td><td>Different <code>limit=N</code> query variants.</td></tr><tr><td><code>requests/upload-{500k,2m,10m,20m}.raw</code></td><td>Upload bodies for the <code>upload</code> profile.</td></tr><tr><td><code>requests/static-rotate.lua</code> / <code>json-tls-rotate.lua</code></td><td>wrk rotation scripts.</td></tr><tr><td><code>requests/static-h2-uris.txt</code></td><td>URI list for <code>h2load -i</code> (static-h2 / static-h3).</td></tr><tr><td><code>requests/benchmark.proto</code></td><td>gRPC service definition.</td></tr><tr><td><code>requests/grpc-sum.bin</code></td><td>Prebuilt <code>GetSum</code> gRPC body for h2load.</td></tr></tbody></table>\n<p>Mount the directory read-only into any tool: <code>-v &quot;$(pwd)/requests:/requests:ro&quot;</code>.</p>\n<h2 id=\"parsing-the-output\">Parsing the output</h2>\n<p>The benchmark driver&#x27;s parsers live in <code>scripts/lib/tools/*.sh</code> - <code>gcannon_parse</code>, <code>h2load_parse</code>, <code>h2load_h3_parse</code>, <code>wrk_parse</code>, <code>ghz_parse</code>. If you want to script on top of a manual run, source one of those modules:</p>\n<pre><code class=\"language-bash\">source scripts/lib/common.sh\nsource scripts/lib/tools/gcannon.sh\n\nout=$(docker run --rm --network host \\\n    --ulimit memlock=-1:-1 --security-opt seccomp=unconfined \\\n    gcannon:latest \\\n    &quot;http://localhost:8080/baseline11?a=1&amp;b=1&quot; \\\n    -c 512 -t 64 -d 5s -p 1 2&gt;&amp;1)\n\ngcannon_parse &quot;&quot; &quot;$out&quot;\n# rps=3401583\n# avg_lat=123us\n# p99_lat=287us\n# ...</code></pre>\n<h2 id=\"when-your-numbers-dont-match-the-leaderboard\">When your numbers don&#x27;t match the leaderboard</h2>\n<p>A manual docker run is ~10\u201320% slower than a native <code>benchmark.sh</code> run on the same hardware because the docker daemon restart, CPU governor flip, loopback MTU change, and kernel socket tuning are all skipped. For relative comparisons between frameworks on your own machine it&#x27;s close enough; for absolute numbers use <code>benchmark.sh</code>.</p></div>"},"running-locally/setup":{"t":"Setup","html":"<div class=\"doc-body\"><p>Clone the repo and (optionally) install native load generators. If you&#x27;d rather skip the native install, jump straight to the <a href=\"#\" data-anchor=\"docker-only-path\">Docker-only path</a>.</p>\n<h2 id=\"clone-the-repository\">Clone the repository</h2>\n<pre><code class=\"language-bash\">git clone https://github.com/MDA2AV/HttpArena.git\ncd HttpArena</code></pre>\n<h2 id=\"docker-only-path\">Docker-only path</h2>\n<p>The fast road: install nothing, let the scripts build everything.</p>\n<pre><code class=\"language-bash\">./scripts/benchmark-lite.sh actix baseline</code></pre>\n<p>First invocation builds <code>gcannon:latest</code>, <code>h2load:latest</code>, <code>h2load-h3:local</code>, <code>wrk:local</code>, and <code>ghz:local</code> from <code>docker/*.Dockerfile</code>. <code>h2load-h3</code> is the slow one - expect a few minutes while quictls + ngtcp2 compile. Cached after that.</p>\n<p>To use the same docker images with the full <code>benchmark.sh</code> driver (all profiles, CPU pinning, etc.) set <code>LOADGEN_DOCKER=true</code>:</p>\n<pre><code class=\"language-bash\">LOADGEN_DOCKER=true ./scripts/benchmark.sh actix --save</code></pre>\n<h2 id=\"native-load-generators\">Native load generators</h2>\n<p>Install these on the host only if you want <code>benchmark.sh</code> to run in its default native mode. You can pick and choose - every tool has a docker fallback, so missing one isn&#x27;t fatal as long as you don&#x27;t run the profiles that need it (or you flip on <code>LOADGEN_DOCKER=true</code>).</p>\n<h3 id=\"gcannon-h1-pipelined-upload-api-416-async-db-ws-echo\">gcannon - h1 / pipelined / upload / api-4/16 / async-db / ws-echo</h3>\n<p>Requires Linux kernel \u2265 6.1 (<code>io_uring</code>). Build <code>liburing 2.9</code> first to match the production binary.</p>\n<pre><code class=\"language-bash\">git clone --branch liburing-2.9 https://github.com/axboe/liburing.git\ncd liburing &amp;&amp; ./configure --prefix=/usr &amp;&amp; make -j&quot;$(nproc)&quot; -C src &amp;&amp; sudo make install -C src &amp;&amp; cd ..\n\ngit clone https://github.com/MDA2AV/gcannon.git\ncd gcannon &amp;&amp; make &amp;&amp; sudo cp gcannon /usr/local/bin/</code></pre>\n<p>If you&#x27;d rather not install system-wide, export <code>GCANNON=/path/to/gcannon/gcannon</code> before running the script.</p>\n<h3 id=\"h2load-baseline-h2-static-h2-unary-grpc-gateway-64\">h2load - baseline-h2 / static-h2 / unary-grpc / gateway-64</h3>\n<p>Distro package - the Ubuntu 24.04 glibc build gives ~20\u201340% more throughput than the alpine/musl one.</p>\n<pre><code class=\"language-bash\">sudo apt install nghttp2-client</code></pre>\n<h3 id=\"h2load-h3-baseline-h3-static-h3\">h2load-h3 - baseline-h3 / static-h3</h3>\n<p>No distro package exists. Either build it yourself from <code>docker/h2load-h3.Dockerfile</code>&#x27;s recipe (quictls \u2192 nghttp3 \u2192 ngtcp2 \u2192 nghttp2 <code>--enable-http3</code>) or use <code>LOADGEN_DOCKER=true</code> and let the docker image handle it.</p>\n<h3 id=\"wrk-static-json-tls\">wrk - static / json-tls</h3>\n<pre><code class=\"language-bash\">sudo apt install wrk</code></pre>\n<h3 id=\"ghz-stream-grpc-stream-grpc-tls\">ghz - stream-grpc / stream-grpc-tls</h3>\n<pre><code class=\"language-bash\">go install github.com/bojand/ghz/cmd/ghz@latest</code></pre>\n<h2 id=\"tls-certificates\">TLS certificates</h2>\n<p>The repo ships self-signed certs in <code>certs/</code> (<code>server.crt</code> + <code>server.key</code>). Every framework container mounts them at <code>/certs</code> automatically - no action needed.</p>\n<p>To regenerate them (optional):</p>\n<pre><code class=\"language-bash\">openssl req -x509 -newkey rsa:2048 -keyout certs/server.key -out certs/server.crt \\\n  -days 365 -nodes -subj &quot;/CN=localhost&quot;</code></pre>\n<h2 id=\"permission-check\">Permission check</h2>\n<p>Confirm your user can talk to Docker without sudo:</p>\n<pre><code class=\"language-bash\">docker ps</code></pre>\n<p>If that prints a permission error, either add your user to the <code>docker</code> group (<code>sudo usermod -aG docker $USER</code>, log out, log back in) or run the scripts as root.</p>\n<p>Host tuning (CPU governor, sysctl, docker daemon restart, MTU, page cache drop) does use <code>sudo</code>. Without it the scripts warn and continue - you&#x27;ll still get usable results, just with noisier numbers.</p></div>"},"running-locally/prerequisites":{"t":"Prerequisites","html":"<div class=\"doc-body\"><p>What you need installed before running HttpArena benchmarks locally.</p>\n<h2 id=\"operating-system\">Operating system</h2>\n<table><thead><tr><th>OS</th><th>Supported</th><th>Notes</th></tr></thead><tbody><tr><td><strong>Linux</strong> (kernel \u2265 6.1)</td><td>\u2705 Yes</td><td>The only fully-supported target. Required for <code>io_uring</code> (gcannon native), <code>cpupower</code>, the host <code>sysctl</code> knobs the tuner writes, and <code>--network host</code> behaviour Docker containers rely on. Tested primarily on Ubuntu 24.04 / 25.04.</td></tr><tr><td><strong>macOS</strong></td><td>\u274c No</td><td>Docker Desktop runs in a Linux VM; <code>--network host</code>, host-side tuning, and cgroup cpuset all behave differently.</td></tr><tr><td><strong>Windows</strong></td><td>\u274c No</td><td>The Windows driver scripts were removed. WSL2 may work for <code>benchmark-lite.sh</code>-style runs but is untested.</td></tr></tbody></table>\n<p><code>benchmark.sh</code> will refuse to do anything destructive without root (tuning is best-effort), but gcannon native mode, Docker <code>--cpuset-cpus</code> pinning, and the postgres sidecar all assume Linux semantics.</p>\n<h2 id=\"common-requirements-both-scripts\">Common requirements (both scripts)</h2>\n<ul><li><strong>Docker Engine \u2265 24</strong> - framework containers, postgres sidecar, and every load-generator image run under Docker.</li><li><strong>bash \u2265 4.4</strong> - the driver uses associative arrays.</li><li><strong>python3</strong> - for result JSON aggregation (<code>scripts/rebuild_site_data.py</code>) and <code>meta.json</code> parsing.</li><li><strong>curl</strong> - readiness probes.</li><li><strong>git</strong> - image builds clone gcannon / nghttp2 / ghz source from GitHub.</li></ul>\n<h2 id=\"choosing-a-script\">Choosing a script</h2>\n<table><thead><tr><th></th><th><code>benchmark.sh</code></th><th><code>benchmark-lite.sh</code></th></tr></thead><tbody><tr><td>Who it&#x27;s for</td><td>Full runs on a tuned benchmark host</td><td>Laptops, CI runners, quick local validation</td></tr><tr><td>Load generators</td><td>Native binaries by default, docker mode opt-in (<code>LOADGEN_DOCKER=true</code>)</td><td><strong>Always</strong> docker, forced</td></tr><tr><td>CPU pinning</td><td>Per-profile <code>--cpuset-cpus</code></td><td>None (container gets all cores)</td></tr><tr><td>Threads</td><td>64 (h1) / 128 (h2) / 64 (h3) - fixed</td><td><code>nproc / 2</code>, or <code>--load-threads N</code></td></tr><tr><td>Profiles</td><td>All 21</td><td>Subset (no api-4/16, json-tls, gateway-64, stream-grpc*)</td></tr><tr><td>Host tuning (CPU gov, sysctl, MTU, daemon restart)</td><td>Yes</td><td>Yes</td></tr><tr><td>Root needed</td><td>For tuning + docker</td><td>For tuning + docker</td></tr></tbody></table>\n<p>Pick <code>benchmark-lite.sh</code> if you just want to sanity-check a framework; pick <code>benchmark.sh</code> if you&#x27;re trying to reproduce leaderboard numbers.</p>\n<h2 id=\"benchmark-litesh-requirements\">benchmark-lite.sh requirements</h2>\n<p>Just Docker, bash, python3. Every load-generator image is built from <code>docker/*.Dockerfile</code> on first run (first invocation takes a few minutes while <code>quictls</code> + <code>ngtcp2</code> compile for <code>h2load-h3</code>; cached after that).</p>\n<p>You do <strong>not</strong> need <code>gcannon</code>, <code>h2load</code>, <code>h2load-h3</code>, <code>wrk</code>, or <code>ghz</code> installed on the host. You do not need a specific kernel for gcannon - the container already carries <code>liburing</code> 2.9.</p>\n<h2 id=\"benchmarksh-requirements-native-load-generator-mode\">benchmark.sh requirements (native load-generator mode)</h2>\n<p>For the default (native) mode, install each tool the host binary. If anything is missing, set <code>LOADGEN_DOCKER=true</code> and the script falls back to the same docker images <code>benchmark-lite.sh</code> uses.</p>\n<ul><li><strong>gcannon</strong> - io_uring HTTP/1.1, WebSocket, and <code>--raw</code> multi-template generator. Requires Linux kernel \u2265 6.1 with <code>io_uring</code> enabled.</li></ul>\n<p><code>bash git clone --branch liburing-2.9 https://github.com/axboe/liburing.git cd liburing &amp;&amp; ./configure --prefix=/usr &amp;&amp; make -j&quot;$(nproc)&quot; -C src &amp;&amp; sudo make install -C src cd .. git clone https://github.com/MDA2AV/gcannon.git cd gcannon &amp;&amp; make &amp;&amp; sudo cp gcannon /usr/local/bin/ </code></p>\n<ul><li><strong>h2load</strong> - HTTP/2 and gRPC (unary) generator from the nghttp2 project.</li></ul>\n<p><code>bash sudo apt install nghttp2-client </code></p>\n<ul><li><strong>h2load-h3</strong> - HTTP/3 over QUIC. No official distro package; the docker image (<code>docker/h2load-h3.Dockerfile</code>) builds <code>quictls</code> + <code>nghttp3</code> + <code>ngtcp2</code> + <code>nghttp2 --enable-http3</code> from source. Easiest path: use <code>LOADGEN_DOCKER=true</code> for h3 profiles.</li><li><strong>wrk</strong> - static-file profile. Any recent build works.</li></ul>\n<p><code>bash sudo apt install wrk </code></p>\n<ul><li><strong>ghz</strong> - gRPC streaming benchmarks.</li></ul>\n<p><code>bash go install github.com/bojand/ghz/cmd/ghz@latest </code></p>\n<h2 id=\"permission-notes\">Permission notes</h2>\n<ul><li>Docker commands assume your user is in the <code>docker</code> group, or you&#x27;ll hit <code>permission denied</code>.</li><li>System tuning (<code>cpupower frequency-set</code>, <code>sysctl -w</code>, <code>ip link set lo mtu</code>, <code>systemctl restart docker</code>, <code>drop_caches</code>) uses <code>sudo</code>. Without it the script warns and continues - you still get usable results, just with less-predictable numbers.</li><li><code>--cpuset-cpus</code> requires cgroup v2 (default on modern Linux).</li></ul></div>"},"running-locally/configuration":{"t":"Configuration","html":"<div class=\"doc-body\"><p>Everything the benchmark driver can be told to do via environment variables, plus the shape of a profile definition. For the full flag-by-flag walkthrough see <a href=\"#doc=running-locally/scripts/benchmark\" data-doc=\"running-locally/scripts/benchmark\">benchmark.sh</a> and <a href=\"#doc=running-locally/scripts/benchmark-lite\" data-doc=\"running-locally/scripts/benchmark-lite\">benchmark-lite.sh</a>.</p>\n<h2 id=\"run-settings\">Run settings</h2>\n<p>Defined in <code>scripts/lib/common.sh</code>. Override by exporting before you run the script, or inline: <code>THREADS=8 ./scripts/benchmark.sh actix baseline</code>.</p>\n<table><thead><tr><th>Variable</th><th>Default</th><th>Description</th></tr></thead><tbody><tr><td><code>DURATION</code></td><td><code>5s</code></td><td>Load-test duration per run (<code>-d</code>/<code>-D</code> passed through to the tool).</td></tr><tr><td><code>RUNS</code></td><td><code>3</code></td><td>Measurement iterations per (profile, connection count). Best wins.</td></tr><tr><td><code>THREADS</code></td><td><code>64</code></td><td>gcannon / wrk worker threads.</td></tr><tr><td><code>H2THREADS</code></td><td><code>64</code></td><td>h2load worker threads (HTTP/2, h2c gRPC).</td></tr><tr><td><code>H3THREADS</code></td><td><code>64</code></td><td>h2load-h3 worker threads (HTTP/3 over QUIC).</td></tr><tr><td><code>SKIP_TUNE</code></td><td><code>false</code></td><td>When <code>true</code>, skips kernel/hardware tuning (CPU governor, socket limits) that requires <code>sudo</code>. Useful for local development.</td></tr></tbody></table>\n<p>In <code>benchmark-lite.sh</code>, <code>THREADS</code> defaults to <code>max(nproc / 2, 1)</code> and <code>H2THREADS</code> / <code>H3THREADS</code> mirror <code>$THREADS</code>. Pass <code>--load-threads N</code> to override all three in one shot.</p>\n<h2 id=\"ports\">Ports</h2>\n<table><thead><tr><th>Variable</th><th>Default</th><th>Description</th></tr></thead><tbody><tr><td><code>PORT</code></td><td><code>8080</code></td><td>HTTP/1.1 plaintext (all <code>h1*</code> profiles + <code>echo-ws</code>); also h2c for gRPC (<code>unary-grpc</code>, <code>stream-grpc</code> - prior-knowledge on the same socket).</td></tr><tr><td><code>H2PORT</code></td><td><code>8443</code></td><td>HTTPS / HTTP/2 over TLS (<code>baseline-h2</code>, <code>static-h2</code>, gateway + production-stack), HTTP/3 over QUIC (<code>baseline-h3</code>, <code>static-h3</code>, <code>gateway-h3</code>), and gRPC-TLS (<code>unary-grpc-tls</code>, <code>stream-grpc-tls</code>).</td></tr><tr><td><code>H1TLS_PORT</code></td><td><code>8081</code></td><td>HTTP/1.1 + TLS, used only by the <code>json-tls</code> profile (ALPN <code>http/1.1</code>).</td></tr><tr><td><code>H2C_PORT</code></td><td><code>8082</code></td><td>HTTP/2 cleartext prior-knowledge for the <code>baseline-h2c</code> and <code>json-h2c</code> profiles. Must be a dedicated listener that refuses HTTP/1.1 - the validator checks this explicitly.</td></tr></tbody></table>\n<p>Every framework <code>Dockerfile</code> reads the same defaults from its env, so you rarely need to change these.</p>\n<h2 id=\"load-generator-mode\">Load-generator mode</h2>\n<table><thead><tr><th>Variable</th><th>Default</th><th>Description</th></tr></thead><tbody><tr><td><code>LOADGEN_DOCKER</code></td><td><code>false</code></td><td>When <code>true</code>, every load generator runs from its Docker image instead of the host binary. Builds missing images automatically from <code>docker/*.Dockerfile</code>. Forced <code>true</code> by <code>benchmark-lite.sh</code>.</td></tr><tr><td><code>GCANNON_MODE</code></td><td><code>native</code></td><td><code>native</code> or <code>docker</code>. Implied by <code>LOADGEN_DOCKER</code>.</td></tr><tr><td><code>GCANNON_CPUS</code></td><td>dynamic</td><td>CPU list the load generators run on. Native mode wraps calls in <code>taskset -c $GCANNON_CPUS</code>; docker mode passes it via <code>--cpuset-cpus</code>. Defaults to the upper half of available cores on smaller machines to avoid resource contention with the server.</td></tr></tbody></table>\n<h2 id=\"tool-binaries-and-images\">Tool binaries and images</h2>\n<p>Each load generator has a pair of variables - native binary name and docker image tag:</p>\n<table><thead><tr><th>Native (<code>$TOOL</code>)</th><th>Docker (<code>$TOOL_IMAGE</code>)</th><th>Used for</th><th>Source</th></tr></thead><tbody><tr><td><code>GCANNON=gcannon</code></td><td><code>GCANNON_IMAGE=gcannon:latest</code></td><td>h1, pipelined, limited-conn, json, json-comp, upload, api-4/16, async-db, echo-ws</td><td><code>docker/gcannon.Dockerfile</code></td></tr><tr><td><code>H2LOAD=h2load</code></td><td><code>H2LOAD_IMAGE=h2load:latest</code></td><td>baseline-h2, static-h2, unary-grpc, unary-grpc-tls, gateway-64</td><td><code>docker/h2load.Dockerfile</code> (Ubuntu + glibc, <strong>not</strong> alpine)</td></tr><tr><td><code>H2LOAD_H3=h2load-h3</code></td><td><code>H2LOAD_H3_IMAGE=h2load-h3:local</code></td><td>baseline-h3, static-h3</td><td><code>docker/h2load-h3.Dockerfile</code> (quictls + ngtcp2 + nghttp3)</td></tr><tr><td><code>WRK=wrk</code></td><td><code>WRK_IMAGE=wrk:local</code></td><td>static, json-tls</td><td><code>docker/wrk.Dockerfile</code></td></tr><tr><td><code>GHZ=ghz</code></td><td><code>GHZ_IMAGE=ghz:local</code></td><td>stream-grpc, stream-grpc-tls, gRPC readiness probe</td><td><code>docker/ghz.Dockerfile</code></td></tr></tbody></table>\n<h2 id=\"postgres-sidecar\">Postgres sidecar</h2>\n<p>Started automatically when the framework subscribes to <code>async-db</code>, <code>api-4</code>, <code>api-16</code>, or <code>gateway-64</code>.</p>\n<table><thead><tr><th>Variable</th><th>Default</th><th>Description</th></tr></thead><tbody><tr><td><code>PG_CONTAINER</code></td><td><code>httparena-postgres</code></td><td>Container name.</td></tr><tr><td><code>DATABASE_URL</code></td><td><code>postgres://bench:bench@localhost:5432/benchmark</code></td><td>Exported into the framework container so the app can connect.</td></tr></tbody></table>\n<p>The sidecar uses <code>postgres:18</code> (Debian, glibc) with <code>-c max_connections=256</code> and is seeded from <code>data/pgdb-seed.sql</code>.</p>\n<h2 id=\"profile-definitions\">Profile definitions</h2>\n<p>Profiles live in <code>scripts/lib/profiles.sh</code>. Format:</p>\n<pre><code>pipeline | req_per_conn | cpu_limit | connections | endpoint</code></pre>\n<ul><li><strong>pipeline</strong> - gcannon <code>-p</code> value. <code>1</code> = sequential, <code>16</code> = pipelined.</li><li><strong>req_per_conn</strong> - gcannon <code>-r</code> value. <code>0</code> = keep-alive forever; a positive number forces reconnect every N requests (exercises the accept path).</li><li><strong>cpu_limit</strong> - cpuset written to the framework container&#x27;s <code>--cpuset-cpus</code>. Blank = no pinning.</li><li><strong>connections</strong> - comma-separated list; each value becomes a separate iteration.</li><li><strong>endpoint</strong> - dispatch key. Tells <code>endpoint_tool()</code> which load generator to use and <code>gcannon_build_args()</code> (etc.) how to shape the request.</li></ul>\n<p><code>benchmark-lite.sh</code> overrides <code>PROFILES</code> and <code>PROFILE_ORDER</code> with a smaller subset and blanks the <code>cpu_limit</code> column; everything else parses identically.</p>\n<h2 id=\"profile-tool-dispatch\">Profile \u2192 tool dispatch</h2>\n<p>From <code>endpoint_tool()</code> in <code>scripts/lib/profiles.sh</code>:</p>\n<table><thead><tr><th>Endpoint</th><th>Tool</th></tr></thead><tbody><tr><td><code>static</code>, <code>json-tls</code></td><td>wrk</td></tr><tr><td><code>h2</code>, <code>static-h2</code>, <code>h2c</code>, <code>json-h2c</code>, <code>gateway-64</code>, <code>grpc</code>, <code>grpc-tls</code>, <code>production-stack</code></td><td>h2load</td></tr><tr><td><code>h3</code>, <code>static-h3</code>, <code>gateway-h3</code></td><td>h2load-h3</td></tr><tr><td><code>grpc-stream</code>, <code>grpc-stream-tls</code></td><td>ghz</td></tr><tr><td>everything else (<code>&quot;&quot;</code>, <code>pipeline</code>, <code>upload</code>, <code>api-4</code>, <code>api-16</code>, <code>async-db</code>, <code>crud</code>, <code>json</code>, <code>json-compressed</code>, <code>ws-echo</code>)</td><td>gcannon</td></tr></tbody></table>\n<h2 id=\"small-machine-overrides\">Small-machine overrides</h2>\n<p>The defaults in <code>benchmark.sh</code> assume the reference 64-core benchmark host. On a laptop, three variables usually get you to something reasonable:</p>\n<pre><code class=\"language-bash\">THREADS=8 H2THREADS=16 H3THREADS=4 ./scripts/benchmark.sh actix baseline</code></pre>\n<p>If native gcannon / h2load / h2load-h3 aren&#x27;t installed, flip the whole thing to docker mode instead of installing each tool:</p>\n<pre><code class=\"language-bash\">LOADGEN_DOCKER=true ./scripts/benchmark.sh actix --save</code></pre>\n<p>Or just use <code>benchmark-lite.sh</code>, which is this combination pre-baked.</p></div>"},"running-locally":{"t":"Running Locally","html":"<div class=\"doc-body\"><p>Run the full HttpArena benchmark suite on your own machine.</p>\n<div class=\"doc-cards\"><a class=\"doc-card\" href=\"#doc=running-locally/scripts\" data-doc=\"running-locally/scripts\"><span class=\"dc-t\">Scripts Reference</span><span class=\"dc-s\">What each script in scripts/ does - run, validate, benchmark, compare, and archive.</span></a><a class=\"doc-card\" href=\"#doc=running-locally/prerequisites\" data-doc=\"running-locally/prerequisites\"><span class=\"dc-t\">Prerequisites</span><span class=\"dc-s\">OS support, Docker, and the native vs Docker-image load-generator choices.</span></a><a class=\"doc-card\" href=\"#doc=running-locally/setup\" data-doc=\"running-locally/setup\"><span class=\"dc-t\">Setup</span><span class=\"dc-s\">Clone the repository, build gcannon, and prepare TLS certificates.</span></a><a class=\"doc-card\" href=\"#doc=running-locally/manual-load-testing\" data-doc=\"running-locally/manual-load-testing\"><span class=\"dc-t\">Manual load testing</span><span class=\"dc-s\">Start a server yourself and fire the dockerized load generators at it directly.</span></a><a class=\"doc-card\" href=\"#doc=running-locally/configuration\" data-doc=\"running-locally/configuration\"><span class=\"dc-t\">Configuration</span><span class=\"dc-s\">Default benchmark parameters - threads, duration, runs, and ports.</span></a><a class=\"doc-card\" href=\"#doc=running-locally/archiving-rounds\" data-doc=\"running-locally/archiving-rounds\"><span class=\"dc-t\">Archiving Rounds</span><span class=\"dc-s\">Snapshot current results as named rounds and manage archived benchmarks.</span></a></div></div>"},"test-profiles/h3":{"t":"H/3","html":"<div class=\"doc-body\"><p>H/3 test profiles measure framework performance over QUIC, the UDP-based transport protocol. Only frameworks with native QUIC support participate.</p>\n<div class=\"doc-cards\"><a class=\"doc-card\" href=\"#doc=test-profiles/h3/baseline-h3\" data-doc=\"test-profiles/h3/baseline-h3\"><span class=\"dc-t\">Baseline</span><span class=\"dc-s\">Raw throughput over QUIC, testing HTTP/3 transport performance on frameworks with native QUIC support.</span></a><a class=\"doc-card\" href=\"#doc=test-profiles/h3/static-h3\" data-doc=\"test-profiles/h3/static-h3\"><span class=\"dc-t\">Static Files</span><span class=\"dc-s\">Serves 20 static files over HTTP/3 (QUIC), simulating browser asset loading over the newest protocol.</span></a></div></div>"},"test-profiles/h1":{"t":"H/1.1","html":"<div class=\"doc-body\"><p>H/1.1 test profiles are split into <strong>Isolated</strong> (single-endpoint benchmarks) and <strong>Workload</strong> (multi-endpoint realistic mixes).</p>\n<div class=\"doc-cards\"><a class=\"doc-card\" href=\"#doc=test-profiles/h1/isolated\" data-doc=\"test-profiles/h1/isolated\"><span class=\"dc-t\">Isolated</span><span class=\"dc-s\">Single-endpoint benchmarks - baseline, JSON, upload, static files, and more.</span></a><a class=\"doc-card\" href=\"#doc=test-profiles/h1/workload\" data-doc=\"test-profiles/h1/workload\"><span class=\"dc-t\">Workload</span><span class=\"dc-s\">Multi-endpoint mixes that exercise multiple code paths under realistic conditions.</span></a></div></div>"},"test-profiles/gateway":{"t":"Gateway","html":"<div class=\"doc-body\"><p>Gateway test profiles benchmark multi-service deployments - proxy + server, with optional auth sidecars, caches, and databases. Unlike isolated tests that measure a single framework container, gateway tests measure the <strong>end-to-end throughput of the entire stack</strong> as a unit.</p>\n<p>All gateway tests use Docker Compose for orchestration, pin services to specific CPU cores via <code>cpuset</code>, and give entries full control over their architecture within a fixed 64-CPU budget.</p>\n<div class=\"doc-cards\"><a class=\"doc-card\" href=\"#doc=test-profiles/gateway/gateway-h2\" data-doc=\"test-profiles/gateway/gateway-h2\"><span class=\"dc-t\">Gateway H2</span><span class=\"dc-s\">Two-service proxy + server stack over HTTP/2 + TLS. Mixed workload: static 30%, JSON 35%, baseline 20%, async-db 15%.</span></a><a class=\"doc-card\" href=\"#doc=test-profiles/gateway/gateway-h3\" data-doc=\"test-profiles/gateway/gateway-h3\"><span class=\"dc-t\">Gateway H3</span><span class=\"dc-s\">Same two-service stack as Gateway H2 but with HTTP/3 + QUIC at the edge.</span></a><a class=\"doc-card\" href=\"#doc=test-profiles/gateway/production-stack\" data-doc=\"test-profiles/gateway/production-stack\"><span class=\"dc-t\">Production Stack H2</span><span class=\"dc-s\">Four-service CRUD API: edge + Redis + JWT auth sidecar + server. 10K-item cache-aside, JWT verified every request, concurrent reads + writes.</span></a></div></div>"},"test-profiles/grpc":{"t":"gRPC","html":"<div class=\"doc-body\"><p>gRPC test profiles measure framework performance using Protocol Buffers over HTTP/2. The server listens on <strong>port 8080</strong> (h2c) and <strong>port 8443</strong> (h2 TLS) and implements the <code>BenchmarkService</code> defined in <code>benchmark.proto</code>.</p>\n<div class=\"doc-cards\"><a class=\"doc-card\" href=\"#doc=test-profiles/grpc/unary\" data-doc=\"test-profiles/grpc/unary\"><span class=\"dc-t\">Unary</span><span class=\"dc-s\">Single unary RPC throughput - GetSum request/response over h2c and h2 TLS.</span></a><a class=\"doc-card\" href=\"#doc=test-profiles/grpc/stream\" data-doc=\"test-profiles/grpc/stream\"><span class=\"dc-t\">Server Streaming</span><span class=\"dc-s\">Server-streaming throughput in messages/sec - one request, N replies per call over h2c and h2 TLS.</span></a></div></div>"},"test-profiles/ws":{"t":"WebSocket","html":"<div class=\"doc-body\"><p>WebSocket test profiles measure framework performance for real-time bidirectional communication. The server listens on <strong>port 8080</strong> and accepts WebSocket upgrade requests.</p>\n<div class=\"doc-cards\"><a class=\"doc-card\" href=\"#doc=test-profiles/ws/echo\" data-doc=\"test-profiles/ws/echo\"><span class=\"dc-t\">Echo</span><span class=\"dc-s\">WebSocket echo throughput - upgrade, send messages, receive echoes.</span></a><a class=\"doc-card\" href=\"#doc=test-profiles/ws/echo-pipeline\" data-doc=\"test-profiles/ws/echo-pipeline\"><span class=\"dc-t\">Echo Pipelined (16x)</span><span class=\"dc-s\">WebSocket echo with 16 messages in flight per connection - measures frame batching and read-buffer draining.</span></a></div></div>"},"test-profiles/h2":{"t":"H/2","html":"<div class=\"doc-body\"><p>H/2 test profiles measure framework performance under multiplexed streams. The TLS variants model edge-facing deployments; the cleartext (h2c) variants model reverse-proxy-to-origin and service-to-service deployments inside a trust boundary.</p>\n<div class=\"doc-cards\"><a class=\"doc-card\" href=\"#doc=test-profiles/h2/baseline-h2\" data-doc=\"test-profiles/h2/baseline-h2\"><span class=\"dc-t\">Baseline (TLS)</span><span class=\"dc-s\">Same workload as baseline over encrypted HTTP/2 connections with TLS and stream multiplexing.</span></a><a class=\"doc-card\" href=\"#doc=test-profiles/h2/static-h2\" data-doc=\"test-profiles/h2/static-h2\"><span class=\"dc-t\">Static Files (TLS)</span><span class=\"dc-s\">Serves 20 static files of various types over HTTP/2 with multiplexed streams, simulating a browser page load.</span></a><a class=\"doc-card\" href=\"#doc=test-profiles/h2/baseline-h2c\" data-doc=\"test-profiles/h2/baseline-h2c\"><span class=\"dc-t\">Baseline (h2c)</span><span class=\"dc-s\">Same /baseline2 endpoint over HTTP/2 cleartext on port 8082 - no TLS, prior-knowledge framing. Anti-cheat verifies the port refuses HTTP/1.1.</span></a><a class=\"doc-card\" href=\"#doc=test-profiles/h2/json-h2c\" data-doc=\"test-profiles/h2/json-h2c\"><span class=\"dc-t\">JSON (h2c)</span><span class=\"dc-s\">JSON serialization workload over HTTP/2 cleartext on port 8082. Same 7 (count, m) rotation as the H/1 json profile.</span></a></div></div>"},"test-profiles/h3/static-h3/validation":{"t":"Validation","html":"<div class=\"doc-body\"><p>The HTTP/3 static files profile does not have dedicated validation checks in <code>validate.sh</code>. HTTP/3 (QUIC) validation relies on:</p>\n<ul><li>The <a href=\"#doc=test-profiles/h2/static-h2/validation\" data-doc=\"test-profiles/h2/static-h2/validation\">HTTP/2 Static Files validation</a> checks, which verify Content-Type headers, response sizes, and 404 handling over HTTPS on port 8443</li><li>The benchmark runner itself, which confirms the framework responds to HTTP/3 requests via <code>oha</code></li></ul>\n<p>The static file endpoints are shared between HTTP/2 and HTTP/3 profiles - both serve on port 8443 with TLS.</p></div>"},"test-profiles/h3/static-h3/implementation":{"t":"Implementation Guidelines","html":"<div class=\"doc-body\"><div class=\"type-rules\"><div class=\"type-rules-tabs\"><button class=\"type-rules-tab active\" onclick=\"var r=this.closest('.type-rules');r.querySelectorAll('.type-rules-tab').forEach(function(t){t.classList.remove('active')});this.classList.add('active');r.querySelectorAll('.type-rules-panel').forEach(function(p){p.classList.remove('active')});r.querySelector('[data-panel=production]').classList.add('active')\"><span class=\"tr-sq\" style=\"background:#22c55e\"></span>Standard</button><button class=\"type-rules-tab\" onclick=\"var r=this.closest('.type-rules');r.querySelectorAll('.type-rules-tab').forEach(function(t){t.classList.remove('active')});this.classList.add('active');r.querySelectorAll('.type-rules-panel').forEach(function(p){p.classList.remove('active')});r.querySelector('[data-panel=tuned]').classList.add('active')\"><span class=\"tr-sq\" style=\"background:#eab308\"></span>Tuned</button><button class=\"type-rules-tab\" onclick=\"var r=this.closest('.type-rules');r.querySelectorAll('.type-rules-tab').forEach(function(t){t.classList.remove('active')});this.classList.add('active');r.querySelectorAll('.type-rules-panel').forEach(function(p){p.classList.remove('active')});r.querySelector('[data-panel=engine]').classList.add('active')\"><span class=\"tr-sq\" style=\"background:#dc2626\"></span>Engine</button></div><div class=\"type-rules-panel active\" data-panel=\"production\">Must load files from disk on every request. No in-memory caching, no memory-mapped files, no pre-loaded file buffers. Compression must use the framework&#x27;s standard middleware or built-in static file handler - no handmade compression code. Serving pre-compressed <code>.br</code>/<code>.gz</code> variants from disk <strong>is allowed</strong>, but only through a documented framework API (e.g. ASP.NET <code>MapStaticAssets</code>, nginx <code>gzip_static</code> / <code>brotli_static</code>, Caddy <code>precompressed</code>). No custom file-suffix lookup logic.</div><div class=\"type-rules-panel\" data-panel=\"tuned\">May cache files in memory at startup, use memory-mapped files, pre-rendered response headers, or any caching strategy. May serve pre-compressed files (.gz, .br) from disk via any mechanism. Free to use any compression approach.</div><div class=\"type-rules-panel\" data-panel=\"engine\">No specific rules. Ranked separately from frameworks.</div></div>\n<p>The HTTP/3 Static Files profile serves 20 static files of various types over QUIC, simulating a browser loading page assets over HTTP/3.</p>\n<p><strong>Connections:</strong> 64</p>\n<h2 id=\"how-it-works\">How it works</h2>\n<ol><li>The load generator (<a href=\"#doc=load-generators/h3/h2load-h3\" data-doc=\"load-generators/h3/h2load-h3\">h2load-h3</a>) connects over HTTP/3 (QUIC) on port 8443</li><li>Cycles through 20 URIs from <code>requests/static-h2-uris.txt</code> (same file set as the HTTP/2 static test)</li><li>All requests include <code>Accept-Encoding: br;q=1, gzip;q=0.8</code></li><li>Each request fetches a different static file - CSS, JavaScript, HTML, fonts, SVGs, WebP images, and JSON</li><li>The server returns file contents with the correct <code>Content-Type</code>, optionally compressed</li></ol>\n<h2 id=\"what-it-measures\">What it measures</h2>\n<ul><li><strong>HTTP/3 static asset serving</strong> - mixed content types and sizes over QUIC</li><li><strong>QUIC multiplexing</strong> - how well the framework handles varied concurrent requests</li><li><strong>Content-Type handling</strong> - correct MIME type mapping across file types</li><li><strong>Compression efficiency</strong> (optional) - reduces payload size at the cost of CPU</li></ul>\n<h2 id=\"static-files\">Static files</h2>\n<p>20 files (~1.16 MB total, ~966 KB compressible text + ~200 KB binary):</p>\n<table><thead><tr><th>Type</th><th>Files</th><th>Examples</th></tr></thead><tbody><tr><td>CSS</td><td>5</td><td><code>reset.css</code>, <code>layout.css</code>, <code>theme.css</code></td></tr><tr><td>JavaScript</td><td>5</td><td><code>app.js</code>, <code>vendor.js</code>, <code>router.js</code></td></tr><tr><td>HTML</td><td>2</td><td><code>header.html</code>, <code>footer.html</code></td></tr><tr><td>Fonts</td><td>2</td><td><code>regular.woff2</code>, <code>bold.woff2</code></td></tr><tr><td>SVG</td><td>2</td><td><code>logo.svg</code>, <code>icon-sprite.svg</code></td></tr><tr><td>WebP</td><td>3</td><td><code>hero.webp</code>, <code>thumb1.webp</code>, <code>thumb2.webp</code></td></tr><tr><td>JSON</td><td>1</td><td><code>manifest.json</code></td></tr></tbody></table>\n<p>Pre-compressed versions (<code>.gz</code>, <code>.br</code>) are available on disk. See the <a href=\"#doc=test-profiles/h1/isolated/static/implementation\" data-doc=\"test-profiles/h1/isolated/static/implementation\" data-anchor=\"compression\">HTTP/1.1 static files compression section</a> for full compression rules.</p>\n<h2 id=\"expected-requestresponse\">Expected request/response</h2>\n<pre><code>GET /static/logo.svg HTTP/3</code></pre>\n<pre><code>HTTP/3 200 OK\nContent-Type: image/svg+xml\n\n(file contents)</code></pre>\n<h2 id=\"parameters\">Parameters</h2>\n<table><thead><tr><th>Parameter</th><th>Value</th></tr></thead><tbody><tr><td>Endpoint</td><td>20 URIs under <code>/static/*</code></td></tr><tr><td>Connections</td><td>64</td></tr><tr><td>Streams per connection</td><td>64 (<code>-m 64</code>)</td></tr><tr><td>Threads</td><td>64 (<code>H3THREADS</code>)</td></tr><tr><td>Duration</td><td>5s</td></tr><tr><td>Runs</td><td>3 (best taken)</td></tr><tr><td>Load generator</td><td>h2load-h3 (<code>--alpn-list=h3 -i \u2026</code>)</td></tr><tr><td>Port</td><td>8443 (TLS + QUIC)</td></tr></tbody></table></div>"},"test-profiles/h3/static-h3":{"t":"Static Files (HTTP/3)","html":"<div class=\"doc-body\"><p>Serves 20 static files of various types over QUIC, simulating a browser loading page assets over HTTP/3.</p>\n<div class=\"doc-cards\"><a class=\"doc-card\" href=\"#doc=test-profiles/h3/static-h3/implementation\" data-doc=\"test-profiles/h3/static-h3/implementation\"><span class=\"dc-t\">Implementation Guidelines</span><span class=\"dc-s\">Endpoint specification, expected request/response format, and type-specific rules.</span></a><a class=\"doc-card\" href=\"#doc=test-profiles/h3/static-h3/validation\" data-doc=\"test-profiles/h3/static-h3/validation\"><span class=\"dc-t\">Validation</span><span class=\"dc-s\">All checks executed by the validation script for this test profile.</span></a></div></div>"},"test-profiles/h3/baseline-h3/validation":{"t":"Validation","html":"<div class=\"doc-body\"><p>The HTTP/3 baseline profile does not have dedicated validation checks in <code>validate.sh</code>. HTTP/3 (QUIC) validation relies on:</p>\n<ul><li>The <a href=\"#doc=test-profiles/h2/baseline-h2/validation\" data-doc=\"test-profiles/h2/baseline-h2/validation\">HTTP/2 Baseline validation</a> checks, which verify the <code>/baseline2</code> endpoint works correctly over HTTPS on port 8443</li><li>The benchmark runner itself, which confirms the framework responds to HTTP/3 requests via <code>oha</code></li></ul>\n<p>The <code>/baseline2</code> endpoint is shared between HTTP/2 and HTTP/3 profiles - both serve on port 8443 with TLS.</p></div>"},"test-profiles/h3/baseline-h3/implementation":{"t":"Implementation Guidelines","html":"<div class=\"doc-body\"><div class=\"type-rules\"><div class=\"type-rules-tabs\"><button class=\"type-rules-tab active\" onclick=\"var r=this.closest('.type-rules');r.querySelectorAll('.type-rules-tab').forEach(function(t){t.classList.remove('active')});this.classList.add('active');r.querySelectorAll('.type-rules-panel').forEach(function(p){p.classList.remove('active')});r.querySelector('[data-panel=production]').classList.add('active')\"><span class=\"tr-sq\" style=\"background:#22c55e\"></span>Standard</button><button class=\"type-rules-tab\" onclick=\"var r=this.closest('.type-rules');r.querySelectorAll('.type-rules-tab').forEach(function(t){t.classList.remove('active')});this.classList.add('active');r.querySelectorAll('.type-rules-panel').forEach(function(p){p.classList.remove('active')});r.querySelector('[data-panel=tuned]').classList.add('active')\"><span class=\"tr-sq\" style=\"background:#eab308\"></span>Tuned</button><button class=\"type-rules-tab\" onclick=\"var r=this.closest('.type-rules');r.querySelectorAll('.type-rules-tab').forEach(function(t){t.classList.remove('active')});this.classList.add('active');r.querySelectorAll('.type-rules-panel').forEach(function(p){p.classList.remove('active')});r.querySelector('[data-panel=engine]').classList.add('active')\"><span class=\"tr-sq\" style=\"background:#dc2626\"></span>Engine</button></div><div class=\"type-rules-panel active\" data-panel=\"production\">Must use the framework native QUIC/HTTP3 support with default configuration.</div><div class=\"type-rules-panel\" data-panel=\"tuned\">May tune QUIC parameters, congestion control, and UDP buffer sizes.</div><div class=\"type-rules-panel\" data-panel=\"engine\">No specific rules. Ranked separately from frameworks.</div></div>\n<p>The HTTP/3 Baseline profile tests raw throughput over QUIC, the UDP-based transport protocol that powers HTTP/3.</p>\n<p><strong>Connections:</strong> 64</p>\n<h2 id=\"how-it-works\">How it works</h2>\n<ol><li>The load generator (<a href=\"#doc=load-generators/h3/h2load-h3\" data-doc=\"load-generators/h3/h2load-h3\">h2load-h3</a>) connects to the server over HTTP/3 (QUIC) on port 8443</li><li>Sends <code>GET /baseline2?a=1&amp;b=1</code> over 64 connections, each multiplexing 64 streams, across 64 worker threads</li><li>The server parses query parameters and returns the sum</li></ol>\n<h2 id=\"what-it-measures\">What it measures</h2>\n<ul><li><strong>QUIC transport performance</strong> - UDP-based connection handling</li><li><strong>HTTP/3 framing overhead</strong> - compared to HTTP/1.1 and HTTP/2</li><li><strong>TLS 1.3 integration</strong> - QUIC mandates encryption</li><li><strong>Framework QUIC implementation quality</strong> - varies significantly across frameworks</li></ul>\n<h2 id=\"expected-requestresponse\">Expected request/response</h2>\n<pre><code>GET /baseline2?a=1&amp;b=1 HTTP/3</code></pre>\n<pre><code>HTTP/3 200 OK\nContent-Type: text/plain\n\n2</code></pre>\n<h2 id=\"parameters\">Parameters</h2>\n<table><thead><tr><th>Parameter</th><th>Value</th></tr></thead><tbody><tr><td>Endpoint</td><td><code>GET /baseline2?a=1&amp;b=1</code></td></tr><tr><td>Connections</td><td>64</td></tr><tr><td>Streams per connection</td><td>64 (<code>-m 64</code>)</td></tr><tr><td>Threads</td><td>64 (<code>H3THREADS</code>)</td></tr><tr><td>Duration</td><td>5s</td></tr><tr><td>Runs</td><td>3 (best taken)</td></tr><tr><td>Load generator</td><td>h2load-h3 (<code>--alpn-list=h3</code>)</td></tr><tr><td>Port</td><td>8443 (TLS + QUIC)</td></tr></tbody></table>\n<h2 id=\"notes\">Notes</h2>\n<ul><li>HTTP/3 support is not universal - only frameworks with native QUIC support participate</li><li>HTTP/3 throughput is typically 4\u20136\u00d7 lower than HTTP/2 on the same framework. This reflects the inherent CPU cost of QUIC (per-packet AEAD, no kernel TLS offload, userspace packet processing) - see the <a href=\"#doc=load-generators/h3/h2load-h3\" data-doc=\"load-generators/h3/h2load-h3\">h2load-h3 load generator docs</a> for details</li></ul></div>"},"test-profiles/h3/baseline-h3":{"t":"Baseline (HTTP/3)","html":"<div class=\"doc-body\"><p>Tests raw throughput over QUIC, the UDP-based transport protocol that powers HTTP/3.</p>\n<div class=\"doc-cards\"><a class=\"doc-card\" href=\"#doc=test-profiles/h3/baseline-h3/implementation\" data-doc=\"test-profiles/h3/baseline-h3/implementation\"><span class=\"dc-t\">Implementation Guidelines</span><span class=\"dc-s\">Endpoint specification, expected request/response format, and type-specific rules.</span></a><a class=\"doc-card\" href=\"#doc=test-profiles/h3/baseline-h3/validation\" data-doc=\"test-profiles/h3/baseline-h3/validation\"><span class=\"dc-t\">Validation</span><span class=\"dc-s\">All checks executed by the validation script for this test profile.</span></a></div></div>"},"test-profiles/h1/isolated":{"t":"Isolated","html":"<div class=\"doc-body\"><p>Single-endpoint benchmarks that measure framework performance on one task at a time.</p>\n<div class=\"doc-cards\"><a class=\"doc-card\" href=\"#doc=test-profiles/h1/isolated/baseline\" data-doc=\"test-profiles/h1/isolated/baseline\"><span class=\"dc-t\">Baseline</span><span class=\"dc-s\">Primary throughput benchmark with persistent keep-alive connections and mixed GET/POST workload.</span></a><a class=\"doc-card\" href=\"#doc=test-profiles/h1/isolated/short-lived\" data-doc=\"test-profiles/h1/isolated/short-lived\"><span class=\"dc-t\">Short-lived Connection</span><span class=\"dc-s\">Connections closed after 10 requests, measuring TCP handshake and connection setup overhead.</span></a><a class=\"doc-card\" href=\"#doc=test-profiles/h1/isolated/json-processing\" data-doc=\"test-profiles/h1/isolated/json-processing\"><span class=\"dc-t\">JSON Processing</span><span class=\"dc-s\">Loads a dataset, computes derived fields, and serializes a JSON response - testing real-world API workloads.</span></a><a class=\"doc-card\" href=\"#doc=test-profiles/h1/isolated/json-compressed\" data-doc=\"test-profiles/h1/isolated/json-compressed\"><span class=\"dc-t\">JSON Compressed</span><span class=\"dc-s\">Same JSON workload with Accept-Encoding: gzip, br and a multiplier parameter - measures serialization plus compression throughput.</span></a><a class=\"doc-card\" href=\"#doc=test-profiles/h1/isolated/json-tls\" data-doc=\"test-profiles/h1/isolated/json-tls\"><span class=\"dc-t\">JSON over TLS</span><span class=\"dc-s\">Same JSON workload transported over HTTP/1.1 + TLS on port 8081 - measures the cost of encryption on top of serialization.</span></a><a class=\"doc-card\" href=\"#doc=test-profiles/h1/isolated/upload\" data-doc=\"test-profiles/h1/isolated/upload\"><span class=\"dc-t\">Upload (20 MB)</span><span class=\"dc-s\">Sends a 20 MB binary payload, server returns byte count. Measures body ingestion throughput.</span></a><a class=\"doc-card\" href=\"#doc=test-profiles/h1/isolated/async-database\" data-doc=\"test-profiles/h1/isolated/async-database\"><span class=\"dc-t\">Async Database (Postgres)</span><span class=\"dc-s\">Async Postgres range query over 100K rows, connection pooling, and JSON serialization. Framework-only benchmark.</span></a><a class=\"doc-card\" href=\"#doc=test-profiles/h1/isolated/static\" data-doc=\"test-profiles/h1/isolated/static\"><span class=\"dc-t\">Static Files</span><span class=\"dc-s\">Serves 20 static files - CSS, JS, HTML, fonts, images - over HTTP/1.1.</span></a><a class=\"doc-card\" href=\"#doc=test-profiles/h1/isolated/pipelined\" data-doc=\"test-profiles/h1/isolated/pipelined\"><span class=\"dc-t\">Pipelined (16x)</span><span class=\"dc-s\">16 requests sent back-to-back per connection, testing raw I/O and pipeline batching.</span></a><a class=\"doc-card\" href=\"#doc=test-profiles/h1/isolated/crud\" data-doc=\"test-profiles/h1/isolated/crud\"><span class=\"dc-t\">CRUD (REST API)</span><span class=\"dc-s\">Realistic REST API with paginated list, cached reads, create, and update against Postgres.</span></a><a class=\"doc-card\" href=\"#doc=test-profiles/h1/isolated/fortunes\" data-doc=\"test-profiles/h1/isolated/fortunes\"><span class=\"dc-t\">Fortunes (Templates) *</span><span class=\"dc-s\">DB query + HTML template rendering with auto-escape. Reference-only - measures template-engine throughput, not part of the composite score.</span></a></div></div>"},"test-profiles/h1/workload":{"t":"Workload","html":"<div class=\"doc-body\"><p>Multi-endpoint benchmarks that exercise multiple code paths simultaneously under realistic conditions.</p>\n<div class=\"doc-cards\"><a class=\"doc-card\" href=\"#doc=test-profiles/h1/workload/api-4\" data-doc=\"test-profiles/h1/workload/api-4\"><span class=\"dc-t\">API-4</span><span class=\"dc-s\">Baseline, JSON, and async-db constrained to 4 CPUs and 16 GB memory. Measures efficiency under limited resources.</span></a><a class=\"doc-card\" href=\"#doc=test-profiles/h1/workload/api-16\" data-doc=\"test-profiles/h1/workload/api-16\"><span class=\"dc-t\">API-16</span><span class=\"dc-s\">Same API workload with 16 CPUs and 32 GB memory. Tests performance scaling.</span></a></div></div>"},"test-profiles/h1/isolated/async-database/validation":{"t":"Validation","html":"<div class=\"doc-body\"><p>The following checks are executed by <code>validate.sh</code> for every framework subscribed to the <code>async-db</code> test. A Postgres sidecar container is started automatically before these checks run.</p>\n<h2 id=\"response-structure-with-varying-limits\">Response structure with varying limits</h2>\n<p>Sends <code>GET /async-db?min=M&amp;max=N&amp;limit=L</code> with varying price ranges and limits (different from the benchmark values to prevent hardcoded responses). Tested combinations: <code>min=5&amp;max=80&amp;limit=7</code>, <code>min=20&amp;max=150&amp;limit=18</code>, <code>min=100&amp;max=400&amp;limit=33</code>, <code>min=10&amp;max=50&amp;limit=50</code>. For each request, verifies:</p>\n<ul><li><strong>Item count</strong> equals the requested limit</li><li>Every item has a nested <code>rating</code> object with a <code>score</code> field</li><li>Every item has a <code>tags</code> field that is an array</li><li>Every item has an <code>active</code> field that is a boolean (<code>true</code>/<code>false</code>)</li></ul>\n<h2 id=\"content-type-header\">Content-Type header</h2>\n<p>Sends <code>GET /async-db?min=10&amp;max=50&amp;limit=50</code> and verifies the <code>Content-Type</code> response header is <code>application/json</code>.</p>\n<h2 id=\"anti-cheat-empty-range\">Anti-cheat: empty range</h2>\n<p>Sends <code>GET /async-db?min=9999&amp;max=9999&amp;limit=50</code> (a price range with no matching items) and verifies the response has <code>count</code> equal to <code>0</code>. This detects hardcoded responses or implementations that ignore the query parameters.</p></div>"},"test-profiles/h1/isolated/async-database/implementation":{"t":"Implementation Guidelines","html":"<div class=\"doc-body\"><div class=\"type-rules\"><div class=\"type-rules-tabs\"><button class=\"type-rules-tab active\" onclick=\"var r=this.closest('.type-rules');r.querySelectorAll('.type-rules-tab').forEach(function(t){t.classList.remove('active')});this.classList.add('active');r.querySelectorAll('.type-rules-panel').forEach(function(p){p.classList.remove('active')});r.querySelector('[data-panel=production]').classList.add('active')\"><span class=\"tr-sq\" style=\"background:#22c55e\"></span>Standard</button><button class=\"type-rules-tab\" onclick=\"var r=this.closest('.type-rules');r.querySelectorAll('.type-rules-tab').forEach(function(t){t.classList.remove('active')});this.classList.add('active');r.querySelectorAll('.type-rules-panel').forEach(function(p){p.classList.remove('active')});r.querySelector('[data-panel=tuned]').classList.add('active')\"><span class=\"tr-sq\" style=\"background:#eab308\"></span>Tuned</button><button class=\"type-rules-tab\" onclick=\"var r=this.closest('.type-rules');r.querySelectorAll('.type-rules-tab').forEach(function(t){t.classList.remove('active')});this.classList.add('active');r.querySelectorAll('.type-rules-panel').forEach(function(p){p.classList.remove('active')});r.querySelector('[data-panel=engine]').classList.add('active')\"><span class=\"tr-sq\" style=\"background:#dc2626\"></span>Engine</button></div><div class=\"type-rules-panel active\" data-panel=\"production\">Must use an async PostgreSQL driver with standard connection pooling. Size the pool from <code>DATABASE_MAX_CONN</code> (currently 256), not from CPU count.</div><div class=\"type-rules-panel\" data-panel=\"tuned\">May use custom pool sizes, prepared statement caching, or driver-specific optimizations beyond defaults.</div><div class=\"type-rules-panel\" data-panel=\"engine\">No specific rules.</div></div>\n<p>The Async Database profile measures how efficiently a framework handles concurrent database queries over a network connection - exercising async I/O scheduling, connection pooling, and async Postgres driver efficiency.</p>\n<p><strong>This test is for framework-type entries only</strong> - engines (nginx, h2o, etc.) are excluded.</p>\n<p><strong>Connections:</strong> 1,024</p>\n<h2 id=\"how-it-works\">How it works</h2>\n<ol><li>A Postgres container runs alongside the framework container on the same host, listening on <code>localhost:5432</code></li><li>The framework reads the <code>DATABASE_URL</code> environment variable at startup and initializes a connection pool</li><li>On each <code>GET /async-db?min=10&amp;max=50&amp;limit=20</code> request, the framework:<ul><li>Parses <code>min</code>, <code>max</code>, and <code>limit</code> as <strong>integers</strong> (defaults: <code>min=10</code>, <code>max=50</code>, <code>limit=50</code>; <code>limit</code> clamped to 1\u201350)</li><li>Executes an async range query with the parameterized <code>LIMIT</code> against the Postgres <code>items</code> table</li><li>Restructures <code>rating_score</code> and <code>rating_count</code> into a nested <code>rating</code> object</li><li>Serializes the result as JSON</li></ul></li><li>Returns <code>Content-Type: application/json</code></li></ol>\n<h2 id=\"what-it-measures\">What it measures</h2>\n<ul><li><strong>Async I/O scheduling</strong> - how efficiently the event loop handles network round-trips to Postgres while serving concurrent HTTP requests</li><li><strong>Connection pooling</strong> - maintaining and multiplexing a pool of Postgres connections across thousands of concurrent requests</li><li><strong>Async driver quality</strong> - the efficiency of the language&#x27;s async Postgres driver (e.g., <code>asyncpg</code>, <code>tokio-postgres</code>, <code>pg</code>)</li><li><strong>Result parsing + JSON serialization</strong> - converting Postgres rows to structured JSON with nested objects</li></ul>\n<h2 id=\"database-schema\">Database schema</h2>\n<p>The <code>items</code> table in Postgres (100,000 rows):</p>\n<pre><code class=\"language-sql\">CREATE TABLE items (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    category TEXT NOT NULL,\n    price INTEGER NOT NULL,\n    quantity INTEGER NOT NULL,\n    active BOOLEAN NOT NULL,\n    tags JSONB NOT NULL,\n    rating_score INTEGER NOT NULL,\n    rating_count INTEGER NOT NULL\n);\n-- No index on price - forces sequential scan</code></pre>\n<h2 id=\"sql-query\">SQL query</h2>\n<pre><code class=\"language-sql\">SELECT id, name, category, price, quantity, active, tags, rating_score, rating_count\nFROM items\nWHERE price BETWEEN $1 AND $2\nLIMIT $3</code></pre>\n<h2 id=\"expected-response\">Expected response</h2>\n<pre><code>GET /async-db?min=10&amp;max=50&amp;limit=20 HTTP/1.1</code></pre>\n<pre><code class=\"language-json\">{\n  &quot;items&quot;: [\n    {\n      &quot;id&quot;: 42,\n      &quot;name&quot;: &quot;Alpha Widget 42&quot;,\n      &quot;category&quot;: &quot;electronics&quot;,\n      &quot;price&quot;: 30,\n      &quot;quantity&quot;: 5,\n      &quot;active&quot;: true,\n      &quot;tags&quot;: [&quot;fast&quot;, &quot;new&quot;],\n      &quot;rating&quot;: { &quot;score&quot;: 42, &quot;count&quot;: 127 }\n    }\n  ],\n  &quot;count&quot;: N\n}</code></pre>\n<p>All numeric fields (<code>price</code>, <code>quantity</code>, <code>rating.score</code>, <code>rating.count</code>) are integers - no floats anywhere. The <code>count</code> field must be dynamically computed from the number of returned items, not hardcoded.</p>\n<p>When Postgres is unavailable or the query returns no rows, return:</p>\n<pre><code class=\"language-json\">{&quot;items&quot;:[],&quot;count&quot;:0}</code></pre>\n<h2 id=\"environment-variables\">Environment variables</h2>\n<p>The benchmark runner provides these environment variables to your container:</p>\n<table><thead><tr><th>Variable</th><th>Value</th><th>Description</th></tr></thead><tbody><tr><td><code>DATABASE_URL</code></td><td><code>postgres://bench:bench@localhost:5432/benchmark</code></td><td>Postgres connection string. Always read from this - never hardcode.</td></tr><tr><td><code>DATABASE_MAX_CONN</code></td><td><code>256</code></td><td>Maximum connections allowed by the Postgres instance. Use this to size your connection pool. May be lower for CPU-constrained tests (e.g. API-4, API-16).</td></tr></tbody></table>\n<h2 id=\"implementation-notes\">Implementation notes</h2>\n<ul><li><strong>Async driver required</strong> - use your language&#x27;s async Postgres driver (e.g., <code>asyncpg</code> for Python, <code>tokio-postgres</code> for Rust, <code>pg</code> for Node.js, <code>r2d2</code>/<code>deadpool</code> for connection pools)</li><li><strong>Connection pool</strong> - initialize a pool at startup. Size it from <code>DATABASE_MAX_CONN</code> (currently 256). Going higher than that will cause Postgres to reject connections under load</li><li><strong>Prepared statements</strong> - prepare the query once per connection, reuse across requests</li><li><strong>Default parameters</strong> - all three query parameters are integers. If <code>min</code> or <code>max</code> is missing, default to <code>10</code> and <code>50</code>. If <code>limit</code> is missing, default to <code>50</code>. Clamp <code>limit</code> to the range 1\u201350</li><li><strong>Integer types matter</strong> - <code>price</code> and <code>rating_score</code> are <code>INTEGER</code> columns. Read them as <code>i32</code>/<code>int</code>/equivalent - using <code>f64</code>/<code>double</code> will fail with type-mismatch errors in strict drivers like <code>tokio-postgres</code></li><li><strong>Tags are JSONB</strong> - Postgres returns them as native JSON, no string parsing needed</li></ul>\n<h2 id=\"important-environment-variables-and-initialization\">Important: environment variables and initialization</h2>\n<p><strong>Never hardcode</strong> connection details. Always read <code>DATABASE_URL</code> for the connection string and <code>DATABASE_MAX_CONN</code> for pool sizing.</p>\n<p>The benchmark runner starts Postgres and waits for the seed data to be fully loaded before starting your framework container. By the time your server starts, Postgres is ready and accepting connections.</p>\n<p><strong>Recommended: lazy initialization with retry.</strong> As a safety net, handle the case where the initial connection fails gracefully. Do not crash the server - return the empty fallback response and retry on the next request.</p>\n<pre><code># Pseudocode\npg_pool = null\n\non_startup:\n    try: pg_pool = connect(DATABASE_URL)\n    catch: pg_pool = null  # don&#x27;t crash\n\non_request /async-db:\n    if pg_pool is null:\n        try: pg_pool = connect(DATABASE_URL)\n        catch: return {&quot;items&quot;:[],&quot;count&quot;:0}\n    try: return query(pg_pool)\n    catch: pg_pool = null; return {&quot;items&quot;:[],&quot;count&quot;:0}</code></pre>\n<h2 id=\"parameters\">Parameters</h2>\n<table><thead><tr><th>Parameter</th><th>Value</th></tr></thead><tbody><tr><td>Endpoint</td><td><code>GET /async-db?min=X&amp;max=Y&amp;limit=N</code></td></tr><tr><td>Limits</td><td>5, 10, 20, 35, 50 (rotated with <code>-r 25</code>)</td></tr><tr><td>Connections</td><td>1,024</td></tr><tr><td>Pipeline</td><td>1</td></tr><tr><td>Duration</td><td>10s</td></tr><tr><td>Runs</td><td>3 (best taken)</td></tr><tr><td>Database</td><td>Postgres 18 (Debian, glibc), 100,000 rows, no index on <code>price</code></td></tr></tbody></table></div>"},"test-profiles/h1/isolated/async-database":{"t":"Async Database (Postgres)","html":"<div class=\"doc-body\"><p>Measures how efficiently a framework handles concurrent async database queries over a network connection with connection pooling.</p>\n<div class=\"doc-cards\"><a class=\"doc-card\" href=\"#doc=test-profiles/h1/isolated/async-database/implementation\" data-doc=\"test-profiles/h1/isolated/async-database/implementation\"><span class=\"dc-t\">Implementation Guidelines</span><span class=\"dc-s\">Endpoint specification, expected request/response format, and type-specific rules.</span></a><a class=\"doc-card\" href=\"#doc=test-profiles/h1/isolated/async-database/validation\" data-doc=\"test-profiles/h1/isolated/async-database/validation\"><span class=\"dc-t\">Validation</span><span class=\"dc-s\">All checks executed by the validation script for this test profile.</span></a></div></div>"},"test-profiles/h1/isolated/pipelined/validation":{"t":"Validation","html":"<div class=\"doc-body\"><p>The following checks are executed by <code>validate.sh</code> for every framework subscribed to the <code>pipelined</code> test.</p>\n<h2 id=\"get-pipeline-response\">GET /pipeline response</h2>\n<p>Sends <code>GET /pipeline</code> and verifies the response body is exactly <code>ok</code>.</p></div>"},"test-profiles/h1/isolated/pipelined/implementation":{"t":"Implementation Guidelines","html":"<div class=\"doc-body\"><div class=\"type-rules\"><div class=\"type-rules-tabs\"><button class=\"type-rules-tab active\" onclick=\"var r=this.closest('.type-rules');r.querySelectorAll('.type-rules-tab').forEach(function(t){t.classList.remove('active')});this.classList.add('active');r.querySelectorAll('.type-rules-panel').forEach(function(p){p.classList.remove('active')});r.querySelector('[data-panel=production]').classList.add('active')\"><span class=\"tr-sq\" style=\"background:#22c55e\"></span>Standard</button><button class=\"type-rules-tab\" onclick=\"var r=this.closest('.type-rules');r.querySelectorAll('.type-rules-tab').forEach(function(t){t.classList.remove('active')});this.classList.add('active');r.querySelectorAll('.type-rules-panel').forEach(function(p){p.classList.remove('active')});r.querySelector('[data-panel=tuned]').classList.add('active')\"><span class=\"tr-sq\" style=\"background:#eab308\"></span>Tuned</button><button class=\"type-rules-tab\" onclick=\"var r=this.closest('.type-rules');r.querySelectorAll('.type-rules-tab').forEach(function(t){t.classList.remove('active')});this.classList.add('active');r.querySelectorAll('.type-rules-panel').forEach(function(p){p.classList.remove('active')});r.querySelector('[data-panel=engine]').classList.add('active')\"><span class=\"tr-sq\" style=\"background:#dc2626\"></span>Engine</button></div><div class=\"type-rules-panel active\" data-panel=\"production\">Must use the framework standard request handling. No custom pipeline batching or read-ahead optimizations.</div><div class=\"type-rules-panel\" data-panel=\"tuned\">May implement custom pipeline batching, read buffer optimizations, or framework-specific pipelining flags.</div><div class=\"type-rules-panel\" data-panel=\"engine\">No specific rules. Ranked separately from frameworks.</div></div>\n<p>16 HTTP requests are sent back-to-back on each connection before waiting for responses. Uses a lightweight <code>GET /pipeline</code> endpoint that returns a fixed <code>ok</code> response, isolating raw I/O throughput from application logic.</p>\n<p><strong>Connections:</strong> 512, 4,096</p>\n<h2 id=\"expected-requestresponse\">Expected request/response</h2>\n<pre><code>GET /pipeline HTTP/1.1</code></pre>\n<pre><code>HTTP/1.1 200 OK\nContent-Type: text/plain\n\nok</code></pre>\n<h2 id=\"what-it-measures\">What it measures</h2>\n<ul><li>HTTP pipelining support and efficiency</li><li>Frameworks that parse multiple requests from a single read buffer gain a major advantage</li><li>Frameworks processing one request at a time per connection see minimal improvement over baseline</li><li>Network batching, write coalescing, and syscall reduction</li></ul>\n<h2 id=\"why-a-separate-endpoint\">Why a separate endpoint?</h2>\n<p>The <code>/pipeline</code> endpoint removes application-level variance (query parsing, body handling) so the benchmark measures pure I/O and protocol handling throughput. This isolates the framework ability to batch and process pipelined requests efficiently.</p></div>"},"test-profiles/h1/isolated/pipelined":{"t":"Pipelined (16x)","html":"<div class=\"doc-body\"><p>16 HTTP requests are sent back-to-back on each connection before waiting for responses, isolating raw I/O throughput from application logic.</p>\n<div class=\"doc-cards\"><a class=\"doc-card\" href=\"#doc=test-profiles/h1/isolated/pipelined/implementation\" data-doc=\"test-profiles/h1/isolated/pipelined/implementation\"><span class=\"dc-t\">Implementation Guidelines</span><span class=\"dc-s\">Endpoint specification, expected request/response format, and type-specific rules.</span></a><a class=\"doc-card\" href=\"#doc=test-profiles/h1/isolated/pipelined/validation\" data-doc=\"test-profiles/h1/isolated/pipelined/validation\"><span class=\"dc-t\">Validation</span><span class=\"dc-s\">All checks executed by the validation script for this test profile.</span></a></div></div>"},"test-profiles/h1/isolated/baseline/validation":{"t":"Validation","html":"<div class=\"doc-body\"><p>The following checks are executed by <code>validate.sh</code> for every framework subscribed to the <code>baseline</code> or <code>limited-conn</code> test.</p>\n<h2 id=\"get-with-query-parameters\">GET with query parameters</h2>\n<p>Sends <code>GET /baseline11?a=13&amp;b=42</code> and verifies the response body is <code>55</code> (sum of <code>a</code> and <code>b</code>).</p>\n<h2 id=\"post-with-content-length-body\">POST with Content-Length body</h2>\n<p>Sends <code>POST /baseline11?a=13&amp;b=42</code> with body <code>20</code> and <code>Content-Type: text/plain</code>. Verifies the response body is <code>75</code> (sum of <code>a</code>, <code>b</code>, and body).</p>\n<h2 id=\"post-with-chunked-transfer-encoding\">POST with chunked Transfer-Encoding</h2>\n<p>Sends <code>POST /baseline11?a=13&amp;b=42</code> with body <code>20</code> and <code>Transfer-Encoding: chunked</code>. Verifies the response body is <code>75</code>.</p>\n<h2 id=\"anti-cheat-randomized-query-parameters\">Anti-cheat: randomized query parameters</h2>\n<p>Generates random values for <code>a</code> and <code>b</code> (100-999), sends <code>GET /baseline11?a={a}&amp;b={b}</code>, and verifies the response matches the expected sum. This detects hardcoded responses.</p>\n<h2 id=\"anti-cheat-post-body-cache-detection\">Anti-cheat: POST body cache detection</h2>\n<p>Sends two POST requests with different random body values to the same endpoint (<code>/baseline11?a=13&amp;b=42</code>). Verifies each response reflects the correct sum for that specific body. This detects response caching or hardcoded POST handling.</p>\n<ul><li>Request 1: body=<code>{random1}</code> - expects <code>13 + 42 + random1</code></li><li>Request 2: body=<code>{random2}</code> - expects <code>13 + 42 + random2</code></li></ul>\n<h2 id=\"tcp-fragmentation\">TCP fragmentation</h2>\n<p>Each request below is sent over a raw TCP socket (<code>TCP_NODELAY</code>, no Nagle coalescing) in multiple <code>sendall()</code> writes with a 30 ms pause between fragments. Every framework&#x27;s HTTP parser must reassemble these partial reads and produce the correct response. Simulates realistic network behavior - slow clients, small MTU, intermediate proxies that chunk data.</p>\n<p>Every fragmented request sets <code>Connection: close</code> so the server closes the socket after the response and the test can read until EOF.</p>\n<ul><li><strong>Split request line</strong> - the request line arrives in two halves (<code>&quot;GET /baseli&quot;</code> + <code>&quot;ne11?a=13&amp;b=42 HTTP/1.1\\r\\n\u2026&quot;</code>). The parser sees an incomplete method/path on the first <code>recv()</code>. Expects body <code>55</code>.</li><li><strong>Split before headers</strong> - the request line arrives in one write, then each header line arrives in its own write (<code>Host:</code>, <code>User-Agent:</code>, <code>Connection:</code>). Expects body <code>55</code>.</li><li><strong>POST split headers/body</strong> - the full header block (including terminating <code>\\r\\n\\r\\n</code>) is one write, then the body arrives in a separate write after the pause. Expects body <code>75</code>.</li><li><strong>POST split body bytes</strong> - headers in one write, then the 2-byte body (<code>&quot;20&quot;</code>) arrives as two 1-byte writes. Stresses the parser&#x27;s ability to reassemble a Content-Length body across multiple <code>recv()</code> calls. Expects body <code>75</code>.</li></ul></div>"},"test-profiles/h1/isolated/baseline/implementation":{"t":"Implementation Guidelines","html":"<div class=\"doc-body\"><div class=\"type-rules\"><div class=\"type-rules-tabs\"><button class=\"type-rules-tab active\" onclick=\"var r=this.closest('.type-rules');r.querySelectorAll('.type-rules-tab').forEach(function(t){t.classList.remove('active')});this.classList.add('active');r.querySelectorAll('.type-rules-panel').forEach(function(p){p.classList.remove('active')});r.querySelector('[data-panel=production]').classList.add('active')\"><span class=\"tr-sq\" style=\"background:#22c55e\"></span>Standard</button><button class=\"type-rules-tab\" onclick=\"var r=this.closest('.type-rules');r.querySelectorAll('.type-rules-tab').forEach(function(t){t.classList.remove('active')});this.classList.add('active');r.querySelectorAll('.type-rules-panel').forEach(function(p){p.classList.remove('active')});r.querySelector('[data-panel=tuned]').classList.add('active')\"><span class=\"tr-sq\" style=\"background:#eab308\"></span>Tuned</button><button class=\"type-rules-tab\" onclick=\"var r=this.closest('.type-rules');r.querySelectorAll('.type-rules-tab').forEach(function(t){t.classList.remove('active')});this.classList.add('active');r.querySelectorAll('.type-rules-panel').forEach(function(p){p.classList.remove('active')});r.querySelector('[data-panel=engine]').classList.add('active')\"><span class=\"tr-sq\" style=\"background:#dc2626\"></span>Engine</button></div><div class=\"type-rules-panel active\" data-panel=\"production\">Must use the framework standard HTTP server with default configuration. No custom TCP tuning, no experimental flags, no worker count beyond framework defaults.</div><div class=\"type-rules-panel\" data-panel=\"tuned\">May adjust worker counts, thread pools, TCP socket options, and use framework-specific performance flags. Custom buffer sizes allowed.</div><div class=\"type-rules-panel\" data-panel=\"engine\">No specific rules. Ranked separately from frameworks.</div></div>\n<p>The primary throughput benchmark. Each connection sends one request at a time over persistent keep-alive connections.</p>\n<p><strong>Connections:</strong> 512, 4,096 <strong>CPU limit:</strong> 64 threads (container pinned to cores 0-31, 64-95 via <code>--cpuset-cpus</code>)</p>\n<h2 id=\"workload\">Workload</h2>\n<p>A mix of three request types, rotated across connections:</p>\n<ul><li><code>GET /baseline11?a=13&amp;b=42</code> - query parameter parsing, response: sum of values</li><li><code>POST /baseline11?a=13&amp;b=42</code> with Content-Length body - query params + body parsing</li><li><code>POST /baseline11?a=13&amp;b=42</code> with chunked Transfer-Encoding body - chunked decoding</li></ul>\n<p>This exercises the full HTTP handling path: request line parsing, header parsing, query string extraction, body reading (both Content-Length and chunked), integer arithmetic, and response serialization.</p>\n<h2 id=\"expected-requestresponse\">Expected request/response</h2>\n<p>GET - sum of query parameters:</p>\n<pre><code>GET /baseline11?a=13&amp;b=42 HTTP/1.1</code></pre>\n<pre><code>HTTP/1.1 200 OK\nContent-Type: text/plain\n\n55</code></pre>\n<p>POST with body - sum of query parameters + body:</p>\n<pre><code>POST /baseline11?a=13&amp;b=42 HTTP/1.1\nContent-Type: text/plain\nContent-Length: 2\n\n20</code></pre>\n<pre><code>HTTP/1.1 200 OK\nContent-Type: text/plain\n\n75</code></pre>\n<h2 id=\"what-it-measures\">What it measures</h2>\n<ul><li>Raw request throughput under ideal conditions</li><li>Full HTTP parsing pipeline performance</li><li>Keep-alive connection handling efficiency</li><li>How frameworks scale with increasing connection counts</li></ul></div>"},"test-profiles/h1/isolated/baseline":{"t":"Baseline","html":"<div class=\"doc-body\"><p>The primary throughput benchmark. Each connection sends one request at a time over persistent keep-alive connections.</p>\n<div class=\"doc-cards\"><a class=\"doc-card\" href=\"#doc=test-profiles/h1/isolated/baseline/implementation\" data-doc=\"test-profiles/h1/isolated/baseline/implementation\"><span class=\"dc-t\">Implementation Guidelines</span><span class=\"dc-s\">Endpoint specification, expected request/response format, and type-specific rules.</span></a><a class=\"doc-card\" href=\"#doc=test-profiles/h1/isolated/baseline/validation\" data-doc=\"test-profiles/h1/isolated/baseline/validation\"><span class=\"dc-t\">Validation</span><span class=\"dc-s\">All checks executed by the validation script for this test profile.</span></a></div></div>"},"test-profiles/h1/isolated/json-compressed/validation":{"t":"Validation","html":"<div class=\"doc-body\"><p>The validation script (<code>scripts/validate.sh</code>) runs these checks for the <code>json-comp</code> test profile. All must pass for a framework to be considered valid for this benchmark.</p>\n<h2 id=\"checks\">Checks</h2>\n<h3 id=\"content-encoding-is-set-when-accept-encoding-is-sent\">Content-Encoding is set when Accept-Encoding is sent</h3>\n<pre><code>GET /json/50?m=1\nAccept-Encoding: gzip, br</code></pre>\n<p>The response must include <code>Content-Encoding: gzip</code> or <code>Content-Encoding: br</code>. Any other value (including absent) is a failure.</p>\n<h3 id=\"response-body-is-correct-for-multiple-count-m-pairs\">Response body is correct for multiple (count, m) pairs</h3>\n<p>Three requests are sent with different counts and multipliers:</p>\n<table><thead><tr><th>Count</th><th>Multiplier</th></tr></thead><tbody><tr><td>25</td><td>3</td></tr><tr><td>40</td><td>7</td></tr><tr><td>50</td><td>2</td></tr></tbody></table>\n<p>For each response, after decompressing, the validator checks:</p>\n<ol><li><code>count</code> field equals the route count</li><li>Every item in <code>items</code> contains the full schema - <code>id</code>, <code>name</code>, <code>category</code>, <code>price</code>, <code>quantity</code>, <code>active</code>, <code>tags</code> (array), <code>rating</code> (object with <code>score</code> and <code>count</code>), and <code>total</code></li><li><code>total == price * quantity * m</code> for every item (integer, exact)</li></ol>\n<p>Any missing field or incorrect arithmetic is a failure. Partial payloads that omit fields are rejected. This confirms the server honors the <code>m</code> parameter and applies it per item.</p>\n<h3 id=\"no-content-encoding-when-accept-encoding-is-absent\">No Content-Encoding when Accept-Encoding is absent</h3>\n<pre><code>GET /json/50?m=1</code></pre>\n<p>Without <code>Accept-Encoding</code>, the response <strong>must not</strong> include a <code>Content-Encoding</code> header. Compression is driven per request by the client - servers that unconditionally compress fail this check.</p>\n<h2 id=\"running-locally\">Running locally</h2>\n<pre><code class=\"language-bash\">./scripts/validate.sh &lt;framework&gt;</code></pre>\n<p>Filter to this profile only:</p>\n<pre><code class=\"language-bash\">./scripts/validate.sh &lt;framework&gt; json-comp</code></pre></div>"},"test-profiles/h1/isolated/json-compressed/implementation":{"t":"Implementation Guidelines","html":"<div class=\"doc-body\"><div class=\"type-rules\"><div class=\"type-rules-tabs\"><button class=\"type-rules-tab active\" onclick=\"var r=this.closest('.type-rules');r.querySelectorAll('.type-rules-tab').forEach(function(t){t.classList.remove('active')});this.classList.add('active');r.querySelectorAll('.type-rules-panel').forEach(function(p){p.classList.remove('active')});r.querySelector('[data-panel=production]').classList.add('active')\"><span class=\"tr-sq\" style=\"background:#22c55e\"></span>Standard</button><button class=\"type-rules-tab\" onclick=\"var r=this.closest('.type-rules');r.querySelectorAll('.type-rules-tab').forEach(function(t){t.classList.remove('active')});this.classList.add('active');r.querySelectorAll('.type-rules-panel').forEach(function(p){p.classList.remove('active')});r.querySelector('[data-panel=tuned]').classList.add('active')\"><span class=\"tr-sq\" style=\"background:#eab308\"></span>Tuned</button><button class=\"type-rules-tab\" onclick=\"var r=this.closest('.type-rules');r.querySelectorAll('.type-rules-tab').forEach(function(t){t.classList.remove('active')});this.classList.add('active');r.querySelectorAll('.type-rules-panel').forEach(function(p){p.classList.remove('active')});r.querySelector('[data-panel=engine]').classList.add('active')\"><span class=\"tr-sq\" style=\"background:#dc2626\"></span>Engine</button></div><div class=\"type-rules-panel active\" data-panel=\"production\">Must use the framework standard JSON serialization and the framework or engine&#x27;s built-in response compression (middleware, filter, or equivalent). No pre-compressed caches, no bypassing the response pipeline.</div><div class=\"type-rules-panel\" data-panel=\"tuned\">May use alternative JSON libraries, tuned compression libraries, and framework-specific optimizations as long as the output is valid gzip or brotli. The JSON body must still be serialized and compressed per request - pre-computed / pre-serialized / pre-compressed response caches are not allowed on either type; they defeat the serialization and compression workload the profile exists to measure.</div><div class=\"type-rules-panel\" data-panel=\"engine\">No specific rules.</div></div>\n<p>The JSON Compressed profile is the same workload as <a href=\"#doc=test-profiles/h1/isolated/json-processing/implementation\" data-doc=\"test-profiles/h1/isolated/json-processing/implementation\">JSON Processing</a> with one difference: the client sends <code>Accept-Encoding: gzip, br</code> and the server must return a compressed response with a matching <code>Content-Encoding</code> header.</p>\n<h2 id=\"how-it-works\">How it works</h2>\n<ol><li>Server reads <code>/data/dataset.json</code> at startup (same 50-item dataset as JSON Processing)</li><li>On each <code>GET /json/{count}?m={multiplier}</code> request, the server:<ul><li>Takes the first <code>count</code> items from the dataset (1\u201350)</li><li>Computes <code>total = price \u00d7 quantity \u00d7 m</code> per item</li><li>Serializes to JSON</li><li>Compresses the response body with gzip or brotli</li><li>Returns <code>Content-Type: application/json</code> and <code>Content-Encoding: gzip</code> (or <code>br</code>)</li></ul></li><li>When the client does <strong>not</strong> send <code>Accept-Encoding</code>, the server <strong>must not</strong> set <code>Content-Encoding</code> - compression is per-request, driven by the client header</li></ol>\n<p>The benchmark round-robins across counts 25, 40, and 50 paired with multipliers 4, 8, and 6.</p>\n<h2 id=\"what-it-measures\">What it measures</h2>\n<ul><li>Everything <a href=\"#doc=test-profiles/h1/isolated/json-processing/implementation\" data-doc=\"test-profiles/h1/isolated/json-processing/implementation\" data-anchor=\"what-it-measures\">JSON Processing</a> measures</li><li><strong>Response compression throughput</strong> - gzip or brotli encoding of the serialized body</li><li><strong>Content negotiation</strong> - honoring <code>Accept-Encoding</code> per request</li><li><strong>Framework compression middleware overhead</strong> - how cheaply the framework wires compression into the response pipeline</li></ul>\n<h2 id=\"expected-response\">Expected response</h2>\n<p>For <code>GET /json/5?m=3</code> with <code>Accept-Encoding: gzip, br</code>:</p>\n<pre><code>HTTP/1.1 200 OK\nContent-Type: application/json\nContent-Encoding: gzip</code></pre>\n<p>Decompressed body:</p>\n<pre><code class=\"language-json\">{\n  &quot;items&quot;: [\n    {\n      &quot;id&quot;: 1,\n      &quot;name&quot;: &quot;Alpha Widget&quot;,\n      &quot;category&quot;: &quot;electronics&quot;,\n      &quot;price&quot;: 328,\n      &quot;quantity&quot;: 15,\n      &quot;active&quot;: true,\n      &quot;tags&quot;: [&quot;fast&quot;, &quot;new&quot;],\n      &quot;rating&quot;: { &quot;score&quot;: 48, &quot;count&quot;: 127 },\n      &quot;total&quot;: 14760\n    }\n  ],\n  &quot;count&quot;: 5\n}</code></pre>\n<p><code>total</code> is <code>price * quantity * m</code> - integer arithmetic, no rounding. For <code>GET /json/5?m=1</code>, <code>total</code> equals <code>price * quantity</code>; the multiplier is never implicitly 1.</p>\n<h2 id=\"parameters\">Parameters</h2>\n<table><thead><tr><th>Parameter</th><th>Value</th></tr></thead><tbody><tr><td>Endpoint</td><td><code>GET /json/{count}?m={multiplier}</code></td></tr><tr><td>Counts \u00d7 multipliers</td><td>(25,4), (40,8), (50,6) (round-robin)</td></tr><tr><td>Connections</td><td>512, 4096, 16384</td></tr><tr><td>Pipeline</td><td>1</td></tr><tr><td>Duration</td><td>5s</td></tr><tr><td>Runs</td><td>3 (best taken)</td></tr><tr><td>Request headers</td><td><code>Accept-Encoding: gzip, br</code></td></tr><tr><td>Dataset</td><td>50 items, mounted at <code>/data/dataset.json</code></td></tr></tbody></table>\n<h2 id=\"scoring\">Scoring</h2>\n<p>Unlike most profiles where rank follows raw requests per second, <strong>JSON Compressed is scored by a compression-weighted formula</strong> so a framework that ships smaller response bodies wins over one that only serves more requests. This is the only profile in the suite that explicitly rewards compression ratio.</p>\n<h3 id=\"the-formula\">The formula</h3>\n<p>For each framework, the server&#x27;s actual bytes-per-response is computed from the benchmark output:</p>\n<pre><code>myBpr = observed_bandwidth / observed_rps</code></pre>\n<p>The framework with the <strong>smallest</strong> <code>myBpr</code> across the field sets the reference - that&#x27;s the compression winner. Every framework&#x27;s effective rps is then scaled by the squared ratio of reference to actual:</p>\n<pre><code>effectiveRps = rps \u00d7 (minBpr / myBpr)\u00b2</code></pre>\n<p>The quadratic exponent is deliberate: <strong>doubling the response size quarters the score</strong>. A framework that serves twice the rps but ships 1.4\u00d7 the bytes roughly breaks even; one that ships 2\u00d7 the bytes pays 4\u00d7 in score and needs to match 4\u00d7 the rps just to stay level. The BW/req column on the leaderboard shows each framework&#x27;s <code>myBpr</code> so you can see exactly what compression ratio the server picked (gzip at its chosen level, brotli, deflate - whatever <code>Accept-Encoding: gzip, br</code> returned).</p>\n<h3 id=\"per-conn-count-table-per-profile-leaderboard\">Per-conn-count table (per-profile leaderboard)</h3>\n<p>On the JSON Compressed tab of the main leaderboard, the Score column is computed <strong>independently per connection count</strong>. Each conn panel (512, 4096, 16384) runs its own <code>minBpr</code> normalization so the top framework in that panel scores 100. The <strong>Best</strong> panel picks each framework&#x27;s highest score across the three conn counts.</p>\n<h3 id=\"composite-score-column\">Composite score column</h3>\n<p>On the <a href=\"/leaderboards/composite/\" target=\"_blank\" rel=\"noopener\">composite leaderboard</a>, the JSON Compressed column follows a different aggregation path:</p>\n<ol><li>Average <code>rps</code> and <code>bandwidth</code> across the 3 conn counts (arithmetic mean, equal weight).</li><li>Compute <code>myBpr = avgBw / avgRps</code> from those averages.</li><li>Apply the same <code>effectiveRps = avgRps \u00d7 (minBpr / myBpr)\u00b2</code> formula.</li><li>Normalize to 0\u2013100.</li></ol>\n<p>The resulting per-profile score feeds into the composite like every other profile - averaged across all scored profiles for that framework&#x27;s type.</p>\n<h3 id=\"consequences\">Consequences</h3>\n<ul><li><strong>A framework that picks brotli can dominate even at lower rps</strong>, provided the smaller bytes-per-response wins back more score than the rps gap costs. This reflects real-world bandwidth-constrained serving.</li><li><strong>Gzip-only frameworks are competitive when their compression level is aggressive enough</strong> to keep <code>myBpr</code> near the brotli leader. The formula rewards ratio, not the specific encoding chosen.</li><li><strong>Pre-computed compressed payloads are out of the question for <code>production</code> type</strong> - the type rules require the response pipeline to actually compress per-request. See <a href=\"#doc=add-framework/meta-json\" data-doc=\"add-framework/meta-json\" data-anchor=\"type-rules\">type rules</a>.</li><li><strong>The best-conn-count panel on the profile tab can rank differently from the composite column</strong>, because one picks the highest-scoring conn count and the other averages. Both are intentional: the profile tab answers &quot;which framework wins at its best setting?&quot;; the composite answers &quot;which framework is most consistent across loads?&quot;.</li></ul></div>"},"test-profiles/h1/isolated/json-compressed":{"t":"JSON Compressed","html":"<div class=\"doc-body\"><p>Same workload as <a href=\"#doc=test-profiles/h1/isolated/json-processing\" data-doc=\"test-profiles/h1/isolated/json-processing\">JSON Processing</a>, but with HTTP content negotiation: the client advertises <code>Accept-Encoding: gzip, br</code> and the server must return a compressed response. Measures serialization plus compression throughput.</p>\n<div class=\"doc-cards\"><a class=\"doc-card\" href=\"#doc=test-profiles/h1/isolated/json-compressed/implementation\" data-doc=\"test-profiles/h1/isolated/json-compressed/implementation\"><span class=\"dc-t\">Implementation Guidelines</span><span class=\"dc-s\">Endpoint specification, compression rules, and multiplier parameter.</span></a><a class=\"doc-card\" href=\"#doc=test-profiles/h1/isolated/json-compressed/validation\" data-doc=\"test-profiles/h1/isolated/json-compressed/validation\"><span class=\"dc-t\">Validation</span><span class=\"dc-s\">All checks executed by the validation script for this test profile.</span></a></div></div>"},"test-profiles/h1/isolated/crud/validation":{"t":"Validation","html":"<div class=\"doc-body\"><p>The following checks are executed by <code>validate.sh</code> for every framework subscribed to the <code>crud</code> test. A Postgres sidecar container is started automatically before these checks run.</p>\n<h2 id=\"list-pagination\">List pagination</h2>\n<p>Sends <code>GET /crud/items?category=electronics&amp;page=1&amp;limit=5</code> and verifies:</p>\n<ul><li>Exactly 5 items returned</li><li><code>total</code> field is present and greater than 0 (with load-more semantics, <code>total</code> equals the returned item count, so a successful response with 5 items satisfies this)</li><li><code>page</code> equals 1</li><li>Every item has a nested <code>rating</code> object</li></ul>\n<h2 id=\"single-item-read\">Single item read</h2>\n<p>Sends <code>GET /crud/items/1</code> and verifies the response contains <code>id</code> equal to <code>1</code>.</p>\n<h2 id=\"cache-aside-miss-then-hit\">Cache-aside (MISS then HIT)</h2>\n<p>Sends two consecutive <code>GET /crud/items/42</code> requests and checks the <code>X-Cache</code> response header:</p>\n<ul><li>First request must return <code>X-Cache: MISS</code></li><li>Second request must return <code>X-Cache: HIT</code></li></ul>\n<h2 id=\"not-found\">Not found</h2>\n<p>Sends <code>GET /crud/items/999999</code> and verifies the response status is HTTP 404.</p>\n<h2 id=\"create-post\">Create (POST)</h2>\n<p>Sends <code>POST /crud/items</code> with body <code>{&quot;id&quot;:200001,&quot;name&quot;:&quot;Bench Test&quot;,&quot;category&quot;:&quot;test&quot;,&quot;price&quot;:25,&quot;quantity&quot;:10}</code> and verifies:</p>\n<ul><li>Response status is HTTP 201</li></ul>\n<h2 id=\"read-back-created-item\">Read back created item</h2>\n<p>Sends <code>GET /crud/items/200001</code> and verifies the response contains <code>id</code> equal to <code>200001</code>.</p>\n<h2 id=\"update-with-cache-invalidation\">Update with cache invalidation</h2>\n<ol><li>Warms the cache with <code>GET /crud/items/200001</code></li><li>Sends <code>PUT /crud/items/200001</code> with updated fields and verifies HTTP 200</li><li>Sends <code>GET /crud/items/200001</code> and verifies <code>X-Cache: MISS</code> (cache was invalidated by the PUT)</li></ol></div>"},"test-profiles/h1/isolated/crud/implementation":{"t":"Implementation Guidelines","html":"<div class=\"doc-body\"><div class=\"type-rules\"><div class=\"type-rules-tabs\"><button class=\"type-rules-tab active\" onclick=\"var r=this.closest('.type-rules');r.querySelectorAll('.type-rules-tab').forEach(function(t){t.classList.remove('active')});this.classList.add('active');r.querySelectorAll('.type-rules-panel').forEach(function(p){p.classList.remove('active')});r.querySelector('[data-panel=production]').classList.add('active')\"><span class=\"tr-sq\" style=\"background:#22c55e\"></span>Standard</button><button class=\"type-rules-tab\" onclick=\"var r=this.closest('.type-rules');r.querySelectorAll('.type-rules-tab').forEach(function(t){t.classList.remove('active')});this.classList.add('active');r.querySelectorAll('.type-rules-panel').forEach(function(p){p.classList.remove('active')});r.querySelector('[data-panel=tuned]').classList.add('active')\"><span class=\"tr-sq\" style=\"background:#eab308\"></span>Tuned</button><button class=\"type-rules-tab\" onclick=\"var r=this.closest('.type-rules');r.querySelectorAll('.type-rules-tab').forEach(function(t){t.classList.remove('active')});this.classList.add('active');r.querySelectorAll('.type-rules-panel').forEach(function(p){p.classList.remove('active')});r.querySelector('[data-panel=engine]').classList.add('active')\"><span class=\"tr-sq\" style=\"background:#dc2626\"></span>Engine</button></div><div class=\"type-rules-panel active\" data-panel=\"production\">Must use a standard async Postgres driver with connection pooling. Cache-aside with 200 ms absolute TTL on single-item reads, invalidated on PUT. In-process cache is the default; multi-process runtimes (e.g. SO_REUSEPORT with one process per core) may use the provided Redis sidecar for a shared cache. No pre-warming or background refresh.</div><div class=\"type-rules-panel\" data-panel=\"tuned\">May use custom pool sizes, prepared statements, multi-tier caches, or any cache backend including the Redis sidecar.</div><div class=\"type-rules-panel\" data-panel=\"engine\">No specific rules.</div></div>\n<p>The CRUD profile benchmarks a realistic REST API with four operations against Postgres: paginated list, cached single-item read, create (upsert), and update with cache invalidation.</p>\n<p><strong>This test is for framework-type entries only</strong> - engines (nginx, h2o, etc.) are excluded.</p>\n<p><strong>Connections:</strong> 4096</p>\n<h2 id=\"endpoints\">Endpoints</h2>\n<h3 id=\"get-cruditems-paginated-list\">GET /crud/items - Paginated list</h3>\n<p>Accepts query parameters:</p>\n<ul><li><code>category</code> (string, default <code>&quot;electronics&quot;</code>) - filter by category</li><li><code>page</code> (integer, default <code>1</code>) - page number (1-indexed)</li><li><code>limit</code> (integer, default <code>10</code>, max <code>50</code>) - items per page</li></ul>\n<p>Executes a single SQL query:</p>\n<pre><code class=\"language-sql\">SELECT ... FROM items WHERE category = $1 ORDER BY id LIMIT $2 OFFSET $3</code></pre>\n<p>&quot;Load-more&quot; pagination: the response returns the items on the requested page. The <code>total</code> field reports the number of items in <em>this</em> response (equivalent to the client&#x27;s <code>items.length</code>) - not the full-filter row count. A separate <code>SELECT COUNT(*)</code> was originally part of the spec but removed because concurrent writes kept the visibility map dirty, making it ~90% of Postgres CPU for a value most real-world list APIs either skip or compute out-of-band.</p>\n<p>Returns:</p>\n<pre><code class=\"language-json\">{\n  &quot;items&quot;: [\n    {\n      &quot;id&quot;: 42,\n      &quot;name&quot;: &quot;Alpha Widget 42&quot;,\n      &quot;category&quot;: &quot;electronics&quot;,\n      &quot;price&quot;: 30,\n      &quot;quantity&quot;: 5,\n      &quot;active&quot;: true,\n      &quot;tags&quot;: [&quot;fast&quot;, &quot;new&quot;],\n      &quot;rating&quot;: { &quot;score&quot;: 42, &quot;count&quot;: 127 }\n    }\n  ],\n  &quot;total&quot;: 10,\n  &quot;page&quot;: 1,\n  &quot;limit&quot;: 10\n}</code></pre>\n<h3 id=\"get-cruditemsid-single-item-read-cached\">GET /crud/items/{id} - Single item read (cached)</h3>\n<p>Reads a single item by ID using a cache with <strong>200 ms</strong> absolute expiration.</p>\n<p>Cache backend options:</p>\n<ul><li><strong>In-process</strong> (e.g. <code>IMemoryCache</code>, <code>HashMap</code>, <code>sync.Map</code>) - the default. Works for single-process frameworks and for multi-threaded runtimes that share a heap.</li><li><strong>Redis</strong> (the provided sidecar at <code>REDIS_URL</code>) - for multi-process frameworks (Bun with <code>reusePort</code>, PHP FPM, Python with uWSGI workers, etc.) where a per-process cache would fragment the working set. Cache the pre-serialized JSON body so HIT responses skip re-serialization.</li></ul>\n<p>Behavior:</p>\n<ul><li>On cache miss: query Postgres, populate cache, return item with <code>X-Cache: MISS</code> header</li><li>On cache hit: return cached item with <code>X-Cache: HIT</code> header</li><li>If item not found: return HTTP 404</li></ul>\n<pre><code class=\"language-json\">{\n  &quot;id&quot;: 1,\n  &quot;name&quot;: &quot;Alpha Widget 1&quot;,\n  &quot;category&quot;: &quot;electronics&quot;,\n  &quot;price&quot;: 30,\n  &quot;quantity&quot;: 5,\n  &quot;active&quot;: true,\n  &quot;tags&quot;: [&quot;fast&quot;, &quot;new&quot;],\n  &quot;rating&quot;: { &quot;score&quot;: 42, &quot;count&quot;: 127 }\n}</code></pre>\n<h3 id=\"post-cruditems-create-item\">POST /crud/items - Create item</h3>\n<p>Accepts a JSON body:</p>\n<pre><code class=\"language-json\">{ &quot;id&quot;: 200001, &quot;name&quot;: &quot;New Product&quot;, &quot;category&quot;: &quot;test&quot;, &quot;price&quot;: 25, &quot;quantity&quot;: 10 }</code></pre>\n<p>Inserts into Postgres with <code>ON CONFLICT (id) DO UPDATE</code> (upsert). Returns HTTP 201 with the created item.</p>\n<h3 id=\"put-cruditemsid-update-item\">PUT /crud/items/{id} - Update item</h3>\n<p>Accepts a JSON body:</p>\n<pre><code class=\"language-json\">{ &quot;name&quot;: &quot;Updated Name&quot;, &quot;price&quot;: 30, &quot;quantity&quot;: 5 }</code></pre>\n<p>Updates the item in Postgres and <strong>invalidates the cache entry</strong> for the updated ID (from whichever cache backend is in use). Returns HTTP 200 with the updated fields. Returns 404 if the item doesn&#x27;t exist.</p>\n<h2 id=\"what-it-measures\">What it measures</h2>\n<ul><li><strong>Connection pooling</strong> - maintaining a Postgres connection pool under mixed read/write load</li><li><strong>Cache-aside correctness</strong> - cache hit/miss with TTL-based expiration and write-through invalidation, whether the cache is in-process or external</li><li><strong>Query diversity</strong> - paginated list queries mixed with high-rate single-item lookups, inserts, and updates running concurrently</li><li><strong>JSON parsing and serialization</strong> - deserializing request bodies (POST/PUT) and serializing response payloads</li></ul>\n<h2 id=\"workload-mix\">Workload mix</h2>\n<p>The load generator (gcannon) rotates across 20 raw HTTP templates. Each connection reconnects every 200 requests (<code>-r 200</code>) so templates rotate within a connection&#x27;s lifetime rather than sticking to whichever template it first picked.</p>\n<table><thead><tr><th>Operation</th><th>Templates</th><th>Weight</th><th>ID distribution</th></tr></thead><tbody><tr><td>Read (GET /crud/items/{id})</td><td>15</td><td>75%</td><td>Random IDs via <code>{RAND:1:50000}</code></td></tr><tr><td>Update (PUT /crud/items/{id})</td><td>3</td><td>15%</td><td>Random IDs via <code>{RAND:1:50000}</code></td></tr><tr><td>List (GET /crud/items)</td><td>1</td><td>5%</td><td><code>category=office</code>, random page via <code>{RAND:1:10}</code> (OFFSET \u2208 0..90)</td></tr><tr><td>Create (POST /crud/items)</td><td>1</td><td>5%</td><td>Sequential IDs via <code>{SEQ:100001}</code> - iteration 1 is pure INSERT, iterations 2+ upsert via <code>ON CONFLICT</code> (gcannon resets the counter per invocation)</td></tr></tbody></table>\n<h2 id=\"environment-variables\">Environment variables</h2>\n<table><thead><tr><th>Variable</th><th>Value</th><th>Description</th></tr></thead><tbody><tr><td><code>DATABASE_URL</code></td><td><code>postgres://bench:bench@localhost:5432/benchmark</code></td><td>Postgres connection string</td></tr><tr><td><code>DATABASE_MAX_CONN</code></td><td><code>256</code></td><td>Hint for pool sizing. Frameworks should honor this rather than hardcoding.</td></tr><tr><td><code>REDIS_URL</code></td><td><code>redis://localhost:6379</code></td><td>Optional - present when the harness runs the crud profile. Use only if the framework&#x27;s architecture (multi-process) needs a shared cache.</td></tr></tbody></table>\n<h2 id=\"parameters\">Parameters</h2>\n<table><thead><tr><th>Parameter</th><th>Value</th></tr></thead><tbody><tr><td>Connections</td><td>4096</td></tr><tr><td>Pipeline</td><td>1</td></tr><tr><td>Requests per connection</td><td>200 (then reconnect)</td></tr><tr><td>CPU limit (server)</td><td>62 threads (cores 1-31, 65-95)</td></tr><tr><td>CPU limit (Redis sidecar, when used)</td><td>2 threads (cores 0, 64), <code>--io-threads 2</code></td></tr><tr><td>Duration</td><td>15s per run</td></tr><tr><td>Runs</td><td>3 (best taken)</td></tr><tr><td>Database</td><td>Postgres 18 (Debian/glibc), 100,000 rows, <code>max_connections=256</code>, tmpfs-backed</td></tr></tbody></table></div>"},"test-profiles/h1/isolated/crud":{"t":"CRUD (REST API)","html":"<div class=\"doc-body\"><p>Realistic REST API benchmark with paginated list, cached single-item read, create, and update operations against Postgres.</p>\n<div class=\"doc-cards\"><a class=\"doc-card\" href=\"#doc=test-profiles/h1/isolated/crud/implementation\" data-doc=\"test-profiles/h1/isolated/crud/implementation\"><span class=\"dc-t\">Implementation Guidelines</span><span class=\"dc-s\">Endpoint specification, expected request/response format, and type-specific rules.</span></a><a class=\"doc-card\" href=\"#doc=test-profiles/h1/isolated/crud/validation\" data-doc=\"test-profiles/h1/isolated/crud/validation\"><span class=\"dc-t\">Validation</span><span class=\"dc-s\">All checks executed by the validation script for this test profile.</span></a></div></div>"},"test-profiles/h1/isolated/upload/validation":{"t":"Validation","html":"<div class=\"doc-body\"><p>The following checks are executed by <code>validate.sh</code> for every framework subscribed to the <code>upload</code> test.</p>\n<h2 id=\"small-body-upload\">Small body upload</h2>\n<p>Sends <code>POST /upload</code> with body <code>Hello, HttpArena!</code> (17 bytes) and <code>Content-Type: application/octet-stream</code>. Verifies the response body is <code>17</code> (the byte count of the uploaded data).</p>\n<h2 id=\"anti-cheat-random-body-upload\">Anti-cheat: random body upload</h2>\n<p>Generates a random 48-byte body (base64-encoded from 64 random bytes), sends <code>POST /upload</code> with that body, and verifies the response matches the exact byte count of the random payload. This detects hardcoded responses or implementations that return <code>Content-Length</code> instead of actually reading the body.</p>\n<h2 id=\"varying-payload-sizes\">Varying payload sizes</h2>\n<p>Sends <code>POST /upload</code> with four different payload sizes: <strong>500 KB, 2 MB, 10 MB, and 20 MB</strong> (random binary data). Verifies the returned byte count matches the exact payload size for each.</p></div>"},"test-profiles/h1/isolated/upload/implementation":{"t":"Implementation Guidelines","html":"<div class=\"doc-body\"><div class=\"type-rules\"><div class=\"type-rules-tabs\"><button class=\"type-rules-tab active\" onclick=\"var r=this.closest('.type-rules');r.querySelectorAll('.type-rules-tab').forEach(function(t){t.classList.remove('active')});this.classList.add('active');r.querySelectorAll('.type-rules-panel').forEach(function(p){p.classList.remove('active')});r.querySelector('[data-panel=production]').classList.add('active')\"><span class=\"tr-sq\" style=\"background:#22c55e\"></span>Standard</button><button class=\"type-rules-tab\" onclick=\"var r=this.closest('.type-rules');r.querySelectorAll('.type-rules-tab').forEach(function(t){t.classList.remove('active')});this.classList.add('active');r.querySelectorAll('.type-rules-panel').forEach(function(p){p.classList.remove('active')});r.querySelector('[data-panel=tuned]').classList.add('active')\"><span class=\"tr-sq\" style=\"background:#eab308\"></span>Tuned</button><button class=\"type-rules-tab\" onclick=\"var r=this.closest('.type-rules');r.querySelectorAll('.type-rules-tab').forEach(function(t){t.classList.remove('active')});this.classList.add('active');r.querySelectorAll('.type-rules-panel').forEach(function(p){p.classList.remove('active')});r.querySelector('[data-panel=engine]').classList.add('active')\"><span class=\"tr-sq\" style=\"background:#dc2626\"></span>Engine</button></div><div class=\"type-rules-panel active\" data-panel=\"production\">Must use the framework standard body reading API. Streaming is allowed if the framework supports it natively.</div><div class=\"type-rules-panel\" data-panel=\"tuned\">May use custom buffer sizes, direct socket reads, or bypass framework body parsing for maximum throughput.</div><div class=\"type-rules-panel\" data-panel=\"engine\">No specific rules.</div></div>\n<p>The Upload profile measures how efficiently a framework handles large request body ingestion. The benchmark rotates across four payload sizes: 500 KB, 2 MB, 10 MB, and 20 MB. The server returns the byte count.</p>\n<p><strong>Connections:</strong> 32, 256</p>\n<h2 id=\"how-it-works\">How it works</h2>\n<ol><li>The load generator sends <code>POST /upload</code> with a binary body (500 KB, 2 MB, 10 MB, or 20 MB) using pre-built raw request files, rotating every 5 requests</li><li>The server reads the entire request body</li><li>Returns the total number of bytes received as plain text</li></ol>\n<h2 id=\"what-it-measures\">What it measures</h2>\n<ul><li><strong>Request body ingestion throughput</strong> - reading large payloads from the network</li><li><strong>Memory management</strong> - buffering 20 MB per concurrent request</li><li><strong>I/O handling efficiency</strong> - how the framework manages sustained large transfers</li><li><strong>Connection overhead</strong> - at 20 MB per request, connection setup/teardown is significant</li></ul>\n<h2 id=\"implementation-rules\">Implementation rules</h2>\n<p>The upload endpoint must actually read and process the request body. The returned byte count must be computed by reading the uploaded data, not inferred from request metadata.</p>\n<ul><li><strong>Do not</strong> return the value of the <code>Content-Length</code> header - this defeats the purpose of the test, which is to measure how efficiently the framework processes uploaded content.</li><li>Frameworks <strong>may</strong> use small read buffers to process the upload incrementally. Holding the entire payload in memory is allowed but not required.</li><li>The goal is to prove that the framework can efficiently compute a result over a large blob being sent to it.</li></ul>\n<h2 id=\"expected-response\">Expected response</h2>\n<pre><code>POST /upload HTTP/1.1\nContent-Length: 20971520</code></pre>\n<pre><code>HTTP/1.1 200 OK\nContent-Type: text/plain\n\n20971520</code></pre>\n<h2 id=\"parameters\">Parameters</h2>\n<table><thead><tr><th>Parameter</th><th>Value</th></tr></thead><tbody><tr><td>Endpoint</td><td><code>POST /upload</code></td></tr><tr><td>Connections</td><td>32, 256</td></tr><tr><td>Pipeline</td><td>1</td></tr><tr><td>Duration</td><td>5s</td></tr><tr><td>Runs</td><td>3 (best taken)</td></tr><tr><td>Payloads</td><td>500 KB, 2 MB, 10 MB, 20 MB (rotated with <code>-r 5</code>)</td></tr></tbody></table>\n<h2 id=\"notes\">Notes</h2>\n<ul><li>I/O is the primary bottleneck - body ingestion dominates request handling time</li><li>Lower connection counts are used because each request transfers 20 MB</li><li>The load generator&#x27;s bandwidth metric only measures response data (~8 bytes), not the uploaded payload</li></ul></div>"},"test-profiles/h1/isolated/upload":{"t":"Upload (20 MB)","html":"<div class=\"doc-body\"><p>Measures how efficiently a framework handles large request body ingestion. Each request sends a 20 MB binary payload and the server returns the byte count.</p>\n<div class=\"doc-cards\"><a class=\"doc-card\" href=\"#doc=test-profiles/h1/isolated/upload/implementation\" data-doc=\"test-profiles/h1/isolated/upload/implementation\"><span class=\"dc-t\">Implementation Guidelines</span><span class=\"dc-s\">Endpoint specification, expected request/response format, and type-specific rules.</span></a><a class=\"doc-card\" href=\"#doc=test-profiles/h1/isolated/upload/validation\" data-doc=\"test-profiles/h1/isolated/upload/validation\"><span class=\"dc-t\">Validation</span><span class=\"dc-s\">All checks executed by the validation script for this test profile.</span></a></div></div>"},"test-profiles/h1/isolated/fortunes/validation":{"t":"Validation","html":"<div class=\"doc-body\"><p>The following checks are executed by <code>validate.sh</code> for every framework subscribed to the <code>fortunes</code> test. A Postgres sidecar container is started automatically before these checks run.</p>\n<p>Validation is <strong>feature-based, not byte-exact</strong>. Engines disagree on whitespace, attribute order, quote style, and self-closing tags - enforcing byte equality would turn the contest into &quot;who can torture their template into emitting the canonical form&quot; rather than &quot;whose engine renders fast.&quot; The checks below pin down the work envelope (loop iterated, runtime row appended, escape applied, layout rendered, body has a real size) without dictating output format.</p>\n<h2 id=\"content-type-header\">Content-Type header</h2>\n<p>Sends <code>GET /fortunes</code> and verifies the <code>Content-Type</code> response header contains <code>text/html</code> (a <code>; charset=utf-8</code> suffix is allowed and recommended).</p>\n<h2 id=\"doctype-present\">DOCTYPE present</h2>\n<p>Verifies the response body contains <code>&lt;!DOCTYPE html&gt;</code> (case-insensitive). This proves a layout / partial / outer template fired - a handler that only emits a fragment will fail this check.</p>\n<h2 id=\"row-count\">Row count</h2>\n<p>Counts <code>&lt;tr</code> occurrences in the response body. The expected band is <strong>201\u2013210</strong>:</p>\n<ul><li>201 data rows (200 seeded + 1 runtime-injected) are required.</li><li>A <code>&lt;tr&gt;</code> header row (<code>&lt;tr&gt;&lt;th&gt;id&lt;/th&gt;&lt;th&gt;message&lt;/th&gt;&lt;/tr&gt;</code>) is allowed but not required.</li><li>The upper bound absorbs implementation-specific extras (a footer summary row, etc.) without forcing a specific shape.</li></ul>\n<p>A count below 201 means the loop didn&#x27;t iterate every row. Above 210 means the engine emitted unexpected scaffolding.</p>\n<h2 id=\"runtime-injected-row-present\">Runtime-injected row present</h2>\n<p>Verifies the body contains the literal string <code>Additional fortune added at request time.</code>. This proves the handler appended the in-memory row instead of rendering only the DB rows. Without this check, &quot;cache the rendered HTML once and serve bytes&quot; passes everything else.</p>\n<h2 id=\"xss-escape-load-bearing\">XSS escape (load-bearing)</h2>\n<p>The DB seed contains <code>&lt;script&gt;alert(&quot;This should not be displayed in a browser alert box.&quot;);&lt;/script&gt;</code> as the message of row 11. The validator asserts:</p>\n<ul><li>Body contains <code>&amp;lt;script&amp;gt;</code> (the escape happened), AND</li><li>Body does NOT contain the raw <code>&lt;script&gt;alert</code> substring anywhere.</li></ul>\n<p>This is the most important check in the profile. Without it, a &quot;template engine&quot; that skips escaping wins the bench - which is both a security regression and the obvious gaming vector. Frameworks failing this check are not running a real template engine, full stop.</p>\n<h2 id=\"body-size-band\">Body size band</h2>\n<p>Verifies the response body length is between <strong>18 KB and 64 KB</strong>. A 201-row HTML table with a layout typically lands around 22\u201330 KB; the band catches:</p>\n<ul><li>Stripped pages (handler returned only fragments to win throughput).</li><li>Empty bodies (template failed silently and the engine emitted nothing).</li><li>Pathologically large output (an unintended layout loop or duplicated content).</li></ul>\n<p>The upper bound is generous - engines that emit more whitespace, longer DOCTYPEs, or a bigger header / footer will still pass.</p>\n<h2 id=\"what-is-not-validated\">What is NOT validated</h2>\n<ul><li><strong>Sort order.</strong> The implementation guidelines specify ordinal-byte sort, but the validator does not parse the rendered HTML and verify row positions. Frameworks shipping a different sort still pass - the production rules govern correctness here, not the validator.</li><li><strong>Exact byte output.</strong> Two correct implementations can produce different HTML and both pass.</li><li><strong>Header row presence.</strong> A <code>&lt;tr&gt;&lt;th&gt;...&lt;/th&gt;&lt;/tr&gt;</code> header row is recommended for parity with the reference rendering but not enforced.</li><li><strong>Performance under load.</strong> Validation is correctness-only; the benchmark driver measures throughput separately.</li></ul></div>"},"test-profiles/h1/isolated/fortunes/implementation":{"t":"Implementation Guidelines","html":"<div class=\"doc-body\"><div class=\"type-rules\"><div class=\"type-rules-tabs\"><button class=\"type-rules-tab active\" onclick=\"var r=this.closest('.type-rules');r.querySelectorAll('.type-rules-tab').forEach(function(t){t.classList.remove('active')});this.classList.add('active');r.querySelectorAll('.type-rules-panel').forEach(function(p){p.classList.remove('active')});r.querySelector('[data-panel=production]').classList.add('active')\"><span class=\"tr-sq\" style=\"background:#22c55e\"></span>Standard</button><button class=\"type-rules-tab\" onclick=\"var r=this.closest('.type-rules');r.querySelectorAll('.type-rules-tab').forEach(function(t){t.classList.remove('active')});this.classList.add('active');r.querySelectorAll('.type-rules-panel').forEach(function(p){p.classList.remove('active')});r.querySelector('[data-panel=tuned]').classList.add('active')\"><span class=\"tr-sq\" style=\"background:#eab308\"></span>Tuned</button><button class=\"type-rules-tab\" onclick=\"var r=this.closest('.type-rules');r.querySelectorAll('.type-rules-tab').forEach(function(t){t.classList.remove('active')});this.classList.add('active');r.querySelectorAll('.type-rules-panel').forEach(function(p){p.classList.remove('active')});r.querySelector('[data-panel=engine]').classList.add('active')\"><span class=\"tr-sq\" style=\"background:#dc2626\"></span>Engine</button></div><div class=\"type-rules-panel active\" data-panel=\"production\">Must render the response with a real template engine - one with a documented templating syntax, variable interpolation, control flow (loop/if), and HTML auto-escaping by default. Examples: Razor / Razor Pages / Razor Components, Jinja2, ERB, Twig, Blade, html/template, Thymeleaf, Freemarker, Handlebars, Pug, Liquid, Tera, Askama, Maud, templ. The template must be a separate artifact (file, embedded resource, or compile-time component) - not inline string concatenation in the handler. No pre-rendered or cached response bodies; the template must execute per request.</div><div class=\"type-rules-panel\" data-panel=\"tuned\">May use any rendering strategy, including hand-rolled string concatenation, custom HTML emitters, StringBuilder / Buffer-based builders, byte-slice writers, or compile-time templating with framework-specific source generators. The handler must still query the database, append the runtime-injected row in memory, sort, and HTML-escape user content per request - pre-rendered response bodies and bypass-the-engine response caches are not allowed on either type. The runtime row&#x27;s text is fixed, but the page still must be rendered per request because validation reads the body byte-for-byte.</div><div class=\"type-rules-panel\" data-panel=\"engine\">No specific rules. Engines without an HTTP application layer typically don&#x27;t subscribe to this profile.</div></div>\n<p>The Fortunes profile measures template-engine throughput on a realistic page-rendering pipeline: query 200 rows from Postgres, append a runtime row in memory, sort, and render an HTML page via the framework&#x27;s chosen template engine.</p>\n<p><strong>Diverges from TechEmpower Fortunes (12-row spec) by design.</strong> With only 12 rows the per-request render time is dwarfed by the PG round-trip, so the profile ends up measuring the database driver more than the template engine. Bumping the seed to 200 rows scales render cost linearly while leaving query cost effectively constant (still one round-trip, still single-page table read), shifting the bottleneck from PG-throughput to framework-CPU - which is what the profile name actually claims to measure.</p>\n<p><strong>This test is reference-only - it does not contribute to the composite score.</strong> It exists for engine-to-engine comparison (Razor vs Jinja2 vs ERB vs Askama, etc.), not framework ranking. Frameworks without a template engine should leave it out of <code>meta.json.tests</code>.</p>\n<p><strong>Connections:</strong> 1,024</p>\n<h2 id=\"how-it-works\">How it works</h2>\n<ol><li>A Postgres container runs alongside the framework container on the same host, listening on <code>localhost:5432</code>. The same connection pool used by <code>async-db</code> / <code>crud</code> is shared.</li><li>On each <code>GET /fortunes</code> request the framework:<ul><li>Reads all 200 rows from the <code>fortune</code> table (<code>id</code>, <code>message</code>).</li><li>Appends a runtime row in memory: <code>{ id: 0, message: &quot;Additional fortune added at request time.&quot; }</code> - total 201 rows.</li><li>Sorts the combined list by <code>message</code> using ASCII / ordinal byte order (not locale-aware).</li><li>Renders an HTML page containing a <code>&lt;table&gt;</code> with one <code>&lt;tr&gt;</code> per fortune.</li></ul></li><li>Returns <code>Content-Type: text/html</code> (with optional <code>; charset=utf-8</code>).</li></ol>\n<p>The runtime-injected row is the load-bearing design decision - it forces the rendered HTML to differ structurally from the raw DB result, which makes &quot;cache the whole page once and serve bytes&quot; optimizations invalid.</p>\n<h2 id=\"what-it-measures\">What it measures</h2>\n<ul><li><strong>Template engine throughput</strong> - interpolation, loop expansion, layout/partial composition.</li><li><strong>HTML escape correctness</strong> - row 11 contains a raw <code>&lt;script&gt;</code> tag in the database; the engine must emit it as <code>&amp;lt;script&amp;gt;</code> in the rendered output. Auto-escape is the safe default in every modern engine; this profile pins that down as a contractual requirement.</li><li><strong>End-to-end pipeline cost</strong> - Postgres read + sort + render + response write, all on one event-loop tick.</li></ul>\n<h2 id=\"database-schema\">Database schema</h2>\n<p>The <code>fortune</code> table in Postgres (200 rows seeded; same Postgres instance as <code>async-db</code> / <code>crud</code>):</p>\n<pre><code class=\"language-sql\">CREATE TABLE fortune (\n    id      INTEGER PRIMARY KEY,\n    message TEXT NOT NULL\n);</code></pre>\n<p>Rows 1\u201312 match the TechEmpower Fortunes seed (including the <code>&lt;script&gt;</code> row at id 11, which is the load-bearing escape check). Rows 13\u2013200 are synthetic adages generated to give the renderer per-row work - each contains <code>&amp;</code>, <code>&#x27;</code>, <code>&quot;</code>, and an em-dash so the engine&#x27;s escape codepath runs on every cell, not just on row 11. See <code>data/pgdb-seed.sql</code> for the exact rows.</p>\n<h2 id=\"sql-query\">SQL query</h2>\n<pre><code class=\"language-sql\">SELECT id, message FROM fortune</code></pre>\n<p>No <code>ORDER BY</code> - the framework sorts in-memory after appending the runtime row.</p>\n<h2 id=\"runtime-row-injection\">Runtime row injection</h2>\n<p>After the DB query and before sorting, append:</p>\n<pre><code>{ id: 0, message: &quot;Additional fortune added at request time.&quot; }</code></pre>\n<p>The id <code>0</code> and the literal text are fixed across all frameworks for validation parity.</p>\n<h2 id=\"sort\">Sort</h2>\n<p>Sort the combined 201-row list by <code>message</code> using ordinal / byte / ASCII comparison. With the seed data this puts the <code>&lt;script&gt;...</code> row first (<code>&lt;</code> is 0x3C, lower than any letter) and the multi-byte UTF-8 rows last.</p>\n<p>Locale-aware comparison is <strong>not</strong> allowed because it produces different orderings on different runtimes - use the ordinal/byte equivalent of your language&#x27;s <code>String.CompareOrdinal</code> / <code>bytes.Compare</code> / <code>strcmp</code>.</p>\n<h2 id=\"expected-response\">Expected response</h2>\n<pre><code>GET /fortunes HTTP/1.1</code></pre>\n<p>A complete HTML document with:</p>\n<ul><li><code>Content-Type: text/html</code> (charset optional but recommended)</li><li><code>&lt;!DOCTYPE html&gt;</code> declaration</li><li>A <code>&lt;table&gt;</code> with one header row (<code>&lt;tr&gt;&lt;th&gt;id&lt;/th&gt;&lt;th&gt;message&lt;/th&gt;&lt;/tr&gt;</code>) and 201 data rows.</li><li>All <code>message</code> values HTML-escaped - row 11 must render as <code>&amp;lt;script&amp;gt;...&amp;lt;/script&amp;gt;</code>, never as a raw <code>&lt;script&gt;</code> tag.</li></ul>\n<p>Total body size lands between roughly 18\u201340 KB depending on engine formatting.</p>\n<p>Validation does not enforce byte-for-byte equality - it checks features (Content-Type, DOCTYPE, row count, runtime-row text, escape correctness, body size band). See <a href=\"#doc=test-profiles/h1/isolated/fortunes/validation\" data-doc=\"test-profiles/h1/isolated/fortunes/validation\">Validation</a> for exact checks.</p>\n<h2 id=\"production-vs-tuned\">Production vs Tuned</h2>\n<p><strong>Production</strong> entries should look like idiomatic templated HTML in the framework - a <code>.cshtml</code> Razor page, a Jinja2 template loaded via <code>flask.render_template</code>, an ERB view in Sinatra, a <code>templ</code> component in Go, etc. The template lives in its own file and is composed (or compiled) by the engine. Auto-escaping is on; user content goes through <code>@variable</code> / <code>{{ variable }}</code> syntax that escapes by default.</p>\n<p><strong>Tuned</strong> entries are free to skip the engine entirely - emit HTML via <code>StringBuilder.Append</code>, <code>bytes.Buffer.WriteString</code>, manual <code>&lt;&lt;</code> concatenation, custom byte-slice writers. This mirrors what many TechEmpower Fortunes entries do to chase peak throughput. The handler must still:</p>\n<ul><li>Query the DB per request (no pre-rendered response cache).</li><li>Append the runtime row (the page must vary per request relative to a hypothetical cache that snapshots only the DB rows).</li><li>HTML-escape user content correctly. A custom emitter must implement escaping for <code>&lt;</code>, <code>&gt;</code>, <code>&amp;</code>, <code>&quot;</code>, <code>&#x27;</code> at minimum.</li></ul>\n<p>The Standard/Tuned distinction is recorded in <code>meta.json.mode</code> and shown on the leaderboard so users can compare &quot;what the framework&#x27;s template engine does&quot; against &quot;what the framework&#x27;s hand-tuned hot path does.&quot;</p>\n<h2 id=\"why-this-profile-is-unscored\">Why this profile is unscored</h2>\n<p>Template engine choice dominates the result far more than HTTP-stack quality. A framework using a compile-time engine like Razor source-gen, Askama, Maud, or <code>templ</code> will look 5\u201310\u00d7 faster than the same framework using a runtime-parsed engine like Jinja2 or ERB. That gap is real and worth showing - but rolling it into the composite would let template-engine choice swamp legitimate framework comparisons on baseline / JSON / async-db. So Fortunes is published, comparable, and visible on the leaderboard, but does not contribute to the composite score.</p>\n<h2 id=\"why-200-rows-instead-of-tes-12\">Why 200 rows instead of TE&#x27;s 12</h2>\n<p>TechEmpower Fortunes uses 12 rows - at that size, the per-request render takes microseconds and the dominant cost is the PG round-trip + network protocol, not the template engine. Frameworks with the same template engine but different DB drivers end up ranked by DB driver, defeating the profile&#x27;s purpose.</p>\n<p>Bumping to 200 rows scales render cost linearly (more interpolation, more escape work, more bytes to write) while leaving query cost effectively flat (the table still lives in a single 8 KB shared-buffer page; one round-trip serializes 200 rows nearly as fast as 12). At 1024 connections the bottleneck shifts from PG-throughput to framework-CPU - i.e. the actual template engine. That&#x27;s the trade: lose direct TE leaderboard comparability, gain a column that means what its name says.</p></div>"},"test-profiles/h1/isolated/fortunes":{"t":"Fortunes (Templates)","html":"<div class=\"doc-body\"><p>Database-backed HTML rendering benchmark - measures template-engine throughput end-to-end: Postgres read, runtime row injection, sort, escape-aware HTML rendering. Reference-only profile (not part of the composite score).</p>\n<div class=\"doc-cards\"><a class=\"doc-card\" href=\"#doc=test-profiles/h1/isolated/fortunes/implementation\" data-doc=\"test-profiles/h1/isolated/fortunes/implementation\"><span class=\"dc-t\">Implementation Guidelines</span><span class=\"dc-s\">Endpoint specification, expected output, and type-specific rules.</span></a><a class=\"doc-card\" href=\"#doc=test-profiles/h1/isolated/fortunes/validation\" data-doc=\"test-profiles/h1/isolated/fortunes/validation\"><span class=\"dc-t\">Validation</span><span class=\"dc-s\">All checks executed by the validation script for this test profile.</span></a></div></div>"},"test-profiles/h1/isolated/static/validation":{"t":"Validation","html":"<div class=\"doc-body\"><p>The following checks are executed by <code>validate.sh</code> for every framework subscribed to the <code>static</code> test.</p>\n<h2 id=\"content-type-headers\">Content-Type headers</h2>\n<p>Verifies correct <code>Content-Type</code> headers for representative file types:</p>\n<ul><li><code>GET /static/reset.css</code> - expects <code>Content-Type: text/css</code></li><li><code>GET /static/app.js</code> - expects <code>Content-Type: application/javascript</code></li><li><code>GET /static/manifest.json</code> - expects <code>Content-Type: application/json</code></li></ul>\n<p>Note: <code>text/javascript</code> is accepted as equivalent to <code>application/javascript</code> per RFC 9239.</p>\n<h2 id=\"file-size-verification-uncompressed\">File size verification (uncompressed)</h2>\n<p>Requests all 20 static files <strong>without</strong> <code>Accept-Encoding</code> and compares the response size against the actual file size on disk. All 20 files must match exactly. This ensures the server returns uncompressed content when no compression is requested.</p>\n<p><code>reset.css</code>, <code>layout.css</code>, <code>theme.css</code>, <code>components.css</code>, <code>utilities.css</code>, <code>analytics.js</code>, <code>helpers.js</code>, <code>app.js</code>, <code>vendor.js</code>, <code>router.js</code>, <code>header.html</code>, <code>footer.html</code>, <code>regular.woff2</code>, <code>bold.woff2</code>, <code>logo.svg</code>, <code>icon-sprite.svg</code>, <code>hero.webp</code>, <code>thumb1.webp</code>, <code>thumb2.webp</code>, <code>manifest.json</code></p>\n<h2 id=\"compression-verification\">Compression verification</h2>\n<p>Requests all 20 static files <strong>with</strong> <code>Accept-Encoding: br;q=1, gzip;q=0.8</code> and checks:</p>\n<ul><li>If the server returns a <code>Content-Encoding</code> header (br or gzip), the decompressed response size must match the original file size on disk</li><li>If the server does not compress a file, it is counted as skipped (not a failure - compression is optional)</li></ul>\n<p><strong>PASS</strong> if all compressed files decompress to the correct size. <strong>SKIP</strong> if the server does not compress any files. <strong>FAIL</strong> if any compressed file decompresses to the wrong size.</p>\n<h2 id=\"404-for-nonexistent-file\">404 for nonexistent file</h2>\n<p>Sends <code>GET /static/nonexistent.txt</code> and verifies the server returns <strong>HTTP 404</strong>.</p></div>"},"test-profiles/h1/isolated/static/implementation":{"t":"Implementation Guidelines","html":"<div class=\"doc-body\"><div class=\"type-rules\"><div class=\"type-rules-tabs\"><button class=\"type-rules-tab active\" onclick=\"var r=this.closest('.type-rules');r.querySelectorAll('.type-rules-tab').forEach(function(t){t.classList.remove('active')});this.classList.add('active');r.querySelectorAll('.type-rules-panel').forEach(function(p){p.classList.remove('active')});r.querySelector('[data-panel=production]').classList.add('active')\"><span class=\"tr-sq\" style=\"background:#22c55e\"></span>Standard</button><button class=\"type-rules-tab\" onclick=\"var r=this.closest('.type-rules');r.querySelectorAll('.type-rules-tab').forEach(function(t){t.classList.remove('active')});this.classList.add('active');r.querySelectorAll('.type-rules-panel').forEach(function(p){p.classList.remove('active')});r.querySelector('[data-panel=tuned]').classList.add('active')\"><span class=\"tr-sq\" style=\"background:#eab308\"></span>Tuned</button><button class=\"type-rules-tab\" onclick=\"var r=this.closest('.type-rules');r.querySelectorAll('.type-rules-tab').forEach(function(t){t.classList.remove('active')});this.classList.add('active');r.querySelectorAll('.type-rules-panel').forEach(function(p){p.classList.remove('active')});r.querySelector('[data-panel=engine]').classList.add('active')\"><span class=\"tr-sq\" style=\"background:#dc2626\"></span>Engine</button></div><div class=\"type-rules-panel active\" data-panel=\"production\">Must load files from disk on every request. No in-memory caching, no memory-mapped files, no pre-loaded file buffers. Compression must use the framework&#x27;s standard middleware or built-in static file handler - no handmade compression code. Serving pre-compressed <code>.br</code>/<code>.gz</code> variants from disk <strong>is allowed</strong>, but only through a documented framework API (e.g. ASP.NET <code>MapStaticAssets</code>, nginx <code>gzip_static</code> / <code>brotli_static</code>, Caddy <code>precompressed</code>). No custom file-suffix lookup logic.</div><div class=\"type-rules-panel\" data-panel=\"tuned\">May cache files in memory at startup, use memory-mapped files, pre-rendered response headers, or any caching strategy. May serve pre-compressed files (.gz, .br) from disk via any mechanism. Free to use any compression approach.</div><div class=\"type-rules-panel\" data-panel=\"engine\">No specific rules.</div></div>\n<p>Serves 20 static files of various types and sizes over HTTP/1.1, simulating a realistic page load with diverse file types and sizes.</p>\n<p><strong>Connections:</strong> 1,024, 4,096, 6,800</p>\n<h2 id=\"workload\">Workload</h2>\n<p>The load generator (<a href=\"https://github.com/wg/wrk\" target=\"_blank\" rel=\"noopener\">wrk</a>) requests 20 static files in a round-robin pattern using a Lua rotation script. All requests include <code>Accept-Encoding: br;q=1, gzip;q=0.8</code>.</p>\n<ul><li><strong>CSS</strong> (5 files, 8\u2013200 KB): <code>reset.css</code>, <code>layout.css</code>, <code>theme.css</code>, <code>components.css</code>, <code>utilities.css</code></li><li><strong>JavaScript</strong> (5 files, 12\u2013300 KB): <code>analytics.js</code>, <code>helpers.js</code>, <code>app.js</code>, <code>vendor.js</code>, <code>router.js</code></li><li><strong>HTML</strong> (2 files, 55\u2013120 KB): <code>header.html</code>, <code>footer.html</code></li><li><strong>Fonts</strong> (2 files, 18\u201322 KB): <code>regular.woff2</code>, <code>bold.woff2</code></li><li><strong>SVG</strong> (2 files, 15\u201370 KB): <code>logo.svg</code>, <code>icon-sprite.svg</code></li><li><strong>Images</strong> (3 files, 6\u201345 KB): <code>hero.webp</code>, <code>thumb1.webp</code>, <code>thumb2.webp</code></li><li><strong>JSON</strong> (1 file, 3 KB): <code>manifest.json</code></li></ul>\n<p>Total payload: ~842 KB across 20 files (~743 KB compressible text + ~99 KB binary). Brotli-compressed total: ~219 KB.</p>\n<p>Pre-compressed versions of all text files (<code>.gz</code> at level 9, <code>.br</code> at level 11) are available in the <code>data/static/</code> directory alongside the originals.</p>\n<h2 id=\"compression\">Compression</h2>\n<p>All requests include <code>Accept-Encoding: br;q=1, gzip;q=0.8</code>, indicating the client prefers Brotli but accepts gzip.</p>\n<p><strong>Compression is optional.</strong> Frameworks that don&#x27;t compress will serve files uncompressed - there is no penalty or validation failure. However, frameworks that do compress will benefit from reduced I/O, which naturally improves throughput.</p>\n<ul><li><strong>Text files</strong> (CSS, JS, HTML, SVG, JSON): good candidates for compression (68\u201394% size reduction with brotli)</li><li><strong>Binary files</strong> (woff2, webp): already compressed formats - servers should skip compression for these</li><li><strong>Pre-compressed files</strong>: <code>.gz</code> and <code>.br</code> versions are available on disk. Frameworks that support serving pre-compressed files via a documented API (e.g. Nginx <code>gzip_static</code>/<code>brotli_static</code>, Caddy <code>precompressed</code>, ASP.NET <code>MapStaticAssets</code>) can serve these directly with zero CPU overhead - this is allowed for both <strong>production</strong> and <strong>tuned</strong> entries.</li></ul>\n<p><strong>Production rule:</strong> compression must come from the framework&#x27;s standard middleware, built-in static file handler, or its documented pre-compressed-file API. No handmade compression code, no custom suffix-lookup logic.</p>\n<p><strong>Tuned rule:</strong> free to use any approach - custom compression, manual <code>.br</code>/<code>.gz</code> lookup, etc.</p>\n<h2 id=\"what-it-measures\">What it measures</h2>\n<ul><li>Static file serving throughput over HTTP/1.1</li><li>Content-Type handling for different file types</li><li>File serving strategy efficiency (disk I/O vs caching, depending on type)</li><li>Response efficiency with varied payload sizes</li><li>Compression efficiency (optional - reduces I/O at the cost of CPU)</li></ul>\n<h2 id=\"expected-requestresponse\">Expected request/response</h2>\n<pre><code>GET /static/reset.css HTTP/1.1\nHost: localhost:8080\nAccept-Encoding: br;q=1, gzip;q=0.8</code></pre>\n<pre><code>HTTP/1.1 200 OK\nContent-Type: text/css\nContent-Encoding: br\n\n(compressed file contents)</code></pre>\n<p>Or without compression:</p>\n<pre><code>HTTP/1.1 200 OK\nContent-Type: text/css\n\n(file contents)</code></pre>\n<h2 id=\"parameters\">Parameters</h2>\n<table><thead><tr><th>Parameter</th><th>Value</th></tr></thead><tbody><tr><td>Endpoint</td><td>20 URIs under <code>/static/*</code></td></tr><tr><td>Connections</td><td>1,024, 4,096, 6,800</td></tr><tr><td>Pipeline</td><td>1</td></tr><tr><td>Duration</td><td>5s</td></tr><tr><td>Runs</td><td>3 (best taken)</td></tr><tr><td>Load generator</td><td>wrk with Lua rotation script</td></tr></tbody></table></div>"},"test-profiles/h1/isolated/static":{"t":"Static Files (HTTP/1.1)","html":"<div class=\"doc-body\"><p>Serves 20 static files of various types and sizes over HTTP/1.1, simulating a realistic page load.</p>\n<div class=\"doc-cards\"><a class=\"doc-card\" href=\"#doc=test-profiles/h1/isolated/static/implementation\" data-doc=\"test-profiles/h1/isolated/static/implementation\"><span class=\"dc-t\">Implementation Guidelines</span><span class=\"dc-s\">Endpoint specification, expected request/response format, and type-specific rules.</span></a><a class=\"doc-card\" href=\"#doc=test-profiles/h1/isolated/static/validation\" data-doc=\"test-profiles/h1/isolated/static/validation\"><span class=\"dc-t\">Validation</span><span class=\"dc-s\">All checks executed by the validation script for this test profile.</span></a></div></div>"},"test-profiles/h1/isolated/json-tls/validation":{"t":"Validation","html":"<div class=\"doc-body\"><p>The validation script (<code>scripts/validate.sh</code>) runs these checks for the <code>json-tls</code> test profile. All must pass for a framework to be considered valid for this benchmark.</p>\n<h2 id=\"checks\">Checks</h2>\n<h3 id=\"alpn-negotiates-http11\">ALPN negotiates HTTP/1.1</h3>\n<pre><code>curl -sk --http1.1 https://localhost:8081/json/1?m=1</code></pre>\n<p>The response must report <code>http_version = 1.1</code>. A framework that advertises only <code>h2</code> on port 8081, or that refuses HTTP/1.1 clients, fails this check.</p>\n<h3 id=\"response-body-is-correct-for-multiple-count-m-pairs\">Response body is correct for multiple (count, m) pairs</h3>\n<p>Three requests are sent over HTTPS on port 8081 with different counts and multipliers:</p>\n<table><thead><tr><th>Count</th><th>Multiplier</th></tr></thead><tbody><tr><td>7</td><td>2</td></tr><tr><td>23</td><td>11</td></tr><tr><td>50</td><td>1</td></tr></tbody></table>\n<p>For each response the validator checks:</p>\n<ol><li><code>count</code> field equals the route count</li><li>Every item in <code>items</code> contains the full schema - <code>id</code>, <code>name</code>, <code>category</code>, <code>price</code>, <code>quantity</code>, <code>active</code>, <code>tags</code> (array), <code>rating</code> (object with <code>score</code> and <code>count</code>), and <code>total</code></li><li><code>total == price * quantity * m</code> for every item (integer, exact)</li></ol>\n<p>These <code>(count, m)</code> pairs are deliberately <strong>different</strong> from the <code>json-comp</code> validation pairs so a framework that tries to cache validation results across profiles can&#x27;t pass both.</p>\n<h3 id=\"content-type-is-applicationjson\">Content-Type is application/json</h3>\n<pre><code>curl -sk -D- -o /dev/null https://localhost:8081/json/1?m=1</code></pre>\n<p>The response must include a <code>Content-Type</code> header containing <code>application/json</code>.</p>\n<h2 id=\"running-locally\">Running locally</h2>\n<pre><code class=\"language-bash\">./scripts/validate.sh &lt;framework&gt;</code></pre>\n<p>Filter to this profile only:</p>\n<pre><code class=\"language-bash\">./scripts/validate.sh &lt;framework&gt; json-tls</code></pre></div>"},"test-profiles/h1/isolated/json-tls/implementation":{"t":"Implementation Guidelines","html":"<div class=\"doc-body\"><div class=\"type-rules\"><div class=\"type-rules-tabs\"><button class=\"type-rules-tab active\" onclick=\"var r=this.closest('.type-rules');r.querySelectorAll('.type-rules-tab').forEach(function(t){t.classList.remove('active')});this.classList.add('active');r.querySelectorAll('.type-rules-panel').forEach(function(p){p.classList.remove('active')});r.querySelector('[data-panel=production]').classList.add('active')\"><span class=\"tr-sq\" style=\"background:#22c55e\"></span>Standard</button><button class=\"type-rules-tab\" onclick=\"var r=this.closest('.type-rules');r.querySelectorAll('.type-rules-tab').forEach(function(t){t.classList.remove('active')});this.classList.add('active');r.querySelectorAll('.type-rules-panel').forEach(function(p){p.classList.remove('active')});r.querySelector('[data-panel=tuned]').classList.add('active')\"><span class=\"tr-sq\" style=\"background:#eab308\"></span>Tuned</button><button class=\"type-rules-tab\" onclick=\"var r=this.closest('.type-rules');r.querySelectorAll('.type-rules-tab').forEach(function(t){t.classList.remove('active')});this.classList.add('active');r.querySelectorAll('.type-rules-panel').forEach(function(p){p.classList.remove('active')});r.querySelector('[data-panel=engine]').classList.add('active')\"><span class=\"tr-sq\" style=\"background:#dc2626\"></span>Engine</button></div><div class=\"type-rules-panel active\" data-panel=\"production\">Must use the framework standard JSON serialization and a standard TLS stack (OpenSSL, BoringSSL, rustls, SChannel, JDK JSSE, etc.). No pre-serialized caches, no bypassing the framework response pipeline, no TLS session-ticket shortcuts that skip real handshakes.</div><div class=\"type-rules-panel\" data-panel=\"tuned\">May use alternative JSON libraries, tuned TLS providers, and framework-specific optimizations. The JSON body must still be serialized per request from live data - pre-computed / pre-serialized response caches are not allowed on either type; they short-circuit the serialization workload the profile exists to measure.</div><div class=\"type-rules-panel\" data-panel=\"engine\">No specific rules.</div></div>\n<p>The JSON over TLS profile is the <a href=\"#doc=test-profiles/h1/isolated/json-processing/implementation\" data-doc=\"test-profiles/h1/isolated/json-processing/implementation\">JSON Processing</a> workload transported over HTTP/1.1 + TLS on a dedicated port. It measures how much of a framework&#x27;s plaintext JSON throughput survives encryption.</p>\n<h2 id=\"how-it-works\">How it works</h2>\n<ol><li>The framework loads <code>/data/dataset.json</code> at startup (same 50-item mixed-type dataset as the plain <code>json</code> profile)</li><li>The framework listens on <strong>port 8081</strong> with HTTPS, serving HTTP/1.1 only (ALPN advertises <code>http/1.1</code>)</li><li>On each <code>GET /json/{count}?m={multiplier}</code> request, the server returns the same response shape as the <code>json</code> profile: first <code>count</code> items with <code>total = price \u00d7 quantity \u00d7 m</code>, wrapped in <code>{items, count}</code></li><li>Returns <code>Content-Type: application/json</code></li><li>Client sends <strong>no</strong> <code>Accept-Encoding</code> header - compression is out of scope for this profile</li></ol>\n<p>The load generator is <strong>wrk</strong> with a Lua rotation script (<code>requests/json-tls-rotate.lua</code>). gcannon is not used for this test because it doesn&#x27;t support TLS.</p>\n<h2 id=\"what-it-measures\">What it measures</h2>\n<ul><li>Everything <a href=\"#doc=test-profiles/h1/isolated/json-processing/implementation\" data-doc=\"test-profiles/h1/isolated/json-processing/implementation\" data-anchor=\"what-it-measures\">JSON Processing</a> measures</li><li><strong>TLS handshake cost amortized over keep-alive</strong> - connections are long-lived at 4096 concurrent</li><li><strong>Record framing overhead</strong> - every HTTP request gets wrapped in one or more TLS records</li><li><strong>Symmetric cipher throughput</strong> - AES-GCM / ChaCha20-Poly1305 on the hot path</li><li><strong>Certificate private-key operations</strong> - RSA/ECDSA cost per new connection, mostly negligible with keep-alive but visible during ramp</li></ul>\n<h2 id=\"port-alpn-and-certificates\">Port, ALPN, and certificates</h2>\n<ul><li><strong>Port</strong>: 8081 (distinct from 8080 plaintext and 8443 which is dedicated to HTTP/2 / HTTP/3 profiles)</li><li><strong>ALPN</strong>: advertise <code>http/1.1</code> only. HTTP/1.1-only clients (wrk) negotiate correctly and never upgrade to h2.</li><li><strong>Certificates</strong>: the same PEM files used by <code>baseline-h2</code> / <code>static-h2</code>, mounted at <code>/certs/server.crt</code> and <code>/certs/server.key</code>. Frameworks typically read them via environment variables (<code>TLS_CERT</code>, <code>TLS_KEY</code>) or a hardcoded path, same pattern as the other TLS tests.</li></ul>\n<h2 id=\"expected-response\">Expected response</h2>\n<p>For <code>GET /json/5?m=3</code> over HTTPS on port 8081:</p>\n<pre><code>HTTP/1.1 200 OK\nContent-Type: application/json</code></pre>\n<p>Body (same as the plain <code>json</code> profile):</p>\n<pre><code class=\"language-json\">{\n  &quot;items&quot;: [\n    {\n      &quot;id&quot;: 1,\n      &quot;name&quot;: &quot;Alpha Widget&quot;,\n      &quot;category&quot;: &quot;electronics&quot;,\n      &quot;price&quot;: 328,\n      &quot;quantity&quot;: 15,\n      &quot;active&quot;: true,\n      &quot;tags&quot;: [&quot;fast&quot;, &quot;new&quot;],\n      &quot;rating&quot;: { &quot;score&quot;: 48, &quot;count&quot;: 127 },\n      &quot;total&quot;: 14760\n    }\n  ],\n  &quot;count&quot;: 5\n}</code></pre>\n<h2 id=\"parameters\">Parameters</h2>\n<table><thead><tr><th>Parameter</th><th>Value</th></tr></thead><tbody><tr><td>Endpoint</td><td><code>GET /json/{count}?m={multiplier}</code></td></tr><tr><td>Transport</td><td>HTTP/1.1 over TLS</td></tr><tr><td>Port</td><td>8081</td></tr><tr><td>ALPN</td><td><code>http/1.1</code></td></tr><tr><td>Count \u00d7 multiplier pairs</td><td><code>(1,3)</code>, <code>(5,7)</code>, <code>(10,2)</code>, <code>(15,5)</code>, <code>(25,4)</code>, <code>(40,8)</code>, <code>(50,6)</code> (round-robin)</td></tr><tr><td>Connections</td><td>4,096</td></tr><tr><td>Pipeline</td><td>1</td></tr><tr><td>Duration</td><td>5s</td></tr><tr><td>Runs</td><td>3 (best taken)</td></tr><tr><td>Load generator</td><td>wrk + <code>requests/json-tls-rotate.lua</code></td></tr><tr><td>Certificates</td><td>mounted at <code>/certs/server.crt</code> + <code>/certs/server.key</code></td></tr><tr><td>Dataset</td><td>50 items, mounted at <code>/data/dataset.json</code></td></tr></tbody></table></div>"},"test-profiles/h1/isolated/json-tls":{"t":"JSON over TLS","html":"<div class=\"doc-body\"><p>Same workload as <a href=\"#doc=test-profiles/h1/isolated/json-processing\" data-doc=\"test-profiles/h1/isolated/json-processing\">JSON Processing</a> - a dataset of 50 items serialized to JSON with a per-request multiplier - but transported over <strong>HTTP/1.1 on top of TLS</strong> through a dedicated port (8081). Measures how much of a framework&#x27;s plaintext throughput survives TLS record framing, symmetric cipher work, and record-boundary scheduling.</p>\n<div class=\"doc-cards\"><a class=\"doc-card\" href=\"#doc=test-profiles/h1/isolated/json-tls/implementation\" data-doc=\"test-profiles/h1/isolated/json-tls/implementation\"><span class=\"dc-t\">Implementation Guidelines</span><span class=\"dc-s\">Endpoint specification, TLS port, ALPN requirements.</span></a><a class=\"doc-card\" href=\"#doc=test-profiles/h1/isolated/json-tls/validation\" data-doc=\"test-profiles/h1/isolated/json-tls/validation\"><span class=\"dc-t\">Validation</span><span class=\"dc-s\">All checks executed by the validation script for this test profile.</span></a></div></div>"},"test-profiles/h1/isolated/json-processing/validation":{"t":"Validation","html":"<div class=\"doc-body\"><p>The following checks are executed by <code>validate.sh</code> for every framework subscribed to the <code>json</code> test.</p>\n<h2 id=\"response-structure-and-computed-totals\">Response structure and computed totals</h2>\n<p>Sends <code>GET /json/{count}</code> for counts <strong>12, 22, 31, and 50</strong> (different from the benchmark counts to prevent hardcoded responses). For each request, verifies:</p>\n<ul><li>The response contains exactly <strong>count</strong> items</li><li>Every item contains the full schema - <code>id</code>, <code>name</code>, <code>category</code>, <code>price</code>, <code>quantity</code>, <code>active</code>, <code>tags</code> (array), <code>rating</code> (object with <code>score</code> and <code>count</code>), and <code>total</code>. Partial payloads that omit fields are rejected.</li><li>Each <code>total</code> is correctly computed as <code>price * quantity * m</code></li></ul>\n<h2 id=\"content-type-header\">Content-Type header</h2>\n<p>Sends <code>GET /json/50</code> and verifies the <code>Content-Type</code> response header is <code>application/json</code>.</p></div>"},"test-profiles/h1/isolated/json-processing/implementation":{"t":"Implementation Guidelines","html":"<div class=\"doc-body\"><div class=\"type-rules\"><div class=\"type-rules-tabs\"><button class=\"type-rules-tab active\" onclick=\"var r=this.closest('.type-rules');r.querySelectorAll('.type-rules-tab').forEach(function(t){t.classList.remove('active')});this.classList.add('active');r.querySelectorAll('.type-rules-panel').forEach(function(p){p.classList.remove('active')});r.querySelector('[data-panel=production]').classList.add('active')\"><span class=\"tr-sq\" style=\"background:#22c55e\"></span>Standard</button><button class=\"type-rules-tab\" onclick=\"var r=this.closest('.type-rules');r.querySelectorAll('.type-rules-tab').forEach(function(t){t.classList.remove('active')});this.classList.add('active');r.querySelectorAll('.type-rules-panel').forEach(function(p){p.classList.remove('active')});r.querySelector('[data-panel=tuned]').classList.add('active')\"><span class=\"tr-sq\" style=\"background:#eab308\"></span>Tuned</button><button class=\"type-rules-tab\" onclick=\"var r=this.closest('.type-rules');r.querySelectorAll('.type-rules-tab').forEach(function(t){t.classList.remove('active')});this.classList.add('active');r.querySelectorAll('.type-rules-panel').forEach(function(p){p.classList.remove('active')});r.querySelector('[data-panel=engine]').classList.add('active')\"><span class=\"tr-sq\" style=\"background:#dc2626\"></span>Engine</button></div><div class=\"type-rules-panel active\" data-panel=\"production\">Must use the framework standard JSON serialization. No pre-serialized caches, no custom serializers, no bypassing the framework response pipeline.</div><div class=\"type-rules-panel\" data-panel=\"tuned\">May use alternative JSON libraries (simd-json, sonic-json) and framework-specific optimizations. The JSON body must still be serialized per request from live data - pre-computed / pre-serialized response caches or response-lookup tables are not allowed on either type; they short-circuit the serialization workload the profile exists to measure.</div><div class=\"type-rules-panel\" data-panel=\"engine\">No specific rules.</div></div>\n<p>The JSON Processing profile measures how efficiently a framework handles a typical real-world API workload: loading data, computing derived fields, and serializing a JSON response.</p>\n<h2 id=\"how-it-works\">How it works</h2>\n<ol><li>At startup, the server reads <code>/data/dataset.json</code> - a file containing 50 items with mixed types (strings, numbers, booleans, arrays, nested objects)</li><li>On each <code>GET /json/{count}?m={multiplier}</code> request, the server:<ul><li>Takes the first <code>count</code> items from the dataset (1\u201350)</li><li>Computes a <code>total</code> field (<code>price \u00d7 quantity \u00d7 m</code>) for each item - <code>m</code> is an integer path-query multiplier that varies per request template</li><li>Builds the response object</li><li>Serializes everything as JSON</li></ul></li><li>Returns <code>Content-Type: application/json</code></li></ol>\n<p>The benchmark round-robins across 7 fixed <code>(count, m)</code> pairs: <code>(1, 3)</code>, <code>(5, 7)</code>, <code>(10, 2)</code>, <code>(15, 5)</code>, <code>(25, 4)</code>, <code>(40, 8)</code>, <code>(50, 6)</code>. Different multipliers per template mean every response body is unique - a framework that tries to cache <code>GET /json/5</code> by path alone will return wrong totals for other requests.</p>\n<h2 id=\"what-it-measures\">What it measures</h2>\n<ul><li><strong>Object allocation</strong> - 1\u201350 new objects built per request, varying across requests</li><li><strong>JSON serialization</strong> - converting native data structures to JSON text</li><li><strong>Mixed-type handling</strong> - strings, numbers, booleans, arrays, nested objects</li><li><strong>Route parameter parsing</strong> - extracting the count from the URL path</li><li><strong>Framework response overhead</strong> - buffering, headers, content-type handling</li></ul>\n<h2 id=\"dataset-format\">Dataset format</h2>\n<p>Each item in <code>dataset.json</code>:</p>\n<pre><code class=\"language-json\">{\n  &quot;id&quot;: 1,\n  &quot;name&quot;: &quot;Alpha Widget&quot;,\n  &quot;category&quot;: &quot;electronics&quot;,\n  &quot;price&quot;: 328,\n  &quot;quantity&quot;: 15,\n  &quot;active&quot;: true,\n  &quot;tags&quot;: [&quot;fast&quot;, &quot;new&quot;],\n  &quot;rating&quot;: {\n    &quot;score&quot;: 48,\n    &quot;count&quot;: 127\n  }\n}</code></pre>\n<h2 id=\"expected-response\">Expected response</h2>\n<p>For <code>GET /json/5?m=3</code>:</p>\n<pre><code class=\"language-json\">{\n  &quot;items&quot;: [\n    {\n      &quot;id&quot;: 1,\n      &quot;name&quot;: &quot;Alpha Widget&quot;,\n      &quot;category&quot;: &quot;electronics&quot;,\n      &quot;price&quot;: 328,\n      &quot;quantity&quot;: 15,\n      &quot;active&quot;: true,\n      &quot;tags&quot;: [&quot;fast&quot;, &quot;new&quot;],\n      &quot;rating&quot;: { &quot;score&quot;: 48, &quot;count&quot;: 127 },\n      &quot;total&quot;: 14760\n    }\n  ],\n  &quot;count&quot;: 5\n}</code></pre>\n<p>The <code>count</code> field must match the number of items returned and the route parameter. The <code>total</code> field is computed as <code>price * quantity * m</code> (all integers - no rounding needed). The server must return the first <code>count</code> items from the dataset.</p>\n<h2 id=\"parameters\">Parameters</h2>\n<table><thead><tr><th>Parameter</th><th>Value</th></tr></thead><tbody><tr><td>Endpoint</td><td><code>GET /json/{count}?m={multiplier}</code></td></tr><tr><td>Count \u00d7 multiplier pairs</td><td><code>(1,3)</code>, <code>(5,7)</code>, <code>(10,2)</code>, <code>(15,5)</code>, <code>(25,4)</code>, <code>(40,8)</code>, <code>(50,6)</code> (round-robin)</td></tr><tr><td>Connections</td><td>4,096</td></tr><tr><td>Pipeline</td><td>1</td></tr><tr><td>Duration</td><td>5s</td></tr><tr><td>Runs</td><td>3 (best taken)</td></tr><tr><td>Dataset</td><td>50 items, mounted at <code>/data/dataset.json</code></td></tr></tbody></table></div>"},"test-profiles/h1/isolated/json-processing":{"t":"JSON Processing","html":"<div class=\"doc-body\"><p>Measures how efficiently a framework handles a typical real-world API workload: loading data, computing derived fields, and serializing a JSON response.</p>\n<div class=\"doc-cards\"><a class=\"doc-card\" href=\"#doc=test-profiles/h1/isolated/json-processing/implementation\" data-doc=\"test-profiles/h1/isolated/json-processing/implementation\"><span class=\"dc-t\">Implementation Guidelines</span><span class=\"dc-s\">Endpoint specification, expected request/response format, and type-specific rules.</span></a><a class=\"doc-card\" href=\"#doc=test-profiles/h1/isolated/json-processing/validation\" data-doc=\"test-profiles/h1/isolated/json-processing/validation\"><span class=\"dc-t\">Validation</span><span class=\"dc-s\">All checks executed by the validation script for this test profile.</span></a></div></div>"},"test-profiles/h1/isolated/short-lived/validation":{"t":"Validation","html":"<div class=\"doc-body\"><p>The short-lived connection profile uses the same <code>/baseline11</code> endpoint as the baseline test. Its validation is covered by the <a href=\"#doc=test-profiles/h1/isolated/baseline/validation\" data-doc=\"test-profiles/h1/isolated/baseline/validation\">Baseline validation</a> checks, which run for every framework subscribed to either <code>baseline</code> or <code>limited-conn</code>.</p>\n<p>No additional validation checks are specific to this profile - the difference is in benchmark behavior (connections close after 10 requests), not in endpoint correctness.</p></div>"},"test-profiles/h1/isolated/short-lived/implementation":{"t":"Implementation Guidelines","html":"<div class=\"doc-body\"><div class=\"type-rules\"><div class=\"type-rules-tabs\"><button class=\"type-rules-tab active\" onclick=\"var r=this.closest('.type-rules');r.querySelectorAll('.type-rules-tab').forEach(function(t){t.classList.remove('active')});this.classList.add('active');r.querySelectorAll('.type-rules-panel').forEach(function(p){p.classList.remove('active')});r.querySelector('[data-panel=production]').classList.add('active')\"><span class=\"tr-sq\" style=\"background:#22c55e\"></span>Standard</button><button class=\"type-rules-tab\" onclick=\"var r=this.closest('.type-rules');r.querySelectorAll('.type-rules-tab').forEach(function(t){t.classList.remove('active')});this.classList.add('active');r.querySelectorAll('.type-rules-panel').forEach(function(p){p.classList.remove('active')});r.querySelector('[data-panel=tuned]').classList.add('active')\"><span class=\"tr-sq\" style=\"background:#eab308\"></span>Tuned</button><button class=\"type-rules-tab\" onclick=\"var r=this.closest('.type-rules');r.querySelectorAll('.type-rules-tab').forEach(function(t){t.classList.remove('active')});this.classList.add('active');r.querySelectorAll('.type-rules-panel').forEach(function(p){p.classList.remove('active')});r.querySelector('[data-panel=engine]').classList.add('active')\"><span class=\"tr-sq\" style=\"background:#dc2626\"></span>Engine</button></div><div class=\"type-rules-panel active\" data-panel=\"production\">Must use the framework default connection handling. No custom keep-alive tuning or connection pooling optimizations.</div><div class=\"type-rules-panel\" data-panel=\"tuned\">May optimize connection recycling, TCP fast-open, and socket reuse settings.</div><div class=\"type-rules-panel\" data-panel=\"engine\">No specific rules. Ranked separately from frameworks.</div></div>\n<p>Same workload as baseline, but each connection is closed and re-established after 10 requests. This forces frequent TCP handshakes.</p>\n<p><strong>Connections:</strong> 512, 4,096</p>\n<h2 id=\"expected-requestresponse\">Expected request/response</h2>\n<p>Same as baseline - sum of query parameters:</p>\n<pre><code>GET /baseline11?a=13&amp;b=42 HTTP/1.1</code></pre>\n<pre><code>HTTP/1.1 200 OK\nContent-Type: text/plain\n\n55</code></pre>\n<h2 id=\"what-it-measures\">What it measures</h2>\n<ul><li>Socket creation and teardown overhead</li><li>Connection accept rate</li><li>Per-connection memory allocation/deallocation</li><li>Any connection pooling or caching strategies</li></ul>\n<h2 id=\"real-world-relevance\">Real-world relevance</h2>\n<p>Many clients (mobile, IoT, load balancers without keepalive) do not maintain long-lived connections. This profile captures how well a framework handles the constant churn of short-lived connections - a common pattern in production environments.</p></div>"},"test-profiles/h1/isolated/short-lived":{"t":"Short-lived Connection","html":"<div class=\"doc-body\"><p>Same workload as baseline, but each connection is closed and re-established after 10 requests, forcing frequent TCP handshakes.</p>\n<div class=\"doc-cards\"><a class=\"doc-card\" href=\"#doc=test-profiles/h1/isolated/short-lived/implementation\" data-doc=\"test-profiles/h1/isolated/short-lived/implementation\"><span class=\"dc-t\">Implementation Guidelines</span><span class=\"dc-s\">Endpoint specification, expected request/response format, and type-specific rules.</span></a><a class=\"doc-card\" href=\"#doc=test-profiles/h1/isolated/short-lived/validation\" data-doc=\"test-profiles/h1/isolated/short-lived/validation\"><span class=\"dc-t\">Validation</span><span class=\"dc-s\">All checks executed by the validation script for this test profile.</span></a></div></div>"},"test-profiles/h1/workload/api-4/validation":{"t":"Validation","html":"<div class=\"doc-body\"><p>The API-4 test uses a subset of endpoints. Subscribing to the <code>mini</code> test automatically triggers validation for all of them, even if the individual tests are not listed in <code>meta.json</code>:</p>\n<ul><li><code>/baseline11</code> - <a href=\"#doc=test-profiles/h1/isolated/baseline/validation\" data-doc=\"test-profiles/h1/isolated/baseline/validation\">Baseline validation</a> (GET, POST, chunked POST, anti-cheat)</li><li><code>/json</code> - <a href=\"#doc=test-profiles/h1/isolated/json-processing/validation\" data-doc=\"test-profiles/h1/isolated/json-processing/validation\">JSON Processing validation</a> (structure, totals, Content-Type)</li><li><code>/async-db</code> - <a href=\"#doc=test-profiles/h1/isolated/async-database/validation\" data-doc=\"test-profiles/h1/isolated/async-database/validation\">Async Database validation</a> (structure, Content-Type, empty range)</li></ul></div>"},"test-profiles/h1/workload/api-4/implementation":{"t":"Implementation Guidelines","html":"<div class=\"doc-body\"><div class=\"type-rules\"><div class=\"type-rules-tabs\"><button class=\"type-rules-tab active\" onclick=\"var r=this.closest('.type-rules');r.querySelectorAll('.type-rules-tab').forEach(function(t){t.classList.remove('active')});this.classList.add('active');r.querySelectorAll('.type-rules-panel').forEach(function(p){p.classList.remove('active')});r.querySelector('[data-panel=production]').classList.add('active')\"><span class=\"tr-sq\" style=\"background:#22c55e\"></span>Standard</button><button class=\"type-rules-tab\" onclick=\"var r=this.closest('.type-rules');r.querySelectorAll('.type-rules-tab').forEach(function(t){t.classList.remove('active')});this.classList.add('active');r.querySelectorAll('.type-rules-panel').forEach(function(p){p.classList.remove('active')});r.querySelector('[data-panel=tuned]').classList.add('active')\"><span class=\"tr-sq\" style=\"background:#eab308\"></span>Tuned</button><button class=\"type-rules-tab\" onclick=\"var r=this.closest('.type-rules');r.querySelectorAll('.type-rules-tab').forEach(function(t){t.classList.remove('active')});this.classList.add('active');r.querySelectorAll('.type-rules-panel').forEach(function(p){p.classList.remove('active')});r.querySelector('[data-panel=engine]').classList.add('active')\"><span class=\"tr-sq\" style=\"background:#dc2626\"></span>Engine</button></div><div class=\"type-rules-panel active\" data-panel=\"production\">All endpoint implementations must follow their respective production rules. No endpoint-specific optimizations that would not be used in production.</div><div class=\"type-rules-panel\" data-panel=\"tuned\">May optimize each endpoint independently. Custom serializers and non-default configurations allowed. Pre-computed / pre-serialized JSON response caches are not allowed on either type - the JSON slice of this workload must still be serialized per request from live data.</div><div class=\"type-rules-panel\" data-panel=\"engine\">No specific rules.</div></div>\n<p>The API-4 profile runs a multi-endpoint workload with the server container constrained to <strong>4 CPUs and 16 GB memory</strong>. Only baseline, JSON, and async database endpoints are tested. The load generator uses 64 threads and 256 connections.</p>\n<p><strong>Connections:</strong> 256</p>\n<h2 id=\"configuration\">Configuration</h2>\n<table><thead><tr><th>Parameter</th><th>Value</th></tr></thead><tbody><tr><td>Server CPUs</td><td>4 (cpuset 0-3)</td></tr><tr><td>Server memory</td><td>16 GB</td></tr><tr><td>Connections</td><td>256</td></tr><tr><td>gcannon threads</td><td>64</td></tr><tr><td>Duration</td><td>15s</td></tr><tr><td>Request templates</td><td>8 (3 baseline, 3 JSON, 2 async-db)</td></tr><tr><td>Requests per connection</td><td>5 (then reconnect)</td></tr></tbody></table>\n<h2 id=\"what-it-measures\">What it measures</h2>\n<ul><li><strong>Resource-constrained performance</strong> - how well a framework utilizes limited CPU and memory</li><li><strong>Real-world relevance</strong> - closer to a typical production deployment (4-core VM, limited RAM) than the full-hardware tests</li><li><strong>Efficiency under contention</strong> - thread pool saturation, memory pressure, and GC behavior when resources are scarce</li><li><strong>Scaling characteristics</strong> - whether a framework&#x27;s performance degrades gracefully with fewer resources</li></ul>\n<h2 id=\"request-mix\">Request mix</h2>\n<ul><li>3x baseline GET (<code>GET /baseline11?a=1&amp;b=2</code>)</li><li>3x JSON processing (<code>GET /json</code>)</li><li>2x async DB query (<code>GET /async-db?min=10&amp;max=50</code>)</li></ul>\n<h2 id=\"docker-constraints\">Docker constraints</h2>\n<p>The server container is started with:</p>\n<pre><code>--cpuset-cpus=0-3 --memory=16g --memory-swap=16g</code></pre>\n<p>If a framework exceeds the 16 GB memory limit, the container will be OOM-killed by Docker.</p>\n<h2 id=\"parameters\">Parameters</h2>\n<table><thead><tr><th>Parameter</th><th>Value</th></tr></thead><tbody><tr><td>Endpoints</td><td><code>/baseline11</code>, <code>/json</code>, <code>/async-db</code></td></tr><tr><td>Connections</td><td>256</td></tr><tr><td>Pipeline</td><td>1</td></tr><tr><td>Requests per connection</td><td>5 (then reconnect with next template)</td></tr><tr><td>Duration</td><td>15s</td></tr><tr><td>Runs</td><td>3 (best taken)</td></tr><tr><td>Templates</td><td>8 (3 baseline GET, 3 JSON, 2 async-db)</td></tr><tr><td>Server CPU limit</td><td>4</td></tr><tr><td>Server memory limit</td><td>16 GB</td></tr><tr><td>gcannon threads</td><td>64</td></tr></tbody></table></div>"},"test-profiles/h1/workload/api-4":{"t":"API-4","html":"<div class=\"doc-body\"><p>Multi-endpoint workload (baseline, JSON, async-db) with the server constrained to 4 CPUs and 16 GB memory. Measures how efficiently a framework performs under limited resources.</p>\n<div class=\"doc-cards\"><a class=\"doc-card\" href=\"#doc=test-profiles/h1/workload/api-4/implementation\" data-doc=\"test-profiles/h1/workload/api-4/implementation\"><span class=\"dc-t\">Implementation Guidelines</span><span class=\"dc-s\">Endpoint specification, resource constraints, and request mix.</span></a><a class=\"doc-card\" href=\"#doc=test-profiles/h1/workload/api-4/validation\" data-doc=\"test-profiles/h1/workload/api-4/validation\"><span class=\"dc-t\">Validation</span><span class=\"dc-s\">All checks executed by the validation script for this test profile.</span></a></div></div>"},"test-profiles/h1/workload/api-16/validation":{"t":"Validation","html":"<div class=\"doc-body\"><p>The API-16 test uses a subset of endpoints. Subscribing to the <code>mini</code> test automatically triggers validation for all of them, even if the individual tests are not listed in <code>meta.json</code>:</p>\n<ul><li><code>/baseline11</code> - <a href=\"#doc=test-profiles/h1/isolated/baseline/validation\" data-doc=\"test-profiles/h1/isolated/baseline/validation\">Baseline validation</a> (GET, POST, chunked POST, anti-cheat)</li><li><code>/json</code> - <a href=\"#doc=test-profiles/h1/isolated/json-processing/validation\" data-doc=\"test-profiles/h1/isolated/json-processing/validation\">JSON Processing validation</a> (structure, totals, Content-Type)</li><li><code>/async-db</code> - <a href=\"#doc=test-profiles/h1/isolated/async-database/validation\" data-doc=\"test-profiles/h1/isolated/async-database/validation\">Async Database validation</a> (structure, Content-Type, empty range)</li></ul></div>"},"test-profiles/h1/workload/api-16/implementation":{"t":"Implementation Guidelines","html":"<div class=\"doc-body\"><div class=\"type-rules\"><div class=\"type-rules-tabs\"><button class=\"type-rules-tab active\" onclick=\"var r=this.closest('.type-rules');r.querySelectorAll('.type-rules-tab').forEach(function(t){t.classList.remove('active')});this.classList.add('active');r.querySelectorAll('.type-rules-panel').forEach(function(p){p.classList.remove('active')});r.querySelector('[data-panel=production]').classList.add('active')\"><span class=\"tr-sq\" style=\"background:#22c55e\"></span>Standard</button><button class=\"type-rules-tab\" onclick=\"var r=this.closest('.type-rules');r.querySelectorAll('.type-rules-tab').forEach(function(t){t.classList.remove('active')});this.classList.add('active');r.querySelectorAll('.type-rules-panel').forEach(function(p){p.classList.remove('active')});r.querySelector('[data-panel=tuned]').classList.add('active')\"><span class=\"tr-sq\" style=\"background:#eab308\"></span>Tuned</button><button class=\"type-rules-tab\" onclick=\"var r=this.closest('.type-rules');r.querySelectorAll('.type-rules-tab').forEach(function(t){t.classList.remove('active')});this.classList.add('active');r.querySelectorAll('.type-rules-panel').forEach(function(p){p.classList.remove('active')});r.querySelector('[data-panel=engine]').classList.add('active')\"><span class=\"tr-sq\" style=\"background:#dc2626\"></span>Engine</button></div><div class=\"type-rules-panel active\" data-panel=\"production\">All endpoint implementations must follow their respective production rules. No endpoint-specific optimizations that would not be used in production.</div><div class=\"type-rules-panel\" data-panel=\"tuned\">May optimize each endpoint independently. Custom serializers and non-default configurations allowed. Pre-computed / pre-serialized JSON response caches are not allowed on either type - the JSON slice of this workload must still be serialized per request from live data.</div><div class=\"type-rules-panel\" data-panel=\"engine\">No specific rules.</div></div>\n<p>The API-16 profile is identical to <a href=\"#doc=test-profiles/h1/workload/api-4/implementation\" data-doc=\"test-profiles/h1/workload/api-4/implementation\">API-4</a> but with the server constrained to <strong>16 CPUs and 32 GB memory</strong> instead of 4 CPUs and 16 GB. This measures how well the framework scales with more available resources.</p>\n<p><strong>Connections:</strong> 1,024</p>\n<p>All <a href=\"#doc=test-profiles/h1/workload/api-4/implementation\" data-doc=\"test-profiles/h1/workload/api-4/implementation\">request mix and implementation rules</a> from API-4 apply.</p>\n<h2 id=\"what-it-measures\">What it measures</h2>\n<ul><li><strong>Resource-constrained performance</strong> - how well a framework utilizes limited CPU and memory</li><li><strong>Real-world relevance</strong> - closer to a typical production deployment (4-core VM, limited RAM) than the full-hardware tests</li><li><strong>Efficiency under contention</strong> - thread pool saturation, memory pressure, and GC behavior when resources are scarce</li><li><strong>Scaling characteristics</strong> - whether a framework&#x27;s performance degrades gracefully with fewer resources</li></ul>\n<h2 id=\"request-mix\">Request mix</h2>\n<ul><li>3x baseline GET (<code>GET /baseline11?a=1&amp;b=2</code>)</li><li>3x JSON processing (<code>GET /json</code>)</li><li>2x async DB query (<code>GET /async-db?min=10&amp;max=50</code>)</li></ul>\n<h2 id=\"docker-constraints\">Docker constraints</h2>\n<p>The server container is started with:</p>\n<pre><code>--cpuset-cpus=0-7,64-71 --memory=32g --memory-swap=32g</code></pre>\n<p>If a framework exceeds the 32 GB memory limit, the container will be OOM-killed by Docker.</p>\n<h2 id=\"parameters\">Parameters</h2>\n<table><thead><tr><th>Parameter</th><th>Value</th></tr></thead><tbody><tr><td>Endpoints</td><td><code>/baseline11</code>, <code>/json</code>, <code>/async-db</code></td></tr><tr><td>Connections</td><td>1,024</td></tr><tr><td>Pipeline</td><td>1</td></tr><tr><td>Requests per connection</td><td>5 (then reconnect with next template)</td></tr><tr><td>Duration</td><td>15s</td></tr><tr><td>Runs</td><td>3 (best taken)</td></tr><tr><td>Templates</td><td>8 (3 baseline GET, 3 JSON, 2 async-db)</td></tr><tr><td>Server CPU limit</td><td>16</td></tr><tr><td>Server memory limit</td><td>32 GB</td></tr><tr><td>gcannon threads</td><td>64</td></tr></tbody></table></div>"},"test-profiles/h1/workload/api-16":{"t":"API-16","html":"<div class=\"doc-body\"><p>Same API workload as <a href=\"#doc=test-profiles/h1/workload/api-4\" data-doc=\"test-profiles/h1/workload/api-4\">API-4</a> but with the server constrained to 16 CPUs and 32 GB memory. Tests performance scaling with more resources.</p>\n<div class=\"doc-cards\"><a class=\"doc-card\" href=\"#doc=test-profiles/h1/workload/api-16/implementation\" data-doc=\"test-profiles/h1/workload/api-16/implementation\"><span class=\"dc-t\">Implementation Guidelines</span><span class=\"dc-s\">Endpoint specification, resource constraints, and request mix.</span></a><a class=\"doc-card\" href=\"#doc=test-profiles/h1/workload/api-16/validation\" data-doc=\"test-profiles/h1/workload/api-16/validation\"><span class=\"dc-t\">Validation</span><span class=\"dc-s\">All checks executed by the validation script for this test profile.</span></a></div></div>"},"test-profiles/gateway/gateway-h2/validation":{"t":"Validation","html":"<div class=\"doc-body\"><p>The following checks are executed by <code>validate.sh</code> for every framework subscribed to the <code>gateway-64</code> test. The validation script builds and starts the full compose stack from <code>compose.gateway.yml</code> before running checks.</p>\n<p>All requests are sent over HTTPS with HTTP/2 to port <strong>8443</strong> - the same port the load generator uses during benchmarking. This validates the full end-to-end path through the proxy (or directly to the server if no proxy is used), not just the server in isolation.</p>\n<h2 id=\"stack-startup\">Stack startup</h2>\n<ol><li>The Postgres sidecar is started (needed for <code>/async-db</code> endpoint)</li><li>The compose stack is built and started via <code>docker compose -f compose.gateway.yml up --build -d</code></li><li>Validation waits up to 30 seconds for HTTPS on port 8443 to respond</li><li>All checks run against <code>https://localhost:8443</code></li><li>The compose stack is torn down after validation completes</li></ol>\n<h2 id=\"http2-protocol-negotiation\">HTTP/2 protocol negotiation</h2>\n<p>Sends a request to <code>https://localhost:8443/static/reset.css</code> and verifies the response uses <strong>HTTP/2</strong> (via ALPN). This confirms TLS and h2 are correctly configured at the entry point (proxy or server).</p>\n<h2 id=\"static-file-checks\">Static file checks</h2>\n<h3 id=\"content-type-headers\">Content-Type headers</h3>\n<p>Verifies correct <code>Content-Type</code> headers for representative file types over HTTPS with HTTP/2:</p>\n<ul><li><code>GET /static/reset.css</code> - expects <code>Content-Type: text/css</code></li><li><code>GET /static/app.js</code> - expects <code>Content-Type: application/javascript</code></li></ul>\n<p>Note: <code>text/javascript</code> is accepted as equivalent to <code>application/javascript</code> per RFC 9239.</p>\n<h3 id=\"response-size\">Response size</h3>\n<p>Requests <code>GET /static/app.js</code> over HTTP/2 and verifies the response size is greater than 0 bytes. This confirms the proxy is correctly forwarding to the server and the server is returning file content.</p>\n<h3 id=\"404-for-nonexistent-file\">404 for nonexistent file</h3>\n<p>Sends <code>GET /static/nonexistent.txt</code> over HTTP/2 and verifies the response is <strong>HTTP 404</strong>. The 404 must propagate correctly through the proxy.</p>\n<h2 id=\"json-endpoint-checks\">JSON endpoint checks</h2>\n<h3 id=\"response-structure\">Response structure</h3>\n<p>Sends <code>GET /json</code> over HTTP/2 and validates:</p>\n<ul><li>Response contains exactly <strong>50 items</strong></li><li>Every item carries the full schema - <code>id</code>, <code>name</code>, <code>category</code>, <code>price</code>, <code>quantity</code>, <code>active</code>, <code>tags</code> (array), <code>rating</code> (object with <code>score</code> and <code>count</code>), and <code>total</code></li><li>Each <code>total</code> is correctly computed as <code>price * quantity</code> (rounded to 2 decimal places)</li></ul>\n<p>This is the same validation as the <a href=\"#doc=test-profiles/h1/isolated/json-processing/validation\" data-doc=\"test-profiles/h1/isolated/json-processing/validation\">JSON Processing test</a>, but routed through the proxy.</p>\n<h3 id=\"content-type-header\">Content-Type header</h3>\n<p>Verifies the response has <code>Content-Type: application/json</code>.</p>\n<h2 id=\"async-database-checks\">Async database checks</h2>\n<h3 id=\"response-structure\">Response structure</h3>\n<p>Sends <code>GET /async-db?min=10&amp;max=50</code> over HTTP/2 and validates:</p>\n<ul><li>Response count is between 1 and 50</li><li>Every item has a nested <code>rating</code> object with a <code>score</code> field</li><li>Every item has a <code>tags</code> array</li><li>Every item&#x27;s <code>active</code> field is a boolean (not a string or integer)</li></ul>\n<p>This is the same validation as the <a href=\"#doc=test-profiles/h1/isolated/async-database/validation\" data-doc=\"test-profiles/h1/isolated/async-database/validation\">Async Database test</a>, but routed through the proxy.</p>\n<h3 id=\"content-type-header\">Content-Type header</h3>\n<p>Verifies the response has <code>Content-Type: application/json</code>.</p>\n<h3 id=\"anti-cheat-empty-range\">Anti-cheat: empty range</h3>\n<p>Sends <code>GET /async-db?min=9999&amp;max=9999</code> and verifies the response returns <code>count: 0</code>. This detects hardcoded or cached responses that don&#x27;t actually query the database.</p>\n<h2 id=\"baseline-endpoint-checks\">Baseline endpoint checks</h2>\n<h3 id=\"fixed-input\">Fixed input</h3>\n<p>Sends <code>GET /baseline2?a=13&amp;b=42</code> over HTTP/2 and verifies the response body is <code>55</code> (the sum of the two query parameters). This confirms the proxy correctly forwards query parameters to the application server.</p>\n<h3 id=\"anti-cheat-randomized-input\">Anti-cheat: randomized input</h3>\n<p>Sends <code>GET /baseline2?a=&lt;random&gt;&amp;b=&lt;random&gt;</code> with random values and verifies the response matches the expected sum. This detects hardcoded responses.</p>\n<h2 id=\"prerequisite-checks\">Prerequisite checks</h2>\n<p>The gateway-64 test relies on endpoint implementations that are validated individually in other test profiles:</p>\n<ul><li><code>/json</code> - <a href=\"#doc=test-profiles/h1/isolated/json-processing/validation\" data-doc=\"test-profiles/h1/isolated/json-processing/validation\">JSON Processing validation</a></li><li><code>/static/*</code> - <a href=\"#doc=test-profiles/h1/isolated/static/validation\" data-doc=\"test-profiles/h1/isolated/static/validation\">Static Files validation</a></li><li><code>/async-db</code> - <a href=\"#doc=test-profiles/h1/isolated/async-database/validation\" data-doc=\"test-profiles/h1/isolated/async-database/validation\">Async Database validation</a></li><li><code>/baseline2</code> - <a href=\"#doc=test-profiles/h2/baseline-h2/validation\" data-doc=\"test-profiles/h2/baseline-h2/validation\">Baseline H2 validation</a></li></ul>\n<p>Frameworks subscribing to <code>gateway-64</code> should also subscribe to these individual tests to ensure each endpoint passes validation independently before testing through the proxy.</p></div>"},"test-profiles/gateway/gateway-h2/implementation":{"t":"Implementation Guidelines","html":"<div class=\"doc-body\"><div class=\"type-rules\"><div class=\"type-rules-tabs\"><button class=\"type-rules-tab active\" onclick=\"var r=this.closest('.type-rules');r.querySelectorAll('.type-rules-tab').forEach(function(t){t.classList.remove('active')});this.classList.add('active');r.querySelectorAll('.type-rules-panel').forEach(function(p){p.classList.remove('active')});r.querySelector('[data-panel=production]').classList.add('active')\"><span class=\"tr-sq\" style=\"background:#22c55e\"></span>Standard</button><button class=\"type-rules-tab\" onclick=\"var r=this.closest('.type-rules');r.querySelectorAll('.type-rules-tab').forEach(function(t){t.classList.remove('active')});this.classList.add('active');r.querySelectorAll('.type-rules-panel').forEach(function(p){p.classList.remove('active')});r.querySelector('[data-panel=tuned]').classList.add('active')\"><span class=\"tr-sq\" style=\"background:#eab308\"></span>Tuned</button><button class=\"type-rules-tab\" onclick=\"var r=this.closest('.type-rules');r.querySelectorAll('.type-rules-tab').forEach(function(t){t.classList.remove('active')});this.classList.add('active');r.querySelectorAll('.type-rules-panel').forEach(function(p){p.classList.remove('active')});r.querySelector('[data-panel=engine]').classList.add('active')\"><span class=\"tr-sq\" style=\"background:#dc2626\"></span>Engine</button></div><div class=\"type-rules-panel active\" data-panel=\"production\">Must ship exactly two services - one reverse proxy and one application server. The proxy must be a widely-used, production-grade server (Nginx, Caddy, Envoy, HAProxy, Traefik, etc.). No custom proxy implementations. No caches, load balancers, or additional sidecars beyond the two services. The proxy must serve /static/* directly from disk; the server must serve /baseline2, /json, and /async-db using standard framework middleware.</div><div class=\"type-rules-panel\" data-panel=\"tuned\">Same two-service shape as production. May optimize proxy configuration (worker counts, buffer sizes, keepalive tuning, connection pooling). May tune the proxy-to-server protocol (h2c, Unix sockets, etc.). Server may use any caching or optimization strategy on its own endpoints.</div><div class=\"type-rules-panel\" data-panel=\"engine\">No specific rules. May use custom proxy implementations. Ranked separately from frameworks.</div></div>\n<p>The Gateway-64 test benchmarks a <strong>proxy + server combination</strong> as a unit. The load generator sends TLS-encrypted HTTP/2 requests to port 8443; the proxy terminates TLS, serves <code>/static/*</code> directly from disk, and forwards dynamic endpoints to the application server over the entry&#x27;s choice of internal protocol.</p>\n<h2 id=\"architecture\">Architecture</h2>\n<p>Exactly two services - nothing else. One proxy, one server. Splitting dynamic endpoints across multiple servers, adding a caching layer, load-balancing across replicas, or running a single combined service are all <strong>not allowed</strong> for this test. The shape is fixed so entries compete on proxy choice, proxy tuning, proxy-to-server protocol, and CPU split - not on architectural creativity.</p>\n<pre><code>                    TLS (h2)              proxy \u2192 server\n  \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510    \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500&gt;    \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510    \u2500\u2500\u2500\u2500\u2500\u2500&gt;    \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n  \u2502  h2load  \u2502                   \u2502   Proxy   \u2502               \u2502  Server  \u2502\n  \u2502  (load   \u2502    port 8443      \u2502 TLS term  \u2502    any proto  \u2502  baseline\u2502\n  \u2502   gen)   \u2502    HTTPS/h2       \u2502 /static/* \u2502               \u2502  json    \u2502\n  \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518                   \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518               \u2502  async-db\u2502\n                                                             \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n                                 CPU: N of 64                CPU: 64-N</code></pre>\n<h2 id=\"endpoint-responsibilities\">Endpoint responsibilities</h2>\n<table><thead><tr><th>Path</th><th>Handled by</th><th>Role</th></tr></thead><tbody><tr><td><code>/static/*</code></td><td>Proxy</td><td>Static files served directly from <code>/data/static/</code></td></tr><tr><td><code>/baseline2?a=N&amp;b=M</code></td><td>Server</td><td>Query-parameter sum</td></tr><tr><td><code>/json</code></td><td>Server</td><td>Dataset processing (~10 KB JSON response)</td></tr><tr><td><code>/async-db?min=N&amp;max=M</code></td><td>Server</td><td>Postgres range query</td></tr></tbody></table>\n<p>Rules:</p>\n<ul><li>The proxy <strong>must</strong> serve <code>/static/*</code> from disk. Forwarding static files to the server is not allowed.</li><li>The server <strong>must</strong> serve all three dynamic endpoints. Proxy-level caching of dynamic responses is not allowed.</li><li>The proxy <strong>must</strong> terminate TLS. Passing raw TLS through to the server is not allowed.</li></ul>\n<p>See the individual test profile docs for each endpoint&#x27;s exact request/response format:</p>\n<ul><li><a href=\"#doc=test-profiles/h1/isolated/static/implementation\" data-doc=\"test-profiles/h1/isolated/static/implementation\"><code>/static/*</code></a></li><li><a href=\"#doc=test-profiles/h2/baseline-h2/implementation\" data-doc=\"test-profiles/h2/baseline-h2/implementation\"><code>/baseline2</code></a></li><li><a href=\"#doc=test-profiles/h1/isolated/json-processing/implementation\" data-doc=\"test-profiles/h1/isolated/json-processing/implementation\"><code>/json</code></a></li><li><a href=\"#doc=test-profiles/h1/isolated/async-database/implementation\" data-doc=\"test-profiles/h1/isolated/async-database/implementation\"><code>/async-db</code></a></li></ul>\n<h2 id=\"docker-compose\">Docker Compose</h2>\n<p>Every gateway entry is orchestrated via a <code>compose.gateway.yml</code> file with exactly two services named <code>proxy</code> and <code>server</code>. The benchmark script builds, starts, and tears down the stack for each run.</p>\n<h3 id=\"example\">Example</h3>\n<pre><code class=\"language-yaml\">services:\n  proxy:\n    build: ./proxy\n    network_mode: host\n    cpuset: &quot;0-7,64-71&quot;\n    ulimits:\n      memlock: -1\n      nofile:\n        soft: 1048576\n        hard: 1048576\n    security_opt:\n      - seccomp:unconfined\n    volumes:\n      - ${CERTS_DIR}:/certs:ro\n      - ${DATA_DIR}/static:/data/static:ro\n    depends_on:\n      - server\n\n  server:\n    build:\n      context: ../../\n      dockerfile: frameworks/my-framework/Dockerfile\n    network_mode: host\n    cpuset: &quot;8-31,72-95&quot;\n    ulimits:\n      memlock: -1\n      nofile:\n        soft: 1048576\n        hard: 1048576\n    security_opt:\n      - seccomp:unconfined\n    environment:\n      - DATABASE_URL=${DATABASE_URL}\n      - DATABASE_MAX_CONN=256\n    volumes:\n      - ${DATA_DIR}/dataset.json:/data/dataset.json:ro</code></pre>\n<p>In this split:</p>\n<ul><li><strong>Proxy</strong> (Nginx) gets 8 physical cores (16 logical: <code>0-7,64-71</code>) - terminates TLS, serves <code>/static/*</code> directly from <code>/data/static/</code>, forwards dynamic endpoints to the server on localhost.</li><li><strong>Server</strong> gets 24 physical cores (48 logical: <code>8-31,72-95</code>) - listens on an internal port (e.g. <code>8080</code> plain HTTP/1.1 or h2c) and handles <code>/baseline2</code>, <code>/json</code>, <code>/async-db</code>.</li></ul>\n<p>Total: 16 + 48 = 64 logical CPUs.</p>\n<h3 id=\"environment-variables\">Environment variables</h3>\n<p>The benchmark script exports three environment variables before every compose command. Your compose file <strong>must</strong> use these - do not hardcode paths.</p>\n<table><thead><tr><th>Variable</th><th>Value</th></tr></thead><tbody><tr><td><code>CERTS_DIR</code></td><td>Absolute path to the TLS certificate directory (<code>server.crt</code>, <code>server.key</code>)</td></tr><tr><td><code>DATA_DIR</code></td><td>Absolute path to the data directory (<code>static/</code>, <code>dataset.json</code>)</td></tr><tr><td><code>DATABASE_URL</code></td><td>Postgres connection string for <code>/async-db</code> (sidecar is started by the benchmark script)</td></tr></tbody></table>\n<h3 id=\"required-compose-settings\">Required compose settings</h3>\n<p>Every service in your compose file <strong>must</strong> include these settings:</p>\n<table><thead><tr><th>Setting</th><th>Value</th><th>Why</th></tr></thead><tbody><tr><td><code>network_mode: host</code></td><td>Both services</td><td>Bridge networking adds measurable latency; host networking keeps proxy-to-server at native localhost speed.</td></tr><tr><td><code>cpuset</code></td><td>CPU range string</td><td>Pins the service to specific cores. See <a href=\"#\" data-anchor=\"cpu-allocation\">CPU allocation</a>.</td></tr><tr><td><code>security_opt: [seccomp:unconfined]</code></td><td>Both services</td><td>Allows io_uring and other syscalls that the default seccomp profile blocks.</td></tr><tr><td><code>ulimits.memlock: -1</code></td><td>Both services</td><td>Allows memory locking for performance-critical operations.</td></tr><tr><td><code>ulimits.nofile: { soft: 1048576, hard: 1048576 }</code></td><td>Both services</td><td>Raises the file descriptor limit.</td></tr></tbody></table>\n<h2 id=\"proxy-server-protocol\">Proxy \u2192 server protocol</h2>\n<p>The proxy-to-server transport is the entry&#x27;s choice and one of the most important tuning decisions:</p>\n<table><thead><tr><th>Protocol</th><th>Pros</th><th>Cons</th></tr></thead><tbody><tr><td><strong>HTTP/1.1 over TCP</strong></td><td>Simplest to configure, universally supported</td><td>No multiplexing - one request per connection at a time</td></tr><tr><td><strong>HTTP/1.1 + keepalive pool</strong></td><td>Reuses connections, avoids handshake per request</td><td>Still no multiplexing</td></tr><tr><td><strong>h2c (HTTP/2 cleartext)</strong></td><td>Multiplexing without the TLS cost of a second hop</td><td>Not all frameworks support h2c</td></tr><tr><td><strong>Unix domain sockets</strong></td><td>Lowest latency, no TCP stack overhead</td><td>Server must listen on a UDS</td></tr></tbody></table>\n<p>The benchmark measures end-to-end throughput from the load generator&#x27;s perspective. How the proxy talks to the server is an implementation detail, but it materially affects the score.</p>\n<h2 id=\"cpu-allocation\">CPU allocation</h2>\n<p>The test uses <strong>64 logical CPUs</strong> - 32 physical cores (0\u201331) plus their SMT siblings (64\u201395). The entry&#x27;s <code>compose.gateway.yml</code> defines how these are split between proxy and server using the <code>cpuset</code> directive. The split is the entry&#x27;s choice - give the proxy as little or as much as you want, as long as the total across both services equals exactly 64 logical CPUs.</p>\n<h3 id=\"cpu-topology-rules\">CPU topology rules</h3>\n<p>CPUs are allocated in <strong>physical core pairs</strong> - each allocation includes both the physical core and its SMT sibling. This avoids two services sharing a physical core and ensures consistent per-service performance.</p>\n<table><thead><tr><th>Proxy cpuset</th><th>Server cpuset</th><th>Split</th></tr></thead><tbody><tr><td><code>0-1,64-65</code></td><td><code>2-31,66-95</code></td><td>4 + 60 = 64</td></tr><tr><td><code>0-3,64-67</code></td><td><code>4-31,68-95</code></td><td>8 + 56 = 64</td></tr><tr><td><code>0-7,64-71</code></td><td><code>8-31,72-95</code></td><td>16 + 48 = 64</td></tr><tr><td><code>0-15,64-79</code></td><td><code>16-31,80-95</code></td><td>32 + 32 = 64</td></tr></tbody></table>\n<h3 id=\"understanding-smt-pairing\">Understanding SMT pairing</h3>\n<p>Each physical core has two logical CPUs: the core itself and its SMT (HyperThreading) sibling. On the benchmark machine, physical core <strong>N</strong> maps to logical CPUs <strong>N</strong> and <strong>N+64</strong>:</p>\n<ul><li>Physical core 0 \u2192 logical CPUs <strong>0</strong> and <strong>64</strong></li><li>Physical core 15 \u2192 logical CPUs <strong>15</strong> and <strong>79</strong></li><li>Physical core 31 \u2192 logical CPUs <strong>31</strong> and <strong>95</strong></li></ul>\n<p>When you allocate cores, you must always allocate <strong>both siblings</strong> to the same service. Two threads sharing a physical core compete for ALU, cache, and branch predictor - splitting siblings across services causes unpredictable cross-service interference.</p>\n<p><strong>Correct</strong> - proxy gets physical cores 0\u20137 (16 logical CPUs):</p>\n<pre><code>cpuset: &quot;0-7,64-71&quot;</code></pre>\n<p><strong>Incorrect</strong> - splits a physical core across services:</p>\n<pre><code>proxy:  cpuset: &quot;0-3&quot;         # gets core 3 without its sibling (67)\nserver: cpuset: &quot;4-31,64-95&quot;  # gets sibling 67 without core 3</code></pre>\n<p><strong>Incorrect</strong> - doesn&#x27;t allocate all 64 CPUs:</p>\n<pre><code>proxy:  cpuset: &quot;0-3,64-67&quot;     # 8 CPUs\nserver: cpuset: &quot;4-15,68-79&quot;    # 24 CPUs - 32 CPUs wasted</code></pre>\n<h3 id=\"choosing-the-split\">Choosing the split</h3>\n<ul><li><strong>Lightweight proxy</strong> (plain forwarding, little static traffic) - 2\u20134 physical cores is usually enough</li><li><strong>TLS-terminating proxy with heavy static</strong> - 4\u20138 physical cores. TLS handshakes and static I/O compete for CPU at high connection counts.</li><li><strong>Even split</strong> - 16+16 physical cores is a good starting point if you don&#x27;t know which side is the bottleneck</li></ul>\n<h2 id=\"metajson\">meta.json</h2>\n<pre><code class=\"language-json\">{\n  &quot;display_name&quot;: &quot;my-framework + nginx&quot;,\n  &quot;language&quot;: &quot;C#&quot;,\n  &quot;type&quot;: &quot;flagship&quot;,\n  &quot;mode&quot;: &quot;standard&quot;,\n  &quot;engine&quot;: &quot;kestrel&quot;,\n  &quot;description&quot;: &quot;ASP.NET minimal API behind Nginx reverse proxy.&quot;,\n  &quot;repo&quot;: &quot;https://github.com/dotnet/aspnetcore&quot;,\n  &quot;tests&quot;: [&quot;gateway-64&quot;]\n}</code></pre>\n<p>The <code>tests</code> array only needs <code>gateway-64</code> for gateway-only entries. Frameworks that also participate in other tests can subscribe to both - the standard <code>Dockerfile</code> is used for non-gateway tests and <code>compose.gateway.yml</code> for <code>gateway-64</code>.</p>\n<h2 id=\"directory-structure\">Directory structure</h2>\n<pre><code>frameworks/my-framework/\n  compose.gateway.yml   # Two services: proxy + server\n  Dockerfile            # Server image\n  meta.json\n  proxy/\n    Dockerfile          # Proxy image (e.g. Nginx with custom config)\n    nginx.conf          # Proxy configuration</code></pre>\n<h2 id=\"workload\">Workload</h2>\n<p>The load generator (<a href=\"https://nghttp2.org/documentation/h2load-howto.html\" target=\"_blank\" rel=\"noopener\">h2load</a>) requests 20 URIs in a round-robin across multiplexed HTTP/2 streams, all requests sent with <code>Accept-Encoding: br;q=1, gzip;q=0.8</code>:</p>\n<table><thead><tr><th>Category</th><th>URIs</th><th>Count</th><th>Weight</th><th>Handled by</th></tr></thead><tbody><tr><td>Static files</td><td><code>/static/reset.css</code>, <code>components.css</code>, <code>app.js</code>, <code>vendor.js</code>, <code>header.html</code>, <code>hero.webp</code> - a mix of CSS, JS, HTML, and an image for <code>sendfile</code>-path coverage</td><td>6</td><td>30%</td><td>Proxy</td></tr><tr><td>JSON</td><td><code>/json/{count}</code> with <code>count \u2208 {1, 5, 10, 15, 25, 40, 50}</code> - 7 payload sizes, same set as the h1-isolated JSON profile</td><td>7</td><td>35%</td><td>Server</td></tr><tr><td>Baseline</td><td><code>/baseline2?a=N&amp;b=M</code> with 4 distinct parameter combinations to defeat URI-keyed caches</td><td>4</td><td>20%</td><td>Server</td></tr><tr><td>Async DB</td><td><code>/async-db?min=10&amp;max=50&amp;limit=N</code> with <code>limit \u2208 {10, 25, 50}</code></td><td>3</td><td>15%</td><td>Server</td></tr></tbody></table>\n<p>The mix is JSON-weighted (35%) because server-side compute is the most expensive part of the stack; static serving (30%) keeps the proxy&#x27;s I/O path under meaningful load; baseline (20%) measures raw forwarding efficiency at minimal server cost; async-db (15%) is the smallest slice because it&#x27;s latency-bound on the Postgres round-trip rather than CPU-bound on either service.</p>\n<p>The leaderboard shows a colored breakdown of how many requests each category handled.</p>\n<h2 id=\"what-it-measures\">What it measures</h2>\n<ul><li><strong>Reverse proxy overhead</strong> - how much throughput is lost by adding a proxy layer vs. direct server access</li><li><strong>TLS termination efficiency</strong> - the proxy&#x27;s cost to terminate HTTPS/h2 at high connection counts</li><li><strong>HTTP/2 multiplexing through a proxy</strong> - whether the proxy can efficiently forward multiplexed streams</li><li><strong>Static file serving from the proxy</strong> - disk I/O, precompressed asset selection, kernel <code>sendfile</code> usage</li><li><strong>Mixed workload throughput</strong> - static I/O (proxy) vs. compute (server JSON) vs. database (server async-db) all competing for the same 64-CPU budget</li><li><strong>Proxy-to-server protocol choice</strong> - HTTP/1.1, h2c, or Unix domain sockets</li><li><strong>CPU split</strong> - how the entry balances proxy and server workload on a fixed CPU budget</li></ul>\n<h2 id=\"parameters\">Parameters</h2>\n<table><thead><tr><th>Parameter</th><th>Value</th></tr></thead><tbody><tr><td>Endpoints</td><td><code>/static/*</code>, <code>/json</code>, <code>/async-db</code>, <code>/baseline2</code></td></tr><tr><td>Connections</td><td>512, 1,024</td></tr><tr><td>Streams per connection</td><td>32 (<code>-m 32</code>)</td></tr><tr><td>Duration</td><td>5s</td></tr><tr><td>Runs</td><td>3 (best taken)</td></tr><tr><td>Load generator</td><td>h2load with <code>-i</code> (multi-URI round-robin)</td></tr><tr><td>Total CPU budget</td><td>64 logical (32 physical + 32 SMT), split freely between proxy and server</td></tr><tr><td>Memory limit</td><td>Unlimited</td></tr><tr><td>Port</td><td>8443 (HTTPS/H2)</td></tr><tr><td>Orchestration</td><td>Docker Compose (<code>compose.gateway.yml</code>)</td></tr></tbody></table></div>"},"test-profiles/gateway/gateway-h2":{"t":"Gateway H2","html":"<div class=\"doc-body\"><p>Two-service proxy + server stack over HTTP/2 + TLS. Proxy serves static files directly from disk, forwards dynamic endpoints (baseline, JSON, async-db) to the application server. 64 CPUs split freely between the two services.</p>\n<div class=\"doc-cards\"><a class=\"doc-card\" href=\"#doc=test-profiles/gateway/gateway-h2/implementation\" data-doc=\"test-profiles/gateway/gateway-h2/implementation\"><span class=\"dc-t\">Implementation Guidelines</span><span class=\"dc-s\">Compose file layout, endpoint responsibilities, proxy-to-server protocol, CPU allocation.</span></a><a class=\"doc-card\" href=\"#doc=test-profiles/gateway/gateway-h2/validation\" data-doc=\"test-profiles/gateway/gateway-h2/validation\"><span class=\"dc-t\">Validation</span><span class=\"dc-s\">Checks executed by validate.sh against the running compose stack.</span></a></div></div>"},"test-profiles/gateway/production-stack/validation":{"t":"Validation","html":"<div class=\"doc-body\"><p><code>validate.sh</code> brings up the full production-stack compose (edge + cache + authsvc + server) with <code>up --build -d</code>, waits up to 60 seconds for the HTTPS port to respond, runs the checks below, and tears the stack down.</p>\n<h2 id=\"checks-performed\">Checks performed</h2>\n<h3 id=\"protocol-and-static-files\">Protocol and static files</h3>\n<ol><li><strong>HTTP/2 protocol negotiation</strong> - <code>curl --http2 /static/reset.css</code> must report HTTP/2</li><li><strong><code>/static/reset.css</code></strong> - Content-Type <code>text/css</code></li><li><strong><code>/static/app.js</code></strong> - non-zero response body</li></ol>\n<h3 id=\"public-endpoints-no-auth\">Public endpoints (no auth)</h3>\n<ol><li><strong><code>/public/baseline?a=13&amp;b=42</code></strong> - returns <code>55</code></li><li><strong><code>/public/baseline?a=&lt;random&gt;&amp;b=&lt;random&gt;</code></strong> - correct sum (anti-cheat)</li><li><strong><code>/public/json/25</code></strong> - 25 items, each with the full schema (<code>id</code>, <code>name</code>, <code>category</code>, <code>price</code>, <code>quantity</code>, <code>active</code>, <code>tags</code>, <code>rating</code> with <code>score</code>+<code>count</code>) and computed <code>total = price \u00d7 quantity</code></li></ol>\n<h3 id=\"auth-wall-jwt-required\">Auth wall - JWT required</h3>\n<ol><li><strong><code>GET /api/items/1</code></strong> (no Authorization header) - <strong>HTTP 401</strong></li><li><strong><code>GET /api/items/1</code></strong> (Authorization: Bearer invalid.token.here) - <strong>HTTP 401</strong></li><li><strong><code>POST /api/items/1</code></strong> (no Authorization header) - <strong>HTTP 401</strong></li><li><strong><code>POST /api/items/1</code></strong> (Authorization: Bearer invalid.token.here) - <strong>HTTP 401</strong></li></ol>\n<h3 id=\"authenticated-crud-valid-jwt\">Authenticated CRUD - valid JWT</h3>\n<ol><li><strong><code>GET /api/items/1</code></strong> with valid JWT - returns item with <code>id=1</code></li><li><strong>Cache-aside cycle</strong> - <code>GET /api/items/7</code> twice:<ul><li>First call: <code>X-Cache: MISS</code> (L1+L2 cold \u2192 Postgres query \u2192 populate both layers)</li><li>Second call: <code>X-Cache: HIT</code> (served from L1 or L2)</li></ul></li><li><strong><code>POST /api/items/2</code></strong> with valid JWT + JSON body - returns <code>204 No Content</code> (Postgres UPDATE + cache invalidation)</li><li><strong>POST invalidation</strong> - warm cache for item 3, POST to item 3, GET item 3 again:<ul><li>GET after POST shows <code>X-Cache: MISS</code> (proves <code>RemoveAsync</code> cleared both L1 and L2)</li></ul></li><li><strong><code>GET /api/me</code></strong> with valid JWT - returns user JSON with <code>id=42</code> (authsvc extracts <code>sub:42</code> from the JWT, edge forwards <code>X-User-Id: 42</code>, server reads user from Postgres via HybridCache)</li></ol>\n<p>Total: <strong>15 production-stack checks</strong> plus the compose startup itself.</p>\n<h2 id=\"what-validation-proves\">What validation proves</h2>\n<ul><li><strong>JWT verification works end-to-end</strong>: valid tokens authenticate, invalid/missing tokens get 401</li><li><strong>Both GET and POST are behind the auth wall</strong>: can&#x27;t read or write without a valid JWT</li><li><strong>Cache-aside populates on miss</strong>: first read of a cold key hits Postgres and populates cache</li><li><strong>Cache-aside serves from cache on hit</strong>: second read returns cached data</li><li><strong>POST invalidates cache</strong>: write clears both L1 and L2, forcing next read to re-query Postgres</li><li><strong>User lookup chain works</strong>: JWT <code>sub</code> claim \u2192 authsvc \u2192 X-User-Id header \u2192 server \u2192 Postgres/cache \u2192 user JSON</li></ul>\n<h2 id=\"jwt-token-for-validation\">JWT token for validation</h2>\n<p>Validation uses the pre-generated token at <code>data/jwt-token.txt</code>, signed with the secret <code>httparena-bench-secret-do-not-use-in-production</code>. The token contains <code>{&quot;sub&quot;:&quot;42&quot;,&quot;name&quot;:&quot;Alice Chen&quot;,&quot;exp&quot;:&lt;far-future&gt;}</code>. authsvc verifies the HMAC-SHA256 signature and returns <code>X-User-Id: 42</code>.</p></div>"},"test-profiles/gateway/production-stack/implementation":{"t":"Implementation Guidelines","html":"<div class=\"doc-body\"><div class=\"type-rules\"><div class=\"type-rules-tabs\"><button class=\"type-rules-tab active\" onclick=\"var r=this.closest('.type-rules');r.querySelectorAll('.type-rules-tab').forEach(function(t){t.classList.remove('active')});this.classList.add('active');r.querySelectorAll('.type-rules-panel').forEach(function(p){p.classList.remove('active')});r.querySelector('[data-panel=production]').classList.add('active')\"><span class=\"tr-sq\" style=\"background:#22c55e\"></span>Standard</button><button class=\"type-rules-tab\" onclick=\"var r=this.closest('.type-rules');r.querySelectorAll('.type-rules-tab').forEach(function(t){t.classList.remove('active')});this.classList.add('active');r.querySelectorAll('.type-rules-panel').forEach(function(p){p.classList.remove('active')});r.querySelector('[data-panel=tuned]').classList.add('active')\"><span class=\"tr-sq\" style=\"background:#eab308\"></span>Tuned</button><button class=\"type-rules-tab\" onclick=\"var r=this.closest('.type-rules');r.querySelectorAll('.type-rules-tab').forEach(function(t){t.classList.remove('active')});this.classList.add('active');r.querySelectorAll('.type-rules-panel').forEach(function(p){p.classList.remove('active')});r.querySelector('[data-panel=engine]').classList.add('active')\"><span class=\"tr-sq\" style=\"background:#dc2626\"></span>Engine</button></div><div class=\"type-rules-panel active\" data-panel=\"production\">Must ship exactly four services: edge (reverse proxy), cache (Redis), authsvc (the shared JWT verifier from frameworks/_shared/authsvc/, built as-is), and server (the framework). No custom auth implementations - JWT verification must happen at the edge via auth_request / forward_auth using the shared authsvc. The framework must implement cache-aside on /api/items reads (check cache \u2192 miss \u2192 query DB \u2192 populate cache with \u22641 second TTL) and cache invalidation on /api/items writes (clear cache after DB update). How the caching is implemented is the framework&#x27;s choice - any combination of in-process cache, Redis, or framework-specific cache abstraction is allowed.</div><div class=\"type-rules-panel\" data-panel=\"tuned\">Same four-service shape as production. May tune proxy configuration, connection pools, CPU split, and cache TTLs. May NOT replace authsvc with a custom implementation or skip JWT verification.</div><div class=\"type-rules-panel\" data-panel=\"engine\">No specific rules. May replace any service with a custom implementation. Ranked separately from frameworks.</div></div>\n<h2 id=\"overview\">Overview</h2>\n<p>The Production Stack test benchmarks a <strong>realistic multi-service CRUD API deployment</strong> as a unit. The load generator sends HTTPS/h2 requests to the edge. Request flow depends on the path:</p>\n<table><thead><tr><th>Path</th><th>Flow</th></tr></thead><tbody><tr><td><code>/static/*</code></td><td>edge serves from disk (no auth, no backend)</td></tr><tr><td><code>/public/*</code></td><td>edge \u2192 server (no auth, no cache)</td></tr><tr><td><code>/api/*</code></td><td>edge \u2192 authsvc (JWT verification) \u2192 server (HybridCache L1 \u2192 Redis L2 \u2192 Postgres)</td></tr></tbody></table>\n<h2 id=\"architecture\">Architecture</h2>\n<p>Four services, plus the shared Postgres sidecar (external, managed by the benchmark script):</p>\n<pre><code>                  h2 + TLS\n  \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510   \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500&gt;    \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510     auth_request        \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n  \u2502  h2load  \u2502   port 8443     \u2502  edge   \u2502  \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u25ba   \u2502 authsvc  \u2502\n  \u2502          \u2502                 \u2502 (nginx) \u2502                         \u2502  (Rust)  \u2502\n  \u2502          \u2502   Authorization \u2502         \u2502  \u25c4\u2500\u2500 200 + X-User-Id   \u2502 JWT+HMAC \u2502\n  \u2502          \u2502   Bearer &lt;jwt&gt;  \u2502         \u2502                         \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n  \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518                 \u2502         \u2502\n                               \u2502         \u2502   /api/*\n                               \u2502         \u2502   \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u25ba    \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n                               \u2502         \u2502   X-User-Id header     \u2502  server  \u2502\n                               \u2502         \u2502                        \u2502(framework\u2502\n                               \u2502         \u2502   /public/*            \u2502          \u2502\n                               \u2502         \u2502   \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u25ba    \u2502 L1 cache \u2502\n                               \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518                        \u2502 L2 Redis \u2502\n                                                                  \u2502 Postgres \u2502\n                                                                  \u2514\u2500\u2500\u2500\u2500\u252c\u2500\u2500\u2500\u2500\u2500\u2518\n                                                                       \u25bc\n                                                                  \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n                                                                  \u2502  cache   \u2502\n                                                                  \u2502 (Redis)  \u2502\n                                                                  \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518</code></pre>\n<h2 id=\"authentication-jwt-hmac-sha256\">Authentication - JWT HMAC-SHA256</h2>\n<p>Every <code>/api/*</code> request carries an <code>Authorization: Bearer &lt;jwt&gt;</code> header. The edge proxy fires an <code>auth_request</code> subrequest to the shared authsvc sidecar on <strong>every single API call</strong> - no caching, no shortcuts. authsvc:</p>\n<ol><li>Reads the <code>Authorization: Bearer &lt;token&gt;</code> header</li><li>Splits the JWT into <code>header.payload.signature</code></li><li>Computes <code>HMAC-SHA256(secret, header.payload)</code> and compares with the provided signature</li><li>Base64-decodes the payload and extracts the <code>sub</code> claim (user_id)</li><li>Returns <code>200 OK</code> with <code>X-User-Id: &lt;user_id&gt;</code> or <code>401 Unauthorized</code></li></ol>\n<p>The JWT secret is shared between the token generator (<code>data/jwt-token.txt</code>) and authsvc via the <code>JWT_SECRET</code> environment variable. The pre-generated token embeds <code>{&quot;sub&quot;:&quot;42&quot;,&quot;name&quot;:&quot;Alice Chen&quot;,&quot;exp&quot;:&lt;future&gt;}</code>.</p>\n<p><strong>Key rules:</strong></p>\n<ul><li><strong>Entries must use the shared authsvc binary unmodified.</strong> The Rust sidecar at <code>frameworks/_shared/authsvc/</code> is the same for every framework entry. Do not reimplement JWT verification in the framework&#x27;s language.</li><li><strong>No auth caching at the edge.</strong> No <code>proxy_cache</code> on the <code>/_auth</code> location. Every <code>/api/*</code> request must trigger a real HMAC-SHA256 verification. The CPU cost of this verification is part of what the test measures.</li><li><strong>No auth logic in the framework.</strong> The framework receives requests with a trusted <code>X-User-Id</code> header set by the edge after successful JWT verification. The framework never parses JWTs, never validates tokens, never touches the <code>Authorization</code> header.</li></ul>\n<h2 id=\"caching\">Caching</h2>\n<p>The framework must implement <strong>cache-aside</strong> on <code>/api/items</code> reads: check cache first, fall through to Postgres on miss, populate cache with a short TTL (\u22641 second). On writes, the framework must invalidate the cache entry so the next read sees fresh data.</p>\n<p><strong>How</strong> the caching is implemented is entirely the framework&#x27;s choice. Examples:</p>\n<ul><li><strong>Two-tier L1+L2</strong>: in-process memory cache + Redis (e.g. .NET HybridCache, Rails ActiveSupport::Cache with memory + Redis). Highest performance because hot keys never leave the process.</li><li><strong>Redis-only</strong>: every cache-aside read goes to Redis. Simpler but slower - Redis single-thread becomes the ceiling at high rps.</li><li><strong>In-process only</strong>: no Redis for caching, just a local hashmap with TTL. Works but loses the shared cache on multi-instance deployments.</li><li><strong>Framework-native cache</strong>: whatever the framework ships (Django cache, Spring Cache, etc.).</li></ul>\n<p>The .NET reference entry uses <code>HybridCache</code> (L1 in-process + L2 Redis) because that&#x27;s the idiomatic .NET 9+ pattern. Other frameworks should use whatever caching approach is natural and documented for their ecosystem. The test measures how well the <strong>framework + its cache choice</strong> perform together - the cache strategy is part of the competition.</p>\n<h3 id=\"cache-aside-pattern\">Cache-aside pattern</h3>\n<p><code>GET /api/items/{id}</code>:</p>\n<ol><li>Check L1 (in-process memory) by key <code>item:{id}</code></li><li>L1 miss \u2192 check L2 (Redis via the distributed cache abstraction)</li><li>L2 miss \u2192 <code>SELECT ... FROM items WHERE id = $1</code> in Postgres</li><li>Populate both L1 and L2 with the result, TTL \u2264 1 second</li><li>Return JSON with <code>X-Cache: HIT</code> or <code>X-Cache: MISS</code> header</li></ol>\n<p><code>POST /api/items/{id}</code>:</p>\n<ol><li>Parse JSON body (<code>name</code>, <code>price</code>, <code>quantity</code>)</li><li><code>UPDATE items SET ... WHERE id = $1</code> in Postgres</li><li>Invalidate cache key <code>item:{id}</code> via the abstraction&#x27;s <code>RemoveAsync</code> / equivalent (must clear both L1 and L2)</li><li>Return <code>204 No Content</code></li></ol>\n<p><code>GET /api/me</code>:</p>\n<ol><li>Read <code>X-User-Id</code> header (set by edge from authsvc JWT verification)</li><li>Cache-aside lookup on <code>user:{id}</code> with TTL \u2264 30 seconds</li><li>Miss \u2192 <code>SELECT id, name, email, plan FROM users WHERE id = $1</code></li><li>Return user JSON</li></ol>\n<h3 id=\"cache-ttls\">Cache TTLs</h3>\n<table><thead><tr><th>Cache key</th><th>L1 TTL</th><th>L2 TTL</th><th>Rationale</th></tr></thead><tbody><tr><td><code>item:{id}</code></td><td>\u2264 1 second</td><td>\u2264 1 second</td><td>Short TTL so the cache constantly cycles and Postgres sees regular traffic. At 10K unique IDs and ~250K rps, each ID is read ~12\u00d7/sec \u2192 ~92% L1 hit rate with 1s TTL.</td></tr><tr><td><code>user:{id}</code></td><td>\u2264 30 seconds</td><td>\u2264 30 seconds</td><td>User profiles change rarely. Longer TTL means near-100% hit rate during a 5-second benchmark.</td></tr></tbody></table>\n<h3 id=\"what-the-cache-hierarchy-looks-like-under-load\">What the cache hierarchy looks like under load</h3>\n<p>With 10,000 unique item IDs at ~250K rps (50% items):</p>\n<table><thead><tr><th>Layer</th><th>Ops/sec</th><th>What it does</th></tr></thead><tbody><tr><td>L1 (in-process memory)</td><td>~115K hits</td><td>~100 ns hashmap lookup</td></tr><tr><td>L2 (Redis)</td><td>~5\u201310K</td><td>HMGET on L1 miss, ~50 \u00b5s</td></tr><tr><td>Postgres</td><td>~5\u201310K</td><td>SELECT on L2 miss, ~200 \u00b5s</td></tr><tr><td>L2 writeback</td><td>~5\u201310K</td><td>HMSET + EXPIRE on Postgres hit</td></tr></tbody></table>\n<p>This is a real three-tier cache hierarchy under real load. Every layer does measurable work.</p>\n<h2 id=\"endpoint-responsibilities\">Endpoint responsibilities</h2>\n<table><thead><tr><th>Endpoint</th><th>Method</th><th>Auth</th><th>Cache</th><th>Database</th><th>What the framework does</th></tr></thead><tbody><tr><td><code>/static/*</code></td><td>GET</td><td>no</td><td>no</td><td>no</td><td><strong>Never reached</strong> - edge serves from disk</td></tr><tr><td><code>/public/baseline</code></td><td>GET</td><td>no</td><td>no</td><td>no</td><td><code>a + b</code> query-sum</td></tr><tr><td><code>/public/json/{count}</code></td><td>GET</td><td>no</td><td>no</td><td>no</td><td>Slice dataset, serialize JSON</td></tr><tr><td><code>/api/items/{id}</code></td><td>GET</td><td>JWT</td><td>L1 \u2192 L2 \u2192 DB</td><td>SELECT on miss</td><td>Cache-aside read</td></tr><tr><td><code>/api/items/{id}</code></td><td>POST</td><td>JWT</td><td>invalidate L1+L2</td><td>UPDATE</td><td>Parse body, write DB, invalidate cache</td></tr><tr><td><code>/api/me</code></td><td>GET</td><td>JWT</td><td>L1 \u2192 L2 \u2192 DB</td><td>SELECT on miss</td><td>Read user by X-User-Id</td></tr></tbody></table>\n<h2 id=\"workload\">Workload</h2>\n<p>Two h2load instances run in parallel against the same compose stack:</p>\n<p><strong>Reads</strong> (20,000 URIs in <code>production-stack-reads.txt</code>, round-robin):</p>\n<table><thead><tr><th>Category</th><th>Count</th><th>Share</th><th>Handled by</th></tr></thead><tbody><tr><td><code>/static/*</code></td><td>6,000</td><td>30%</td><td>edge (disk, gzip_static)</td></tr><tr><td><code>/public/baseline</code></td><td>2,000</td><td>10%</td><td>server (no auth, no cache)</td></tr><tr><td><code>/api/items/{id}</code> (IDs 1\u201310,000)</td><td>10,000</td><td>50%</td><td>edge \u2192 authsvc \u2192 server (cache-aside)</td></tr><tr><td><code>/api/me</code></td><td>2,000</td><td>10%</td><td>edge \u2192 authsvc \u2192 server (cache-aside)</td></tr></tbody></table>\n<p><strong>Writes</strong> (20 URIs in <code>production-stack-writes.txt</code>, round-robin, with JSON body):</p>\n<table><thead><tr><th>Category</th><th>URIs</th><th>What</th></tr></thead><tbody><tr><td><code>POST /api/items/{id}</code> (IDs 1\u201320)</td><td>20</td><td>UPDATE + cache invalidate</td></tr></tbody></table>\n<p>The reads instance uses <code>-c $conns -m 16</code>. The writes instance uses <code>-c $(conns/8) -m 8</code>. Both carry <code>Authorization: Bearer &lt;jwt&gt;</code> so all <code>/api/*</code> requests authenticate successfully.</p>\n<p>IDs 1\u201320 are targeted by both reads and writes, creating realistic cache churn on those entries. IDs 21\u201310,000 are read-only with stable cache behavior.</p>\n<h2 id=\"docker-compose\">Docker Compose</h2>\n<p>Every production-stack entry ships <code>compose.production-stack.yml</code> with exactly four services:</p>\n<table><thead><tr><th>Service</th><th>Role</th><th>Image</th></tr></thead><tbody><tr><td><code>edge</code></td><td>Reverse proxy, TLS, static files, auth_request</td><td>nginx (or equivalent)</td></tr><tr><td><code>cache</code></td><td>Redis for L2 cache (framework&#x27;s HybridCache backend)</td><td><code>redis:7-alpine</code> with seed entrypoint</td></tr><tr><td><code>authsvc</code></td><td>JWT HMAC-SHA256 verifier (shared, unmodified)</td><td>Built from <code>frameworks/_shared/authsvc/</code></td></tr><tr><td><code>server</code></td><td>The framework - CRUD handlers with HybridCache</td><td>Built from the framework&#x27;s Dockerfile</td></tr></tbody></table>\n<h3 id=\"required-compose-settings\">Required compose settings</h3>\n<p>Same as <a href=\"#doc=test-profiles/gateway/gateway-h2/implementation\" data-doc=\"test-profiles/gateway/gateway-h2/implementation\" data-anchor=\"required-compose-settings\">Gateway-64</a>: <code>network_mode: host</code>, pinned <code>cpuset</code>, <code>seccomp:unconfined</code>, and memlock/nofile ulimits on all four services.</p>\n<h3 id=\"environment-variables\">Environment variables</h3>\n<table><thead><tr><th>Variable</th><th>Set on</th><th>Value</th></tr></thead><tbody><tr><td><code>JWT_SECRET</code></td><td>authsvc</td><td><code>httparena-bench-secret-do-not-use-in-production</code></td></tr><tr><td><code>AUTHSVC_LISTEN</code></td><td>authsvc</td><td><code>0.0.0.0:9090</code></td></tr><tr><td><code>DATABASE_URL</code></td><td>server</td><td>Postgres connection string (set by benchmark script)</td></tr><tr><td><code>REDIS_URL</code></td><td>server</td><td><code>redis://127.0.0.1:6379</code></td></tr><tr><td><code>CERTS_DIR</code></td><td>edge</td><td>TLS certificate directory (set by benchmark script)</td></tr><tr><td><code>DATA_DIR</code></td><td>edge, cache</td><td>Dataset + static files + seed data</td></tr></tbody></table>\n<h2 id=\"cpu-allocation\">CPU allocation</h2>\n<p>64 logical CPUs (32 physical + 32 SMT siblings) split across four services. The split is the entry&#x27;s choice, but the reference entry&#x27;s empirically-tuned split is:</p>\n<table><thead><tr><th>Service</th><th>Physical</th><th>Logical</th><th>Rationale</th></tr></thead><tbody><tr><td>edge</td><td>15</td><td>30</td><td>TLS termination + h2 framing + auth_request subrequests</td></tr><tr><td>cache</td><td>1</td><td>2</td><td>Redis is single-threaded; more cores don&#x27;t help</td></tr><tr><td>authsvc</td><td>4</td><td>8</td><td>HMAC-SHA256 is CPU-bound and parallelizable. 4 cores is empirically optimal on the reference hardware - going to 5 causes a cliff (likely CCX/L3 boundary).</td></tr><tr><td>server</td><td>12</td><td>24</td><td>Framework CRUD handlers + HybridCache + Postgres client</td></tr></tbody></table>\n<p><strong>Important tuning finding:</strong> authsvc scales linearly with cores (HMAC-SHA256 is embarrassingly parallel) BUT stealing cores from server or edge to give to authsvc often makes overall throughput worse because the downstream services can&#x27;t keep up. The optimal split is where edge, authsvc, and server are all near their utilization ceiling simultaneously.</p>\n<h2 id=\"database\">Database</h2>\n<p>Two tables, both seeded by <code>data/pgdb-seed.sql</code>:</p>\n<p><strong><code>items</code></strong> - 100,000 rows, used by <code>/api/items/{id}</code>:</p>\n<pre><code class=\"language-sql\">CREATE TABLE items (\n    id INTEGER PRIMARY KEY,\n    name TEXT, category TEXT, price INTEGER, quantity INTEGER,\n    active BOOLEAN, tags JSONB, rating_score INTEGER, rating_count INTEGER\n);</code></pre>\n<p><strong><code>users</code></strong> - 4 rows, used by <code>/api/me</code>:</p>\n<pre><code class=\"language-sql\">CREATE TABLE users (\n    id INTEGER PRIMARY KEY,\n    name TEXT, email TEXT, plan TEXT\n);</code></pre>\n<h2 id=\"what-this-test-measures\">What this test measures</h2>\n<ul><li><strong>Full production stack throughput</strong> with real JWT auth, real cache hierarchy, real DB writes</li><li><strong>Framework cache abstraction efficiency</strong> - HybridCache, Rails.cache, Django cache, etc. under L1/L2 pressure with 10K-item working set</li><li><strong>JWT verification cost</strong> - every <code>/api/*</code> request does real HMAC-SHA256 crypto at the edge</li><li><strong>Cache-aside correctness</strong> - reads populate cache on miss, writes invalidate, TTL cycles naturally</li><li><strong>CRUD write path</strong> - JSON body parsing, Postgres UPDATE, cache invalidation</li><li><strong>CPU allocation strategy</strong> - how entries balance edge/auth/server on a fixed 64-CPU budget</li></ul>\n<h2 id=\"parameters\">Parameters</h2>\n<table><thead><tr><th>Parameter</th><th>Value</th></tr></thead><tbody><tr><td>Endpoints</td><td><code>/static/*</code>, <code>/public/baseline</code>, <code>/public/json/{count}</code>, <code>/api/items/{id}</code> (GET+POST), <code>/api/me</code></td></tr><tr><td>Connections</td><td>256, 1,024</td></tr><tr><td>Streams per connection</td><td>16 (<code>-m 16</code>) for reads, 8 for writes</td></tr><tr><td>Duration</td><td>5s</td></tr><tr><td>Runs</td><td>3 (best taken)</td></tr><tr><td>Load generator</td><td>Two parallel h2load instances (reads + writes)</td></tr><tr><td>Auth</td><td>JWT HMAC-SHA256 verified on every <code>/api/*</code> request (no caching)</td></tr><tr><td>Cache</td><td>L1 in-process + L2 Redis, \u22641s TTL for items, \u226430s for users</td></tr><tr><td>Item working set</td><td>10,000 unique IDs (IDs 1\u201310,000)</td></tr><tr><td>Write targets</td><td>20 IDs (1\u201320), overlapping with reads</td></tr><tr><td>Total CPU budget</td><td>64 logical, split across 4 services</td></tr><tr><td>Port</td><td>8443 (HTTPS/h2)</td></tr><tr><td>Orchestration</td><td>Docker Compose (<code>compose.production-stack.yml</code>)</td></tr></tbody></table></div>"},"test-profiles/gateway/production-stack":{"t":"Production Stack","html":"<div class=\"doc-body\"><p>The Production Stack profile benchmarks a <strong>realistic multi-service CRUD API deployment</strong> - reverse proxy, JWT auth sidecar, Redis cache, application server, and Postgres database - with a 10,000-item working set that exercises a real three-tier cache hierarchy (L1 in-process \u2192 L2 Redis \u2192 Postgres) under authenticated load.</p>\n<p>This is the closest any HttpArena test comes to modeling real production traffic:</p>\n<ul><li><strong>JWT authentication on every API call</strong> - a shared Rust sidecar verifies HMAC-SHA256 signatures with no caching. Real cryptographic work on every request, not a cached lookup.</li><li><strong>Cache-aside required</strong> - frameworks must implement cache-aside on reads (check cache \u2192 miss \u2192 DB \u2192 populate) and invalidation on writes. How the caching works (in-process, Redis, two-tier, framework-native) is the framework&#x27;s choice - the cache strategy is part of the competition.</li><li><strong>Cache-aside with short TTL</strong> - items cached \u22641 second so the cache constantly cycles: L1 hits \u2192 L2 (Redis) hits \u2192 Postgres queries \u2192 populate both layers. 10K unique IDs mean the cache hierarchy is under real pressure, not permanently warm.</li><li><strong>CRUD writes</strong> - POST requests parse a JSON body, UPDATE in Postgres, and invalidate both cache layers. Reads and writes target overlapping IDs to create realistic cache churn.</li></ul>\n<div class=\"doc-cards\"><a class=\"doc-card\" href=\"#doc=test-profiles/gateway/production-stack/implementation\" data-doc=\"test-profiles/gateway/production-stack/implementation\"><span class=\"dc-t\">Implementation Guidelines</span><span class=\"dc-s\">Four-service compose, JWT auth contract, HybridCache rules, cache-aside pattern, endpoint specs, CPU allocation.</span></a><a class=\"doc-card\" href=\"#doc=test-profiles/gateway/production-stack/validation\" data-doc=\"test-profiles/gateway/production-stack/validation\"><span class=\"dc-t\">Validation</span><span class=\"dc-s\">15 checks: auth wall (4), cache-aside cycle, POST invalidation, user lookup chain.</span></a></div></div>"},"test-profiles/gateway/gateway-h3/validation":{"t":"Validation","html":"<div class=\"doc-body\"><p>The Gateway-H3 validation flow reuses the same checks as <a href=\"#doc=test-profiles/gateway/gateway-h2/validation\" data-doc=\"test-profiles/gateway/gateway-h2/validation\">Gateway-64</a> - same endpoints, same expected responses, same anti-cheat probes. The only differences:</p>\n<ul><li>A separate <code>compose.gateway-h3.yml</code> is built and started (instead of <code>compose.gateway.yml</code>).</li><li>Endpoint probes go to the same proxy on port 8443 but use <code>curl --http2</code> rather than <code>curl --http3</code>.</li></ul>\n<p>Why curl <code>--http2</code> for an h3 test? Most curl builds don&#x27;t ship HTTP/3 support by default, and requiring QUIC-enabled curl would break <code>validate.sh</code> on contributors&#x27; machines. The compromise: h3-capable proxies (Caddy, nginx-quic, Envoy, HAProxy) advertise h2 on the same port anyway, so curl can reach the same endpoints for <strong>correctness</strong> validation. The <strong>h3-specific</strong> path is exercised at benchmark time by <code>h2load-h3</code> with <code>--alpn-list=h3</code> - if h3 is broken, the benchmark will show 0 req/s and you&#x27;ll see it immediately.</p>\n<h2 id=\"checks-performed\">Checks performed</h2>\n<p>Same set as <a href=\"#doc=test-profiles/gateway/gateway-h2/validation\" data-doc=\"test-profiles/gateway/gateway-h2/validation\">Gateway-64 validation</a>:</p>\n<ol><li>HTTPS port responds within 30 seconds</li><li>HTTP/2 protocol negotiation (as a proxy-health sanity check)</li><li><code>/static/reset.css</code> - <code>Content-Type: text/css</code></li><li><code>/static/app.js</code> - <code>Content-Type: application/javascript</code></li><li><code>/static/app.js</code> - non-zero response body</li><li><code>/static/nonexistent.txt</code> - HTTP 404</li><li><code>/json/50</code> - returns 50 items, each with the full schema (<code>id</code>, <code>name</code>, <code>category</code>, <code>price</code>, <code>quantity</code>, <code>active</code>, <code>tags</code>, <code>rating</code> with <code>score</code>+<code>count</code>) and a computed <code>total</code></li><li><code>/json/50</code> - <code>Content-Type: application/json</code></li><li><code>/async-db?min=10&amp;max=50&amp;limit=50</code> - returns 1\u201350 items with nested <code>rating</code>, <code>tags</code>, boolean <code>active</code></li><li><code>/async-db</code> - <code>Content-Type: application/json</code></li><li><code>/async-db?min=9999&amp;max=9999</code> - returns <code>count: 0</code> (anti-cheat)</li><li><code>/baseline2?a=13&amp;b=42</code> - returns <code>55</code></li><li><code>/baseline2?a=&lt;random&gt;&amp;b=&lt;random&gt;</code> - returns correct sum (anti-cheat)</li></ol>\n<h2 id=\"what-validation-does-not-check\">What validation does NOT check</h2>\n<ul><li><strong>HTTP/3 protocol negotiation at the edge</strong> - requires QUIC-capable curl, not available in most builds. Covered implicitly by the benchmark: if the proxy can&#x27;t speak h3, <code>h2load-h3</code> will report 0 req/s.</li><li><strong>QUIC connection migration, 0-RTT, or specific QUIC features</strong> - out of scope for correctness validation.</li><li><strong>Precompressed sidecar serving</strong> - validated at runtime by the benchmark&#x27;s <code>Accept-Encoding: br;q=1, gzip;q=0.8</code> request header. If the proxy serves the raw file instead of the <code>.br</code> / <code>.gz</code> sidecar, response size will be larger and bandwidth numbers will reflect that.</li></ul></div>"},"test-profiles/gateway/gateway-h3/implementation":{"t":"Implementation Guidelines","html":"<div class=\"doc-body\"><div class=\"type-rules\"><div class=\"type-rules-tabs\"><button class=\"type-rules-tab active\" onclick=\"var r=this.closest('.type-rules');r.querySelectorAll('.type-rules-tab').forEach(function(t){t.classList.remove('active')});this.classList.add('active');r.querySelectorAll('.type-rules-panel').forEach(function(p){p.classList.remove('active')});r.querySelector('[data-panel=production]').classList.add('active')\"><span class=\"tr-sq\" style=\"background:#22c55e\"></span>Standard</button><button class=\"type-rules-tab\" onclick=\"var r=this.closest('.type-rules');r.querySelectorAll('.type-rules-tab').forEach(function(t){t.classList.remove('active')});this.classList.add('active');r.querySelectorAll('.type-rules-panel').forEach(function(p){p.classList.remove('active')});r.querySelector('[data-panel=tuned]').classList.add('active')\"><span class=\"tr-sq\" style=\"background:#eab308\"></span>Tuned</button><button class=\"type-rules-tab\" onclick=\"var r=this.closest('.type-rules');r.querySelectorAll('.type-rules-tab').forEach(function(t){t.classList.remove('active')});this.classList.add('active');r.querySelectorAll('.type-rules-panel').forEach(function(p){p.classList.remove('active')});r.querySelector('[data-panel=engine]').classList.add('active')\"><span class=\"tr-sq\" style=\"background:#dc2626\"></span>Engine</button></div><div class=\"type-rules-panel active\" data-panel=\"production\">Must ship exactly two services - one HTTP/3-capable reverse proxy and one application server. The proxy must be a widely-used, production-grade server with QUIC support (Caddy, nginx 1.25+ with ngx_http_v3_module, Envoy, HAProxy 2.8+, h2o). No custom QUIC implementations. No caches, load balancers, or additional sidecars beyond the two services. The proxy must serve /static/* directly from disk; the server must serve /baseline2, /json, and /async-db using standard framework middleware.</div><div class=\"type-rules-panel\" data-panel=\"tuned\">Same two-service shape as production. May optimize proxy configuration (worker counts, buffer sizes, keepalive tuning, QUIC parameter tuning). May tune the proxy-to-server protocol (h1/h2c/UDS). Server may use any caching or optimization strategy on its own endpoints.</div><div class=\"type-rules-panel\" data-panel=\"engine\">No specific rules. May use custom QUIC implementations. Ranked separately from frameworks.</div></div>\n<p>The Gateway-H3 test is the HTTP/3 sibling of <a href=\"#doc=test-profiles/gateway/gateway-h2\" data-doc=\"test-profiles/gateway/gateway-h2\">Gateway-64</a>. Same endpoint surface, same two-service shape, same 64-CPU budget, same 20-URI round-robin mix - <strong>the only difference is the edge protocol</strong>. The load generator sends requests over QUIC to port 8443 (UDP), the proxy terminates h3 + TLS, and the upstream backend is still reached over plain h1 (or whatever the entry chooses internally).</p>\n<h2 id=\"architecture\">Architecture</h2>\n<p>Exactly two services: one HTTP/3-capable reverse proxy and one application server. The proxy handles QUIC termination and serves <code>/static/*</code> directly from disk. The application server handles <code>/baseline2</code>, <code>/json/{count}</code>, and <code>/async-db</code>.</p>\n<pre><code>                    h3/QUIC (UDP)          proxy \u2192 server\n  \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510    \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500&gt;    \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510    \u2500\u2500\u2500\u2500\u2500\u2500&gt;    \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n  \u2502 h2load   \u2502                       \u2502   Proxy   \u2502               \u2502  Server  \u2502\n  \u2502 (-h3)    \u2502    port 8443/udp      \u2502 h3 + TLS  \u2502    any proto  \u2502  baseline\u2502\n  \u2502          \u2502                       \u2502 /static/* \u2502               \u2502  json    \u2502\n  \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518                       \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518               \u2502  async-db\u2502\n                                                                 \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n                                     CPU: N of 64                CPU: 64-N</code></pre>\n<h2 id=\"why-split-out-from-gateway-64\">Why split out from Gateway-64?</h2>\n<p>HTTP/3 shifts work around compared to HTTP/2 in ways that are worth measuring separately:</p>\n<ul><li><strong>No head-of-line blocking</strong> at the TCP layer - QUIC streams are independent, so a slow response on one stream doesn&#x27;t stall others on the same connection</li><li><strong>Stream and datagram framing happens in userspace</strong>, not in the kernel&#x27;s TCP stack - moves CPU cost from <code>softirq</code> to the proxy process</li><li><strong>Encryption is per-packet, not per-record</strong> - different cost profile than TLS-over-TCP</li><li><strong>UDP send/recv syscall overhead</strong> is higher than TCP <code>sendfile()</code>, but <code>SO_TXTIME</code> / <code>SO_TIMESTAMPING</code> / GRO/GSO mitigations vary by kernel version and proxy implementation</li><li><strong>Connection migration and 0-RTT</strong> are h3-specific features that production proxies handle very differently</li></ul>\n<p>The H/2 Gateway numbers can&#x27;t predict any of this. Running the same workload over h3 gives you the other half of the picture.</p>\n<h2 id=\"endpoint-responsibilities\">Endpoint responsibilities</h2>\n<p>Same as <a href=\"#doc=test-profiles/gateway/gateway-h2/implementation\" data-doc=\"test-profiles/gateway/gateway-h2/implementation\" data-anchor=\"endpoint-responsibilities\">Gateway-64</a>:</p>\n<table><thead><tr><th>Path</th><th>Handled by</th><th>Role</th></tr></thead><tbody><tr><td><code>/static/*</code></td><td>Proxy</td><td>Static files served directly from <code>/data/static/</code> (precompressed <code>.br</code>/<code>.gz</code> sidecars allowed)</td></tr><tr><td><code>/baseline2?a=N&amp;b=M</code></td><td>Server</td><td>Query-parameter sum</td></tr><tr><td><code>/json/{count}</code></td><td>Server</td><td>Dataset processing (~10 KB JSON response)</td></tr><tr><td><code>/async-db?min=N&amp;max=M&amp;limit=L</code></td><td>Server</td><td>Postgres range query</td></tr></tbody></table>\n<p>Rules (identical to Gateway-64):</p>\n<ul><li>The proxy <strong>must</strong> serve <code>/static/*</code> from disk. Forwarding static files to the server is not allowed.</li><li>The server <strong>must</strong> serve all three dynamic endpoints. Proxy-level caching of dynamic responses is not allowed.</li><li>The proxy <strong>must</strong> terminate QUIC + TLS at the edge.</li></ul>\n<h2 id=\"docker-compose\">Docker Compose</h2>\n<p>Entries ship a <code>compose.gateway-h3.yml</code> file with exactly two services named <code>proxy</code> and <code>server</code>. The benchmark script builds, starts, and tears down the stack for each run.</p>\n<h3 id=\"example\">Example</h3>\n<pre><code class=\"language-yaml\">services:\n  proxy:\n    build: ./proxy\n    network_mode: host\n    cpuset: &quot;0-19,64-83&quot;\n    ulimits:\n      memlock: -1\n      nofile:\n        soft: 1048576\n        hard: 1048576\n    security_opt:\n      - seccomp:unconfined\n    volumes:\n      - ${CERTS_DIR}:/certs:ro\n      - ${DATA_DIR}/static:/data/static:ro\n    depends_on:\n      - server\n\n  server:\n    build:\n      context: ../../\n      dockerfile: frameworks/my-framework/Dockerfile\n    network_mode: host\n    cpuset: &quot;20-31,84-95&quot;\n    ulimits:\n      memlock: -1\n      nofile:\n        soft: 1048576\n        hard: 1048576\n    security_opt:\n      - seccomp:unconfined\n    environment:\n      - DATABASE_URL=${DATABASE_URL}\n      - DATABASE_MAX_CONN=256\n    volumes:\n      - ${DATA_DIR}/dataset.json:/data/dataset.json:ro</code></pre>\n<p>Proxy options (pick one):</p>\n<ul><li><strong>Caddy</strong> - h3 is enabled by default when you bind a TLS listener. Stock <code>caddy:2-alpine</code> image works out of the box. See the reference entry at <a href=\"https://github.com/MDA2AV/HttpArena/tree/main/frameworks/aspnet-minimal_caddy\" target=\"_blank\" rel=\"noopener\"><code>frameworks/aspnet-minimal_caddy/</code></a> for a minimal working Caddyfile.</li><li><strong>nginx with QUIC</strong> - nginx 1.25+ supports h3 via <code>ngx_http_v3_module</code>, but the stock <code>nginx:alpine</code> image is not built with it. You need to either build from source or use a community image that includes QUIC.</li><li><strong>Envoy</strong> - supports h3 via the <code>envoy.quic.connection_id_generator</code> + <code>QuicProtocolOptions</code> listener config.</li><li><strong>HAProxy 2.8+</strong> - supports h3 via the <code>quic4@:8443</code> bind spec.</li></ul>\n<h3 id=\"required-compose-settings\">Required compose settings</h3>\n<table><thead><tr><th>Setting</th><th>Value</th><th>Why</th></tr></thead><tbody><tr><td><code>network_mode: host</code></td><td>Both services</td><td>Bridge networking adds measurable latency; host networking keeps proxy-to-server at native localhost speed, and lets the proxy bind UDP 8443 directly on the host for QUIC.</td></tr><tr><td><code>cpuset</code></td><td>CPU range string</td><td>Pins the service to specific cores. See <a href=\"#\" data-anchor=\"cpu-allocation\">CPU allocation</a>.</td></tr><tr><td><code>security_opt: [seccomp:unconfined]</code></td><td>Both services</td><td>Allows io_uring and other syscalls the default seccomp profile blocks. QUIC-specific syscalls like <code>SO_TXTIME</code> also benefit.</td></tr><tr><td><code>ulimits.memlock: -1</code></td><td>Both services</td><td>Allows memory locking for performance-critical operations.</td></tr><tr><td><code>ulimits.nofile: { soft: 1048576, hard: 1048576 }</code></td><td>Both services</td><td>Raises the file descriptor limit.</td></tr></tbody></table>\n<h2 id=\"cpu-allocation\">CPU allocation</h2>\n<p>Identical to <a href=\"#doc=test-profiles/gateway/gateway-h2/implementation\" data-doc=\"test-profiles/gateway/gateway-h2/implementation\" data-anchor=\"cpu-allocation\">Gateway-64</a> - 64 logical CPUs (32 physical + 32 SMT siblings) split freely between proxy and server, with SMT-sibling pairing required. See the Gateway-64 page for the full rules.</p>\n<h2 id=\"workload\">Workload</h2>\n<p>The load generator (<code>h2load-h3</code>) requests 20 URIs in a round-robin across multiplexed HTTP/3 streams. All requests include <code>Accept-Encoding: br;q=1, gzip;q=0.8</code>:</p>\n<table><thead><tr><th>Category</th><th>URIs</th><th>Count</th><th>Weight</th><th>Handled by</th></tr></thead><tbody><tr><td>Static files</td><td><code>/static/reset.css</code>, <code>components.css</code>, <code>app.js</code>, <code>vendor.js</code>, <code>header.html</code>, <code>hero.webp</code></td><td>6</td><td>30%</td><td>Proxy</td></tr><tr><td>JSON</td><td><code>/json/{count}</code> with <code>count \u2208 {1, 5, 10, 15, 25, 40, 50}</code></td><td>7</td><td>35%</td><td>Server</td></tr><tr><td>Baseline</td><td><code>/baseline2?a=N&amp;b=M</code> with 4 distinct parameter combinations</td><td>4</td><td>20%</td><td>Server</td></tr><tr><td>Async DB</td><td><code>/async-db?min=10&amp;max=50&amp;limit=N</code> with <code>limit \u2208 {10, 25, 50}</code></td><td>3</td><td>15%</td><td>Server</td></tr></tbody></table>\n<p>Same mix and weighting as Gateway-64 - the <code>requests/gateway-64-uris.txt</code> URI file is shared between both profiles so benchmark numbers are directly comparable across the edge protocol dimension.</p>\n<h2 id=\"what-it-measures\">What it measures</h2>\n<ul><li><strong>QUIC termination cost</strong> at the proxy at realistic connection counts</li><li><strong>HTTP/3 stream multiplexing</strong> through a proxy</li><li><strong>Static file serving over h3</strong> - disk I/O + precompressed asset selection + UDP send path</li><li><strong>Mixed workload throughput</strong> when the edge is QUIC instead of TCP+TLS</li><li><strong>h3-vs-h2 delta</strong> for the same stack - comparing Gateway-H3 to Gateway-64 numbers tells you how much of a framework&#x27;s gateway performance is attributable to edge protocol choice</li></ul>\n<h2 id=\"parameters\">Parameters</h2>\n<table><thead><tr><th>Parameter</th><th>Value</th></tr></thead><tbody><tr><td>Endpoints</td><td><code>/static/*</code>, <code>/json/{count}</code>, <code>/async-db</code>, <code>/baseline2</code></td></tr><tr><td>Connections</td><td>64, 256</td></tr><tr><td>Streams per connection</td><td>32 (<code>-m 32</code>)</td></tr><tr><td>Duration</td><td>5s</td></tr><tr><td>Runs</td><td>3 (best taken)</td></tr><tr><td>Load generator</td><td>h2load-h3 with <code>-i</code> (multi-URI round-robin) and <code>--alpn-list=h3</code></td></tr><tr><td>Total CPU budget</td><td>64 logical (32 physical + 32 SMT), split freely between proxy and server</td></tr><tr><td>Memory limit</td><td>Unlimited</td></tr><tr><td>Port</td><td>8443 (UDP for h3, TCP as fallback for h1/h2 depending on proxy)</td></tr><tr><td>Orchestration</td><td>Docker Compose (<code>compose.gateway-h3.yml</code>)</td></tr></tbody></table></div>"},"test-profiles/gateway/gateway-h3":{"t":"Gateway-H3","html":"<div class=\"doc-body\"><p>Proxy + server stack with HTTP/3 at the edge and a realistic mixed workload - static files served by the proxy from disk, dynamic endpoints forwarded to the application server.</p>\n<div class=\"doc-cards\"><a class=\"doc-card\" href=\"#doc=test-profiles/gateway/gateway-h3/implementation\" data-doc=\"test-profiles/gateway/gateway-h3/implementation\"><span class=\"dc-t\">Implementation Guidelines</span><span class=\"dc-s\">Compose file layout, endpoint responsibilities, proxy-to-server protocol choices, CPU allocation.</span></a><a class=\"doc-card\" href=\"#doc=test-profiles/gateway/gateway-h3/validation\" data-doc=\"test-profiles/gateway/gateway-h3/validation\"><span class=\"dc-t\">Validation</span><span class=\"dc-s\">Checks executed by validate.sh against the running compose stack.</span></a></div></div>"},"test-profiles/grpc/unary/validation":{"t":"Validation","html":"<div class=\"doc-body\"><p>The gRPC unary profile does not have dedicated validation checks in <code>validate.sh</code>. Correctness is verified during the benchmark run itself:</p>\n<ul><li>The load generator (h2load) sends pre-encoded protobuf <code>SumRequest{a=1, b=2}</code> frames and counts successful <code>2xx</code> responses with <code>grpc-status: 0</code></li><li>Failed responses (wrong protobuf encoding, missing trailers, non-zero grpc-status) are counted as errors and excluded from the RPS score</li></ul>\n<p>Frameworks subscribed to <code>unary-grpc</code> must correctly implement the <code>BenchmarkService/GetSum</code> RPC as defined in the proto definition. The benchmark runner validates this implicitly through successful response counting.</p></div>"},"test-profiles/grpc/unary/implementation":{"t":"Implementation Guidelines","html":"<div class=\"doc-body\"><div class=\"type-rules\"><div class=\"type-rules-tabs\"><button class=\"type-rules-tab active\" onclick=\"var r=this.closest('.type-rules');r.querySelectorAll('.type-rules-tab').forEach(function(t){t.classList.remove('active')});this.classList.add('active');r.querySelectorAll('.type-rules-panel').forEach(function(p){p.classList.remove('active')});r.querySelector('[data-panel=production]').classList.add('active')\"><span class=\"tr-sq\" style=\"background:#22c55e\"></span>Standard</button><button class=\"type-rules-tab\" onclick=\"var r=this.closest('.type-rules');r.querySelectorAll('.type-rules-tab').forEach(function(t){t.classList.remove('active')});this.classList.add('active');r.querySelectorAll('.type-rules-panel').forEach(function(p){p.classList.remove('active')});r.querySelector('[data-panel=tuned]').classList.add('active')\"><span class=\"tr-sq\" style=\"background:#eab308\"></span>Tuned</button><button class=\"type-rules-tab\" onclick=\"var r=this.closest('.type-rules');r.querySelectorAll('.type-rules-tab').forEach(function(t){t.classList.remove('active')});this.classList.add('active');r.querySelectorAll('.type-rules-panel').forEach(function(p){p.classList.remove('active')});r.querySelector('[data-panel=engine]').classList.add('active')\"><span class=\"tr-sq\" style=\"background:#dc2626\"></span>Engine</button></div><div class=\"type-rules-panel active\" data-panel=\"production\">Must use the standard gRPC library for the language with default configuration. No custom protobuf serialization.</div><div class=\"type-rules-panel\" data-panel=\"tuned\">May optimize gRPC channel settings, thread pools, and use custom protobuf serialization.</div><div class=\"type-rules-panel\" data-panel=\"engine\">No specific rules. Ranked separately from frameworks.</div></div>\n<p>Measures unary gRPC call throughput over cleartext HTTP/2 (h2c). The server implements a simple <code>GetSum</code> RPC that adds two integers.</p>\n<p><strong>Connections:</strong> 256, 1,024 <strong>Concurrent streams per connection:</strong> 100</p>\n<h2 id=\"proto-definition\">Proto definition</h2>\n<pre><code class=\"language-protobuf\">syntax = &quot;proto3&quot;;\npackage benchmark;\n\nservice BenchmarkService {\n  rpc GetSum (SumRequest) returns (SumReply);\n}\n\nmessage SumRequest {\n  int32 a = 1;\n  int32 b = 2;\n}\n\nmessage SumReply {\n  int32 result = 1;\n}</code></pre>\n<h2 id=\"workload\">Workload</h2>\n<p><code>POST /benchmark.BenchmarkService/GetSum</code> sent as a gRPC unary call over h2c. The load generator (<a href=\"https://nghttp2.org/documentation/h2load-howto.html\" target=\"_blank\" rel=\"noopener\">h2load</a>) sends pre-encoded protobuf frames with gRPC headers (<code>content-type: application/grpc</code>, <code>te: trailers</code>).</p>\n<p>The request payload is a 9-byte gRPC frame: 5-byte frame header + 4-byte protobuf-encoded <code>SumRequest{a=1, b=2}</code>.</p>\n<h2 id=\"what-it-measures\">What it measures</h2>\n<ul><li>gRPC/HTTP2 frame processing overhead</li><li>Protocol Buffers serialization/deserialization performance</li><li>HTTP/2 stream multiplexing efficiency for gRPC</li><li>Server-side gRPC service dispatch latency</li></ul>\n<h2 id=\"expected-requestresponse\">Expected request/response</h2>\n<pre><code>POST /benchmark.BenchmarkService/GetSum HTTP/2\ncontent-type: application/grpc\nte: trailers\n\n&lt;binary: 00 00000004 08011002&gt;</code></pre>\n<pre><code>HTTP/2 200 OK\ncontent-type: application/grpc\n\n&lt;binary: 00 00000002 0803&gt;\n\ngrpc-status: 0</code></pre>\n<h2 id=\"how-it-differs-from-http2-baseline\">How it differs from HTTP/2 baseline</h2>\n<table><thead><tr><th></th><th>Baseline (HTTP/2)</th><th>Unary (gRPC)</th></tr></thead><tbody><tr><td>Protocol</td><td>HTTP/2 over TLS</td><td>gRPC over h2c</td></tr><tr><td>Serialization</td><td>Plain text</td><td>Protocol Buffers</td></tr><tr><td>Port</td><td>8443</td><td>8080</td></tr><tr><td>Request method</td><td>GET</td><td>POST</td></tr><tr><td>Load generator</td><td>h2load</td><td>h2load (raw gRPC frames)</td></tr></tbody></table>\n<h2 id=\"parameters\">Parameters</h2>\n<table><thead><tr><th>Parameter</th><th>Value</th></tr></thead><tbody><tr><td>RPC</td><td><code>BenchmarkService/GetSum</code></td></tr><tr><td>Connections</td><td>256, 1,024</td></tr><tr><td>Streams per connection</td><td>100 (<code>-m 100</code>)</td></tr><tr><td>Duration</td><td>5s</td></tr><tr><td>Runs</td><td>3 (best taken)</td></tr><tr><td>Load generator</td><td>h2load</td></tr></tbody></table></div>"},"test-profiles/grpc/unary":{"t":"Unary (gRPC)","html":"<div class=\"doc-body\"><p>Measures unary gRPC call throughput over cleartext HTTP/2 (h2c) using a simple <code>GetSum</code> RPC.</p>\n<div class=\"doc-cards\"><a class=\"doc-card\" href=\"#doc=test-profiles/grpc/unary/implementation\" data-doc=\"test-profiles/grpc/unary/implementation\"><span class=\"dc-t\">Implementation Guidelines</span><span class=\"dc-s\">Endpoint specification, expected request/response format, and type-specific rules.</span></a><a class=\"doc-card\" href=\"#doc=test-profiles/grpc/unary/validation\" data-doc=\"test-profiles/grpc/unary/validation\"><span class=\"dc-t\">Validation</span><span class=\"dc-s\">All checks executed by the validation script for this test profile.</span></a></div></div>"},"test-profiles/grpc/stream/validation":{"t":"Validation","html":"<div class=\"doc-body\"><p>The following checks are executed by <code>validate.sh</code> for every framework subscribed to the <code>stream-grpc</code> or <code>stream-grpc-tls</code> tests.</p>\n<h2 id=\"server-streaming-response-shape\">Server-streaming response shape</h2>\n<p>Calls <code>BenchmarkService/StreamSum</code> with <code>{a: 13, b: 42, count: 10}</code> over h2c (or h2 TLS for the <code>stream-grpc-tls</code> variant) and verifies:</p>\n<ul><li>The call completes with <code>OK</code> status (no <code>CANCELLED</code>, <code>UNAVAILABLE</code>, etc.)</li><li>Exactly <strong>10</strong> <code>SumReply</code> messages are received</li><li>Each <code>result</code> equals <code>13 + 42 + i</code> where <code>i</code> is the zero-based index of the message in the stream</li></ul>\n<h2 id=\"large-count-stream\">Large-count stream</h2>\n<p>Calls <code>StreamSum</code> with <code>count: 5000</code> and verifies the full stream is delivered without deadline exceeded and the final reply carries <code>result = 13 + 42 + 4999 = 5054</code>. Catches frameworks that silently truncate long streams or drop trailing messages under pressure.</p>\n<h2 id=\"empty-stream-anti-cheat\">Empty stream (anti-cheat)</h2>\n<p>Calls <code>StreamSum</code> with <code>count: 0</code> and verifies the call completes with <code>OK</code> and <strong>zero</strong> messages delivered. Catches frameworks that hard-code a minimum message count.</p></div>"},"test-profiles/grpc/stream/implementation":{"t":"Implementation Guidelines","html":"<div class=\"doc-body\"><div class=\"type-rules\"><div class=\"type-rules-tabs\"><button class=\"type-rules-tab active\" onclick=\"var r=this.closest('.type-rules');r.querySelectorAll('.type-rules-tab').forEach(function(t){t.classList.remove('active')});this.classList.add('active');r.querySelectorAll('.type-rules-panel').forEach(function(p){p.classList.remove('active')});r.querySelector('[data-panel=production]').classList.add('active')\"><span class=\"tr-sq\" style=\"background:#22c55e\"></span>Standard</button><button class=\"type-rules-tab\" onclick=\"var r=this.closest('.type-rules');r.querySelectorAll('.type-rules-tab').forEach(function(t){t.classList.remove('active')});this.classList.add('active');r.querySelectorAll('.type-rules-panel').forEach(function(p){p.classList.remove('active')});r.querySelector('[data-panel=tuned]').classList.add('active')\"><span class=\"tr-sq\" style=\"background:#eab308\"></span>Tuned</button><button class=\"type-rules-tab\" onclick=\"var r=this.closest('.type-rules');r.querySelectorAll('.type-rules-tab').forEach(function(t){t.classList.remove('active')});this.classList.add('active');r.querySelectorAll('.type-rules-panel').forEach(function(p){p.classList.remove('active')});r.querySelector('[data-panel=engine]').classList.add('active')\"><span class=\"tr-sq\" style=\"background:#dc2626\"></span>Engine</button></div><div class=\"type-rules-panel active\" data-panel=\"production\">Must use the framework&#x27;s standard gRPC server implementation. No bypassing of protobuf serialization or HTTP/2 framing.</div><div class=\"type-rules-panel\" data-panel=\"tuned\">May tune HTTP/2 frame sizes, flow-control window sizes, and server concurrency limits beyond defaults.</div><div class=\"type-rules-panel\" data-panel=\"engine\">No specific rules. Ranked separately from frameworks.</div></div>\n<p>The Server Streaming profile measures how efficiently a framework can emit a stream of protobuf messages from a single server-streaming gRPC call. One unary-style request lands on the server, and the handler writes <strong>N</strong> replies to a single <code>IServerStreamWriter</code> (or equivalent) before completing the call. The load generator (<a href=\"#doc=load-generators/grpc/ghz\" data-doc=\"load-generators/grpc/ghz\">ghz</a>) opens many concurrent streams and sums the total messages delivered per second.</p>\n<p><strong>Connections:</strong> 64 <strong>Streams per connection:</strong> 4 (64 conns \u00d7 256 workers = 4-way stream multiplexing per conn) <strong>Messages per call:</strong> 5,000</p>\n<h2 id=\"how-it-works\">How it works</h2>\n<ol><li>Client issues <code>StreamSum(StreamRequest{ a, b, count: 5000 })</code> on a new HTTP/2 stream</li><li>Server responds with <code>count</code> <code>SumReply</code> messages, each carrying <code>result = a + b + i</code> for <code>i</code> in <code>0..count-1</code></li><li>Client consumes all <code>count</code> replies before closing the stream and opening another call</li><li>ghz reports calls/sec; the benchmark runner multiplies by <code>msgs/call</code> and records messages/sec as the headline throughput</li></ol>\n<p>The shape rewards frameworks that amortize per-call overhead across many stream writes - a single call incurs one gRPC framing setup, one protobuf marshal of the request, and then a tight loop emitting replies through Kestrel&#x27;s HTTP/2 frame writer (or the equivalent in other frameworks). Frameworks that do per-message context allocation or contention on a shared writer will lose ground.</p>\n<h2 id=\"what-it-measures\">What it measures</h2>\n<ul><li><strong>Stream dispatch efficiency</strong> - per-message cost inside a live HTTP/2 stream, not per-call cost</li><li><strong>HTTP/2 frame writer throughput</strong> - how fast the server can serialize protobuf messages into DATA frames</li><li><strong>Flow-control handling</strong> - with 4 concurrent streams per TCP connection, WINDOW_UPDATE frames must not stall the stream</li><li><strong>Per-stream CPU efficiency</strong> - a single stream runs on a single logical thread in most frameworks; scaling comes from many parallel streams</li></ul>\n<h2 id=\"protobuf-service\">Protobuf service</h2>\n<pre><code class=\"language-protobuf\">service BenchmarkService {\n  rpc StreamSum (StreamRequest) returns (stream SumReply);\n}\n\nmessage StreamRequest {\n  int32 a = 1;\n  int32 b = 2;\n  int32 count = 3;\n}\n\nmessage SumReply {\n  int32 result = 1;\n}</code></pre>\n<h2 id=\"handler-contract\">Handler contract</h2>\n<p>For every incoming <code>StreamRequest</code>:</p>\n<ul><li>Compute <code>sum = request.a + request.b</code></li><li>Loop <code>request.count</code> times emitting <code>SumReply { result: sum + i }</code> through the server-stream writer</li><li>Clamp <code>count</code> to <code>&gt;= 1</code> defensively (anti-crash only, the load gen always sends 5,000)</li></ul>\n<h2 id=\"parameters\">Parameters</h2>\n<table><thead><tr><th>Parameter</th><th>Value</th></tr></thead><tbody><tr><td>Endpoint</td><td><code>BenchmarkService/StreamSum</code></td></tr><tr><td>Profiles</td><td><code>stream-grpc</code> (h2c :8080) and <code>stream-grpc-tls</code> (h2 TLS :8443)</td></tr><tr><td>Connections</td><td>64</td></tr><tr><td>Concurrent workers</td><td>256 (4 streams per TCP connection)</td></tr><tr><td>Messages per call</td><td>5,000</td></tr><tr><td>Duration</td><td>5s</td></tr><tr><td>Runs</td><td>3 (best taken)</td></tr><tr><td>Load generator</td><td><a href=\"#doc=load-generators/grpc/ghz\" data-doc=\"load-generators/grpc/ghz\">ghz</a></td></tr><tr><td>Metric</td><td>Messages per second (calls/sec \u00d7 msgs/call)</td></tr></tbody></table>\n<h2 id=\"why-messagessec-instead-of-callssec\">Why messages/sec instead of calls/sec</h2>\n<p>Streaming RPC calls are not comparable to unary calls on a per-call basis - a single server-streaming call can deliver thousands of messages through the framework&#x27;s dispatch path. Reporting calls/sec would make a 100\u00d7 msgs/call workload look 100\u00d7 slower than unary even when the framework is actually pushing more aggregate protobuf work.</p>\n<p>Messages/sec normalizes against unary throughput: for aspnet-grpc on this hardware, unary hits ~2.3M req/sec (via h2load) and server-streaming hits ~6M msg/sec (via ghz). The streaming number is ~3\u00d7 the unary number because amortization more than compensates for the per-call overhead savings h2load gets by not doing real client-side gRPC work.</p>\n<h2 id=\"notes\">Notes</h2>\n<ul><li>The streaming test does not subscribe to the <code>-r</code> request-per-conn rotation that gcannon-based profiles use - ghz&#x27;s streaming model opens fresh calls continuously until the duration elapses</li><li>Frameworks must return exactly <code>count</code> replies per call - validation checks <code>reply.length == count</code></li><li>Use of server-push or trailing-only responses to &quot;fake&quot; streaming is not allowed. Each reply must be a distinct gRPC DATA frame containing a valid <code>SumReply</code>.</li></ul></div>"},"test-profiles/grpc/stream":{"t":"Server Streaming (gRPC)","html":"<div class=\"doc-body\"><p>Measures gRPC <strong>server-streaming</strong> throughput in messages per second. Each call sends one request and the server emits N replies on the same HTTP/2 stream. Runs over both h2c (port 8080) and h2 TLS (port 8443).</p>\n<div class=\"doc-cards\"><a class=\"doc-card\" href=\"#doc=test-profiles/grpc/stream/implementation\" data-doc=\"test-profiles/grpc/stream/implementation\"><span class=\"dc-t\">Implementation Guidelines</span><span class=\"dc-s\">Endpoint specification, expected request/response format, and type-specific rules.</span></a><a class=\"doc-card\" href=\"#doc=test-profiles/grpc/stream/validation\" data-doc=\"test-profiles/grpc/stream/validation\"><span class=\"dc-t\">Validation</span><span class=\"dc-s\">All checks executed by the validation script for this test profile.</span></a></div></div>"},"test-profiles/ws/echo/validation":{"t":"Validation","html":"<div class=\"doc-body\"><p>The following checks are executed by <code>validate-ws.py</code> for every framework subscribed to the <code>echo-ws</code> test. The script uses a raw socket WebSocket client with no external dependencies.</p>\n<h2 id=\"upgrade-handshake-101-status\">Upgrade handshake (101 status)</h2>\n<p>Sends an HTTP/1.1 WebSocket upgrade request to <code>/ws</code> with a random <code>Sec-WebSocket-Key</code>. Verifies the server responds with <strong>HTTP 101 Switching Protocols</strong>.</p>\n<h2 id=\"sec-websocket-accept-header\">Sec-WebSocket-Accept header</h2>\n<p>Verifies the server returns a correct <code>Sec-WebSocket-Accept</code> value, computed as <code>Base64(SHA-1(key + &quot;258EAFA5-E914-47DA-95CA-5BAB11DC85B6&quot;))</code> per RFC 6455.</p>\n<h2 id=\"text-echo\">Text echo</h2>\n<p>Sends a text frame containing a random string (<code>HttpArena-validate-{random_hex}</code>) and verifies the server echoes back the exact same text in a text frame (opcode 0x1).</p>\n<h2 id=\"binary-echo\">Binary echo</h2>\n<p>Sends a binary frame containing 256 random bytes and verifies the server echoes back the exact same bytes in a binary frame (opcode 0x2).</p>\n<h2 id=\"multi-message-echo\">Multi-message echo</h2>\n<p>Sends 5 text frames rapidly in sequence, each with a unique random payload. Verifies all 5 are echoed back in order with correct content. This tests message ordering and buffering under burst conditions.</p>\n<h2 id=\"clean-close\">Clean close</h2>\n<p>Sends a close frame with code 1000 (normal closure) and reason <code>&quot;validate done&quot;</code>. Verifies the server responds with a close frame containing code 1000.</p>\n<h2 id=\"reject-non-upgrade-request\">Reject non-upgrade request</h2>\n<p>Sends a regular <code>GET /ws</code> request (without WebSocket upgrade headers) and verifies the server returns a <strong>4xx status code</strong> (400 or 426), not 101 or 5xx. The server must not crash on non-WebSocket requests to the WebSocket endpoint.</p>\n<h2 id=\"post-validation-health-check\">Post-validation health check</h2>\n<p>After all tests complete, opens a new WebSocket connection, sends a text frame <code>&quot;health&quot;</code>, verifies the echo, and performs a clean close. This confirms the server is still alive and functioning correctly after handling the full validation suite.</p></div>"},"test-profiles/ws/echo/implementation":{"t":"Implementation Guidelines","html":"<div class=\"doc-body\"><div class=\"type-rules\"><div class=\"type-rules-tabs\"><button class=\"type-rules-tab active\" onclick=\"var r=this.closest('.type-rules');r.querySelectorAll('.type-rules-tab').forEach(function(t){t.classList.remove('active')});this.classList.add('active');r.querySelectorAll('.type-rules-panel').forEach(function(p){p.classList.remove('active')});r.querySelector('[data-panel=production]').classList.add('active')\"><span class=\"tr-sq\" style=\"background:#22c55e\"></span>Standard</button><button class=\"type-rules-tab\" onclick=\"var r=this.closest('.type-rules');r.querySelectorAll('.type-rules-tab').forEach(function(t){t.classList.remove('active')});this.classList.add('active');r.querySelectorAll('.type-rules-panel').forEach(function(p){p.classList.remove('active')});r.querySelector('[data-panel=tuned]').classList.add('active')\"><span class=\"tr-sq\" style=\"background:#eab308\"></span>Tuned</button><button class=\"type-rules-tab\" onclick=\"var r=this.closest('.type-rules');r.querySelectorAll('.type-rules-tab').forEach(function(t){t.classList.remove('active')});this.classList.add('active');r.querySelectorAll('.type-rules-panel').forEach(function(p){p.classList.remove('active')});r.querySelector('[data-panel=engine]').classList.add('active')\"><span class=\"tr-sq\" style=\"background:#dc2626\"></span>Engine</button></div><div class=\"type-rules-panel active\" data-panel=\"production\">Must use the framework standard WebSocket API with default buffer sizes.</div><div class=\"type-rules-panel\" data-panel=\"tuned\">May optimize WebSocket frame handling, buffer sizes, and use custom frame parsers.</div><div class=\"type-rules-panel\" data-panel=\"engine\">No specific rules. Ranked separately from frameworks.</div></div>\n<p>Measures WebSocket echo throughput. Each connection upgrades via HTTP/1.1, then sends text messages and receives echoes. Each echo counts as one completed response.</p>\n<p><strong>Connections:</strong> 512, 4,096, 16,384 <strong>Pipeline:</strong> 1 (one message in flight per connection - send, await echo, repeat)</p>\n<h2 id=\"workload\">Workload</h2>\n<ol><li>Open TCP connection to port 8080</li><li>Send HTTP/1.1 upgrade request to <code>/ws</code></li><li>After receiving <code>101 Switching Protocols</code>, switch to WebSocket framing</li><li>Send text frames containing <code>&quot;hello&quot;</code>, receive echo frames</li><li>Measure messages per second</li></ol>\n<h2 id=\"what-it-measures\">What it measures</h2>\n<ul><li>WebSocket upgrade handshake performance</li><li>WebSocket frame parsing and construction efficiency</li><li>Echo round-trip latency under load</li><li>Connection scalability for real-time workloads</li></ul>\n<h2 id=\"expected-upgrade-requestresponse\">Expected upgrade request/response</h2>\n<pre><code>GET /ws HTTP/1.1\nHost: localhost:8080\nUpgrade: websocket\nConnection: Upgrade\nSec-WebSocket-Key: dGhlIHNhbXBsZSBub25jZQ==\nSec-WebSocket-Version: 13</code></pre>\n<pre><code>HTTP/1.1 101 Switching Protocols\nUpgrade: websocket\nConnection: Upgrade\nSec-WebSocket-Accept: s3pPLMBiTxaQ9kYGzzhZRbK+xOo=</code></pre>\n<h2 id=\"parameters\">Parameters</h2>\n<table><thead><tr><th>Parameter</th><th>Value</th></tr></thead><tbody><tr><td>Endpoint</td><td><code>/ws</code> (WebSocket upgrade)</td></tr><tr><td>Connections</td><td>512, 4,096, 16,384</td></tr><tr><td>Pipeline</td><td>1 (one message in flight per connection)</td></tr><tr><td>Message</td><td><code>&quot;hello&quot;</code> (5 bytes, text frame)</td></tr><tr><td>Duration</td><td>5s</td></tr><tr><td>Runs</td><td>3 (best taken)</td></tr><tr><td>Load generator</td><td>gcannon <code>--ws</code></td></tr></tbody></table></div>"},"test-profiles/ws/echo":{"t":"Echo (WebSocket)","html":"<div class=\"doc-body\"><p>Measures WebSocket echo throughput. Each connection upgrades via HTTP/1.1, then sends text messages and receives echoes.</p>\n<div class=\"doc-cards\"><a class=\"doc-card\" href=\"#doc=test-profiles/ws/echo/implementation\" data-doc=\"test-profiles/ws/echo/implementation\"><span class=\"dc-t\">Implementation Guidelines</span><span class=\"dc-s\">Endpoint specification, expected request/response format, and type-specific rules.</span></a><a class=\"doc-card\" href=\"#doc=test-profiles/ws/echo/validation\" data-doc=\"test-profiles/ws/echo/validation\"><span class=\"dc-t\">Validation</span><span class=\"dc-s\">All checks executed by the validation script for this test profile.</span></a></div></div>"},"test-profiles/ws/echo-pipeline/validation":{"t":"Validation","html":"<div class=\"doc-body\"><p>The same <code>validate-ws.py</code> checks executed for the <a href=\"#doc=test-profiles/ws/echo/validation\" data-doc=\"test-profiles/ws/echo/validation\">Echo</a> profile apply here - pipelining is a load-generator behavior, not a separate server contract. The endpoint and frame semantics are identical, so a server that passes the Echo validation passes for Echo Pipelined as well.</p>\n<p>See the <a href=\"#doc=test-profiles/ws/echo/validation\" data-doc=\"test-profiles/ws/echo/validation\">Echo validation page</a> for the full list of checks (handshake, accept header, text/binary echo, multi-message echo, clean close, non-upgrade rejection, post-validation health check).</p></div>"},"test-profiles/ws/echo-pipeline/implementation":{"t":"Implementation Guidelines","html":"<div class=\"doc-body\"><div class=\"type-rules\"><div class=\"type-rules-tabs\"><button class=\"type-rules-tab active\" onclick=\"var r=this.closest('.type-rules');r.querySelectorAll('.type-rules-tab').forEach(function(t){t.classList.remove('active')});this.classList.add('active');r.querySelectorAll('.type-rules-panel').forEach(function(p){p.classList.remove('active')});r.querySelector('[data-panel=production]').classList.add('active')\"><span class=\"tr-sq\" style=\"background:#22c55e\"></span>Standard</button><button class=\"type-rules-tab\" onclick=\"var r=this.closest('.type-rules');r.querySelectorAll('.type-rules-tab').forEach(function(t){t.classList.remove('active')});this.classList.add('active');r.querySelectorAll('.type-rules-panel').forEach(function(p){p.classList.remove('active')});r.querySelector('[data-panel=tuned]').classList.add('active')\"><span class=\"tr-sq\" style=\"background:#eab308\"></span>Tuned</button><button class=\"type-rules-tab\" onclick=\"var r=this.closest('.type-rules');r.querySelectorAll('.type-rules-tab').forEach(function(t){t.classList.remove('active')});this.classList.add('active');r.querySelectorAll('.type-rules-panel').forEach(function(p){p.classList.remove('active')});r.querySelector('[data-panel=engine]').classList.add('active')\"><span class=\"tr-sq\" style=\"background:#dc2626\"></span>Engine</button></div><div class=\"type-rules-panel active\" data-panel=\"production\">Must use the framework standard WebSocket API with default buffer sizes. No custom batching or read-ahead optimizations.</div><div class=\"type-rules-panel\" data-panel=\"tuned\">May optimize WebSocket frame handling, buffer sizes, and use custom frame parsers or batched read paths.</div><div class=\"type-rules-panel\" data-panel=\"engine\">No specific rules. Ranked separately from frameworks.</div></div>\n<p>Measures WebSocket echo throughput with pipelining. Each connection upgrades via HTTP/1.1, then sends 16 text messages back-to-back before waiting for the echoes. Each echo counts as one completed response.</p>\n<p><strong>Connections:</strong> 512, 4,096, 16,384 <strong>Pipeline:</strong> 16 (16 messages in flight per connection - send batch, drain echoes, repeat)</p>\n<h2 id=\"workload\">Workload</h2>\n<ol><li>Open TCP connection to port 8080</li><li>Send HTTP/1.1 upgrade request to <code>/ws</code></li><li>After receiving <code>101 Switching Protocols</code>, switch to WebSocket framing</li><li>Send 16 text frames containing <code>&quot;hello&quot;</code> back-to-back, then read 16 echo frames</li><li>Measure messages per second</li></ol>\n<h2 id=\"what-it-measures\">What it measures</h2>\n<ul><li>WebSocket frame parsing efficiency under burst load</li><li>Frameworks that drain multiple frames from a single read buffer gain a major advantage</li><li>Frameworks processing one frame at a time per connection see minimal improvement over the non-pipelined echo</li><li>Write coalescing and syscall reduction on the send path</li></ul>\n<h2 id=\"expected-upgrade-requestresponse\">Expected upgrade request/response</h2>\n<pre><code>GET /ws HTTP/1.1\nHost: localhost:8080\nUpgrade: websocket\nConnection: Upgrade\nSec-WebSocket-Key: dGhlIHNhbXBsZSBub25jZQ==\nSec-WebSocket-Version: 13</code></pre>\n<pre><code>HTTP/1.1 101 Switching Protocols\nUpgrade: websocket\nConnection: Upgrade\nSec-WebSocket-Accept: s3pPLMBiTxaQ9kYGzzhZRbK+xOo=</code></pre>\n<h2 id=\"parameters\">Parameters</h2>\n<table><thead><tr><th>Parameter</th><th>Value</th></tr></thead><tbody><tr><td>Endpoint</td><td><code>/ws</code> (WebSocket upgrade)</td></tr><tr><td>Connections</td><td>512, 4,096, 16,384</td></tr><tr><td>Pipeline</td><td>16 (16 messages in flight per connection)</td></tr><tr><td>Message</td><td><code>&quot;hello&quot;</code> (5 bytes, text frame)</td></tr><tr><td>Duration</td><td>5s</td></tr><tr><td>Runs</td><td>3 (best taken)</td></tr><tr><td>Load generator</td><td>gcannon <code>--ws -p 16</code></td></tr></tbody></table></div>"},"test-profiles/ws/echo-pipeline":{"t":"Echo Pipelined (WebSocket)","html":"<div class=\"doc-body\"><p>Measures WebSocket echo throughput with pipelining. Each connection upgrades via HTTP/1.1, then sends 16 messages back-to-back before waiting for the echoes.</p>\n<div class=\"doc-cards\"><a class=\"doc-card\" href=\"#doc=test-profiles/ws/echo-pipeline/implementation\" data-doc=\"test-profiles/ws/echo-pipeline/implementation\"><span class=\"dc-t\">Implementation Guidelines</span><span class=\"dc-s\">Endpoint specification, expected request/response format, and type-specific rules.</span></a><a class=\"doc-card\" href=\"#doc=test-profiles/ws/echo-pipeline/validation\" data-doc=\"test-profiles/ws/echo-pipeline/validation\"><span class=\"dc-t\">Validation</span><span class=\"dc-s\">All checks executed by the validation script for this test profile.</span></a></div></div>"},"test-profiles/h2/baseline-h2/validation":{"t":"Validation","html":"<div class=\"doc-body\"><p>The following checks are executed by <code>validate.sh</code> for every framework subscribed to the <code>baseline-h2</code> test. The HTTPS port (8443) must be responding before checks begin.</p>\n<h2 id=\"http2-protocol-negotiation\">HTTP/2 protocol negotiation</h2>\n<p>Sends a request to <code>https://localhost:8443/baseline2?a=1&amp;b=1</code> using <code>--http2</code> and checks the negotiated protocol version. The server must respond with <strong>HTTP/2</strong> (not HTTP/1.1 fallback).</p>\n<h2 id=\"get-baseline2-over-http2\">GET /baseline2 over HTTP/2</h2>\n<p>Sends <code>GET /baseline2?a=13&amp;b=42</code> over HTTPS with HTTP/2 and verifies the response body is <code>55</code>.</p>\n<h2 id=\"anti-cheat-randomized-query-parameters\">Anti-cheat: randomized query parameters</h2>\n<p>Generates random values for <code>a</code> and <code>b</code> (100-999), sends <code>GET /baseline2?a={a}&amp;b={b}</code> over HTTP/2, and verifies the response matches the expected sum. This detects hardcoded responses.</p></div>"},"test-profiles/h2/baseline-h2/implementation":{"t":"Implementation Guidelines","html":"<div class=\"doc-body\"><div class=\"type-rules\"><div class=\"type-rules-tabs\"><button class=\"type-rules-tab active\" onclick=\"var r=this.closest('.type-rules');r.querySelectorAll('.type-rules-tab').forEach(function(t){t.classList.remove('active')});this.classList.add('active');r.querySelectorAll('.type-rules-panel').forEach(function(p){p.classList.remove('active')});r.querySelector('[data-panel=production]').classList.add('active')\"><span class=\"tr-sq\" style=\"background:#22c55e\"></span>Standard</button><button class=\"type-rules-tab\" onclick=\"var r=this.closest('.type-rules');r.querySelectorAll('.type-rules-tab').forEach(function(t){t.classList.remove('active')});this.classList.add('active');r.querySelectorAll('.type-rules-panel').forEach(function(p){p.classList.remove('active')});r.querySelector('[data-panel=tuned]').classList.add('active')\"><span class=\"tr-sq\" style=\"background:#eab308\"></span>Tuned</button><button class=\"type-rules-tab\" onclick=\"var r=this.closest('.type-rules');r.querySelectorAll('.type-rules-tab').forEach(function(t){t.classList.remove('active')});this.classList.add('active');r.querySelectorAll('.type-rules-panel').forEach(function(p){p.classList.remove('active')});r.querySelector('[data-panel=engine]').classList.add('active')\"><span class=\"tr-sq\" style=\"background:#dc2626\"></span>Engine</button></div><div class=\"type-rules-panel active\" data-panel=\"production\">Must use the framework standard HTTP/2 + TLS configuration. No custom ALPN settings or TLS cipher tuning.</div><div class=\"type-rules-panel\" data-panel=\"tuned\">May optimize TLS settings, HTTP/2 stream limits, window sizes, and connection parameters.</div><div class=\"type-rules-panel\" data-panel=\"engine\">No specific rules. Ranked separately from frameworks.</div></div>\n<p>Same workload as the HTTP/1.1 baseline - query parameter parsing and sum computation - but over encrypted HTTP/2 connections using TLS and ALPN negotiation.</p>\n<p><strong>Connections:</strong> 256, 1,024 <strong>Concurrent streams per connection:</strong> 100</p>\n<h2 id=\"workload\">Workload</h2>\n<p><code>GET /baseline2?a=1&amp;b=1</code> sent over HTTP/2 with TLS. The load generator (<a href=\"https://nghttp2.org/documentation/h2load-howto.html\" target=\"_blank\" rel=\"noopener\">h2load</a>) opens multiple connections, each multiplexing up to 100 concurrent streams.</p>\n<h2 id=\"what-it-measures\">What it measures</h2>\n<ul><li>HTTP/2 multiplexing efficiency</li><li>TLS handshake and encryption overhead</li><li>How frameworks handle many concurrent streams per connection</li><li>HPACK header compression performance</li></ul>\n<h2 id=\"expected-requestresponse\">Expected request/response</h2>\n<pre><code>GET /baseline2?a=1&amp;b=1 HTTP/2</code></pre>\n<pre><code>HTTP/2 200 OK\nContent-Type: text/plain\n\n2</code></pre>\n<h2 id=\"how-it-differs-from-baseline\">How it differs from baseline</h2>\n<table><thead><tr><th></th><th>Baseline (HTTP/1.1)</th><th>Baseline (HTTP/2)</th></tr></thead><tbody><tr><td>Protocol</td><td>HTTP/1.1 plaintext</td><td>HTTP/2 over TLS</td></tr><tr><td>Connections</td><td>512 - 4,096</td><td>256 - 1,024</td></tr><tr><td>Requests per connection</td><td>1 at a time</td><td>100 concurrent streams</td></tr><tr><td>Load generator</td><td>gcannon</td><td>h2load</td></tr><tr><td>Port</td><td>8080</td><td>8443</td></tr></tbody></table>\n<p>HTTP/2 uses far fewer connections because each connection multiplexes many streams. The lower connection counts reflect real-world HTTP/2 usage patterns.</p>\n<h2 id=\"parameters\">Parameters</h2>\n<table><thead><tr><th>Parameter</th><th>Value</th></tr></thead><tbody><tr><td>Endpoint</td><td><code>GET /baseline2?a=1&amp;b=1</code></td></tr><tr><td>Connections</td><td>256, 1,024</td></tr><tr><td>Streams per connection</td><td>100 (<code>-m 100</code>)</td></tr><tr><td>Duration</td><td>5s</td></tr><tr><td>Runs</td><td>3 (best taken)</td></tr><tr><td>Load generator</td><td>h2load</td></tr></tbody></table></div>"},"test-profiles/h2/baseline-h2":{"t":"Baseline (HTTP/2)","html":"<div class=\"doc-body\"><p>Same workload as the HTTP/1.1 baseline but over encrypted HTTP/2 connections using TLS and ALPN negotiation.</p>\n<div class=\"doc-cards\"><a class=\"doc-card\" href=\"#doc=test-profiles/h2/baseline-h2/implementation\" data-doc=\"test-profiles/h2/baseline-h2/implementation\"><span class=\"dc-t\">Implementation Guidelines</span><span class=\"dc-s\">Endpoint specification, expected request/response format, and type-specific rules.</span></a><a class=\"doc-card\" href=\"#doc=test-profiles/h2/baseline-h2/validation\" data-doc=\"test-profiles/h2/baseline-h2/validation\"><span class=\"dc-t\">Validation</span><span class=\"dc-s\">All checks executed by the validation script for this test profile.</span></a></div></div>"},"test-profiles/h2/baseline-h2c/validation":{"t":"Validation","html":"<div class=\"doc-body\"><p>The following checks are executed by <code>validate.sh</code> for every framework subscribed to the <code>baseline-h2c</code> test. Port 8082 must be responding to a prior-knowledge h2 connection before checks begin.</p>\n<h2 id=\"http2-cleartext-prior-knowledge\">HTTP/2 cleartext (prior-knowledge)</h2>\n<p>Sends <code>GET /baseline2?a=1&amp;b=1</code> to <code>http://localhost:8082</code> with <code>curl --http2-prior-knowledge</code>. The negotiated protocol (<code>%{http_version}</code>) must report <strong>HTTP/2</strong>. A server answering HTTP/1.1 here fails this check.</p>\n<h2 id=\"anti-cheat-h2c-only-listener\">Anti-cheat: h2c-only listener</h2>\n<p>Sends the same request with <code>curl --http1.1</code>. The server must <strong>not</strong> respond with an HTTP/1.1 200. If it does, the port is dual-serving h1 and h2c, which means the benchmark could silently measure HTTP/1.1 throughput instead of h2c. The check accepts any non-200 response (connection reset, GOAWAY, 400, etc.).</p>\n<h2 id=\"get-baseline2-over-h2c\">GET /baseline2 over h2c</h2>\n<p>Sends <code>GET /baseline2?a=13&amp;b=42</code> with prior-knowledge h2c and verifies the response body is <code>55</code>.</p>\n<h2 id=\"anti-cheat-randomized-query-parameters\">Anti-cheat: randomized query parameters</h2>\n<p>Generates random values for <code>a</code> and <code>b</code> (100\u2013999), sends the request over h2c, and verifies the response matches the expected sum. Detects hardcoded responses.</p>\n<h2 id=\"content-type\">Content-Type</h2>\n<p>Response must include <code>Content-Type: text/plain</code> (charset suffix permitted).</p></div>"},"test-profiles/h2/baseline-h2c/implementation":{"t":"Implementation Guidelines","html":"<div class=\"doc-body\"><div class=\"type-rules\"><div class=\"type-rules-tabs\"><button class=\"type-rules-tab active\" onclick=\"var r=this.closest('.type-rules');r.querySelectorAll('.type-rules-tab').forEach(function(t){t.classList.remove('active')});this.classList.add('active');r.querySelectorAll('.type-rules-panel').forEach(function(p){p.classList.remove('active')});r.querySelector('[data-panel=production]').classList.add('active')\"><span class=\"tr-sq\" style=\"background:#22c55e\"></span>Standard</button><button class=\"type-rules-tab\" onclick=\"var r=this.closest('.type-rules');r.querySelectorAll('.type-rules-tab').forEach(function(t){t.classList.remove('active')});this.classList.add('active');r.querySelectorAll('.type-rules-panel').forEach(function(p){p.classList.remove('active')});r.querySelector('[data-panel=tuned]').classList.add('active')\"><span class=\"tr-sq\" style=\"background:#eab308\"></span>Tuned</button><button class=\"type-rules-tab\" onclick=\"var r=this.closest('.type-rules');r.querySelectorAll('.type-rules-tab').forEach(function(t){t.classList.remove('active')});this.classList.add('active');r.querySelectorAll('.type-rules-panel').forEach(function(p){p.classList.remove('active')});r.querySelector('[data-panel=engine]').classList.add('active')\"><span class=\"tr-sq\" style=\"background:#dc2626\"></span>Engine</button></div><div class=\"type-rules-panel active\" data-panel=\"production\">Must use the framework standard HTTP/2 cleartext (h2c) configuration. No custom ALPN settings or TLS cipher tuning (TLS isn&#x27;t used on this port).</div><div class=\"type-rules-panel\" data-panel=\"tuned\">May tune HTTP/2 stream limits, window sizes, and connection parameters.</div><div class=\"type-rules-panel\" data-panel=\"engine\">No specific rules. Ranked separately from frameworks.</div></div>\n<p>Same <code>/baseline2?a=\u2026&amp;b=\u2026</code> sum endpoint as the HTTP/2-TLS baseline, served as HTTP/2 <strong>cleartext</strong> - no TLS, h2 framing from the first byte. This matches the deployment pattern behind TLS-terminating load balancers (ALB \u2192 backend, nginx \u2192 app server) and inside service meshes where mTLS is handled by sidecars.</p>\n<p><strong>Port:</strong> 8082 <strong>Connections:</strong> 256, 1,024, 4,096 <strong>Concurrent streams per connection:</strong> 100 <strong>Negotiation:</strong> prior-knowledge (<code>h2load -p h2c</code>)</p>\n<h2 id=\"workload\">Workload</h2>\n<p><code>GET /baseline2?a=1&amp;b=1</code> sent over HTTP/2 cleartext. h2load opens multiple connections, each multiplexing up to 100 concurrent streams. The first bytes on every connection are the h2 preface - there is no HTTP/1.1 Upgrade dance and no ALPN (no TLS).</p>\n<h2 id=\"what-it-measures\">What it measures</h2>\n<ul><li>HTTP/2 framing + HPACK + multiplexing <em>without</em> TLS overhead</li><li>Protocol implementation cost in isolation - the delta against <code>baseline-h2</code> is roughly the TLS cost</li><li>How cleanly the framework refuses non-h2 traffic on a port declared h2c-only</li></ul>\n<h2 id=\"the-port-must-be-h2c-only\">The port must be h2c-only</h2>\n<p>Validation explicitly checks that port 8082 refuses plain HTTP/1.1 requests. A server that dual-serves h1 and h2c on the same port would let the benchmark measure whichever protocol the client picked - useless for ranking. Frameworks that want to expose h1 too must do it on a <strong>different</strong> port.</p>\n<h2 id=\"expected-requestresponse\">Expected request/response</h2>\n<pre><code>GET /baseline2?a=1&amp;b=1 HTTP/2</code></pre>\n<pre><code>HTTP/2 200 OK\nContent-Type: text/plain\n\n2</code></pre>\n<h2 id=\"how-it-differs-from-baseline-h2\">How it differs from baseline-h2</h2>\n<table><thead><tr><th></th><th>Baseline (h2)</th><th>Baseline (h2c)</th></tr></thead><tbody><tr><td>Protocol</td><td>HTTP/2 over TLS</td><td>HTTP/2 cleartext</td></tr><tr><td>Port</td><td>8443</td><td>8082</td></tr><tr><td>Negotiation</td><td>ALPN (<code>h2</code>)</td><td>prior-knowledge</td></tr><tr><td>TLS</td><td>required</td><td>not used</td></tr><tr><td>Real-world match</td><td>edge-facing servers</td><td>backend / service-to-service</td></tr></tbody></table>\n<h2 id=\"parameters\">Parameters</h2>\n<table><thead><tr><th>Parameter</th><th>Value</th></tr></thead><tbody><tr><td>Endpoint</td><td><code>GET /baseline2?a=1&amp;b=1</code></td></tr><tr><td>Connections</td><td>256, 1,024, 4,096</td></tr><tr><td>Streams per connection</td><td>100 (<code>-m 100</code>)</td></tr><tr><td>Duration</td><td>5s</td></tr><tr><td>Runs</td><td>3 (best taken)</td></tr><tr><td>Load generator</td><td>h2load with <code>-p h2c</code></td></tr></tbody></table></div>"},"test-profiles/h2/baseline-h2c":{"t":"Baseline (H2c)","html":"<div class=\"doc-body\"><p>HTTP/2 cleartext on port 8082 - prior-knowledge framing, no TLS. Models the reverse-proxy-to-origin and service-to-service patterns where HTTP/2 runs inside the trust boundary without encryption.</p>\n<div class=\"doc-cards\"><a class=\"doc-card\" href=\"#doc=test-profiles/h2/baseline-h2c/implementation\" data-doc=\"test-profiles/h2/baseline-h2c/implementation\"><span class=\"dc-t\">Implementation Guidelines</span><span class=\"dc-s\">Endpoint specification, expected request/response format, and type-specific rules.</span></a><a class=\"doc-card\" href=\"#doc=test-profiles/h2/baseline-h2c/validation\" data-doc=\"test-profiles/h2/baseline-h2c/validation\"><span class=\"dc-t\">Validation</span><span class=\"dc-s\">All checks executed by the validation script for this test profile.</span></a></div></div>"},"test-profiles/h2/json-h2c/validation":{"t":"Validation","html":"<div class=\"doc-body\"><p>The following checks are executed by <code>validate.sh</code> for every framework subscribed to the <code>json-h2c</code> test. Port 8082 must already be answering h2c prior-knowledge before these checks begin (the <code>baseline-h2c</code> anti-cheat block covers the h1-rejection check - this block assumes it has passed).</p>\n<h2 id=\"json-http2-cleartext\">/json HTTP/2 cleartext</h2>\n<p>Sends <code>GET /json/1?m=1</code> to <code>http://localhost:8082</code> with <code>curl --http2-prior-knowledge</code>. The negotiated protocol must report <strong>HTTP/2</strong> - verifies the <code>/json</code> path isn&#x27;t routed through an HTTP/1.1 fallback handler while <code>/baseline2</code> correctly speaks h2c.</p>\n<h2 id=\"content-type\">Content-Type</h2>\n<p>Response must include <code>Content-Type: application/json</code> (charset suffix permitted).</p>\n<h2 id=\"correctness-across-four-count-m-pairs\">Correctness across four (count, m) pairs</h2>\n<p>Sends four requests with <code>(count, m)</code> \u2208 <code>{(12, 3), (22, 7), (31, 2), (50, 5)}</code> - deliberately distinct from the benchmark&#x27;s seven rotation pairs so any caching-by-key strategy returns stale data. For each response: the <code>count</code> field must equal the requested count, <code>items.length</code> must equal the count, every item must carry the full schema (<code>id</code>, <code>name</code>, <code>category</code>, <code>price</code>, <code>quantity</code>, <code>active</code>, <code>tags</code> array, <code>rating</code> object with <code>score</code>+<code>count</code>, and <code>total</code>), and <code>total == price * quantity * m</code>. Partial payloads that omit fields are rejected.</p></div>"},"test-profiles/h2/json-h2c/implementation":{"t":"Implementation Guidelines","html":"<div class=\"doc-body\"><div class=\"type-rules\"><div class=\"type-rules-tabs\"><button class=\"type-rules-tab active\" onclick=\"var r=this.closest('.type-rules');r.querySelectorAll('.type-rules-tab').forEach(function(t){t.classList.remove('active')});this.classList.add('active');r.querySelectorAll('.type-rules-panel').forEach(function(p){p.classList.remove('active')});r.querySelector('[data-panel=production]').classList.add('active')\"><span class=\"tr-sq\" style=\"background:#22c55e\"></span>Standard</button><button class=\"type-rules-tab\" onclick=\"var r=this.closest('.type-rules');r.querySelectorAll('.type-rules-tab').forEach(function(t){t.classList.remove('active')});this.classList.add('active');r.querySelectorAll('.type-rules-panel').forEach(function(p){p.classList.remove('active')});r.querySelector('[data-panel=tuned]').classList.add('active')\"><span class=\"tr-sq\" style=\"background:#eab308\"></span>Tuned</button><button class=\"type-rules-tab\" onclick=\"var r=this.closest('.type-rules');r.querySelectorAll('.type-rules-tab').forEach(function(t){t.classList.remove('active')});this.classList.add('active');r.querySelectorAll('.type-rules-panel').forEach(function(p){p.classList.remove('active')});r.querySelector('[data-panel=engine]').classList.add('active')\"><span class=\"tr-sq\" style=\"background:#dc2626\"></span>Engine</button></div><div class=\"type-rules-panel active\" data-panel=\"production\">Must use the framework standard JSON serialization and standard HTTP/2 cleartext configuration. No pre-serialized caches, no custom serializers, no bypassing the framework response pipeline.</div><div class=\"type-rules-panel\" data-panel=\"tuned\">May use alternative JSON libraries (simd-json, sonic-json), tune HTTP/2 stream and window parameters, and apply framework-specific optimizations. The JSON body must still be serialized per request from live data - pre-computed / pre-serialized response caches or response-lookup tables are not allowed on either type; they short-circuit the serialization workload the profile exists to measure.</div><div class=\"type-rules-panel\" data-panel=\"engine\">No specific rules. Ranked separately from frameworks.</div></div>\n<p>Same <a href=\"#doc=test-profiles/h1/isolated/json-processing\" data-doc=\"test-profiles/h1/isolated/json-processing\">JSON Processing</a> workload (dataset slice + per-item derived field + JSON serialization) served over HTTP/2 cleartext. Exercises the JSON pipeline under multiplexed h2 streams without the TLS CPU tax the <code>baseline-h2</code> profile carries.</p>\n<p><strong>Port:</strong> 8082 <strong>Connections:</strong> 1,024, 4,096 <strong>Concurrent streams per connection:</strong> 32 <strong>Negotiation:</strong> prior-knowledge (<code>h2load -p h2c</code>)</p>\n<h2 id=\"workload\">Workload</h2>\n<p>The load generator rotates through the same seven <code>(count, m)</code> pairs as the H/1 JSON profile:</p>\n<p><code>(1, 3)</code>, <code>(5, 7)</code>, <code>(10, 2)</code>, <code>(15, 5)</code>, <code>(25, 4)</code>, <code>(40, 8)</code>, <code>(50, 6)</code></p>\n<p><code>count</code> selects a slice of the shared <code>/data/dataset.json</code> (50 items); <code>m</code> is the per-request multiplier that feeds into each item&#x27;s <code>total = price \u00d7 quantity \u00d7 m</code>. Different multipliers per request ensure naive caching by path returns wrong values.</p>\n<h2 id=\"what-it-measures\">What it measures</h2>\n<ul><li>JSON serialization throughput over h2c (no TLS in the way)</li><li>h2 multiplexing efficiency when response bodies vary in size (1 \u2192 50 items)</li><li>How cleanly the framework refuses HTTP/1.1 on the h2c-only port (validated per <code>baseline-h2c</code>&#x27;s anti-cheat)</li></ul>\n<h2 id=\"expected-requestresponse\">Expected request/response</h2>\n<pre><code>GET /json/5?m=3 HTTP/2</code></pre>\n<pre><code>HTTP/2 200 OK\nContent-Type: application/json\n\n{&quot;count&quot;:5,&quot;items&quot;:[\u20265 items, each with total = price*quantity*3\u2026]}</code></pre>\n<h2 id=\"parameters\">Parameters</h2>\n<table><thead><tr><th>Parameter</th><th>Value</th></tr></thead><tbody><tr><td>Endpoint</td><td><code>GET /json/{count}?m={multiplier}</code></td></tr><tr><td>Connections</td><td>1,024, 4,096</td></tr><tr><td>Streams per connection</td><td>32 (<code>-m 32</code>)</td></tr><tr><td>Duration</td><td>5s</td></tr><tr><td>Runs</td><td>3 (best taken)</td></tr><tr><td>Load generator</td><td>h2load with <code>-p h2c</code> and URI rotation (<code>-i json-h2c-uris.txt</code>)</td></tr></tbody></table></div>"},"test-profiles/h2/json-h2c":{"t":"JSON (H2c)","html":"<div class=\"doc-body\"><p>The <a href=\"#doc=test-profiles/h1/isolated/json-processing\" data-doc=\"test-profiles/h1/isolated/json-processing\">JSON Processing</a> workload transported over HTTP/2 cleartext on port 8082 - measures JSON serialization throughput under multiplexed h2c streams without TLS overhead.</p>\n<div class=\"doc-cards\"><a class=\"doc-card\" href=\"#doc=test-profiles/h2/json-h2c/implementation\" data-doc=\"test-profiles/h2/json-h2c/implementation\"><span class=\"dc-t\">Implementation Guidelines</span><span class=\"dc-s\">Endpoint specification, expected request/response format, and type-specific rules.</span></a><a class=\"doc-card\" href=\"#doc=test-profiles/h2/json-h2c/validation\" data-doc=\"test-profiles/h2/json-h2c/validation\"><span class=\"dc-t\">Validation</span><span class=\"dc-s\">All checks executed by the validation script for this test profile.</span></a></div></div>"},"test-profiles/h2/static-h2/validation":{"t":"Validation","html":"<div class=\"doc-body\"><p>The following checks are executed by <code>validate.sh</code> for every framework subscribed to the <code>static-h2</code> test. The HTTPS port (8443) must be responding before checks begin.</p>\n<h2 id=\"content-type-headers\">Content-Type headers</h2>\n<p>Verifies correct <code>Content-Type</code> headers for representative file types over HTTPS with HTTP/2:</p>\n<ul><li><code>GET /static/reset.css</code> - expects <code>Content-Type: text/css</code></li><li><code>GET /static/app.js</code> - expects <code>Content-Type: application/javascript</code></li><li><code>GET /static/manifest.json</code> - expects <code>Content-Type: application/json</code></li></ul>\n<p>Note: <code>text/javascript</code> is accepted as equivalent to <code>application/javascript</code> per RFC 9239.</p>\n<h2 id=\"response-size\">Response size</h2>\n<p>Requests <code>GET /static/reset.css</code> over HTTP/2 and verifies the response size is greater than 0 bytes. This confirms the server is actually serving file content, not empty responses.</p>\n<h2 id=\"404-for-nonexistent-file\">404 for nonexistent file</h2>\n<p>Sends <code>GET /static/nonexistent.txt</code> over HTTP/2 and verifies the server returns <strong>HTTP 404</strong>.</p></div>"},"test-profiles/h2/static-h2/implementation":{"t":"Implementation Guidelines","html":"<div class=\"doc-body\"><div class=\"type-rules\"><div class=\"type-rules-tabs\"><button class=\"type-rules-tab active\" onclick=\"var r=this.closest('.type-rules');r.querySelectorAll('.type-rules-tab').forEach(function(t){t.classList.remove('active')});this.classList.add('active');r.querySelectorAll('.type-rules-panel').forEach(function(p){p.classList.remove('active')});r.querySelector('[data-panel=production]').classList.add('active')\"><span class=\"tr-sq\" style=\"background:#22c55e\"></span>Standard</button><button class=\"type-rules-tab\" onclick=\"var r=this.closest('.type-rules');r.querySelectorAll('.type-rules-tab').forEach(function(t){t.classList.remove('active')});this.classList.add('active');r.querySelectorAll('.type-rules-panel').forEach(function(p){p.classList.remove('active')});r.querySelector('[data-panel=tuned]').classList.add('active')\"><span class=\"tr-sq\" style=\"background:#eab308\"></span>Tuned</button><button class=\"type-rules-tab\" onclick=\"var r=this.closest('.type-rules');r.querySelectorAll('.type-rules-tab').forEach(function(t){t.classList.remove('active')});this.classList.add('active');r.querySelectorAll('.type-rules-panel').forEach(function(p){p.classList.remove('active')});r.querySelector('[data-panel=engine]').classList.add('active')\"><span class=\"tr-sq\" style=\"background:#dc2626\"></span>Engine</button></div><div class=\"type-rules-panel active\" data-panel=\"production\">Must load files from disk on every request. No in-memory caching, no memory-mapped files, no pre-loaded file buffers. Compression must use the framework&#x27;s standard middleware or built-in static file handler - no handmade compression code. Serving pre-compressed <code>.br</code>/<code>.gz</code> variants from disk <strong>is allowed</strong>, but only through a documented framework API (e.g. ASP.NET <code>MapStaticAssets</code>, nginx <code>gzip_static</code> / <code>brotli_static</code>, Caddy <code>precompressed</code>). No custom file-suffix lookup logic.</div><div class=\"type-rules-panel\" data-panel=\"tuned\">May cache files in memory at startup, use memory-mapped files, pre-rendered response headers, or any caching strategy. May serve pre-compressed files (.gz, .br) from disk via any mechanism. Free to use any compression approach.</div><div class=\"type-rules-panel\" data-panel=\"engine\">No specific rules. Ranked separately from frameworks.</div></div>\n<p>Serves 20 static files of various types and sizes over HTTP/2 with TLS, simulating a realistic browser page load with multiplexed streams.</p>\n<p><strong>Connections:</strong> 256, 1,024 <strong>Concurrent streams per connection:</strong> 32</p>\n<h2 id=\"workload\">Workload</h2>\n<p>The load generator (<a href=\"https://nghttp2.org/documentation/h2load-howto.html\" target=\"_blank\" rel=\"noopener\">h2load</a>) requests 20 static files in a round-robin pattern across multiplexed streams. All requests include <code>Accept-Encoding: br;q=1, gzip;q=0.8</code>.</p>\n<ul><li><strong>CSS</strong> (5 files, 8\u201355 KB): <code>reset.css</code>, <code>layout.css</code>, <code>theme.css</code>, <code>components.css</code>, <code>utilities.css</code></li><li><strong>JavaScript</strong> (5 files, 15\u2013400 KB): <code>analytics.js</code>, <code>helpers.js</code>, <code>app.js</code>, <code>vendor.js</code>, <code>router.js</code></li><li><strong>HTML</strong> (2 files, 5\u20138 KB): <code>header.html</code>, <code>footer.html</code></li><li><strong>Fonts</strong> (2 files, 20\u201325 KB): <code>regular.woff2</code>, <code>bold.woff2</code></li><li><strong>SVG</strong> (2 files, 12\u201355 KB): <code>logo.svg</code>, <code>icon-sprite.svg</code></li><li><strong>Images</strong> (3 files, 15\u2013120 KB): <code>hero.webp</code>, <code>thumb1.webp</code>, <code>thumb2.webp</code></li><li><strong>JSON</strong> (1 file, 3 KB): <code>manifest.json</code></li></ul>\n<p>Total payload: ~1.16 MB across 20 files (~966 KB compressible text + ~200 KB binary).</p>\n<p>Pre-compressed versions (<code>.gz</code>, <code>.br</code>) are available on disk. See the <a href=\"#doc=test-profiles/h1/isolated/static/implementation\" data-doc=\"test-profiles/h1/isolated/static/implementation\" data-anchor=\"compression\">HTTP/1.1 static files compression section</a> for full compression rules.</p>\n<h2 id=\"what-it-measures\">What it measures</h2>\n<ul><li>Static file serving throughput over HTTP/2</li><li>HTTP/2 multiplexing efficiency with varied response sizes</li><li>Content-Type handling for different file types</li><li>File serving strategy efficiency (disk I/O vs caching, depending on type)</li><li>TLS overhead with realistic mixed payloads</li><li>Compression efficiency (optional - reduces I/O at the cost of CPU)</li></ul>\n<h2 id=\"expected-requestresponse\">Expected request/response</h2>\n<pre><code>GET /static/reset.css HTTP/2</code></pre>\n<pre><code>HTTP/2 200 OK\nContent-Type: text/css\n\n(file contents)</code></pre>\n<pre><code>GET /static/app.js HTTP/2</code></pre>\n<pre><code>HTTP/2 200 OK\nContent-Type: application/javascript\n\n(file contents)</code></pre>\n<h2 id=\"how-it-differs-from-baseline-h2\">How it differs from baseline-h2</h2>\n<table><thead><tr><th></th><th>Baseline (HTTP/2)</th><th>Static Files (HTTP/2)</th></tr></thead><tbody><tr><td>Endpoint</td><td>Single <code>GET /baseline2</code></td><td>20 different <code>/static/*</code> URIs</td></tr><tr><td>Response size</td><td>~2 bytes</td><td>3\u2013400 KB (varied)</td></tr><tr><td>Content types</td><td><code>text/plain</code></td><td>CSS, JS, HTML, fonts, SVG, WebP, JSON</td></tr><tr><td>h2load mode</td><td>Single URI</td><td>Multi-URI (<code>-i</code> flag, round-robin)</td></tr></tbody></table>\n<h2 id=\"parameters\">Parameters</h2>\n<table><thead><tr><th>Parameter</th><th>Value</th></tr></thead><tbody><tr><td>Endpoint</td><td>20 URIs under <code>/static/*</code></td></tr><tr><td>Connections</td><td>256, 1,024</td></tr><tr><td>Streams per connection</td><td>32 (<code>-m 32</code>)</td></tr><tr><td>Duration</td><td>5s</td></tr><tr><td>Runs</td><td>3 (best taken)</td></tr><tr><td>Load generator</td><td>h2load with <code>-i</code> (multi-URI)</td></tr></tbody></table></div>"},"test-profiles/h2/static-h2":{"t":"Static Files (HTTP/2)","html":"<div class=\"doc-body\"><p>Serves 20 static files of various types and sizes over HTTP/2 with TLS, simulating a realistic browser page load with multiplexed streams.</p>\n<div class=\"doc-cards\"><a class=\"doc-card\" href=\"#doc=test-profiles/h2/static-h2/implementation\" data-doc=\"test-profiles/h2/static-h2/implementation\"><span class=\"dc-t\">Implementation Guidelines</span><span class=\"dc-s\">Endpoint specification, expected request/response format, and type-specific rules.</span></a><a class=\"doc-card\" href=\"#doc=test-profiles/h2/static-h2/validation\" data-doc=\"test-profiles/h2/static-h2/validation\"><span class=\"dc-t\">Validation</span><span class=\"dc-s\">All checks executed by the validation script for this test profile.</span></a></div></div>"},"load-generators/h3/h2load-h3":{"t":"h2load-h3","html":"<div class=\"doc-body\"><p><a href=\"https://nghttp2.org/documentation/h2load-howto.html\" target=\"_blank\" rel=\"noopener\">h2load</a> is part of the nghttp2 project. HttpArena uses a custom build that links against <a href=\"https://github.com/ngtcp2/ngtcp2\" target=\"_blank\" rel=\"noopener\">ngtcp2</a>, <a href=\"https://github.com/ngtcp2/nghttp3\" target=\"_blank\" rel=\"noopener\">nghttp3</a>, and <a href=\"https://github.com/quictls/openssl\" target=\"_blank\" rel=\"noopener\">quictls</a> to enable HTTP/3 (QUIC) support. This build is invoked as <code>h2load-h3</code> and drives both <code>baseline-h3</code> and <code>static-h3</code> profiles.</p>\n<h2 id=\"installation\">Installation</h2>\n<p>The h2load binary shipped by Linux distributions does not include HTTP/3. HttpArena builds its own image:</p>\n<pre><code class=\"language-bash\">docker build -f docker/h2load-h3.Dockerfile -t h2load-h3:local docker/</code></pre>\n<p>For native execution under <code>benchmark.sh</code>, the binary and runtime libraries are extracted from the image to <code>/opt/h2load-h3/</code> and a wrapper is installed at <code>/usr/local/bin/h2load-h3</code> that sets <code>LD_LIBRARY_PATH</code> and execs the binary.</p>\n<h2 id=\"how-its-used\">How it&#x27;s used</h2>\n<pre><code class=\"language-bash\">h2load-h3 --alpn-list=h3 https://localhost:8443/baseline2?a=1&amp;b=1 \\\n  -c 64 -m 64 -t 64 -D 5s</code></pre>\n<p>For multi-URI static file tests:</p>\n<pre><code class=\"language-bash\">h2load-h3 --alpn-list=h3 -i requests/static-h2-uris.txt \\\n  -H &quot;Accept-Encoding: br;q=1, gzip;q=0.8&quot; \\\n  -c 64 -m 64 -t 64 -D 5s</code></pre>\n<table><thead><tr><th>Flag</th><th>Description</th><th>Value</th></tr></thead><tbody><tr><td><code>--alpn-list=h3</code></td><td>Negotiate HTTP/3 over QUIC</td><td>-</td></tr><tr><td><code>-c</code></td><td>Number of QUIC connections</td><td>64</td></tr><tr><td><code>-m</code></td><td>Max concurrent streams per connection</td><td>64</td></tr><tr><td><code>-t</code></td><td>Worker threads</td><td>64</td></tr><tr><td><code>-D</code></td><td>Duration</td><td>5s</td></tr><tr><td><code>-i</code></td><td>URI list file (multi-URL rotation)</td><td>-</td></tr></tbody></table>\n<h2 id=\"why-h2load-h3-instead-of-oha\">Why h2load-h3 instead of oha?</h2>\n<p>The previous HTTP/3 driver (<code>oha</code>) topped out at ~85k req/s and could not saturate any framework - its single-threaded <code>quinn</code> client became the bottleneck before the server. h2load-h3 uses ngtcp2&#x27;s multi-threaded worker model with <code>sendmmsg</code> / <code>recvmmsg</code> syscall batching, reaching ~580k req/s on the same hardware against the same server (Kestrel + msquic). Both ends are CPU-saturated at that point, which makes the result a fair measurement of the framework&#x27;s QUIC stack rather than the load generator&#x27;s.</p>\n<h2 id=\"why-http3-results-are-still-lower-than-http2\">Why HTTP/3 results are still lower than HTTP/2</h2>\n<p>HTTP/3 carries an inherent CPU cost vs HTTP/2 that loopback benchmarks make particularly visible:</p>\n<ul><li>Per-packet AEAD encryption with no kernel TLS offload</li><li>All packet processing in userspace via msquic / ngtcp2</li><li>More syscalls per request than TCP&#x27;s buffered read/write paths</li><li>Loopback at MTU 1500 - required for proper GSO/GRO behavior - gives no help from a real NIC</li></ul>\n<p>Expect a 4-6\u00d7 gap between h3 and h2 numbers for the same framework. This reflects the state of the QUIC ecosystem in 2026, not a configuration issue.</p></div>"},"load-generators/h3":{"t":"HTTP/3","html":"<div class=\"doc-body\"><div class=\"doc-cards\"><a class=\"doc-card\" href=\"#doc=load-generators/h3/h2load-h3\" data-doc=\"load-generators/h3/h2load-h3\"><span class=\"dc-t\">h2load-h3</span><span class=\"dc-s\">nghttp2&#x27;s h2load built with ngtcp2 + nghttp3 + quictls for HTTP/3 (QUIC) load testing.</span></a></div></div>"},"load-generators/h1/gcannon":{"t":"gcannon","html":"<div class=\"doc-body\"><p>gcannon is a custom HTTP load generator built specifically for HttpArena. It uses Linux&#x27;s <code>io_uring</code> interface for high-performance, zero-copy networking, ensuring the benchmarking tool itself never becomes the bottleneck.</p>\n<h2 id=\"why-a-custom-load-generator\">Why a custom load generator?</h2>\n<p>Traditional HTTP benchmarking tools like <code>wrk</code> or <code>ab</code> use <code>epoll</code> or <code>kqueue</code> for I/O multiplexing. While effective, these tools can become the bottleneck when testing extremely fast servers (millions of requests per second). gcannon uses <code>io_uring</code> to push the client-side ceiling higher.</p>\n<h2 id=\"architecture\">Architecture</h2>\n<p>gcannon spawns <strong>N worker threads</strong>, each managing its own:</p>\n<ul><li><strong>io_uring ring</strong> with <code>IORING_SETUP_SINGLE_ISSUER</code> and <code>IORING_SETUP_DEFER_TASKRUN</code> for minimal kernel overhead</li><li><strong>Provided buffer ring</strong> for zero-copy multishot receives -- the kernel writes directly into pre-registered buffers</li><li><strong>Connection pool</strong> -- each thread manages <code>connections / threads</code> TCP connections</li></ul>\n<h3 id=\"io-flow\">I/O flow</h3>\n<ol><li><strong>Connect</strong> -- async <code>io_uring_prep_connect</code> for non-blocking TCP setup</li><li><strong>Send</strong> -- pipelined requests are pre-built into a single buffer and sent with <code>io_uring_prep_send</code></li><li><strong>Receive</strong> -- multishot <code>io_uring_prep_recv_multishot</code> with provided buffers; a single SQE produces multiple CQEs as data arrives</li><li><strong>Parse</strong> -- a streaming HTTP response parser counts completed responses and extracts headers</li><li><strong>Refill</strong> -- as responses complete, new requests are fired to maintain pipeline depth</li></ol>\n<h3 id=\"cqe-batching\">CQE batching</h3>\n<p>gcannon processes completions in batches using <code>io_uring_peek_batch_cqe</code>. With <code>DEFER_TASKRUN</code>, SEND and RECV completions can arrive in the same batch, which requires careful handling to avoid pipeline stalls.</p>\n<h2 id=\"request-templates\">Request templates</h2>\n<p>gcannon supports two modes:</p>\n<h3 id=\"url-mode\">URL mode</h3>\n<p>When given a plain URL (e.g., <code>http://host:8080/pipeline</code>), gcannon generates a standard HTTP/1.1 GET request and replicates it N times for pipelining.</p>\n<h3 id=\"raw-template-mode-raw\">Raw template mode (<code>--raw</code>)</h3>\n<p>When given raw request files (e.g., <code>--raw get.raw,post_cl.raw,post_chunked.raw</code>), gcannon sends the exact bytes from each file. This enables:</p>\n<ul><li>Mixed GET/POST workloads</li><li>Requests with specific headers, body encodings, or query parameters</li><li>Bit-perfect request reproduction</li></ul>\n<p>Templates are assigned round-robin to connections, so each connection sends one request type consistently.</p>\n<h3 id=\"dynamic-placeholders\">Dynamic placeholders</h3>\n<p>Raw templates support per-request value substitution via two placeholder types:</p>\n<p><strong><code>{RAND:min:max}</code></strong> - replaced with a random number between <code>min</code> and <code>max</code> (inclusive) on every request. Uses a per-connection xorshift64 PRNG with no cross-thread contention. Ideal for distributing reads and writes across a large ID space.</p>\n<pre><code class=\"language-http\">GET /items/{RAND:1:100000} HTTP/1.1\nHost: localhost:8080\n</code></pre>\n<p><strong><code>{SEQ:start}</code></strong> - replaced with a globally incrementing counter starting at <code>start</code>. Uses a shared atomic counter across all threads, so every request gets a unique value. Ideal for INSERT operations where each row needs a distinct ID.</p>\n<pre><code class=\"language-http\">POST /items HTTP/1.1\nHost: localhost:8080\nContent-Type: application/json\nContent-Length: 72\n\n{&quot;id&quot;:{SEQ:100001},&quot;name&quot;:&quot;Bench&quot;,&quot;category&quot;:&quot;test&quot;,&quot;price&quot;:100,&quot;qty&quot;:50}</code></pre>\n<p>Values are <strong>zero-padded</strong> to the digit width of the max value, so the substituted buffer is always the same length as the original placeholder. This means <code>Content-Length</code> stays correct for POST/PUT bodies without recalculation.</p>\n<p>One placeholder per template (the first <code>{RAND:</code> or <code>{SEQ:</code> found). Each template gets its own independent counter/RNG state. A per-connection scratch buffer is used for substitution - the shared template buffer is never modified.</p>\n<h2 id=\"pipelining\">Pipelining</h2>\n<p>With <code>-p N</code>, gcannon sends N requests in a single write operation. As responses arrive, it refills the pipeline proportionally -- if 5 responses are received, 5 new requests are queued. This maintains steady pressure without overwhelming the server&#x27;s receive buffer.</p>\n<h2 id=\"latency-measurement\">Latency measurement</h2>\n<p>Latency is measured per-request from the moment the send SQE is prepared (not when the kernel completes the send) to when the corresponding response is fully parsed. This captures the true round-trip time as seen by the application.</p>\n<p>For pipelined requests, each request in the batch gets its own send timestamp, and latencies are matched FIFO to response completions.</p>\n<h2 id=\"command-line-reference\">Command-line reference</h2>\n<pre><code>Usage: gcannon &lt;url&gt; -c &lt;conns&gt; -t &lt;threads&gt; -d &lt;duration&gt;\n              [-p &lt;pipeline&gt;] [-r &lt;req/conn&gt;]\n              [-R|--raw file1,file2,...]</code></pre>\n<table><thead><tr><th>Flag</th><th>Description</th><th>Default</th></tr></thead><tbody><tr><td><code>-c</code></td><td>Total connections</td><td>required</td></tr><tr><td><code>-t</code></td><td>Worker threads</td><td>required</td></tr><tr><td><code>-d</code></td><td>Test duration (e.g., <code>5s</code>, <code>30s</code>)</td><td>required</td></tr><tr><td><code>-p</code></td><td>Pipeline depth</td><td>1</td></tr><tr><td><code>-r</code></td><td>Requests per connection (0 = unlimited)</td><td>0</td></tr><tr><td><code>--raw</code></td><td>Comma-separated raw request template files</td><td>--</td></tr></tbody></table></div>"},"load-generators/h1/wrk":{"t":"wrk","html":"<div class=\"doc-body\"><p><a href=\"https://github.com/wg/wrk\" target=\"_blank\" rel=\"noopener\">wrk</a> is a multi-threaded HTTP benchmarking tool used for the <strong>static file serving</strong> test profile.</p>\n<h2 id=\"why-wrk-for-static-files\">Why wrk for static files?</h2>\n<p>The static file test serves 20 files with response sizes ranging from 3 KB to 67 KB (brotli-compressed). At these sizes, gcannon&#x27;s io_uring provided buffer ring path introduces per-completion overhead that limits throughput to ~14 GB/s on loopback. wrk&#x27;s simpler epoll + <code>read()</code> approach achieves ~19 GB/s on the same workload, ensuring the load generator is never the bottleneck.</p>\n<p>For small-response tests (baseline, JSON, pipelined), gcannon&#x27;s io_uring batched submission is faster than wrk. The tools complement each other.</p>\n<h2 id=\"lua-rotation-script\">Lua rotation script</h2>\n<p>wrk uses a Lua script (<code>requests/static-rotate.lua</code>) to cycle through all 20 static file paths. Each request includes <code>Accept-Encoding: br;q=1, gzip;q=0.8</code> to test compressed file serving.</p>\n<pre><code class=\"language-lua\">request = function()\n  counter = counter + 1\n  local path = paths[((counter - 1) % 20) + 1]\n  return wrk.format(&quot;GET&quot;, path, {[&quot;Accept-Encoding&quot;] = ae_header})\nend</code></pre>\n<h2 id=\"parameters\">Parameters</h2>\n<table><thead><tr><th>Parameter</th><th>Value</th></tr></thead><tbody><tr><td>Test profiles</td><td><code>static</code></td></tr><tr><td>Threads</td><td>64</td></tr><tr><td>Connections</td><td>1,024, 4,096, 6,800</td></tr><tr><td>Duration</td><td>5s</td></tr><tr><td>Runs</td><td>3 (best taken)</td></tr></tbody></table></div>"},"load-generators/h1":{"t":"HTTP/1.1","html":"<div class=\"doc-body\"><div class=\"doc-cards\"><a class=\"doc-card\" href=\"#doc=load-generators/h1/gcannon\" data-doc=\"load-generators/h1/gcannon\"><span class=\"dc-t\">gcannon</span><span class=\"dc-s\">Custom io_uring-based load generator for baseline, JSON, upload, and other tests.</span></a><a class=\"doc-card\" href=\"#doc=load-generators/h1/wrk\" data-doc=\"load-generators/h1/wrk\"><span class=\"dc-t\">wrk</span><span class=\"dc-s\">Multi-threaded HTTP benchmark tool used for the static file serving test with Lua rotation.</span></a></div></div>"},"load-generators/grpc/ghz":{"t":"ghz","html":"<div class=\"doc-body\"><p><a href=\"https://ghz.sh/\" target=\"_blank\" rel=\"noopener\">ghz</a> is a proto-aware gRPC load testing tool written in Go. HttpArena uses it to drive the <code>stream-grpc</code> and <code>stream-grpc-tls</code> profiles. Unlike h2load (which ships pre-serialized binary bodies as raw HTTP/2 DATA frames), ghz is a full gRPC client - it parses the <code>.proto</code> file at runtime, marshals requests into protobuf, and issues actual gRPC calls of the correct shape (unary, server-streaming, client-streaming, or bidi) based on the method&#x27;s proto definition.</p>\n<h2 id=\"installation\">Installation</h2>\n<pre><code class=\"language-bash\">go install github.com/bojand/ghz/cmd/ghz@latest</code></pre>\n<p>The binary ends up in <code>$GOPATH/bin/ghz</code>. Ensure it&#x27;s on <code>$PATH</code> or move it to <code>/usr/local/bin/</code>. HttpArena respects the <code>GHZ</code> environment variable (<code>GHZ=&quot;${GHZ:-ghz}&quot;</code> in <code>benchmark.sh</code>), so <code>GHZ=/custom/path/ghz ./scripts/benchmark.sh ...</code> also works.</p>\n<h2 id=\"how-its-used\">How it&#x27;s used</h2>\n<h3 id=\"server-streaming-throughput-stream-grpc\">Server-streaming throughput (<code>stream-grpc</code>)</h3>\n<pre><code class=\"language-bash\">ghz --insecure --proto requests/benchmark.proto \\\n    --call benchmark.BenchmarkService/StreamSum \\\n    -d &#x27;{&quot;a&quot;:1,&quot;b&quot;:2,&quot;count&quot;:5000}&#x27; \\\n    --connections 64 -c 256 -z 5s \\\n    localhost:8080</code></pre>\n<h3 id=\"server-streaming-over-tls-stream-grpc-tls\">Server-streaming over TLS (<code>stream-grpc-tls</code>)</h3>\n<pre><code class=\"language-bash\">ghz --skipTLS --proto requests/benchmark.proto \\\n    --call benchmark.BenchmarkService/StreamSum \\\n    -d &#x27;{&quot;a&quot;:1,&quot;b&quot;:2,&quot;count&quot;:5000}&#x27; \\\n    --connections 64 -c 256 -z 5s \\\n    localhost:8443</code></pre>\n<table><thead><tr><th>Flag</th><th>Description</th><th>Value</th></tr></thead><tbody><tr><td><code>--insecure</code></td><td>Plaintext h2c - no TLS at all</td><td>for <code>stream-grpc</code></td></tr><tr><td><code>--skipTLS</code></td><td>TLS with server cert verification disabled</td><td>for <code>stream-grpc-tls</code></td></tr><tr><td><code>--proto</code></td><td>Path to canonical <code>benchmark.proto</code></td><td><code>requests/benchmark.proto</code></td></tr><tr><td><code>--call</code></td><td>Fully-qualified method name</td><td><code>benchmark.BenchmarkService/StreamSum</code></td></tr><tr><td><code>-d</code></td><td>JSON request payload (marshaled to protobuf)</td><td><code>{&quot;a&quot;:1,&quot;b&quot;:2,&quot;count&quot;:5000}</code></td></tr><tr><td><code>--connections</code></td><td>Number of TCP connections</td><td>64</td></tr><tr><td><code>-c</code></td><td>Number of concurrent worker goroutines</td><td>256 (4 streams per TCP connection)</td></tr><tr><td><code>-z</code></td><td>Benchmark duration</td><td>5s</td></tr></tbody></table>\n<p>The ratio of <code>--connections</code> to <code>-c</code> controls stream multiplexing. <code>64 \u00d7 256</code> means each TCP connection carries an average of 4 concurrent streams via HTTP/2 - empirically the optimal shape under TLS with 5000 msgs/call: ~8.6M msgs/sec peak with under 2% error rate and ~145 ms average latency. Denser ratios (8:1 or 16:1) push throughput slightly higher on paper but the error rate explodes to 10-30% as Kestrel&#x27;s HTTP/2 flow-control windows and per-connection write loops start shedding streams under pressure.</p>\n<h2 id=\"how-the-number-is-reported\">How the number is reported</h2>\n<p>ghz&#x27;s text output has the shape:</p>\n<pre><code>Summary:\n  Count:        2500\n  Total:        5.00 s\n  Slowest:      89.3 ms\n  Fastest:      4.1 ms\n  Average:      37.6 ms\n  Requests/sec: 500.00</code></pre>\n<p>For server-streaming calls, <strong><code>Requests/sec</code> is calls per second, not messages per second</strong>. HttpArena multiplies it by the messages-per-call constant (<code>5000</code> for the current profiles) to arrive at the headline <strong>messages/sec</strong> figure shown on the leaderboard. That way streaming throughput is directly comparable to unary calls/sec - both represent &quot;protobuf messages delivered through the framework per second.&quot;</p>\n<h2 id=\"why-ghz-and-not-h2load-for-streaming\">Why ghz and not h2load for streaming</h2>\n<p>h2load cannot generate gRPC streams at all. It only knows how to send opaque HTTP/2 DATA frames with a pre-built body - it has no concept of reading a response stream, let alone distinguishing a server-streaming call from a unary one. For the existing <code>unary-grpc</code> profile h2load works fine because each call is a single request/response; for anything where the shape matters, you need a real gRPC client. ghz&#x27;s overhead (protobuf marshal on every call) means its unary throughput is ~50\u00d7 lower than h2load&#x27;s raw-wire throughput, but that&#x27;s the honest gRPC number - the h2load unary-grpc figure is actually a raw h2-frame-throughput measurement, not a gRPC client/server round-trip.</p>\n<h2 id=\"limitations\">Limitations</h2>\n<ul><li><strong>Unary throughput caps around ~47k req/sec</strong> on our test hardware due to grpc-go client-side overhead. h2load still wins for unary-only raw throughput - but <code>stream-grpc</code> is where ghz shines, reaching 6M+ msg/sec because the per-call cost amortizes over thousands of messages.</li><li><strong>ghz does not measure per-message latency</strong> inside a stream - only call-level latency (time from stream open to stream close). For per-message latency you&#x27;d need a custom client.</li><li><strong>No built-in warm-up phase</strong> - first few calls may include TLS handshakes and channel setup. The 5-second default duration is long enough that handshake cost rounds out.</li><li><strong>Client-streaming and bidi</strong> work in ghz (<code>-d</code> accepts a JSON array of messages), but HttpArena doesn&#x27;t currently use them - they measure round-trip latency more than framework throughput.</li></ul></div>"},"load-generators/grpc":{"t":"gRPC","html":"<div class=\"doc-body\"><div class=\"doc-cards\"><a class=\"doc-card\" href=\"#doc=load-generators/grpc/ghz\" data-doc=\"load-generators/grpc/ghz\"><span class=\"dc-t\">ghz</span><span class=\"dc-s\">Proto-aware gRPC load generator supporting unary, server streaming, client streaming, and bidirectional RPCs.</span></a></div></div>"},"load-generators/ws":{"t":"WebSocket","html":"<div class=\"doc-body\"><p>HttpArena drives the <code>echo-ws</code> profile with <strong>gcannon in <code>--ws</code> mode</strong>. The same io_uring engine documented under <a href=\"#doc=load-generators/h1/gcannon\" data-doc=\"load-generators/h1/gcannon\">HTTP/1.1 \u2192 gcannon</a> is reused here - worker threads, per-thread provided-buffer rings, multishot receives, per-connection state - with a frame-aware send/recv loop layered on top. Using one tool across transports keeps the client-side ceiling, threading model, and CPU-pinning behavior consistent so differences in the measurement land on the server, not the generator.</p>\n<h2 id=\"handshake\">Handshake</h2>\n<p>Each worker opens TCP connections and issues an HTTP/1.1 upgrade request to the target URL (typically <code>http://localhost:8080/ws</code>). The server must respond with <code>HTTP/1.1 101 Switching Protocols</code> and the correct <code>Sec-WebSocket-Accept</code> value derived from the client&#x27;s <code>Sec-WebSocket-Key</code>. Connections that fail the handshake are reported as reconnects; the validator (<a href=\"#doc=test-profiles/ws/echo/validation\" data-doc=\"test-profiles/ws/echo/validation\">WebSocket validation</a>) checks the handshake path separately and catches framework-side bugs before benchmarks run.</p>\n<h2 id=\"echo-loop\">Echo loop</h2>\n<p>Once upgraded, each connection runs the steady-state loop:</p>\n<ol><li>Build a masked client-to-server text frame with a short payload</li><li>Send the frame via <code>io_uring_prep_send</code></li><li>Wait for the server to echo it back (matched server-to-client frame)</li><li>On receipt, increment the per-thread frame counter and immediately send the next frame</li></ol>\n<p>Pipeline depth is 1 for the <code>echo-ws</code> profile - one message in flight per connection - so the measurement is effectively a back-to-back request/response loop rather than a batched burst. With thousands of concurrent connections each running this loop in parallel, the steady-state throughput reflects the server&#x27;s ability to multiplex WebSocket frames across a large connection count without head-of-line blocking.</p>\n<p>Both text frames (opcode <code>0x1</code>) and binary frames (opcode <code>0x2</code>) are exercised against the server during validation; benchmark runs use the text shape for simplicity. Framing follows RFC 6455: masked from client to server, unmasked from server to client, FIN bit set on every frame (no fragmented messages in the benchmark path).</p>\n<h2 id=\"command-line-usage\">Command-line usage</h2>\n<pre><code class=\"language-bash\">gcannon http://localhost:8080/ws --ws \\\n        -c &lt;connections&gt; -t &lt;threads&gt; -d &lt;duration&gt; -p 1</code></pre>\n<table><thead><tr><th>Flag</th><th>Description</th></tr></thead><tbody><tr><td><code>&lt;url&gt;</code></td><td>The WebSocket endpoint served over HTTP/1.1 (uses <code>http://</code> scheme; the upgrade is implicit)</td></tr><tr><td><code>--ws</code></td><td>Switches gcannon from HTTP request mode into WebSocket echo mode</td></tr><tr><td><code>-c</code></td><td>Total concurrent connections (distributed evenly across <code>-t</code> threads)</td></tr><tr><td><code>-t</code></td><td>Worker threads (each owns an io_uring and a slice of connections; defaults to <code>$THREADS=64</code>)</td></tr><tr><td><code>-d</code></td><td>Test duration - <code>5s</code> for <code>echo-ws</code></td></tr><tr><td><code>-p</code></td><td>Pipeline depth - fixed at <code>1</code> for <code>echo-ws</code> (one message in flight per connection)</td></tr></tbody></table>\n<p>The profile dispatcher (<code>scripts/lib/tools/gcannon.sh:ws-echo</code>) wires all of this automatically when you invoke <code>./scripts/benchmark.sh &lt;framework&gt; echo-ws</code>.</p>\n<h2 id=\"output-shape\">Output shape</h2>\n<p>gcannon reports WebSocket results with the same layout as HTTP requests, except the summary line reads &quot;frames sent / frames received&quot; instead of &quot;requests / responses&quot;:</p>\n<pre><code>  2400000 frames sent     in 5.00s, 2400000 frames received\n  Throughput: 480.00K frames/s\n  WS frames: 2400000</code></pre>\n<p>The parser (<code>gcannon_parse ws-echo</code>) records <code>frames received</code> as the <code>status_2xx</code> equivalent and divides by the measured duration to produce the headline RPS number shown on the <a href=\"/leaderboards/websocket/\" target=\"_blank\" rel=\"noopener\">WebSocket leaderboard</a>. One echo round-trip counts as one unit - the frames-received count from the client side, not frames-sent, because the metric is &quot;how many echoes the framework completed,&quot; not &quot;how many messages the benchmarker pushed into the socket.&quot;</p>\n<h2 id=\"why-not-a-dedicated-websocket-tool\">Why not a dedicated WebSocket tool</h2>\n<p>The two common alternatives - <code>wrk2</code> with a Lua WebSocket plugin, or <code>artillery</code> - either can&#x27;t saturate the server at 64-core scale (GC + per-connection Lua overhead becomes the bottleneck) or produce non-deterministic per-thread CPU pinning that makes cross-framework comparison unreliable. Reusing gcannon means the generator&#x27;s tuning story is the same one already vetted against the HTTP/1.1 profiles, and the operator-side flags (<code>$GCANNON_CPUS</code>, cpuset pinning, provided buffer ring sizing) compose identically.</p></div>"},"load-generators/h2/h2load":{"t":"h2load","html":"<div class=\"doc-body\"><p><a href=\"https://nghttp2.org/documentation/h2load-howto.html\" target=\"_blank\" rel=\"noopener\">h2load</a> is part of the nghttp2 project and is used for the <code>baseline-h2</code> profile. It supports HTTP/2 multiplexing with configurable concurrent streams per connection.</p>\n<h2 id=\"installation\">Installation</h2>\n<pre><code class=\"language-bash\">sudo apt install nghttp2-client</code></pre>\n<h2 id=\"how-its-used\">How it&#x27;s used</h2>\n<pre><code class=\"language-bash\">h2load https://localhost:8443/baseline2?a=1&amp;b=1 -c 256 -m 100 -t 128 -D 5s</code></pre>\n<table><thead><tr><th>Flag</th><th>Description</th><th>Value</th></tr></thead><tbody><tr><td><code>-c</code></td><td>Number of connections</td><td>256 or 1,024</td></tr><tr><td><code>-m</code></td><td>Max concurrent streams per connection</td><td>100</td></tr><tr><td><code>-t</code></td><td>Threads</td><td>128</td></tr><tr><td><code>-D</code></td><td>Duration</td><td>5s</td></tr></tbody></table>\n<h2 id=\"why-h2load\">Why h2load?</h2>\n<p>gcannon is HTTP/1.1 only (by design - it uses raw request templates and io_uring for maximum throughput). h2load handles TLS negotiation, ALPN, HPACK header compression, and HTTP/2 stream multiplexing natively.</p></div>"},"load-generators/h2":{"t":"HTTP/2","html":"<div class=\"doc-body\"><div class=\"doc-cards\"><a class=\"doc-card\" href=\"#doc=load-generators/h2/h2load\" data-doc=\"load-generators/h2/h2load\"><span class=\"dc-t\">h2load</span><span class=\"dc-s\">HTTP/2 load generator from nghttp2 with TLS and stream multiplexing support.</span></a></div></div>"},"add-framework/implementation-rules/engine":{"t":"Engine","html":"<div class=\"doc-body\"><p>Engine entries (<code>type: engine</code>) are bare-metal HTTP implementations - raw sockets, custom parsers, low-level I/O. They are not frameworks and are ranked separately. (Reverse proxies and static-file servers like nginx and h2o are classified as <a href=\"#doc=add-framework/implementation-rules/infrastructure\" data-doc=\"add-framework/implementation-rules/infrastructure\">Infrastructure</a>, not Engine.)</p>\n<h2 id=\"what-qualifies-as-an-engine\">What qualifies as an engine</h2>\n<ul><li>Raw TCP socket servers with custom HTTP parsing</li><li>Minimal HTTP libraries without routing or middleware</li><li>Direct io_uring or epoll implementations</li></ul>\n<h2 id=\"rules\">Rules</h2>\n<ul><li>Must implement the endpoint spec correctly</li><li>Must pass the validation suite</li><li>No restrictions on implementation approach</li><li>Ranked separately from framework entries (flagship and emerging)</li><li>Only participates in connection-level tests (baseline, pipelined, limited-conn) and protocol tests (H2, H3, gRPC, WebSocket) by default</li></ul></div>"},"add-framework/implementation-rules/infrastructure":{"t":"Infrastructure","html":"<div class=\"doc-body\"><p>Infrastructure entries (<code>type: infrastructure</code>) are reverse proxies and static-file servers - nginx, h2o, Caddy and the like - run without an application framework layer. Like engines, they are not frameworks and are ranked separately.</p>\n<h2 id=\"what-qualifies\">What qualifies</h2>\n<ul><li>Reverse proxies terminating TLS and forwarding upstream (nginx, Caddy, h2o)</li><li>Standalone static-file servers</li><li>Edge servers used purely as a proxy in front of an application</li></ul>\n<h2 id=\"rules\">Rules</h2>\n<ul><li>Must implement the endpoint spec correctly and pass the validation suite</li><li>No restrictions on configuration</li><li>Ranked separately from framework entries (flagship and emerging)</li><li>Participates in the static-file and protocol tests where applicable</li></ul></div>"},"add-framework/implementation-rules":{"t":"Implementation Rules","html":"<div class=\"doc-body\"><p>Every entry declares a <strong>type</strong> in <code>meta.json</code> - what it is and how it is ranked. Framework entries (Flagship / Emerging / Experimental) additionally declare a <strong>mode</strong> (Standard or Tuned).</p>\n<div class=\"doc-cards\"><a class=\"doc-card\" href=\"#doc=add-framework/implementation-rules/frameworks\" data-doc=\"add-framework/implementation-rules/frameworks\"><span class=\"dc-t\">Frameworks</span><span class=\"dc-s\">Flagship, Emerging and Experimental tiers - run in Standard or Tuned mode.</span></a><a class=\"doc-card\" href=\"#doc=add-framework/implementation-rules/engine\" data-doc=\"add-framework/implementation-rules/engine\"><span class=\"dc-t\">Engine</span><span class=\"dc-s\">Bare-metal HTTP implementations (raw sockets, custom parser). Ranked separately.</span></a><a class=\"doc-card\" href=\"#doc=add-framework/implementation-rules/infrastructure\" data-doc=\"add-framework/implementation-rules/infrastructure\"><span class=\"dc-t\">Infrastructure</span><span class=\"dc-s\">Reverse proxies and static-file servers (nginx, h2o) without an app framework layer.</span></a></div></div>"},"add-framework/implementation-rules/frameworks/standard":{"t":"Standard","html":"<div class=\"doc-body\"><p>Standard is the default mode for a framework entry (<code>mode: standard</code>). It measures what the framework gives you out of the box, the way developers actually use it in real applications - documented APIs, production-grade settings, and standard libraries.</p>\n<h2 id=\"use-framework-level-apis\">Use framework-level APIs</h2>\n<p>If a framework provides a documented, high-level way to accomplish a task, the benchmark implementation <strong>must</strong> use it. Bypassing the framework to hand-roll a faster solution is not permitted.</p>\n<div class=\"doc-tabset\"><div class=\"doc-tabs\"><button class=\"doc-tab active\" onclick=\"var r=this.closest('.doc-tabset');r.querySelectorAll('.doc-tab').forEach(function(t){t.classList.remove('active')});this.classList.add('active');var ps=r.querySelectorAll('.doc-tabpanel');ps.forEach(function(p){p.classList.remove('active')});ps[0].classList.add('active')\">Good</button><button class=\"doc-tab\" onclick=\"var r=this.closest('.doc-tabset');r.querySelectorAll('.doc-tab').forEach(function(t){t.classList.remove('active')});this.classList.add('active');var ps=r.querySelectorAll('.doc-tabpanel');ps.forEach(function(p){p.classList.remove('active')});ps[1].classList.add('active')\">Bad</button></div><div class=\"doc-tabpanel active\"><pre><code class=\"language-python\"># Use the framework&#x27;s built-in parameter binding\n@app.get(&quot;/baseline&quot;)\ndef baseline(a: int, b: int):\n    return str(a + b)</code></pre></div><div class=\"doc-tabpanel\"><pre><code class=\"language-python\"># Manually parse query string for speed\n@app.get(&quot;/baseline&quot;)\ndef baseline(request):\n    qs = request.url.query.encode()\n    a = fast_parse_int(qs, b&quot;a=&quot;)\n    b = fast_parse_int(qs, b&quot;b=&quot;)\n    return custom_serialize(a + b)</code></pre></div></div>\n<h2 id=\"settings-must-be-production-documented\">Settings must be production-documented</h2>\n<p>Non-default configuration is allowed <strong>only if the framework&#x27;s official production deployment guide recommends it</strong>. If there is no official documentation recommending a setting for production use, it does not belong in the benchmark.</p>\n<p><strong>Allowed:</strong></p>\n<ul><li>GC settings recommended in production deployment guides</li><li>Worker/thread counts matching available CPU cores</li><li>Connection pool sizes for the environment</li></ul>\n<p><strong>Not allowed:</strong></p>\n<ul><li>Undocumented flags found by reading framework source code</li><li>Experimental or unstable options that trade safety for speed</li><li>Settings that disable buffering, validation, or error handling</li></ul>\n<h2 id=\"use-standard-libraries-and-drivers\">Use standard libraries and drivers</h2>\n<p>If the ecosystem has a well-established, production-grade library for a task (database driver, JSON serializer), use it. Experimental or hand-rolled alternatives solely for benchmark performance are not permitted.</p>\n<p><strong>Exception:</strong> If the framework itself bundles or officially recommends a specific library, that library is acceptable.</p>\n<h2 id=\"static-files-must-be-read-from-disk\">Static files must be read from disk</h2>\n<p>For static file tests, standard entries must read files from disk on every request. No in-memory caching, no memory-mapped files, no pre-loaded file buffers.</p>\n<h2 id=\"static-file-compression\">Static file compression</h2>\n<p>Compression of static files is optional but recommended for better results. All static file requests include <code>Accept-Encoding: br;q=1, gzip;q=0.8</code> - frameworks that compress will naturally benefit from reduced I/O.</p>\n<p><strong>Standard rule:</strong> compression must use the framework&#x27;s standard middleware or built-in static file handler (e.g., Nginx <code>gzip on</code>/<code>gzip_static on</code>, ASP.NET response compression middleware, Express <code>compression()</code> middleware). No handmade compression code.</p>\n<p>Pre-compressed files (<code>.gz</code>, <code>.br</code>) are available on disk alongside the originals. Frameworks that support serving pre-compressed files as a documented, official feature (e.g., Nginx <code>gzip_static</code>/<code>brotli_static</code>, ASP.NET <code>MapStaticAssets</code>) may use them.</p>\n<h2 id=\"deployment-environment-tuning\">Deployment-environment tuning</h2>\n<p>Adapting to the benchmark hardware is permitted:</p>\n<ul><li>Setting worker count to match CPU cores</li><li>Configuring connection pool sizes</li><li>Adjusting memory limits for the container</li></ul>\n<p>The boundary is: <strong>adapt to the environment, do not exploit it.</strong></p></div>"},"add-framework/implementation-rules/frameworks/tuned":{"t":"Tuned","html":"<div class=\"doc-body\"><p>Tuned mode (<code>mode: tuned</code>) gives a framework entry more freedom. It can use non-default configurations, experimental flags, and optimizations that go beyond standard framework usage. Tuned entries are marked with a ring on the leaderboard and shown alongside standard ones.</p>\n<h2 id=\"what-is-allowed\">What is allowed</h2>\n<ul><li>Alternative JSON serializers (simd-json, sonic-json, etc.)</li><li>Custom buffer sizes and TCP socket options</li><li>Experimental or unstable framework flags</li><li>Memory-mapped files and in-memory static file caching</li><li>Custom thread pools and worker configurations</li><li>Non-default GC settings without documentation requirement</li><li>Framework-specific performance flags not recommended for production</li><li>Any compression approach for static files - custom compression, pre-compressed file serving, alternative compression libraries</li></ul>\n<h2 id=\"what-is-not-allowed\">What is NOT allowed</h2>\n<ul><li><strong>Pre-computed response bodies</strong> - serializing a fixed response at startup and returning the same bytes per request (e.g. caching a JSON blob and writing it back unchanged). The serialization + compression work is the workload; bypassing it defeats the measurement.</li><li><strong>Response caching</strong> - memoizing the full HTTP response body keyed by URL/params and replaying it. This is distinct from upstream data caching (DB query results, JWT verification, etc.), which remains allowed where the profile calls for it (e.g. the CRUD profile&#x27;s read cache).</li></ul>\n<h2 id=\"what-is-still-required\">What is still required</h2>\n<ul><li>Must use the framework&#x27;s HTTP server (not a raw socket replacement)</li><li>Must implement all endpoint specs correctly</li><li>Must pass the validation suite</li><li>The framework dependency must be a real, published framework</li></ul>\n<h2 id=\"when-to-choose-tuned\">When to choose Tuned</h2>\n<p>If your submission uses any setting or optimization that a typical production team would not use, set <code>mode</code> to tuned. If in doubt, start with standard and switch to tuned only once you add non-standard optimizations.</p></div>"},"add-framework/implementation-rules/frameworks":{"t":"Frameworks","html":"<div class=\"doc-body\"><p>Framework entries are real web frameworks, grouped into three maturity tiers. The tier reflects maturity, not how the code is written - pick your best fit and it may be adjusted on review. Set it with <code>meta.json.type</code>.</p>\n<ul><li><strong>Flagship</strong> - a mature framework backed by an active development team, with a solid ecosystem (libraries, middleware, tooling) and an established community around it. Full-featured, and covers a complete test category (e.g. all HTTP/1.1 profiles).</li><li><strong>Emerging</strong> - a genuine framework that does not yet meet the full flagship bar: newer, more minimal, or only partial coverage.</li><li><strong>Experimental</strong> - very new work that has not proved itself yet. Ranked alongside frameworks, but hidden by default on the leaderboard (opt-in via the type filter).</li></ul>\n<p>All three are scored in the same framework normalization pool and can be combined on the leaderboard.</p>\n<h2 id=\"mode\">Mode</h2>\n<p>Every framework entry also declares a <strong>mode</strong> in <code>meta.json.mode</code> - how strictly it follows the implementation rules. The same framework can be submitted in either mode; tuned entries are marked with a ring on the leaderboard and ranked alongside standard ones.</p>\n<div class=\"doc-cards\"><a class=\"doc-card\" href=\"#doc=add-framework/implementation-rules/frameworks/standard\" data-doc=\"add-framework/implementation-rules/frameworks/standard\"><span class=\"dc-t\">Standard</span><span class=\"dc-s\">Default, production-style usage: documented framework APIs, production settings, and standard libraries.</span></a><a class=\"doc-card\" href=\"#doc=add-framework/implementation-rules/frameworks/tuned\" data-doc=\"add-framework/implementation-rules/frameworks/tuned\"><span class=\"dc-t\">Tuned</span><span class=\"dc-s\">Non-default configs, experimental flags, and custom optimizations allowed.</span></a></div></div>"},"running-locally/scripts/benchmark":{"t":"benchmark.sh","html":"<div class=\"doc-body\"><p><code>scripts/benchmark.sh</code> is the full-fidelity benchmark driver. It runs one framework across every profile it subscribes to, tunes the host, collects metrics, and (with <code>--save</code>) persists results to <code>results/</code> and the site data JSONs.</p>\n<p>The driver itself is small (~320 lines of orchestration) - all of the real work lives in the composable library modules under <code>scripts/lib/</code>.</p>\n<h2 id=\"synopsis\">Synopsis</h2>\n<pre><code class=\"language-bash\">./scripts/benchmark.sh &lt;framework&gt; [profile] [--save]</code></pre>\n<table><thead><tr><th>Argument</th><th>Description</th></tr></thead><tbody><tr><td><code>&lt;framework&gt;</code></td><td>Name of the framework directory under <code>frameworks/</code>. Required.</td></tr><tr><td><code>[profile]</code></td><td>Optional - run only this profile (e.g. <code>baseline</code>, <code>async-db</code>, <code>echo-ws</code>). If omitted, runs every profile the framework subscribes to.</td></tr><tr><td><code>--save</code></td><td>Persist results. Without it, you get a dry run - numbers printed, nothing written.</td></tr></tbody></table>\n<p><code>--save</code> can appear in any position. There is no other positional argument.</p>\n<h2 id=\"what-a-run-does-step-by-step\">What a run does, step by step</h2>\n<ol><li><strong>Cleanup</strong> - stops and removes any leftover <code>httparena-*</code> containers.</li><li><strong>Load-generator images</strong> (docker mode only) - builds any missing <code>gcannon</code>, <code>h2load</code>, <code>h2load-h3</code>, <code>wrk</code>, or <code>ghz</code> image from <code>docker/*.Dockerfile</code>. Runs <em>before</em> host tuning on purpose: tuning restarts the Docker daemon, and buildkit&#x27;s DNS takes a few seconds to recover afterwards, long enough to break a <code>git clone</code> inside a build container.</li><li><strong>Framework build</strong> - <code>frameworks/&lt;fw&gt;/build.sh</code> if present, otherwise <code>docker build frameworks/&lt;fw&gt;</code>.</li><li><strong>Host tuning</strong> (<code>scripts/lib/system.sh</code>):<ul><li>CPU governor \u2192 <code>performance</code> via <code>cpupower</code> (falls back to writing <code>/sys/devices/system/cpu/cpu*/cpufreq/scaling_governor</code>).</li><li><code>net.core.somaxconn=65535</code>, <code>tcp_max_syn_backlog=65535</code>, <code>netdev_max_backlog=65535</code>, <code>rmem_max=wmem_max=7500000</code> (QUIC).</li><li><code>ip link set lo mtu 1500</code> - realistic Ethernet MTU, not the kernel&#x27;s default 65536.</li><li><code>systemctl restart docker</code> - guarantees every subsequent container starts from a fresh daemon state.</li><li><code>echo 3 &gt; /proc/sys/vm/drop_caches</code>.</li></ul></li><li><strong>Postgres sidecar</strong> - started if the framework subscribes to any of <code>async-db</code>, <code>api-4</code>, <code>api-16</code>, <code>crud</code>, <code>gateway-64</code>, <code>gateway-h3</code>, <code>production-stack</code>. Uses <code>postgres:18</code> (Debian, glibc), tmpfs-backed, seeded from <code>data/pgdb-seed.sql</code>, <code>max_connections=256</code>, host network.</li><li><strong>Profile loop</strong> - for each subscribed profile \u00d7 each connection count:<ul><li>Starts the framework container (or <code>docker compose up</code> for gateway profiles).</li><li>Waits up to 30s for the right endpoint to respond.</li><li>Builds the tool-specific argument vector.</li><li>Runs <code>$RUNS</code> measurement iterations (default 3), keeps the best by rps.</li><li>Each iteration: starts <code>docker stats</code> polling, runs the load generator (<code>timeout 45s</code>), stops polling, parses output.</li><li>For ghz, a 2s warmup precedes the first measurement.</li><li>Raw load-generator output from every run is written to <code>site/static/logs/&lt;profile&gt;/&lt;conns&gt;/&lt;framework&gt;.&lt;tool&gt;.run&lt;N&gt;.txt</code> - useful when a parser misbehaves.</li></ul></li><li><strong>Save</strong> (<code>--save</code> only) - writes <code>results/&lt;profile&gt;/&lt;conns&gt;/&lt;framework&gt;.json</code> + framework <code>docker logs</code> to <code>site/static/logs/&lt;profile&gt;/&lt;conns&gt;/&lt;framework&gt;.log</code>.</li><li><strong>Restore</strong> - trap runs <code>framework_stop</code>, <code>gateway_down</code>, <code>postgres_stop</code>, then restores the original CPU governor and loopback MTU.</li><li><strong>Rebuild site data</strong> (<code>--save</code> only) - re-runs <code>scripts/rebuild_site_data.py</code> to regenerate <code>site/data/&lt;profile&gt;-&lt;conns&gt;.json</code> and <code>site/data/frameworks.json</code>.</li></ol>\n<h2 id=\"flags\">Flags</h2>\n<table><thead><tr><th>Flag</th><th>Description</th></tr></thead><tbody><tr><td><code>--save</code></td><td>Persist result JSONs and rebuild site data. Default is dry run.</td></tr></tbody></table>\n<p>Everything else is controlled through environment variables (see below).</p>\n<h2 id=\"environment-variables\">Environment variables</h2>\n<p>Set via <code>VAR=value ./scripts/benchmark.sh ...</code> or <code>export VAR=value</code>.</p>\n<h3 id=\"run-settings\">Run settings</h3>\n<table><thead><tr><th>Variable</th><th>Default</th><th>Description</th></tr></thead><tbody><tr><td><code>DURATION</code></td><td><code>5s</code></td><td><code>-d</code>/<code>-D</code> value passed to each load generator.</td></tr><tr><td><code>RUNS</code></td><td><code>3</code></td><td>Measurement iterations per (profile, conns). Best result wins.</td></tr><tr><td><code>THREADS</code></td><td><code>64</code></td><td>Load-generator threads for gcannon, wrk, and the default path.</td></tr><tr><td><code>H2THREADS</code></td><td><code>64</code></td><td>h2load worker threads (h2, h2c gRPC).</td></tr><tr><td><code>H3THREADS</code></td><td><code>64</code></td><td>h2load-h3 worker threads (HTTP/3 over QUIC).</td></tr></tbody></table>\n<h3 id=\"ports\">Ports</h3>\n<table><thead><tr><th>Variable</th><th>Default</th><th>Description</th></tr></thead><tbody><tr><td><code>PORT</code></td><td><code>8080</code></td><td>HTTP/1.1 plaintext (all <code>h1*</code> profiles + <code>echo-ws</code>); also h2c for gRPC (<code>unary-grpc</code>, <code>stream-grpc</code>).</td></tr><tr><td><code>H2PORT</code></td><td><code>8443</code></td><td>HTTPS / HTTP/2 TLS (<code>baseline-h2</code>, <code>static-h2</code>, gateway + production-stack), HTTP/3 QUIC (<code>baseline-h3</code>, <code>static-h3</code>, <code>gateway-h3</code>), gRPC-TLS (<code>unary-grpc-tls</code>, <code>stream-grpc-tls</code>).</td></tr><tr><td><code>H1TLS_PORT</code></td><td><code>8081</code></td><td>HTTP/1.1 + TLS - only used by the <code>json-tls</code> profile.</td></tr><tr><td><code>H2C_PORT</code></td><td><code>8082</code></td><td>HTTP/2 cleartext prior-knowledge for <code>baseline-h2c</code> and <code>json-h2c</code>. Must refuse HTTP/1.1 - the validator checks this.</td></tr></tbody></table>\n<h3 id=\"load-generator-selection\">Load generator selection</h3>\n<p>Switch every load generator from native binary to the pre-built Docker image in one env var:</p>\n<pre><code class=\"language-bash\">LOADGEN_DOCKER=true ./scripts/benchmark.sh aspnet-minimal</code></pre>\n<table><thead><tr><th>Variable</th><th>Default</th><th>Description</th></tr></thead><tbody><tr><td><code>LOADGEN_DOCKER</code></td><td><code>false</code></td><td>When <code>true</code>, every load generator runs from its Docker image instead of the host binary. Builds missing images automatically.</td></tr><tr><td><code>GCANNON_MODE</code></td><td><code>native</code></td><td><code>native</code> or <code>docker</code>. <code>LOADGEN_DOCKER=true</code> sets this to <code>docker</code> for you.</td></tr><tr><td><code>GCANNON_CPUS</code></td><td><code>32-63,96-127</code></td><td>Cores the load generators are pinned to (via <code>taskset</code> in native mode or <code>--cpuset-cpus</code> in docker mode). Framework containers use the <em>other</em> half of the CPU topology.</td></tr></tbody></table>\n<h3 id=\"tool-binaries-images\">Tool binaries + images</h3>\n<table><thead><tr><th>Variable</th><th>Default</th><th>Used for</th></tr></thead><tbody><tr><td><code>GCANNON</code></td><td><code>gcannon</code></td><td>Native binary - baseline, pipelined, limited-conn, json, json-comp, upload, api-4/16, async-db, crud, echo-ws.</td></tr><tr><td><code>GCANNON_IMAGE</code></td><td><code>gcannon:latest</code></td><td>Docker image when <code>LOADGEN_DOCKER=true</code>.</td></tr><tr><td><code>H2LOAD</code></td><td><code>h2load</code></td><td>Native binary - baseline-h2, static-h2, baseline-h2c, json-h2c, unary-grpc, unary-grpc-tls, gateway-64, production-stack.</td></tr><tr><td><code>H2LOAD_IMAGE</code></td><td><code>h2load:latest</code></td><td>Docker image (Ubuntu 24.04 + glibc build; do <strong>not</strong> use the alpine/musl image - it&#x27;s 20\u201340% slower).</td></tr><tr><td><code>H2LOAD_H3</code></td><td><code>h2load-h3</code></td><td>Native binary - baseline-h3, static-h3, gateway-h3.</td></tr><tr><td><code>H2LOAD_H3_IMAGE</code></td><td><code>h2load-h3:local</code></td><td>Docker image with <code>quictls</code> + <code>nghttp3</code> + <code>ngtcp2</code> + <code>nghttp2 --enable-http3</code> built from source.</td></tr><tr><td><code>WRK</code></td><td><code>wrk</code></td><td>Native binary - static, json-tls.</td></tr><tr><td><code>WRK_IMAGE</code></td><td><code>wrk:local</code></td><td>Docker image.</td></tr><tr><td><code>GHZ</code></td><td><code>ghz</code></td><td>Native binary - stream-grpc, stream-grpc-tls, gRPC readiness probe.</td></tr><tr><td><code>GHZ_IMAGE</code></td><td><code>ghz:local</code></td><td>Docker image.</td></tr></tbody></table>\n<h3 id=\"postgres-sidecar\">Postgres sidecar</h3>\n<table><thead><tr><th>Variable</th><th>Default</th><th>Description</th></tr></thead><tbody><tr><td><code>PG_CONTAINER</code></td><td><code>httparena-postgres</code></td><td>Name of the sidecar container.</td></tr><tr><td><code>DATABASE_URL</code></td><td><code>postgres://bench:bench@localhost:5432/benchmark</code></td><td>Passed to framework containers for <code>async-db</code>, <code>crud</code>, <code>api-4</code>, <code>api-16</code>, <code>gateway-64</code>, <code>gateway-h3</code>, <code>production-stack</code>.</td></tr></tbody></table>\n<h2 id=\"profiles\">Profiles</h2>\n<p>Each profile is one line in <code>scripts/lib/profiles.sh</code> with the format:</p>\n<pre><code>pipeline | req_per_conn | cpu_limit | connections | endpoint</code></pre>\n<table><thead><tr><th>Profile</th><th>Pipeline</th><th>Req/conn</th><th>CPU pinning</th><th>Connections</th><th>Tool</th><th>Endpoint</th></tr></thead><tbody><tr><td><code>baseline</code></td><td>1</td><td>\u221e</td><td><code>0-31,64-95</code></td><td>512, 4096</td><td>gcannon</td><td><code>/baseline11</code></td></tr><tr><td><code>pipelined</code></td><td>16</td><td>\u221e</td><td><code>0-31,64-95</code></td><td>512, 4096</td><td>gcannon</td><td><code>/pipeline</code></td></tr><tr><td><code>limited-conn</code></td><td>1</td><td>10</td><td><code>0-31,64-95</code></td><td>512, 4096</td><td>gcannon</td><td><code>/baseline11</code> (reconnect every 10 req)</td></tr><tr><td><code>json</code></td><td>1</td><td>\u221e</td><td><code>0-31,64-95</code></td><td>4096</td><td>gcannon</td><td><code>/json/{1..50}</code> - 7 body sizes</td></tr><tr><td><code>json-comp</code></td><td>1</td><td>\u221e</td><td><code>0-31,64-95</code></td><td>512, 4096, 16384</td><td>gcannon</td><td><code>/json/{count}</code> + <code>Accept-Encoding: gzip, br</code></td></tr><tr><td><code>json-tls</code></td><td>1</td><td>\u221e</td><td><code>0-31,64-95</code></td><td>4096</td><td>wrk</td><td><code>/json/{count}</code> over TLS on <code>H1TLS_PORT</code></td></tr><tr><td><code>upload</code></td><td>1</td><td>\u221e</td><td><code>0-31,64-95</code></td><td>32, 256</td><td>gcannon</td><td><code>/upload</code> - 500K / 2M / 10M / 20M bodies, <code>-r 5</code></td></tr><tr><td><code>api-4</code></td><td>1</td><td>5</td><td><code>0-3</code></td><td>256</td><td>gcannon</td><td>8-template mix (baseline / json / async-db)</td></tr><tr><td><code>api-16</code></td><td>1</td><td>5</td><td><code>0-7,64-71</code></td><td>1024</td><td>gcannon</td><td>8-template mix</td></tr><tr><td><code>static</code></td><td>1</td><td>200</td><td><code>0-31,64-95</code></td><td>1024, 4096, 6800</td><td>wrk</td><td>20 files via <code>static-rotate.lua</code></td></tr><tr><td><code>async-db</code></td><td>1</td><td>\u221e</td><td><code>0-31,64-95</code></td><td>1024</td><td>gcannon</td><td>5 limit variants (5/10/20/35/50), <code>-r 25</code></td></tr><tr><td><code>baseline-h2</code></td><td>1</td><td>\u221e</td><td><code>0-31,64-95</code></td><td>256, 1024</td><td>h2load</td><td><code>/baseline2</code> on <code>H2PORT</code> TLS</td></tr><tr><td><code>static-h2</code></td><td>1</td><td>\u221e</td><td><code>0-31,64-95</code></td><td>256, 1024</td><td>h2load</td><td><code>/static/*</code> on <code>H2PORT</code> TLS</td></tr><tr><td><code>baseline-h3</code></td><td>1</td><td>\u221e</td><td><code>0-31,64-95</code></td><td>64</td><td>h2load-h3</td><td><code>/baseline2</code> on <code>H2PORT</code> QUIC</td></tr><tr><td><code>static-h3</code></td><td>1</td><td>\u221e</td><td><code>0-31,64-95</code></td><td>64</td><td>h2load-h3</td><td><code>/static/*</code> on <code>H2PORT</code> QUIC</td></tr><tr><td><code>unary-grpc</code></td><td>1</td><td>\u221e</td><td><code>0-31,64-95</code></td><td>256, 1024</td><td>h2load</td><td><code>benchmark.BenchmarkService/GetSum</code> h2c</td></tr><tr><td><code>unary-grpc-tls</code></td><td>1</td><td>\u221e</td><td><code>0-31,64-95</code></td><td>256, 1024</td><td>h2load</td><td>same, TLS</td></tr><tr><td><code>stream-grpc</code></td><td>1</td><td>\u221e</td><td><code>0-31,64-95</code></td><td>64</td><td>ghz</td><td><code>StreamSum</code> h2c, 5000 msgs/call</td></tr><tr><td><code>stream-grpc-tls</code></td><td>1</td><td>\u221e</td><td><code>0-31,64-95</code></td><td>64</td><td>ghz</td><td>same, TLS</td></tr><tr><td><code>gateway-64</code></td><td>1</td><td>\u221e</td><td><code>0-31,64-95</code></td><td>256, 1024</td><td>h2load</td><td>20-URI mix behind nginx via <code>docker compose</code></td></tr><tr><td><code>echo-ws</code></td><td>1</td><td>\u221e</td><td><code>0-31,64-95</code></td><td>512, 4096, 16384</td><td>gcannon <code>--ws</code></td><td><code>/ws</code></td></tr></tbody></table>\n<p><strong>Subscription:</strong> a framework only runs profiles listed in its <code>meta.json</code> <code>tests</code> array. Profiles it&#x27;s not subscribed to are silently skipped.</p>\n<h2 id=\"examples\">Examples</h2>\n<pre><code class=\"language-bash\"># Dry run - everything the framework subscribes to, nothing saved\n./scripts/benchmark.sh actix\n\n# Save a full run\n./scripts/benchmark.sh actix --save\n\n# One profile only\n./scripts/benchmark.sh actix baseline --save\n\n# Run with every load generator in docker (useful if native gcannon / h2load-h3 aren&#x27;t installed)\nLOADGEN_DOCKER=true ./scripts/benchmark.sh actix --save\n\n# Short runs for iterating on a framework\nDURATION=2s RUNS=1 ./scripts/benchmark.sh actix baseline\n\n# Override load-gen threading for a small machine\nTHREADS=8 H2THREADS=16 H3THREADS=8 ./scripts/benchmark.sh actix baseline</code></pre>\n<h2 id=\"output-layout\">Output layout</h2>\n<pre><code>results/&lt;profile&gt;/&lt;conns&gt;/&lt;framework&gt;.json     # metrics (RPS, p99, CPU, mem, status buckets)\nsite/static/logs/&lt;profile&gt;/&lt;conns&gt;/&lt;framework&gt;.log            # docker logs of the framework container\nsite/static/logs/&lt;profile&gt;/&lt;conns&gt;/&lt;framework&gt;.&lt;tool&gt;.runN.txt # raw load-generator stdout per iteration\nsite/data/&lt;profile&gt;-&lt;conns&gt;.json                # aggregated for the leaderboard\nsite/data/frameworks.json                       # framework metadata\nsite/data/current.json                          # host/OS/commit snapshot</code></pre></div>"},"running-locally/scripts/benchmark-lite":{"t":"benchmark-lite.sh","html":"<div class=\"doc-body\"><p><code>scripts/benchmark-lite.sh</code> is the local-dev variant of <code>benchmark.sh</code>. Same module structure, same metrics, same output layout - but with defaults tuned for &quot;laptop with Docker and nothing else installed&quot;.</p>\n<h2 id=\"whats-different\">What&#x27;s different</h2>\n<table><thead><tr><th></th><th><code>benchmark.sh</code></th><th><code>benchmark-lite.sh</code></th></tr></thead><tbody><tr><td>Default load generators</td><td>Native binaries</td><td><strong>Always</strong> docker (forced - no env override)</td></tr><tr><td>CPU pinning</td><td>Per-profile <code>--cpuset-cpus</code></td><td>None - all containers see every core</td></tr><tr><td><code>THREADS</code> default</td><td>64</td><td><code>nproc / 2</code></td></tr><tr><td><code>H2THREADS</code> / <code>H3THREADS</code> default</td><td>64 / 64</td><td>Same as <code>THREADS</code></td></tr><tr><td>Profile set</td><td>26 profiles</td><td>15 - skips <code>api-4</code>, <code>api-16</code>, <code>json-tls</code>, <code>crud</code>, <code>baseline-h2c</code>, <code>json-h2c</code>, <code>gateway-64</code>, <code>gateway-h3</code>, <code>production-stack</code>, <code>stream-grpc</code>, <code>stream-grpc-tls</code></td></tr><tr><td>Connection counts</td><td>Varies (512, 1024, 4096, 16384, \u2026)</td><td>One per profile (mostly 512; upload 128; h3 64)</td></tr><tr><td>Framework selection</td><td>One framework, always</td><td>Optional - runs every enabled framework if omitted</td></tr></tbody></table>\n<p>Everything that isn&#x27;t listed stays identical: <code>--save</code> behavior, host tuning, result layout, postgres sidecar for <code>async-db</code>, <code>gcannon_parse</code> version fallbacks, etc.</p>\n<h2 id=\"synopsis\">Synopsis</h2>\n<pre><code class=\"language-bash\">./scripts/benchmark-lite.sh [framework] [profile] [--save] [--load-threads N]</code></pre>\n<table><thead><tr><th>Argument</th><th>Description</th></tr></thead><tbody><tr><td><code>[framework]</code></td><td>Framework directory name. If omitted, every framework with <code>&quot;enabled&quot;: true</code> in its <code>meta.json</code> is run in sequence.</td></tr><tr><td><code>[profile]</code></td><td>Optional - restrict to a single profile.</td></tr><tr><td><code>--save</code></td><td>Persist result JSONs + rebuild site data.</td></tr><tr><td><code>--load-threads N</code></td><td>Shortcut for <code>THREADS=N H2THREADS=N H3THREADS=N</code> for a specific run.</td></tr></tbody></table>\n<p>Arguments can appear in any order; flags and positionals are separated during parsing.</p>\n<h2 id=\"first-run\">First run</h2>\n<p>First time you invoke it (or after a <code>docker rmi</code>), the script builds every load-generator image it needs from <code>docker/*.Dockerfile</code>:</p>\n<ul><li><code>gcannon:latest</code> - clones <code>github.com/MDA2AV/gcannon</code> main, pulls <code>liburing-2.9</code>, compiles.</li><li><code>h2load:latest</code> - Ubuntu 24.04 + <code>apt install nghttp2-client</code> (glibc build, not musl).</li><li><code>h2load-h3:local</code> - Ubuntu 24.04 + builds <code>quictls</code> + <code>nghttp3</code> + <code>ngtcp2</code> + <code>nghttp2 --enable-http3</code> from source. Takes 5\u201310 minutes the first time.</li><li><code>wrk:local</code> - Ubuntu 24.04 + <code>wrk</code> source build.</li><li><code>ghz:local</code> - <code>ghz</code> from <code>github.com/bojand/ghz@v0.121.0</code>, static CGO_DISABLED build.</li></ul>\n<p>All images are built <strong>before</strong> the host tuning step, because <code>system_tune()</code> restarts the Docker daemon and buildkit DNS takes a few seconds to recover - long enough to break <code>git clone</code> inside a build container.</p>\n<p>To force a rebuild (e.g. to pick up a new gcannon commit):</p>\n<pre><code class=\"language-bash\">docker rmi gcannon:latest\n./scripts/benchmark-lite.sh actix baseline</code></pre>\n<h2 id=\"environment-variables\">Environment variables</h2>\n<p>Everything in <a href=\"#doc=running-locally/scripts/benchmark\" data-doc=\"running-locally/scripts/benchmark\" data-anchor=\"environment-variables\">benchmark.sh \u2192 Environment variables</a> applies, with these lite-specific defaults / overrides:</p>\n<table><thead><tr><th>Variable</th><th>Default in lite</th><th>Notes</th></tr></thead><tbody><tr><td><code>LOADGEN_DOCKER</code></td><td><code>true</code> (forced, non-overridable)</td><td><code>export</code> at the top of the script.</td></tr><tr><td><code>GCANNON_MODE</code></td><td><code>docker</code> (forced)</td><td>Same.</td></tr><tr><td><code>GCANNON_CPUS</code></td><td><code>0-$(nproc-1)</code></td><td>Effectively &quot;all cores&quot; - the <code>--cpuset-cpus</code> value covers the whole CPU.</td></tr><tr><td><code>THREADS</code></td><td><code>$(( $(nproc) / 2 ))</code> (min 1)</td><td>Half the cores, leaving room for the framework container.</td></tr><tr><td><code>H2THREADS</code></td><td>Same as <code>THREADS</code></td><td></td></tr><tr><td><code>H3THREADS</code></td><td>Same as <code>THREADS</code></td><td></td></tr></tbody></table>\n<h2 id=\"profile-set\">Profile set</h2>\n<table><thead><tr><th>Profile</th><th>Pipeline</th><th>Req/conn</th><th>Connections</th><th>Tool</th><th>Endpoint</th></tr></thead><tbody><tr><td><code>baseline</code></td><td>1</td><td>\u221e</td><td>512</td><td>gcannon</td><td><code>/baseline11</code></td></tr><tr><td><code>pipelined</code></td><td>16</td><td>\u221e</td><td>512</td><td>gcannon</td><td><code>/pipeline</code></td></tr><tr><td><code>limited-conn</code></td><td>1</td><td>10</td><td>512</td><td>gcannon</td><td><code>/baseline11</code></td></tr><tr><td><code>json</code></td><td>1</td><td>\u221e</td><td>512</td><td>gcannon</td><td><code>/json/{count}</code></td></tr><tr><td><code>json-comp</code></td><td>1</td><td>\u221e</td><td>512</td><td>gcannon</td><td><code>/json/{count}</code> + compression</td></tr><tr><td><code>upload</code></td><td>1</td><td>\u221e</td><td>128</td><td>gcannon</td><td><code>/upload</code></td></tr><tr><td><code>static</code></td><td>1</td><td>10</td><td>512</td><td>wrk</td><td><code>/static/*</code></td></tr><tr><td><code>async-db</code></td><td>1</td><td>\u221e</td><td>512</td><td>gcannon</td><td><code>/async-db?limit=N</code></td></tr><tr><td><code>baseline-h2</code></td><td>1</td><td>\u221e</td><td>512</td><td>h2load</td><td><code>/baseline2</code> (TLS)</td></tr><tr><td><code>static-h2</code></td><td>1</td><td>\u221e</td><td>512</td><td>h2load</td><td><code>/static/*</code> (TLS)</td></tr><tr><td><code>baseline-h3</code></td><td>1</td><td>\u221e</td><td>64</td><td>h2load-h3</td><td><code>/baseline2</code> (QUIC)</td></tr><tr><td><code>static-h3</code></td><td>1</td><td>\u221e</td><td>64</td><td>h2load-h3</td><td><code>/static/*</code> (QUIC)</td></tr><tr><td><code>unary-grpc</code></td><td>1</td><td>\u221e</td><td>512</td><td>h2load</td><td><code>GetSum</code> h2c</td></tr><tr><td><code>unary-grpc-tls</code></td><td>1</td><td>\u221e</td><td>512</td><td>h2load</td><td><code>GetSum</code> TLS</td></tr><tr><td><code>echo-ws</code></td><td>1</td><td>\u221e</td><td>512</td><td>gcannon <code>--ws</code></td><td><code>/ws</code></td></tr></tbody></table>\n<h2 id=\"requirements\">Requirements</h2>\n<p>The only hard requirement is <strong>Docker Engine</strong>. Everything else - gcannon, h2load, h2load-h3, wrk, ghz - is built automatically inside containers. You don&#x27;t need <code>io_uring</code> on the host kernel (the gcannon container carries its own <code>liburing 2.9</code>), you don&#x27;t need <code>nghttp2-client</code> installed, and you don&#x27;t need a Rust/Go toolchain.</p>\n<p>Host tuning (CPU governor, sysctl, docker daemon restart, MTU, page-cache drop) is still best-effort - it uses <code>sudo</code> where needed and warns + continues if you don&#x27;t have it. Numbers without tuning are noisier but still usable for relative comparisons.</p>\n<h2 id=\"examples\">Examples</h2>\n<pre><code class=\"language-bash\"># Every enabled framework, every subset profile (dry run)\n./scripts/benchmark-lite.sh\n\n# One framework, one profile\n./scripts/benchmark-lite.sh actix baseline\n\n# Persist results and rebuild site data\n./scripts/benchmark-lite.sh actix --save\n\n# 4-thread load generators for a low-core machine\n./scripts/benchmark-lite.sh --load-threads 4 actix baseline\n\n# Shorter iterations while debugging a framework\nDURATION=2s RUNS=1 ./scripts/benchmark-lite.sh actix baseline</code></pre>\n<p>Lite runs are great for CI-style smoke tests and for comparing frameworks relative to each other on your own hardware. Absolute numbers will not match the published leaderboard - that&#x27;s produced on a dedicated 128-core host with CPU isolation and native load generators.</p></div>"},"running-locally/scripts/archive":{"t":"archive.sh","html":"<div class=\"doc-body\"><p>Manage benchmark result archives (rounds). Snapshots current results as a named round, lists archived rounds, or deletes a round.</p>\n<pre><code class=\"language-bash\">./scripts/archive.sh create &lt;name&gt;\n./scripts/archive.sh list\n./scripts/archive.sh delete &lt;id&gt;</code></pre>\n<h2 id=\"commands\">Commands</h2>\n<h3 id=\"create\">create</h3>\n<p>Archives all current benchmark results as a named round.</p>\n<pre><code class=\"language-bash\">./scripts/archive.sh create &quot;Round 1 - March 2026&quot;</code></pre>\n<p>What it does:</p>\n<ol><li>Bundles all <code>site/data/*.json</code> files into a single round file at <code>site/data/rounds/&lt;id&gt;.json</code></li><li>Records hardware info (CPU, cores, RAM, governor), OS, kernel, Docker version, and git commit</li><li>Reads system info from <code>site/data/current.json</code> if available (written by <code>benchmark.sh --save</code>)</li><li>Updates the round index at <code>site/data/rounds/index.json</code></li><li><strong>Clears <code>results/</code></strong> and resets site data files to start a fresh round</li></ol>\n<h3 id=\"list\">list</h3>\n<p>Lists all archived rounds with their ID, name, date, and file size.</p>\n<pre><code class=\"language-bash\">./scripts/archive.sh list</code></pre>\n<p>Example output:</p>\n<pre><code>  # 1  Round 1 - March 2026                    2026-03-15  (1248KB)\n  # 2  Round 2 - Pre-optimization baseline      2026-03-20  (1305KB)</code></pre>\n<h3 id=\"delete\">delete</h3>\n<p>Deletes an archived round by ID.</p>\n<pre><code class=\"language-bash\">./scripts/archive.sh delete 1</code></pre>\n<p>Removes the round&#x27;s data file and its entry from the index.</p></div>"},"running-locally/scripts/compare":{"t":"compare.sh","html":"<div class=\"doc-body\"><p>Compare a framework&#x27;s benchmark results against the published leaderboard data on the main branch. Outputs a Markdown table with deltas, suitable for PR comments.</p>\n<pre><code class=\"language-bash\">./scripts/compare.sh &lt;framework&gt; [profile]</code></pre>\n<h2 id=\"options\">Options</h2>\n<table><thead><tr><th>Parameter</th><th>Description</th></tr></thead><tbody><tr><td><code>&lt;framework&gt;</code></td><td>Name of the framework to compare</td></tr><tr><td><code>[profile]</code></td><td>Optional - compare only this test profile</td></tr></tbody></table>\n<h2 id=\"what-it-does\">What it does</h2>\n<ol><li>Reads new results from <code>results/&lt;profile&gt;/&lt;connections&gt;/&lt;framework&gt;.json</code></li><li>Reads published leaderboard data from <code>site/data/&lt;profile&gt;-&lt;connections&gt;.json</code></li><li>Matches the framework by its <code>display_name</code> from <code>meta.json</code></li><li>Outputs a Markdown table for each profile with columns per connection count</li></ol>\n<h2 id=\"metrics-compared\">Metrics compared</h2>\n<p>For each profile and connection count, the table shows:</p>\n<table><thead><tr><th>Metric</th><th>Direction</th><th>Description</th></tr></thead><tbody><tr><td><strong>RPS</strong></td><td>Higher is better</td><td>Requests per second</td></tr><tr><td><strong>p99</strong></td><td>Lower is better</td><td>99th percentile latency</td></tr><tr><td><strong>CPU</strong></td><td>Lower is better</td><td>CPU utilization percentage</td></tr><tr><td><strong>Memory</strong></td><td>Lower is better</td><td>Memory usage</td></tr></tbody></table>\n<p>Each value includes a percentage delta against the main branch. New frameworks with no prior data show <code>NEW</code> instead of a delta.</p>\n<h2 id=\"example-output\">Example output</h2>\n<pre><code class=\"language-markdown\">### baseline\n\n| Metric | 512c |  | 4096c |  |\n|--------|------|--|-------|--|\n| **RPS** | 1.30M | +2.1% | 1.37M | -0.5% |\n| **p99** | 2.00ms | ~0% | 33.80ms | +1.2% |\n| **CPU** | 6530% | ~0% | 6274% | -0.3% |\n| **Memory** | 408MB | ~0% | 922MB | +0.8% |</code></pre>\n<h2 id=\"ci-usage\">CI usage</h2>\n<p>The <code>benchmark-pr.yml</code> workflow calls this script automatically after benchmarking a PR branch, and posts the comparison table as a PR comment.</p></div>"},"running-locally/scripts/validate":{"t":"validate.sh","html":"<div class=\"doc-body\"><p>Run the validation suite against a framework. Builds the Docker image, starts the container (and Postgres sidecar if needed), and runs correctness checks for every test profile listed in the framework&#x27;s <code>meta.json</code>.</p>\n<pre><code class=\"language-bash\">./scripts/validate.sh &lt;framework&gt;</code></pre>\n<h2 id=\"what-it-does\">What it does</h2>\n<ol><li>Reads the framework&#x27;s <code>meta.json</code> to determine which tests are subscribed</li><li>Builds the Docker image</li><li>Mounts only the data volumes required by the subscribed tests</li><li>Starts a Postgres sidecar when <code>async-db</code>, <code>api-4</code>, or <code>api-16</code> tests are enabled</li><li>Waits for the server to start (up to 30 seconds)</li><li>Runs all validation checks for the subscribed tests</li><li>Prints a pass/fail summary and exits with code 1 if any check failed</li></ol>\n<h2 id=\"validation-coverage\">Validation coverage</h2>\n<p>Each subscribed test triggers its corresponding validation checks. Workload profiles (<code>api-4</code>, <code>api-16</code>) automatically trigger validation for all endpoints they use, even if those individual tests are not in <code>meta.json</code>.</p>\n<p>On failure, each check prints a link to the relevant validation documentation page for reference.</p>\n<h2 id=\"anti-cheat-checks\">Anti-cheat checks</h2>\n<p>Several validations use randomized inputs to detect hardcoded responses:</p>\n<ul><li><strong>Baseline</strong>: random query parameters and POST bodies</li><li><strong>Upload</strong>: random binary payload with byte count verification</li><li><strong>Database / Async DB</strong>: empty price range that must return zero results</li></ul>\n<h2 id=\"example-output\">Example output</h2>\n<pre><code>=== Validating: express ===\n[info] Subscribed tests: baseline json upload\n[build] Building Docker image...\n[ready] Server is up\n[test] baseline endpoints\n  PASS [GET /baseline11?a=13&amp;b=42]\n  PASS [POST /baseline11?a=13&amp;b=42 body=20]\n  ...\n[test] json endpoint\n  PASS [GET /json/{count}?m=X] (4 counts \u00d7 multipliers + full item schema verified)\n  PASS [GET /json Content-Type] (Content-Type: application/json)\n  ...\n\n=== Results: 15 passed, 0 failed ===</code></pre></div>"},"running-locally/scripts/run":{"t":"run.sh","html":"<div class=\"doc-body\"><p>Run a framework&#x27;s Docker container interactively for manual testing. Builds the image, starts a Postgres sidecar, mounts all data volumes, and streams container logs until you press Ctrl+C.</p>\n<pre><code class=\"language-bash\">./scripts/run.sh &lt;framework&gt;</code></pre>\n<h2 id=\"what-it-does\">What it does</h2>\n<ol><li>Builds the Docker image for the framework (or runs <code>build.sh</code> if one exists)</li><li>Starts a Postgres sidecar container with the seeded benchmark database</li><li>Mounts all data files unconditionally - datasets, static files, TLS certs</li><li>Sets <code>DATABASE_URL</code> and <code>DATABASE_MAX_CONN</code> environment variables</li><li>Runs the container attached so logs stream to your terminal</li><li>Cleans up all containers on exit (Ctrl+C or script termination)</li></ol>\n<h2 id=\"options\">Options</h2>\n<table><thead><tr><th>Parameter</th><th>Description</th></tr></thead><tbody><tr><td><code>&lt;framework&gt;</code></td><td>Name of the framework directory under <code>frameworks/</code></td></tr><tr><td><code>PORT</code> env var</td><td>Override HTTP port (default: 8080)</td></tr><tr><td><code>H2PORT</code> env var</td><td>Override HTTPS/H2 port (default: 8443)</td></tr></tbody></table>\n<h2 id=\"networking\">Networking</h2>\n<p>Uses <code>--network host</code> so the container binds directly to the host&#x27;s network interfaces. No port mapping is needed - the framework listens on ports 8080 and 8443 directly.</p>\n<h2 id=\"example\">Example</h2>\n<pre><code class=\"language-bash\">./scripts/run.sh express\n\n# In another terminal:\ncurl http://localhost:8080/baseline11?a=1&amp;b=2\ncurl http://localhost:8080/json/5?m=3\ncurl http://localhost:8080/async-db?min=10&amp;max=50&amp;limit=20</code></pre></div>"},"running-locally/scripts":{"t":"Scripts Reference","html":"<div class=\"doc-body\"><p>All scripts live in the <code>scripts/</code> directory and are meant to be run from the repository root.</p>\n<div class=\"doc-cards\"><a class=\"doc-card\" href=\"#doc=running-locally/scripts/validate\" data-doc=\"running-locally/scripts/validate\"><span class=\"dc-t\">validate.sh</span><span class=\"dc-s\">Run the correctness validation suite against a framework&#x27;s endpoints.</span></a><a class=\"doc-card\" href=\"#doc=running-locally/scripts/run\" data-doc=\"running-locally/scripts/run\"><span class=\"dc-t\">run.sh</span><span class=\"dc-s\">Run a framework container interactively for manual testing and endpoint development.</span></a><a class=\"doc-card\" href=\"#doc=running-locally/scripts/benchmark\" data-doc=\"running-locally/scripts/benchmark\"><span class=\"dc-t\">benchmark.sh</span><span class=\"dc-s\">Run benchmarks across test profiles with system tuning and result collection.</span></a><a class=\"doc-card\" href=\"#doc=running-locally/scripts/benchmark-lite\" data-doc=\"running-locally/scripts/benchmark-lite\"><span class=\"dc-t\">benchmark-lite.sh</span><span class=\"dc-s\">Lightweight benchmarks for any machine with Docker - no CPU pinning, containerized load generators.</span></a><a class=\"doc-card\" href=\"#doc=running-locally/scripts/compare\" data-doc=\"running-locally/scripts/compare\"><span class=\"dc-t\">compare.sh</span><span class=\"dc-s\">Compare benchmark results against the published leaderboard on main.</span></a><a class=\"doc-card\" href=\"#doc=running-locally/scripts/archive\" data-doc=\"running-locally/scripts/archive\"><span class=\"dc-t\">archive.sh</span><span class=\"dc-s\">Snapshot current results as named rounds and manage archived benchmarks.</span></a></div></div>"}};
